These Playwright specs are archived because the old Expertise Atlas page is outside the locked MVP launch corridor.

Keep them out of active `*.spec.ts` discovery unless a future source-of-truth update explicitly reactivates this surface.
