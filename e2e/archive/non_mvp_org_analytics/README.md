These Playwright specs are archived because org fairness analytics and team coverage pages are outside the locked MVP launch corridor.

Keep them out of active `*.spec.ts` discovery unless a future source-of-truth update explicitly reactivates these surfaces.
