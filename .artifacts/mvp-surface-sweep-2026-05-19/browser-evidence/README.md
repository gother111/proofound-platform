# Browser Evidence

> Doc Class: `artifact`
> Last Verified: `2026-05-19`

Screenshots in this folder were captured with the Codex Browser plugin against the local mock app at `http://127.0.0.1:3000`.

Captured checks:

- `portfolio-demo-unavailable-desktop.png`: `/portfolio/demo` desktop unavailable/public-gated state.
- `login-desktop.png`: `/login` desktop public entry surface.
- `login-mobile.png`: `/login` mobile public entry surface at `390x844`.

These files are visual sweep evidence only. They do not replace automated route, privacy, accessibility, or launch gate checks.
