# Archived critical-gaps mock test

This archived integration test is retained as historical evidence only.

It is not active launch evidence because it asserted mock "critical gaps" behavior for broad fairness reporting, generic performance instrumentation, Zoom interview scheduling, and matching-profile controls that no longer define the locked MVP corridor. Active launch evidence lives in focused assignment, review, matching, reveal, interview, decision, engagement verification, route-surface, and launch-smoke tests.

Do not move this test back into `tests/integration` unless the locked MVP source of truth changes and the assertions are rewritten against real active behavior instead of placeholder mock values.
