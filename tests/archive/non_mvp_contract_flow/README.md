# Archived Contract Flow E2E

The old complete user journey and page-object helpers live here as historical coverage only.

They are not part of the active MVP launch signal because they describe signup-to-contract-signing and `/app/contracts/:id` flows. Contract signing is archived outside the locked proof-first hiring corridor. Active launch coverage should use onboarding, Proof Pack, assignment, review, intro, reveal, interview, decision, engagement verification, export/delete, and launch-ops tests.
