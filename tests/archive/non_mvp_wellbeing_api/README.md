# Archived Wellbeing API Tests

Historical scripts and tests for the retired wellbeing/Zen API surface live here.

These files are outside the locked MVP corridor. Active launch evidence should use:

- `src/lib/launch/__tests__/surface-policy.test.ts`
- `tests/api/launch-surface-inventory.test.ts`
- `tests/api/archived-api-handlers-route.test.ts`

Do not move these tests back into the active tree unless the locked MVP source of truth is updated to make wellbeing/Zen routes active again.
