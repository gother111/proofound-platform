# Non-MVP Python Internal Worker Tests

> Doc Class: `historical`
> Last Verified: `2026-05-19`

These suites cover the retired TypeScript-side Python internal worker queue, temp CV import job store, service URL resolver, and worker trigger.

The active launch surface does not schedule `/api/cron/python-internal-worker` or `/api/cron/cv-import-temp-cleanup`. Keep default launch evidence on route classification, archived cron disablement, Start from CV, and Proof Artifact Text Extraction.
