# Archived Moderation API Tests

These tests preserve pre-launch moderation appeal, statement-of-reasons, and transparency-report behavior.

Those moderation APIs are outside the locked MVP corridor and are covered in the active launch signal by route-surface/archive tests. Do not move these tests back into active `tests/api` unless moderation becomes an explicit launch requirement.
