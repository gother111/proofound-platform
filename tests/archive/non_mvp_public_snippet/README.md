# Archived Public Snippet Tests

These tests cover the legacy `/p/[token]` public snippet model. The locked launch MVP keeps those routes archived and uses Public Page / organization trust-page sharing instead.
