# Archived Dashboard UI Tests

The old draggable/customizable dashboard UI tests, persisted layout tests, and unmounted dashboard-card tests live here as historical coverage only.

They are not part of the active MVP launch signal because the generic customizable dashboard shell, persisted layout engine, and loose widget-card layer are archived outside the locked proof-first hiring corridor. Active tests should cover mounted proof-first, organization, assignment, review, privacy, and launch-ops surfaces.
