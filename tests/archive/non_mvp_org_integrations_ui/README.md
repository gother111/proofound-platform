# Archived Organization Integration Tests

These tests preserve behavior for organization integration/settings and test-match surfaces that are outside the locked MVP corridor.

The active organization launch corridor is the lean trust profile, assignments, matching/review, intro/reveal/interview/decision, engagement verification, and protected launch ops. Do not move these tests back into active `tests/api` or `tests/ui` unless the route-surface policy is intentionally reopened.
