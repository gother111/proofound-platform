# Session Log Entry

- Date/time (UTC): 2026-03-13T18:04:13Z
- Branch: master
- Base commit: 4767ea77
  Task summary:
- Block 3 reduced org corridor latency by removing shortlist readiness N+1 work, taking non-critical interview scheduling side effects off the response path, and adding interview hot-path indexes.
- Captured fresh before/after evidence from direct matcher benchmarks, targeted strict org reruns, full strict org rerun, and live dev-server traces.

What worked:

- Batched portfolio-readiness evaluation cut direct `computeAssignmentMatches()` timing from about `76.5s` to about `5.1s` on the same active assignment.
- Interview scheduling stayed launch-safe by keeping auth, duplicate prevention, meeting creation, and insert synchronous while moving workflow and messaging fanout async.
- Focused and full strict org reruns both passed after the changes, and no server restart was observed in the tested path.

What failed / wrong assumptions:

- The first typecheck pass failed because the new completion helper expected full row types while the bulk query intentionally selected partial columns.
- One follow-up test command had a typo in the `source ~/.nvm/nvm.sh` path and had to be rerun.

User corrections:

- None.

Assumptions taken without asking:

- The shortlist gate only needs exact `portfolioReady` semantics because `assignmentMatcher` consumes `readiness.flags.discoverable`, which is equivalent to `portfolioReady` in the current authority-aligned implementation.
- The `host_user_id` interview index should be added only conditionally in raw SQL because that column is not represented in the current Drizzle schema.

What the user corrected afterward:

- None.

Improvements next time:

- Build the benchmark harness with repo-local dotenv loading from the start instead of trying to source `.env.local` in the shell.
- Re-run `npm run typecheck` immediately after introducing partial-row helper types to catch shape drift sooner.

Commands run + outcomes:

- `node ./scripts/playwright-node20.mjs test e2e/strict/organization.strict.spec.ts --project=chromium --reporter=line --workers=1 --grep "O-08..O-12|O-13..O-16"`: PASS before fix in `3.7m`, PASS after fix in `2.0m`.
- `npx tsx --eval "(async () => { ... computeAssignmentMatches ... })()"`: about `76480ms` before, about `5088ms` after.
- `npm run test -- src/lib/__tests__/matching-eligibility.test.ts`: PASS.
- `npm run test -- tests/api/interviews-schedule-route.test.ts`: PASS after wrapping sync mocks in `Promise.resolve(...)`.
- `node run-migrations.mjs`: PASS, applied `20260313185000_add_interview_hot_path_indexes`.
- `node ./scripts/playwright-node20.mjs test e2e/strict/organization.strict.spec.ts --project=chromium --reporter=line --workers=1`: PASS, `6/6` in `4.8m`.
- `npm run typecheck`: PASS.
- `npm run test -- src/lib/__tests__/matching-eligibility.test.ts tests/lib/individual-readiness-state.test.ts`: PASS.

Open TODOs / follow-ups:

- Refresh launch smoke so live monitoring can prove a fresh green state instead of a truthful stale-artifact block.
- Continue P1 drift cleanup for Proof Pack anchor enforcement and mixed verification semantics.
