# Session Log Entry

- Date/time (UTC): 2026-03-11T09:10:56Z
- Branch: master
- Base commit: 806b9b3c
  Task summary:
- Reverted the temporary multi-project Vercel split work and restored a single-project deployment model around `proofound-platform`.
- Added a release-batch workflow and an ignored-build-step script so production can ship selected commits in smaller batches while skipping docs-only Vercel builds.

What worked:

- Deleting `proofound-platform-api` through the Vercel API returned `204`, and the follow-up project list showed only `proofound-platform`.
- `scripts/vercel-should-build.mjs` correctly skipped non-runtime paths and forced builds for runtime-critical paths and uncertain diffs.
- The release-candidate workflow and master PR gate could be added entirely in repo automation without touching public routing.
- `npm run lint`, `npm run typecheck`, `npm run docs:freshness`, and `VERCEL=1 npm run build` all passed for this change set.

What failed / wrong assumptions:

- `npm run test` is not clean at HEAD because `tests/lib/python-cv-proxy.test.ts` currently disagrees with unrelated local edits in `src/lib/python-cv-proxy.ts`.
- The earlier assumption that “deploy in parts” meant separate Vercel projects was wrong. The user wanted one project with smaller release batches.

User corrections:

- The user clarified that everything must remain in the single `proofound-platform` Vercel project and that release batching, not multi-project topology, was the desired solution.

Assumptions taken without asking:

- Historical sharded logs about the temporary API-project experiment should remain untouched as audit history, while live runbooks and tooling should be reverted.
- The ignored build step should default to building whenever file classification is uncertain.

What the user corrected afterward:

- They rejected the multi-project interpretation and asked to keep all deployments inside `proofound-platform`.

Improvements next time:

- Confirm whether “split deployment” means release batching or multi-project routing before creating infrastructure.
- Run a quick semantic check against existing dirty worktree files earlier so unrelated test failures can be isolated sooner.
- Prefer safe build-skip logic that is explicit about runtime-critical files before wiring it into Vercel.

Commands run + outcomes:

- `node ./scripts/vercel-should-build.mjs --changed-files project/example.md` -> PASS, exit `0`.
- `node ./scripts/vercel-should-build.mjs --changed-files docs/example.md` -> PASS, exit `0`.
- `node ./scripts/vercel-should-build.mjs --changed-files src/app/page.tsx` -> PASS, exit `1`.
- `node ./scripts/vercel-should-build.mjs --changed-files next.config.js` -> PASS, exit `1`.
- `node ./scripts/vercel-should-build.mjs --base does-not-exist` -> PASS, exit `1` by safe default.
- `npm run lint` -> PASS with the existing two `no-img-element` warnings.
- `npm run typecheck` -> PASS.
- `npm run docs:freshness` -> PASS in warning mode with the existing orphan-doc warnings.
- `npm run test` -> FAIL, one unrelated failure in `tests/lib/python-cv-proxy.test.ts`.
- `VERCEL=1 npm run build` -> PASS.
- Vercel API delete for `proofound-platform-api` -> PASS, HTTP `204`.

Open TODOs / follow-ups:

- Push the single-project release-batching commit to `master`.
- Manually test the `Prepare Release Candidate` workflow with real cherry-pick SHAs in GitHub.
- Confirm Vercel honors the ignored build step on a docs-only push after this change lands.
