# Session Log Entry

- Date/time (UTC): 2026-03-06T17:03:33Z
- Branch: master
- Base commit: 5165daf5
  Task summary:
- Restored legacy individual login behavior after a production regression from using `handle` as an auth/onboarding gate.
- Re-aligned onboarding compatibility checks with the existing individual completion model and added focused regressions.

What worked:

- Tracing `resolveUserHomePath()` and `src/app/onboarding/page.tsx` against `src/lib/profile/completion-flow.server.ts` exposed the repo-level mismatch quickly.
- Focused route-level testing on `src/app/onboarding/page.tsx` locked the exact regression without needing E2E coverage.
- Running `build` before re-running `typecheck` refreshed `.next/types` and avoided false negatives from stale generated files.

What failed / wrong assumptions:

- The earlier fix assumed `handle` was the product’s onboarding-complete signal for individual users.
- That assumption conflicted with the repo’s actual completion model, which is based on core profile completeness rather than public handle availability.

User corrections:

- Onboarding had already happened for the affected account, so login should go to the dashboard, not onboarding.
- `handle` was not part of the previous working version for that account.

Assumptions taken without asking:

- Legacy individual users without handles are valid and should regain dashboard access after login.
- Keeping `handle` checks on public-portfolio-only routes is still correct.

What the user corrected afterward:

- The deployed change was live on Vercel, so the remaining issue was behavior, not deployment lag.
- The correct target behavior was restoring the pre-regression dashboard flow, not forcing onboarding.

Improvements next time:

- When touching auth in this repo, verify the completion model in `src/lib/profile/completion-flow*.ts` before using a single profile field as a readiness proxy.
- Distinguish login destination rules from feature-specific prerequisites like public portfolio handles before shipping auth redirects.

Commands run + outcomes:

- `git status --short` -> existing unrelated modified and untracked files confirmed before work.
- `npm run test -- tests/lib/auth-request-cache.test.ts tests/routes/onboarding-page.test.ts tests/api/auth-callback-route.test.ts` -> PASS.
- `npm run lint` -> PASS with 2 pre-existing `no-img-element` warnings.
- `npm run build` -> PASS after standard deploy-readiness env warnings.
- `npm run typecheck` -> PASS after refreshing `.next/types` via build.
- `npm run test` -> PASS (`239` files, `1010` tests).
- `npm run test:privacy` -> PASS (`20` tests).
- `npm run test:privacy:extended` -> PASS (`22` tests).

Open TODOs / follow-ups:

- Check whether the `persona === 'unknown'` branch in `src/app/onboarding/page.tsx` needs the same legacy compatibility treatment for no-handle users.
