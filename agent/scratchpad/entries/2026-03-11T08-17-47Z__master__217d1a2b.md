# Session Log Entry

- Date/time (UTC): 2026-03-11T08:17:47Z
- Branch: master
- Base commit: 217d1a2b
  Task summary:
- Added repo-native tooling to bootstrap a dedicated `proofound-platform-api` Vercel project, sync its env vars, and document the API split workflow.
- Created the live Vercel child project, linked it to the GitHub repo, and verified the extracted API app still builds locally.

What worked:

- `scripts/vercel-ensure-project.mjs` successfully created `proofound-platform-api` and set `rootDirectory=apps/api`.
- `scripts/vercel-sync-project-env.mjs` synced the API project env vars from the canonical project after filtering out `NODE_OPTIONS` and `VERCEL_*`.
- A clean temp clone with a normal `.git` directory allowed `vercel git connect` to attach the GitHub repo to the API project.
- `npm run build:api`, `npm run typecheck`, `npm run lint`, and `npm run docs:freshness` all completed successfully for this phase.

What failed / wrong assumptions:

- Updating Git linkage through the Vercel project PATCH endpoint did not work; `gitRepository`, `projectProductionBranch`, and `productionBranch` were all rejected as unsupported update properties.
- `vercel git connect` failed from a git worktree and from a clone whose `origin` remote pointed at a local filesystem path.
- Direct CLI deploys to the child project were queued as production deployments even when `--target preview` was specified, so I canceled those instead of letting them compete with the main queue.

User corrections:

- None.

Assumptions taken without asking:

- The next safe increment after the repo-side API extraction was Vercel project bootstrap, not yet the public `/api/*` rewrite cutover.
- It was acceptable to create the child Vercel project and connect the repo before the product and marketing projects exist.

What the user corrected afterward:

- None.

Improvements next time:

- Use an isolated clone with a GitHub `origin` immediately when a Vercel CLI flow depends on `.git` metadata.
- Treat Vercel production-branch settings as dashboard-only unless the public API clearly documents a writable field.
- Add a scoped smoke path for protected preview deployments using `vercel curl` or a bypass token before attempting plain `curl`.

Commands run + outcomes:

- `npm run vercel:api:ensure` -> PASS after trimming unsupported PATCH fields.
- `npm run vercel:api:sync-env` -> PASS, synced 46 env entries to `proofound-platform-api`.
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx -y vercel@latest link --yes --project proofound-platform-api ...` -> PASS in temp worktree and temp clone.
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx -y vercel@latest git connect --yes ...` -> PASS from temp clone after setting `origin` to `https://github.com/gother111/proofound-platform.git`.
- `npm run build:api` -> PASS.
- `npm run typecheck` -> PASS.
- `npm run lint` -> PASS with the existing two `no-img-element` warnings.
- `npm run docs:freshness` -> PASS in warning mode with the existing orphan-doc warnings.

Open TODOs / follow-ups:

- Change `proofound-platform-api` production branch from `main` to `master` in the Vercel dashboard.
- Add same-domain rewrite routing for `/api/*` only after the API project has a stable promotion path.
- Split the product app next so the main web deployment can stop compiling both `/api/*` and authenticated product routes together.
