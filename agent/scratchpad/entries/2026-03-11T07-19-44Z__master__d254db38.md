# Session Log Entry

- Date/time (UTC): 2026-03-11T07:19:44Z
- Branch: master
- Base commit: d254db38

Task summary:

- Stabilize the current Vercel production build after repeated long-running and OOM production deployments.
- Implement the first phase of the split plan as an immediate hotfix instead of attempting the full multi-app extraction in one live production change.

What worked:

- Inspecting `next.config.js` and the Vercel logs isolated the current pain to webpack filesystem cache behavior and build-time artifact work.
- The existing stale-deployment cleanup changes were already in place and did not need further edits for this phase.

What failed / wrong assumptions:

- The earlier forced no-cache production redeploy was not enough by itself. It bypassed restored cache state but still produced an unacceptably long build.

User corrections:

- The user explicitly asked to divide deployments by parts because the current single deployment path remained too slow.

Assumptions taken without asking:

- It is safer to land the immediate stabilization change first and treat the 3-app split as staged follow-up work rather than force a monorepo rewrite during an active production incident.
- Disabling webpack filesystem cache only on Vercel is an acceptable tradeoff for production stability.

What the user corrected afterward:

- The user rejected the earlier single-path stabilization outcome because the production deployment was still running far longer than normal.

Improvements next time:

- Ship the stabilization patch before triggering any more forced production redeploys.
- Treat architectural split work as staged deploys with separate acceptance gates, not as a single production intervention.

Commands run + outcomes:

- `npx -y vercel@latest ls proofound-platform --token "$VERCEL_TOKEN"`: showed the latest forced production deploy had already failed after 20 minutes.
- `rg -n "sourcemaps|withSentryConfig|disable" next.config.js node_modules/@sentry ...`: confirmed `sourcemaps.disable` is supported by the Sentry webpack plugin.
- `git status --short`: confirmed unrelated working tree changes were still present and should remain untouched.

Open TODOs / follow-ups:

- Verify the new hotfix on Vercel production after push.
- If production is stable, begin the API-app extraction before touching product or marketing routing.
