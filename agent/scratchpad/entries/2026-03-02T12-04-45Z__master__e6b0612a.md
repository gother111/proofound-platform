# Session Log Entry

- Date/time (UTC): 2026-03-02T12:04:45Z
- Branch: master
- Base commit: e6b0612a
  Task summary:
- Investigated repeated Vercel production deploy failures that happened after successful build completion.
- Applied deployment packaging hardening in `.vercelignore` and `vercel.json`.

What worked:

- Reproduced local Node 20 build/lint/typecheck successfully after config updates.
- Identified that deployment source scope and Python function exclusion list were too permissive.

What failed / wrong assumptions:

- Assumed `npm run log:change -- --help` and `npm run log:session -- --help` were non-mutating; both generated entries.
- Local `npx vercel build --prod` without auth did not provide usable parity for the Python function pattern check.
- First `vercel.json` hardening attempt exceeded Vercel validation limit (`excludeFiles` must be <=256 chars), causing deploy hook failure.

User corrections:

- None.

Assumptions taken without asking:

- Reducing deployment payload scope is safe because excluded directories are non-runtime for production execution.
- Expanding Python function excludes to remove `src/**` and other app trees will not impact Python CV endpoint runtime behavior.

What the user corrected afterward:

- None.

Improvements next time:

- Avoid using `--help` on repo log scripts without checking script implementation, because it can create files.
- Keep a small helper to compute tracked files after `.vercelignore` filtering before and after changes.

Commands run + outcomes:

- `git fetch origin && git log --oneline -n 25 origin/master` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS with two existing image warnings)
- `curl -sS https://proofound.io/api/health` (PASS, live commit still behind current master)

Open TODOs / follow-ups:

- Monitor the next Vercel deployment to confirm publish-stage failure is resolved.
- If failure persists, split Python runtime from this Next.js deployment or disable Python endpoint on Vercel.
