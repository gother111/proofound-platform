# Session Log Entry

- Date/time (UTC): 2026-03-08T21:22:46Z
- Branch: master
- Base commit: 3bac1846

Task summary:

- Fixed the post-push Vercel build break from the readiness/onboarding rollout by removing accidental imports to excluded modules and restoring the affected onboarding and matching routes to the approved scope.

What worked:

- Diffing the broken files against base commit `1352ef4e` made it clear which imports and logic came from unrelated workflow/portfolio work.
- Verifying in a temporary clean worktree isolated the deploy fix from unrelated dirty-worktree type errors and confirmed the final build path.

What failed / wrong assumptions:

- Initial local `npm run typecheck` in the main workspace was polluted by unrelated local changes in `src/lib/proof-trust/snapshots.ts`, so it was not a reliable signal for the deploy fix.
- The first clean build attempt used `/usr/local/bin/node` `v16.14.0`; the temp worktree needed an explicit Node 20 path for Next.js build parity.

User corrections:

- None.

Assumptions taken without asking:

- The readiness behavior from the shipped commit should remain intact, and only the accidental dependencies on excluded modules and schema fields should be removed.
- `PRO-131` should receive a follow-up status comment rather than opening a separate Linear issue for the deploy fix.

What the user corrected afterward:

- None.

Improvements next time:

- Before pushing a scoped fix, run `git grep` on the to-be-pushed commit for imports that point to uncommitted or excluded files.
- For deploy-sensitive follow-ups, use a clean worktree earlier instead of relying on the main dirty workspace.

Commands run + outcomes:

- `git status --short` -> confirmed many unrelated local changes in the main workspace.
- `rg -n "review-contract|workflow/service|slug-history|slug-policy" src` -> identified the exact leaking imports.
- `npm run vercel:preflight` -> PASS.
- Clean worktree verification at `2e6110e` + patch: `npm run typecheck` -> PASS.
- Clean worktree prod-like build with Node 20 -> PASS.

Open TODOs / follow-ups:

- Push the deploy-fix commit to `master`.
- Update `PRO-131` with the follow-up commit SHA and clean-build verification details.
