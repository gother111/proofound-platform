# Session Log Entry

- Date/time (UTC): 2026-03-11T07:48:14Z
- Branch: master
- Base commit: 88b9f751

Task summary:

- Implement the next phase of the deployment split by extracting a standalone API app scaffold from the monolith.
- Keep the live production app stable while preparing a separate deployable surface for all `/api/*` routes and Python CV import.

What worked:

- Generating API route wrappers from `src/app/api` avoided a manual migration of 311 handlers.
- The standalone API app typechecks and builds using the existing route modules and shared root code.
- The Python CV import function can be preserved through a thin wrapper that adds the repo root to `sys.path`.

What failed / wrong assumptions:

- A simple `export *` wrapper strategy caused large Next warnings because it made Next probe for missing HTTP method exports.
- Some source routes are already thin alias modules, so the generator had to learn both direct exports and re-exported symbols.

User corrections:

- The user explicitly wanted the deployment path divided into parts because a single long-running deployment was still unacceptable.

Assumptions taken without asking:

- It is better to extract the API app first and leave traffic on the stable monolith until same-domain routing is ready.
- Preparing the API app in-repo without creating the separate Vercel project yet is still valuable and low-risk.

What the user corrected afterward:

- The user pushed for continuing beyond the stabilization hotfix instead of stopping after the production build recovered.

Improvements next time:

- Add a dedicated env-safe verification path for extracted API builds so local output is less noisy when `DATABASE_URL` is intentionally absent.
- Follow this with same-domain API routing before touching product route extraction.

Commands run + outcomes:

- `npm run api:wrappers:sync`: PASS, generated 311 API wrappers.
- `npm run typecheck:api`: PASS.
- `npm run build:api`: PASS after improving wrapper generation for direct exports and source re-exports.
- `npm run lint`: pending in this session while changes are being finalized.
- `npm run typecheck`: pending in this session while changes are being finalized.

Open TODOs / follow-ups:

- Create and link a dedicated Vercel project rooted at `apps/api`.
- Add same-domain rewrites from the primary app to the API app.
- Extract product routes after the API cutover is working.
