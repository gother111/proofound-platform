# Session Log Entry

- Date/time (UTC): 2026-02-27T22:39:05Z
- Branch: master
- Base commit: f39d97de
  Task summary:
- Built and executed a 1000-case CV autosuggestion benchmark with measurable quality metrics.
- Tuned deterministic extractor/matcher logic to raise precision and keep strong recall under free-tier constraints.

What worked:

- Metric-driven debugging (top false-positive skill IDs + candidate examples) quickly isolated matching issues.
- Fixing single-token substring fuzzy boosts removed major false positives (`java`/`javascript`, `jupyter`/`py`).
- Candidate sanitization for noisy experience phrases improved unmapped handling and reduced bad fuzzy mappings.
- Repeated benchmark runs stayed stable.

What failed / wrong assumptions:

- First benchmark design unintentionally included extra detectable skills in text that were absent from expected labels, skewing precision downward.
- Initial matcher thresholds alone were insufficient; logic changes were required.

User corrections:

- None.

Assumptions taken without asking:

- Used a deterministic synthetic-but-diverse 1000-case benchmark because no labeled 1000 real-CV corpus exists in-repo.
- Kept model training/Colab as optional follow-up because deterministic pipeline exceeded requested precision band.

What the user corrected afterward:

- None.

Improvements next time:

- Add a reusable benchmark runner script that exports JSON reports without relying on test logs.
- Add a second benchmark profile with OCR-like noisy text to stress extraction robustness.

Commands run + outcomes:

- `npm run test -- tests/lib/cv-import-suggest-1000-benchmark.test.ts` -> FAIL initially (precision ~0.80), then PASS after tuning (precision ~0.9565, recall ~0.8729, F1 ~0.9128).
- `for i in {1..5}; do npm run test -- tests/lib/cv-import-suggest-1000-benchmark.test.ts; done` -> PASS.
- `npm run test -- tests/lib/cv-import-suggest.test.ts tests/lib/cv-import-suggest-quality.test.ts tests/lib/cv-import-suggest-1000-benchmark.test.ts tests/api/cv-import-suggest-route.test.ts tests/ui/cvjd-auto-suggest.test.tsx` -> PASS.
- `npm run lint` -> PASS (2 pre-existing landing warnings).
- `npm run typecheck` -> PASS.
- `npm run build` -> PASS.
- `npm run test` -> FAIL (pre-existing unrelated impact-story test failures).

Open TODOs / follow-ups:

- Add real anonymized CV evaluation set integration (possible Colab track) for external-validity benchmarking.
- Investigate and fix existing unrelated full-suite failures in `tests/actions/create-impact-story.test.ts` and `tests/actions/profile.test.ts`.
