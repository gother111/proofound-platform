# Session Log Entry

- Date/time (UTC): 2026-03-02T09:27:52Z
- Branch: master
- Base commit: fa220c66
  Task summary:
- Implemented CV wizard reliability hardening for Gemini structured extraction, taxonomy shortlist latency control, and always-on client recovery.
- Verified offline tests and a live Gemini smoke call against local env.

What worked:

- Flattened schema (`skills[]`) removed Gemini schema-depth/state 400 errors.
- Boosted-token retry for invalid JSON resolved truncation failures in live smoke.
- Shortlist timeout/concurrency caps significantly reduced preselection latency.
- Wizard fallback now reuses baseline deterministic payload without duplicate recomputation.
- Metadata timing fields provided visibility (`shortlist_ms`, `gemini_ms`, `total_ms`).

What failed / wrong assumptions:

- Initial flat schema still failed live due `MAX_TOKENS` truncation; required follow-up retry strategy and tighter prompt output constraints.
- One live smoke attempt failed due missing env injection; rerun with dotenv preload fixed it.

User corrections:

- None.

Assumptions taken without asking:

- Kept deterministic fallback authoritative when Gemini fails.
- Added `ai_schema_mode='flat_skills_v1'` as additive metadata for observability.
- Applied new shortlist timeout/concurrency envs with conservative defaults.

What the user corrected afterward:

- None.

Improvements next time:

- Add dedicated unit tests around Gemini invalid JSON retry path (mocked model outputs with fenced/truncated payloads).
- Add route-level metrics export assertions for timing metadata in suggest route as well.

Commands run + outcomes:

- `npm run typecheck` ✅
- `npm run test -- tests/lib/gemini-config.test.ts tests/lib/python-cv-proxy.test.ts tests/api/cv-import-wizard-suggest-route.test.ts tests/api/cv-import-suggest-route.test.ts tests/ui/cv-import-wizard.test.tsx` ✅
- `npm run test:python` ✅
- `npm run lint` ✅ (2 pre-existing warnings in landing image components)
- Live smoke (`suggestSkillsWithGemini`) ✅ success after retry hardening.

Open TODOs / follow-ups:

- Optional: propagate timing metadata normalization into UI-facing metadata cards.
- Optional: add automated perf guard test for shortlist timeout budget behavior.
