# Session Log Entry

- Date/time (UTC): 2026-03-02T12:58:21Z
- Branch: master
- Base commit: 90206924
  Task summary:
- Investigated Vercel compile failure for missing `@/data/zen` and patched deployment ignore rules.

What worked:

- Root-anchored `.vercelignore` patterns prevent accidental exclusion of `src/data/zen.ts`.
- Local Node 20 `build` and `typecheck` both passed after patch.

What failed / wrong assumptions:

- Previous `.vercelignore` hardening was too broad and removed required source files from Vercel build input.

User corrections:

- None.

Assumptions taken without asking:

- Converting ignores to root-anchored paths would keep deployment payload reduction while restoring required source files.
- Removing global extension ignores was safe and preferable to avoid hidden runtime asset losses.

What the user corrected afterward:

- None.

Improvements next time:

- Validate `.vercelignore` changes against known critical import paths (`src/data/**`) before pushing.

Commands run + outcomes:

- `rg --files src | rg 'data/zen|src/data'` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)

Open TODOs / follow-ups:

- Verify latest Vercel deployment succeeds in remote environment.
