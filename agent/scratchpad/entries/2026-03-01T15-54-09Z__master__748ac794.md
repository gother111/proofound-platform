# Session Log Entry

- Date/time (UTC): 2026-03-01T15:54:09Z
- Branch: master
- Base commit: 748ac794
  Task summary:
- Improved Python CV matching robustness for noisy text variants and added 1000-CV deterministic stress test coverage.

What worked:

- Confusable-character normalization and token-variant expansion materially improved recall without changing API contracts.
- The 1000-CV synthetic test completed quickly and remained deterministic across repeated runs.
- Existing UI/API suites remained green after matcher normalization changes.

What failed / wrong assumptions:

- Initial implementation scope was centered on parser replacement only; this pass showed additional normalization improvements were needed for noisy user input.

User corrections:

- None.

Assumptions taken without asking:

- Interpreted “test over 1000 cvs” as deterministic synthetic CV fixtures in automated tests.
- Kept fallback logic and matching architecture zero-LLM and deterministic.

What the user corrected afterward:

- None.

Improvements next time:

- Add optional fixture import path for anonymized real CV corpora (when available) to complement synthetic stress tests.

Commands run + outcomes:

- `source .venv311/bin/activate && PYTHONPATH=. python -m pytest tests/python -q` -> PASS (15 passed)
- `npm run test -- tests/ui/cvjd-auto-suggest.test.tsx tests/ui/cv-import-wizard.test.tsx tests/api/cv-import-wizard-suggest-route.test.ts tests/api/cv-import-suggest-route.test.ts tests/lib/cv-import-wizard-extractor.test.ts tests/lib/cv-import-wizard-quality.test.ts tests/lib/pdf-client-extractor.test.ts` -> PASS
- `npm run typecheck` -> PASS
- `npm run lint` -> PASS (2 pre-existing `no-img-element` warnings)
- `npm run build 2>&1 | tee /tmp/pro100-build.log` -> PASS
- `rg -q "Critical dependency: the request of a dependency is an expression" /tmp/pro100-build.log` -> NOT_FOUND

Open TODOs / follow-ups:

- Run manual multi-browser production smoke with real CV PDFs and review parser/matching telemetry after deploy.
