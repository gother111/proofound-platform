# Session Log Entry

- Date/time (UTC): 2026-03-01T20:24:16Z
- Branch: master
- Base commit: 27e5c851
  Task summary:
- Investigated Vercel production build failure for Python CV import runtime and applied deployment-scope fixes to avoid Lambda bundle-size overflow.

What worked:

- Root cause from logs was reproducible in deployment inspect output: Python function bundling exceeded Lambda limit.
- Tightening function `excludeFiles` and runtime dependency list was straightforward and low risk for the active API surface.
- Added `.vercelignore` to prevent local CLI deploy drift from build artifacts and local env folders.

What failed / wrong assumptions:

- Local `npx vercel build --prod` used uv metadata and failed on local Python toolchain constraints; this was not representative of Vercel’s production Python environment.

User corrections:

- None.

Assumptions taken without asking:

- Runtime API path does not require `pandas`, `pytest`, or `psycopg` in production dependencies.
- It is safe to scope Python function bundle away from repo directories unrelated to `api/python/cv_import.py` and `python_cv/data/skills_db.json`.

What the user corrected afterward:

- None.

Improvements next time:

- Avoid running local `vercel build` in dirty worktrees to prevent generated uv metadata from contaminating CLI deployment context.

Commands run + outcomes:

- `du`, `find`, `git ls-tree` inspections -> identified large source/dependency surfaces and potential bundle contributors.
- `npx vercel deploy --prod --yes --token ...` -> produced deployment logs confirming bundle/packaging failures before patch.
- `npx vercel inspect <deployment> --logs --wait` -> captured definitive failure signature and Python packaging behavior.
- `npm run typecheck` -> fail due unrelated in-progress local files outside this patch scope.
- `npm run lint` -> pass with two pre-existing landing warnings.

Open TODOs / follow-ups:

- Push this patch and validate next production deploy logs are free of Lambda bundle-size overflow errors.
- Re-run authenticated CV analyze smoke once deployment is healthy.
