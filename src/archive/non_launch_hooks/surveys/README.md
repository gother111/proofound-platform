> Doc Class: `historical`
> Last Verified: `2026-05-20`

# Archived SUS Survey Hook

The SUS survey hook is outside the locked launch MVP corridor because it checks
retired `/api/surveys/sus*` eligibility routes and can reopen survey UI. Active
launch flows should use corridor-specific readiness, privacy, verification, and
assignment feedback surfaces instead.
