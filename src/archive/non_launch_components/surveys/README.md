> Doc Class: `historical`
> Last Verified: `2026-05-20`

# Archived SUS Survey Components

System Usability Scale survey prompts are outside the locked launch MVP corridor.
The launch app must not show SUS prompt dialogs or call `/api/surveys/sus*`
endpoints from active UI.

These components are preserved only as historical context for the retired survey
prompt flow.
