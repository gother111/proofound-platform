# Archived Profile Components

This folder preserves non-launch profile components that are outside the locked MVP corridor.

- `PublicSnippetView.archived.tsx` belonged to the legacy `/p/[token]` public snippet surface. The launch public surface is the Public Page / organization trust-page corridor, and route policy keeps legacy snippet pages archived.
