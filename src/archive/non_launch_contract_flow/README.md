# Archived Contract Flow Code

The old contract-signing email template and related launch-inactive contract helpers live here as historical implementation context.

Contract signing is outside the locked MVP corridor. Active launch workflows should use assignment, review, intro, reveal, interview, decision, and engagement verification surfaces instead of `/app/contracts/*` or contract-signed messaging.
