# Archived CV Import Wizard Implementation

The retired CV import wizard implementation lives here as historical implementation context.

The locked MVP launch corridor keeps `/api/expertise/cv-import/wizard-*` available only as archived `410` compatibility handlers. Active import/proof work belongs to the approved Start from CV and Proof Artifact Text Extraction paths.

Do not move these modules back into active `src/lib/expertise` unless the locked MVP source of truth explicitly reopens the old wizard route family.
