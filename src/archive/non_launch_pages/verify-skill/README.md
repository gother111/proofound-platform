> Doc Class: `historical`
> Last Verified: `2026-05-20`

# Archived Verify Skill Page

`/verify-skill` is outside the locked launch MVP corridor. Current verifier links use
`/verify/[token]` or `/verify/custom/[token]`, backed by the active verification request
APIs and Proof Pack consent model.

The old `/verify-skill` implementation called the archived
`/api/verification/skill/respond` endpoint and is preserved only as historical context.
