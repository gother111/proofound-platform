# Archived Fairness Settings Page

`/app/i/settings/fairness` is archived outside the locked MVP corridor. The old client and demographic opt-in component are preserved in `implementation/` only for historical context.

Active privacy settings should stay focused on consent, visibility, export/delete, auditability, and reveal/privacy behavior that is part of the proof-first MVP corridor.
