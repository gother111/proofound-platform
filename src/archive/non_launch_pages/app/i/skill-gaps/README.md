# Archived Skill Gaps Page

`/app/i/skill-gaps` is archived outside the locked MVP corridor. The active route
is removed from `src/app`, and the preserved page plus old client implementation
remain here only as historical context for the retired Expertise Atlas and skill
gap dashboard.

Active launch work must stay on Proof Packs, proof-backed skill selection,
assignment expertise mapping, verification requests, portfolio publishing, and
privacy-safe review. Do not restore this page or its `/api/skill-gaps*` calls
without an explicit route-surface policy change.
