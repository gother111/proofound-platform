# Archived Expertise Atlas Page

`/app/i/expertise` is archived outside the locked MVP corridor. The route handler is removed from active `src/app`, and the old UI implementation is preserved in `implementation/` only for historical context.

Active individual proof work belongs in onboarding, Proof Packs, portfolio publishing, verification requests, privacy settings, and the proof-first review corridor. Do not restore this page or its standalone skills UI without a source-of-truth update.
