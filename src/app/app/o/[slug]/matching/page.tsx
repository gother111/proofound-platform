import { DeferredOrgMatchingClient } from './DeferredOrgMatchingClient';

export const dynamic = 'force-dynamic';

export default function OrgMatchingPage() {
  return <DeferredOrgMatchingClient />;
}
