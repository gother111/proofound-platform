#!/usr/bin/env sh

# Run lint-staged directly to avoid npm/npx permission issues and husky shim deprecation.
if [ -x "./node_modules/.bin/lint-staged" ]; then
  ./node_modules/.bin/lint-staged
else
  echo "lint-staged not installed; skipping pre-commit checks"
fi

