# Archived platform health script

Archived: `2026-05-19`

`check-platform-health.mjs` is retained as historical launch-readiness tooling only.

It is not active launch evidence because it checks old "critical gaps", demo-data counts, `fairness_reports`, native Zoom integration wrappers, and broad matching-profile UI files that no longer define the locked MVP corridor. Current launch evidence lives in route-surface policy tests, launch-status checks, docs freshness, focused API/UI tests, and Browser/Playwright smoke evidence.

Do not restore this script unless it is rewritten against the locked MVP source of truth and current route/API inventory.
