> Doc Class: `historical`
> Last Verified: `2026-05-19`

# Legacy Vercel Deploy Trigger

This timestamp file is preserved only as historical deployment context:

```
Deployment triggered Fri Nov 7 20:46:35 CET 2025
```

Do not use this file to trigger or prove current MVP launch deployment. Current production
deploys use the prebuilt GitHub Actions workflow in
`.github/workflows/retry-vercel-deploy.yml` with `VERCEL_TOKEN`, `VERCEL_ORG_ID`, and
`VERCEL_PROJECT_ID`.

Current launch evidence belongs in the release checklist, production-readiness checklist,
GitHub Actions summaries, Vercel deployment metadata, and the active sweep artifact.
