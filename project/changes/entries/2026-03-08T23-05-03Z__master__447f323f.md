# Project Change Entry

- Date/time (UTC): 2026-03-08T23:05:03Z
- Branch: master
- Base commit: 447f323f
  What changed:
- Added `PRD_TECHNICAL_REQUIREMENTS.md` Section 7 as the canonical Block 12 launch appendix for security, auth, storage, privacy, accessibility, reliability, abuse controls, and spec reconciliation.
- Updated `README.md` and `project/Architecture.md` to point to the canonical launch contract and summarize the launch auth, publication, and storage posture.
- Added launch-contract deference notes to `LAUNCH_RUNBOOK.md`, `PRODUCTION_CHECKLIST.md`, `docs/structured-logging.md`, and `docs/monitoring-alerting.md`, and standardized recovery targets to RTO 8h / RPO 24h.

Why:

- Launch-facing docs had conflicting stack, auth, CSRF, logging, locale, and recovery guidance that was too contradictory to implement or operate safely.
- The repo needed one implementation-ready launch contract that matches the current code and archives older speculative architecture choices.

How to verify:

- `npm run docs:freshness`
- `npm run lint`
- `npm run typecheck`
- Review `PRD_TECHNICAL_REQUIREMENTS.md` Section 7 and confirm the active docs above defer to it instead of restating conflicting launch assumptions.

Open risks / TODO:

- `npm run docs:freshness` still warns about missing freshness metadata near the top of `project/Documentation.md`; that predates this change.
- `npm run typecheck` currently fails on unrelated existing repo errors outside this doc-only scope and should be addressed separately before a clean launch gate.
