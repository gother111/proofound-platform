# Project Change Entry

- Date/time (UTC): 2026-03-02T01:53:27Z
- Branch: master
- Base commit: a88b98fa
  What changed:
- Updated `e2e/strict/organization.strict.spec.ts` to satisfy strict quality guard rules.
- Removed forbidden URL-only assertion (`toHaveURL`) in org home check.
- Kept robust home readiness check using `page.waitForLoadState('domcontentloaded')` + `main` visibility.

Why:

- `Strict Quality` run `22557963441` failed before suites with guard violation (`URL-only assertion helper` pattern).
- The test needed to remain resilient while complying with enforced strict-quality linting rules.

How to verify:

- `npm run test:strict:quality` (PASS)
- `npm run test:e2e:org:strict` (PASS, `6 passed`)
- `gh run view 22557963441 --log-failed` (triage evidence before patch)

Open risks / TODO:

- Retry deploy workflow still fails when `VERCEL_DEPLOY_HOOK_URL` is unset.
