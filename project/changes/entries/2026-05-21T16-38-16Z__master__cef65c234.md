# Project Change Entry

- Date/time (UTC): 2026-05-21T16:38:16Z
- Branch: master
- Base commit: cef65c234
  What changed:
- Removed the active `SKIP_GO_NOGO=1` bypass from `scripts/go-no-go-check.ts` so the launch go/no-go command cannot exit successfully without executing the gate checks.
- Added a launch-gate guardrail test that keeps the bypass out of active go/no-go and strict-gate runner code while preserving the archived legacy script as history.

Why:

- The final launch gate must fail closed. A success-exit environment bypass can turn missing or stale evidence into a false green signal.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Production-candidate backup, restore, perf-status, launch-status, and final go/no-go evidence still need to be rerun against the intended target before launch readiness can be claimed.
