# Project Change Entry

- Date/time (UTC): 2026-02-25T10:16:08Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Fixed PRO-37 proof UX reliability for Individual Expertise flow.
- Mounted legacy toast renderer in root layout so `useToast` notifications are visible again.
- Updated edit-skill proof flow to:
- require either title or URL with explicit validation feedback,
- refresh parent skill state after proof add/delete,
- keep proof title deterministic when URL-only input is provided.
- Updated add-skill proof attachment flow to:
- surface proof-attach errors to users,
- preserve successful skill creation even when proof attach fails,
- increment optimistic `proof_count` when proof attach succeeds.
- Tightened proof API validation and response messaging:
- allow URL-only payloads with deterministic fallback title,
- return explicit validation `message`,
- include backend error message on failed insert.
- Added focused regression tests:
- `tests/api/expertise-user-skill-proofs-route.test.ts`
- `tests/ui/edit-skill-window-proofs.test.tsx`

Why:

- Users reported clicking Add proof produced no visible feedback and no visible proof state change.
- The flow required clearer validation, visible notifications, and state refresh after proof mutations.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/expertise-user-skill-proofs-route.test.ts tests/ui/edit-skill-window-proofs.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test` (FAIL - unrelated pre-existing failures in `tests/ui/public-org-portfolio-page.test.tsx` due Next request scope cookies error)

Open risks / TODO:

- Root cause for failing `tests/ui/public-org-portfolio-page.test.tsx` should be addressed separately (outside PRO-37 scope).
- Repo currently runs with mixed toast systems (`sonner` and legacy `useToast` renderer); consider migration to a single toast system.
