# Project Change Entry

- Date/time (UTC): 2026-06-19T14:55:00Z
- Branch: master
- Base commit: b153c9cab
  What changed:
- Reworked the top Privacy Overview card into a real icon/content grid.
- Removed the old left-padding alignment shim and kept the title, explanation, and actions on one shared content spine.
- Updated the Privacy Overview test to protect the new alignment structure.

Why:

- The privacy controls tile looked visually unbalanced because the shield icon was paired only with the heading while the rest of the card content sat below it. The icon now balances against the whole privacy controls content cluster on desktop and stays aligned with the heading on mobile.

How to verify:

- Browser plugin on `/app/i/settings/privacy`: desktop 1280x720 geometry showed icon center equals content cluster center, no console warnings or horizontal overflow.
- Browser plugin on `/app/i/settings/privacy`: mobile 390x844 showed no horizontal overflow and all primary privacy actions remained 44px tall.
- `npm test -- tests/ui/privacy-overview-copy.test.tsx`
- `npm test -- tests/ui/privacy-overview-copy.test.tsx tests/ui/settings-privacy-visibility-placement.test.tsx tests/ui/privacy-audit-log-mobile-clarity.test.tsx tests/ui/privacy-data-breakdown.test.tsx tests/ui/field-visibility-controls.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Browser screenshot capture timed out twice, so rendered verification used Browser DOM, geometry, viewport, and console evidence instead of screenshot output.
- `npm run docs:freshness` still reports pre-existing orphan artifact warnings in warning mode.
