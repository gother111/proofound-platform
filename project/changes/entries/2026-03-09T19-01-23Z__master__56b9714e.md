# Project Change Entry

- Date/time (UTC): 2026-03-09T19:01:23Z
- Branch: master
- Base commit: 56b9714e

What changed:

- Added schema-compatibility helpers for missing relations and combined schema drift checks in `src/lib/db/schemaCompatibility.ts`.
- Wrapped dynamic portfolio sitemap queries in `src/app/sitemap.ts` so known migration drift falls back to static sitemap entries with a structured warning instead of failing the build.
- Added `src/lib/__tests__/schema-compatibility.test.ts` to cover missing column, missing relation, and non-compatibility error cases.

Why:

- Vercel production builds were failing during `/sitemap.xml` prerender when the target database did not yet expose the expected `profiles` relation or `public_portfolio_state` contract.
- Sitemap generation can degrade safely by omitting dynamic portfolio URLs until schema parity is restored.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- src/lib/__tests__/schema-compatibility.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`

Open risks / TODO:

- When schema drift happens, dynamic portfolio URLs are intentionally absent from `/sitemap.xml` until migrations are applied.
- Local verification still emitted pre-existing `no-img-element` lint warnings in landing components, but the build completed successfully.
