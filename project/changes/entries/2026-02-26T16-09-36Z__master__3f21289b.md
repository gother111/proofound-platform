# Project Change Entry

- Date/time (UTC): 2026-02-26T16:09:36Z
- Branch: master
- Base commit: 3f21289b
  What changed:
- Updated LinkedIn OAuth setup documentation to remove ambiguity around required callback URIs and reduce `redirect_uri` mismatch risk:
  - `docs/LINKEDIN_VERIFICATION_SETUP.md`
  - `docs/ENV_VARIABLES.md`
- Documented the exact callback split that must be present in the LinkedIn app:
  - App-managed callback (`/api/auth/linkedin/callback`)
  - Supabase social-auth callback (`/auth/v1/callback`)
- Added explicit guidance for optional local and additional live-domain callback entries when those domains can initiate `/api/auth/linkedin`.

Why:

- PRO-30 reported provider-side OAuth failure (`The redirect_uri does not match the registered value`).
- Existing docs mentioned callback split, but setup steps did not make the mandatory dual-callback requirement explicit enough for reliable operations.

How to verify:

- `npm run test -- tests/api/linkedin-oauth-redirects.test.ts src/lib/integrations/__tests__/oauth-helpers.test.ts tests/lib/linkedin-redirect-uri.test.ts` (PASS)
- `npm run docs:freshness` (PASS)
- `npm run lint` (PASS with pre-existing warnings in landing components only)
- `npm run typecheck` (PASS)
- Manual provider-console verification:
  - LinkedIn app includes `https://proofound.io/api/auth/linkedin/callback`
  - LinkedIn app includes `https://<supabase-project-ref>.supabase.co/auth/v1/callback`

Open risks / TODO:

- If any additional live domain (for example demo/staging) can initiate `/api/auth/linkedin`, its `/api/auth/linkedin/callback` must also be allowlisted in LinkedIn.
- Local `.env.local` in this workspace currently has empty `LINKEDIN_REDIRECT_URI`; production value must be confirmed in deployment env settings.
