# Project Change Entry

- Date/time (UTC): 2026-03-01T15:54:08Z
- Branch: master
- Base commit: 748ac794
  What changed:
- Hardened skill matching normalization in `python_cv/text_normalize.py` with:
  - cross-script confusable character mapping (Cyrillic/Greek lookalikes),
  - token variant expansion for punctuation/compact forms,
  - controlled leet-style normalization variants.
- Upgraded matcher behavior in `python_cv/skill_matcher.py`:
  - stricter short-token fuzzy thresholds,
  - multi-variant exact/synonym/fuzzy lookups,
  - bounded fuzzy variant fan-out for deterministic performance.
- Expanded skill DB term indexing in `python_cv/skill_db.py` to index all normalization variants.
- Improved per-document parser failure classification in `api/python/cv_import.py`:
  - added per-document `parse_error_code`,
  - added clearer messages for encrypted/invalid PDFs.
- Added regression coverage:
  - `tests/python/test_text_normalize.py`
  - `tests/python/test_bulk_cv_robustness.py` (1000 synthetic CV documents)
  - extended `tests/python/test_skill_matcher.py` for confusable + leet inputs.

Why:

- Increase matching recall on noisy CV text inputs (mixed scripts, casing, punctuation, and digit-substituted tokens).
- Ensure parser errors remain actionable and machine-classifiable at document level.
- Add scale-oriented confidence by validating deterministic behavior and high mapping success on 1000 varied CV-like fixtures.

How to verify:

- `source .venv311/bin/activate && PYTHONPATH=. python -m pytest tests/python -q`
- `npm run test -- tests/ui/cvjd-auto-suggest.test.tsx tests/ui/cv-import-wizard.test.tsx tests/api/cv-import-wizard-suggest-route.test.ts tests/api/cv-import-suggest-route.test.ts tests/lib/cv-import-wizard-extractor.test.ts tests/lib/cv-import-wizard-quality.test.ts tests/lib/pdf-client-extractor.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run build 2>&1 | tee /tmp/pro100-build.log`
- `! rg -q "Critical dependency: the request of a dependency is an expression" /tmp/pro100-build.log`

Open risks / TODO:

- The 1000-CV validation uses deterministic synthetic fixtures, not real user CV corpora; keep manual browser smoke and production telemetry review in the release checklist.
