# Project Change Entry

- Date/time (UTC): 2026-05-16T10:52:34Z
- Branch: master
- Base commit: 12cd0775
  What changed:
- Added Codex in-app Browser evidence for the mock-safe Start from CV onboarding dialog and individual verifications page.
- Refreshed `.artifacts/ai-provider-smoke.json` so the tracked provider-smoke artifact now fails closed for `gemini-3.1-flash-lite` instead of showing stale success for `gemini-3.1-flash-lite-preview`.
- Updated the AI verification report to remove the Browser-backend blocker and keep live Gemini provider smoke as the remaining unverified item.

Why:

- The previous report was correct when Browser was unavailable, but the bundled Browser plugin could be used after reconnecting.
- The stale successful preview-model smoke artifact was no longer accepted by launch code, but it was still misleading as saved evidence.

How to verify:

- `npm run ai:provider:smoke` (expected fail-closed locally without `DATABASE_URL`, server-only provider credentials, and AI spend-cap env)
- Codex in-app Browser: open `http://127.0.0.1:33172/onboarding?next=/candidate-invite/local-ai-browser-check` and `http://127.0.0.1:33172/app/i/verifications` against a mock-safe dev server.
- `npm run docs:freshness`

Open risks / TODO:

- Live Gemini provider smoke remains unverified until `DATABASE_URL`, server-only provider credentials, and AI spend-cap env are available.
- Production-only provider/account state was not touched.
