# Project Change Entry

- Date/time (UTC): 2026-03-10T16:32:46Z
- Branch: master
- Base commit: 0a22dfb7
  What changed:
- Added a shared public trust-layer reader for individual and organization portfolios in `src/lib/portfolio/public-projection.ts`.
- Added shared public cache invalidation helpers in `src/lib/portfolio/public-invalidation.ts`.
- Rewired canonical public pages, export/summary consumers, sitemap eligibility, and publication-state computation to use the shared projection.
- Hardened proof visibility so public counts and summaries only include currently public proof and never expose hidden child artifacts, reviewer/internal notes, reveal-only metadata, or verifier identity beyond coarse labels.
- Added lower-level invalidation hooks in canonical proof, workflow, and account-deletion lifecycle code so public projections refresh even when proof trust changes outside the narrow API handlers.
- Added focused tests for noindex defaults, explicit index opt-in, public-safe proof summaries, withdrawn proof removal, invalidation hooks, and safe proof rendering.

Why:

- The canonical public portfolio surface had drifted across page rendering, metadata, export, and sitemap codepaths.
- Legacy heuristics still allowed proof summaries and counts to consider non-public or reveal-gated records.
- Public publication and indexing policy needed one source of truth so link-only default publication and opt-in indexing behave consistently for both individuals and organizations.

How to verify:

- `npm run typecheck`
- `npm run lint`
- `npx vitest run tests/ui/public-portfolio-page.test.tsx tests/ui/public-org-portfolio-page.test.tsx tests/lib/public-profile-metadata.test.ts tests/lib/public-portfolio-projection.test.ts tests/api/organization-visibility-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/api/expertise-user-skill-proof-delete-route.test.ts tests/api/public-portfolio-summary-route.test.ts tests/api/public-portfolio-export-route.test.ts tests/api/portfolio-view-route.test.ts`
- `npx vitest run tests/lib/lifecycle-reconciliation.test.ts tests/api/expertise-user-skill-proof-delete-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/lib/public-portfolio-projection.test.ts tests/ui/public-portfolio-page.test.tsx tests/api/organization-visibility-route.test.ts`
- `npm run test` still reports unrelated pre-existing failures in non-portfolio suites. The focused public-portfolio coverage above passes.

Open risks / TODO:

- External crawler and social preview caches can retain previously fetched previews briefly after Proofound invalidates its own responses.
- Handle and slug rename flows still depend on mutation callers to revalidate retired paths; if additional rename entrypoints exist, they should also invoke the new public invalidation helper.
- The repo currently has unrelated failing suites outside this trust-layer work, so global `npm run test` is not yet green.
