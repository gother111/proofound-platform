# Project Change Entry

- Date/time (UTC): 2026-03-16T16:40:06Z
- Branch: master
- Base commit: 0a509b0c
  What changed:
- Added `getCanonicalActiveOrgMembership()` in `src/lib/api/auth.ts` so legacy role alias normalization stays at the API auth boundary.
- Updated active org launch routes and pages to consume canonical org roles directly instead of re-normalizing raw membership rows inline.
- Tightened `src/actions/org.ts` so invitation acceptance canonicalizes persisted invitation roles once and then reuses the canonical value.
- Updated focused tests to use canonical launch-role fixtures and added a boundary test for persisted legacy aliases.

Why:

- The hard audit flagged residual compatibility drift because active launch paths still depended on legacy role normalization in multiple places.
- This change keeps canonical roles primary in launch behavior while preserving compatibility only for historical persisted data.

How to verify:

- `npm run test -- tests/lib/authz-policy.test.ts tests/lib/membership-normalization.test.ts tests/actions/org-invitations.test.ts tests/actions/org-canonical-membership.test.ts tests/api/organizations-route.test.ts tests/api/organization-assignments-route.test.ts tests/api/org-candidate-invites-route.test.ts tests/api/portfolio-org-export-route.test.ts`
- `npm run test -- tests/lib/authz-policy.test.ts tests/lib/membership-normalization.test.ts tests/lib/api-auth.test.ts tests/actions/org-invitations.test.ts tests/actions/org-canonical-membership.test.ts tests/api/organizations-route.test.ts tests/api/organization-assignments-route.test.ts tests/api/org-candidate-invites-route.test.ts tests/api/portfolio-org-export-route.test.ts tests/ui/organization-trust-profile-page.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run build`

Open risks / TODO:

- Historical persisted org membership and invitation rows may still contain legacy aliases; this change intentionally does not backfill storage.
- Non-launch org/dashboard surfaces still contain broader legacy drift and were left untouched for this task.
