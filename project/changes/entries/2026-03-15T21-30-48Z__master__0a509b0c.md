# Project Change Entry

- Date/time (UTC): 2026-03-15T21:30:48Z
- Branch: master
- Base commit: 0a509b0c
  What changed:
- Added a shared `seed:public-org-trust-fixture` path that upserts a narrow `proofound-labs` organization trust fixture with `public_noindex`, one active assignment, and MVP-safe visibility.
- Added `e2e/public-org-trust.smoke.spec.ts` plus `test:e2e:org-trust:smoke` to prove the live public org trust route returns `200` and excludes non-MVP org-suite surfaces.
- Added a local/staging-only launch monitor for `/portfolio/org/proofound-labs`, updated launch contract tests, and documented the seed-plus-smoke verification flow in active docs/checklists.
- Tightened public org trust page tests to assert required trust-card sections and absence of excluded headings.

Why:

- The 2026-03-15 audit left public org trust runtime `UNVERIFIED` because no deterministic seeded org slug was reachable in local runtime.
- MVP launch verification needs one stable public org trust URL without reintroducing broader org profile, analytics, or identity-bearing surfaces.

How to verify:

- `npm run seed:public-org-trust-fixture`
- `npm run test -- tests/ui/public-org-portfolio-page.test.tsx tests/lib/launch-hardening-contract.test.ts tests/lib/launch-synthetic-monitors.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts`
- `npm run typecheck`
- Start the app with real Supabase env, then run `BASE_URL=http://127.0.0.1:<port> npm run test:e2e:org-trust:smoke`
- Refresh smoke evidence with `BASE_URL=http://127.0.0.1:<port> npm run test:launch:smoke`
- Run the dotenv-backed launch monitor sweep against the same port and confirm `public_org_trust_page` passes in `/api/monitoring/launch-status`

Open risks / TODO:

- The seeded org trust monitor is intentionally disabled in `VERCEL_ENV=production`; production does not currently guarantee a `proofound-labs` fixture.
- Local runtime proof hit slow cold-start timings on `/portfolio/org/proofound-labs`, so future work may still want a lighter monitor path or a warmed-route strategy.
