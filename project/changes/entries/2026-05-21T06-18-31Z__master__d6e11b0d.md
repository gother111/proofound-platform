# Project Change Entry

- Date/time (UTC): 2026-05-21T06:18:31Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Reframed the preserved project specification exclusion from `public candidate search index` to `public people search index`.
- Strengthened the preserved-project-spec launch-gate guard so the people-search exclusion is required and the stale candidate-search phrase cannot return.

Why:

- This keeps the root reference spec aligned with the current assignment-review vocabulary while preserving the same MVP non-goal.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "preserved project specification" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- Targeted `public candidate search index` / `public people search index` scan.
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Remaining candidate/hiring terms now skew heavily toward explicit prohibitions, archived/audit material, historical change logs, migration/database names, or narrowly scoped field identifiers. Continue classifying them individually before editing.
