# Project Change Entry

- Date/time (UTC): 2026-02-28T21:36:31Z
- Branch: master
- Base commit: 4041e20f
  What changed:
- Added Google schedule failure classification helper at `src/lib/interviews/schedule-errors.ts` to normalize `invalid_grant`, scope mismatch, verification-blocked, and calendar-access-blocked errors into actionable codes/messages.
- Updated `src/app/api/interviews/schedule/route.ts` to split provider flow by step (`integration_lookup`, `token_refresh`, `meeting_create`, `interview_insert`), return structured `400` payloads for known Google failures, auto-disconnect revoked Google integrations on `invalid_grant`, and emit structured `interview.schedule.failed` logs.
- Updated `src/components/interviews/ScheduleInterviewModal.tsx` to surface backend `message` first, then fallback to `error`, with reconnect guidance for `GOOGLE_RECONNECT_REQUIRED`.
- Replaced/extended tests:
  - `tests/api/interviews-schedule-route.test.ts`
  - `tests/ui/schedule-interview-modal.test.tsx`

Why:

- Users were seeing opaque `"Failed to schedule interview"` errors even when root cause was a known Google auth/scope state.
- The schedule flow needed explicit guidance and self-healing behavior (disconnect stale revoked tokens) so UI state matches provider reality.

How to verify:

- `npm run test -- tests/api/interviews-schedule-route.test.ts tests/ui/schedule-interview-modal.test.tsx tests/api/interviews-edit-route.test.ts tests/api/interviews-cancel-route.test.ts tests/ui/organization-interviews-page-actions.test.tsx` (PASS)
- `npm run lint` (PASS with 2 unrelated existing warnings in landing components)
- `npm run typecheck` (PASS)

Open risks / TODO:

- `ScheduleInterviewDialog`, `InterviewScheduler`, and `useInterviewScheduling` still prioritize `error` text only; they do not yet consume the new `message` field.
- Production validation should include a stale/revoked Google integration smoke check to confirm auto-disconnect and actionable UX in the live environment.
