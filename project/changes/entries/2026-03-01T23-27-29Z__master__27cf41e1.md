# Project Change Entry

- Date/time (UTC): 2026-03-01T23:27:29Z
- Branch: master
- Base commit: 27cf41e1

What changed:

- Fixed CI-only failing regex assertion in `tests/actions/create-impact-story.test.ts`.
- Updated URL-match pattern to correctly treat `\s` as whitespace class inside the regex character class.

Why:

- GitHub `CI` failed on `master` because the previous regex accidentally excluded the letter `s`, causing mismatch on `http://localhost:3000/...` URLs used in CI.

How to verify:

- `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 20.20.0`
- `npm run test -- tests/actions/create-impact-story.test.ts`
- `npm run test`
- `npm run lint`
- `npm run typecheck`

Open risks / TODO:

- None specific to this patch; this is a narrow test-only fix.
