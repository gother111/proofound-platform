# Project Change Entry

- Date/time (UTC): 2026-03-01T23:27:29Z
- Branch: master
- Base commit: 27cf41e1

What changed:

- Finalized CI-only failing URL assertion in `tests/actions/create-impact-story.test.ts`.
- Replaced fragile whitespace-escaped pattern with a simpler verification-link matcher:
  - from `toMatch(/https?:\\/\\/[^...\\s]+\\/verify\\/[a-f0-9]{64}/)`
  - to extracting `verificationUrl` via `/https?:\\/\\/[^"']+\\/verify\\/[a-f0-9]{64}/` and asserting it is defined.

Why:

- GitHub CI on `master` still failed on localhost-based rendered email HTML because the previous regex remained overly escape-sensitive.

How to verify:

- `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 20.20.0`
- `npm run test -- tests/actions/create-impact-story.test.ts`
- `npm run test`
- `npm run lint`
- `npm run typecheck`

Open risks / TODO:

- None specific to this patch; test-only robustness update.
