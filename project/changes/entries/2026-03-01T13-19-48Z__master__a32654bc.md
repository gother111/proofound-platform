# Project Change Entry

- Date/time (UTC): 2026-03-01T13:19:48Z
- Branch: master
- Base commit: a32654bc
  What changed:
- Hardened PDF parser init in both CV import UIs by trying `pdfjs-dist/webpack.mjs` first and falling back to `pdfjs-dist/build/pdf.mjs`.
- Added wizard-level partial-success UX in `CvImportWizard` when entity extraction succeeds but skill suggestion dependencies are unavailable.
- Made `suggestWizardForDocuments` resilient to skill-suggestion dependency failures by returning extracted entities with empty skill candidates and fallback metadata.
- Added regression coverage for dependency-failure fallback in extractor tests and parser-init/toast behavior in UI tests.

Why:

- Users still experienced failures in the PDF CV flow due to parser-init/runtime dependency issues.
- The flow needed to preserve extracted CV entities instead of failing the full wizard when downstream suggestion dependencies degraded.

How to verify:

- `npm run test -- tests/api/cv-import-wizard-suggest-route.test.ts tests/ui/cv-import-wizard.test.tsx tests/ui/cvjd-auto-suggest.test.tsx`
- `npm run test -- tests/lib/cv-import-wizard-extractor.test.ts tests/lib/cv-import-wizard-quality.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run build`

Open risks / TODO:

- Build still reports dynamic-import critical dependency warnings for pdf.js modules in CV import components; behavior is verified but warning noise remains.
