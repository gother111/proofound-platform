# Project Change Entry

- Date/time (UTC): 2026-02-27T17:20:46Z
- Branch: master
- Base commit: 2f083861
  What changed:
- Added platform performance cache flag key `FF_PLATFORM_PERF_CACHE` in `src/lib/featureFlags.ts`.
- Implemented request-level auth/org read dedupe in `src/lib/auth.ts` using a compatibility cache wrapper (React cache when available, in-flight dedupe fallback).
- Added new in-process TTL cache utility in `src/lib/performance/ttl-cache.ts` with key-based storage and in-flight promise dedupe.
- Added cached wrappers and route-level feature-flag gating for readiness/momentum/updates endpoints.
- Deferred global `SUSPromptHost` and `ChatWidget` mounting behind new `DeferredAppEnhancements` gate on `/app/*` idle time.
- Removed duplicate individual readiness fetch from `MatchingResultsCard`.
- Added unit/integration tests for auth dedupe, TTL cache behavior, route cache-flag behavior, and deferred enhancement gating.

Why:

- Reduce redundant server reads and non-critical client startup work to improve authenticated page load performance while keeping a fast rollback path.

How to verify:

- `npm run test -- tests/lib/ttl-cache.test.ts tests/lib/auth-request-cache.test.ts tests/ui/deferred-app-enhancements.test.tsx tests/api/individual-readiness-cache-flag-route.test.ts tests/api/org-readiness-cache-flag-route.test.ts tests/api/momentum-summary-cache-flag-route.test.ts tests/api/updates-cache-flag-route.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run start` (separate session)
- `BASE_URL=http://localhost:3000 npm run perf:budgets`

Open risks / TODO:

- Build bundle sizes on key routes did not improve materially in this iteration; follow-up optimization is still needed for JS weight reduction.
- Auth dedupe fallback is in-flight based when `React.cache` is unavailable in this runtime.
- `perf:budgets` requires a running local server and can fail fast if `/api/health` is unavailable.
