# Project Change Entry

- Date/time (UTC): 2026-03-09T10:16:01Z
- Branch: master
- Base commit: 7b9cc13b
  What changed:
- Added canonical residual lifecycle states, timestamps, and transition logging for profiles, proof artifacts, matches, invites, exports, imports, and deletion tracking.
- Added dedicated `data_portability_exports`, `data_portability_imports`, and `profile_deletion_requests` records plus the residual lifecycle SQL migration.
- Updated org invite acceptance to retain invites as `accepted` instead of deleting them, and blocked duplicate active org invites by org/email.
- Persisted export and deletion business-state transitions in the API layer, and updated the PRD appendix with the residual lifecycle contract and the Application vs Intro relationship.

Why:

- The repo already had core workflow state machines, but these lifecycle objects were still split across scattered fields, generic lifecycle ops, and narrative documentation.
- This pass makes terminal states, timeout states, and audit expectations explicit and durable without redesigning the existing MVP workflow objects.

How to verify:

- `npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/residual-lifecycle-contracts.test.ts`
- `npm run lint`
- `npm run docs:freshness`

Open risks / TODO:

- Verification invite routes still need follow-up wiring if product wants `opened`, `cancelled`, and `revoked` written on every path instead of only being represented in the schema contract.
- Export currently persists `ready` and `downloaded` metadata for inline JSON delivery; a future async packaged-download flow can reuse the same lifecycle table.
