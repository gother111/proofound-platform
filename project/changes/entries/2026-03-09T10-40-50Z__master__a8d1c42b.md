# Project Change Entry

- Date/time (UTC): 2026-03-09T10:40:50Z
- Branch: master
- Base commit: a8d1c42b
  What changed:
- Added the missing deploy-blocking module `src/lib/security/capability-tokens.ts` to the working tree fix set so clean checkouts include the capability-token implementation referenced by feedback and verification routes.
- Tightened `vercel.json` Python function packaging for `api/python/cv_import.py` to exclude generated and local-only heavy directories that are not runtime dependencies: `node_modules/**`, `.next/**`, `.venv/**`, `.venv311/**`, `audit/**`, and `public/**`, while keeping Python PDF extraction on `pdfplumber`.

Why:

- Recent Vercel failures split between clean-checkout module resolution errors and Python function packaging pressure.
- The current tracked tree still referenced `@/lib/security/capability-tokens` without the file being present in git.
- The Python CV import function should keep server-side PDF extraction, so the deploy fix needed to shrink packaging scope instead of moving extraction into Next.js.

How to verify:

- `PYTHONPATH=. ./.venv311/bin/python -m pytest tests/python/test_pdf_extract.py -q` -> PASS
- `npx vitest run tests/api/expertise-skill-verification-request-route.test.ts --reporter=dot` -> PASS
- Create a clean archive from `HEAD`, overlay `src/lib/security/capability-tokens.ts` and `vercel.json`, install under Node 20, then run `npx vercel build --prod --yes` -> PASS
- Confirm generated Python function output stays small after packaging: `.vercel/output/functions/api/python/cv_import.func` was `172K` in the clean archive build

Open risks / TODO:

- `src/lib/security/capability-tokens.ts` is still untracked in this workspace and must be included in the eventual commit, or the clean-checkout module-not-found failure will return.
- Local plain `npm run build` in the clean archive hit a Node heap OOM without `vercel build`; the Vercel-style build passed and is the stronger deploy signal for this task.
