# Project Change Entry

- Date/time (UTC): 2026-05-21T12:42:23Z
- Branch: master
- Base commit: 0cf1fbd0
  What changed:
- Replaced raw diagnostics in the shared auth helper with structured `auth.*` logging for current-user profile lookup, user organization lookup, active organization lookup, and organization-role verification failures.
- Added focused auth helper failure coverage and runtime guardrail coverage.
- Updated the active MVP surface sweep artifact with the auth helper verification record.

Why:

- Shared auth helpers sit on active MVP route and page authorization paths, so failure diagnostics need to be structured and non-leaky.
- The change preserves existing null/empty fallback behavior, safe permission errors, request-scoped cache behavior, persona routing, and org home resolution.

How to verify:

- `npm test -- --run tests/lib/auth-request-cache.test.ts tests/lib/runtime-debug-output-guardrails.test.ts tests/api/user-current-and-consent-routes.test.ts tests/api/organizations-route.test.ts tests/api/core-matching-profile-route.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- src/lib/auth.ts tests/lib/auth-request-cache.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/lib/auth.ts -n`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment; the affected test runs passed.
- Broader raw diagnostics remain in unrelated active action, lib, and client surfaces and need separate scoped sweeps.
