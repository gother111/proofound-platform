# Project Change Entry

- Date/time (UTC): 2026-03-01T14:33:05Z
- Branch: master
- Base commit: 2124c302
  What changed:
- Implemented minimalist, custom WebKit scrollbars in `globals.css`.
- Replaced hardcoded `bg-white`, `bg-gray-*`, and `border-gray-*` with semantic tokens (`bg-background`, `bg-muted`, `border-border`) across major Admin and Settings components.
- Standardized loading spinners to use `border-primary` instead of hardcoded grays in `AuditLogViewer` and `MatchingProfileEditor`.

Why:

- To achieve absolute UI/UX consistency and perfection across the platform, aligning with the V3 Japandi design system. Hardcoded colors create friction in dark mode scaling, and custom scrollbars elevate the premium feel of the platform.

How to verify:

- Inspect any scrollable area on WebKit browsers to see the custom thin scrollbar.
- Toggle dark mode to ensure panels in `AdminVerificationDashboard`, `ModerationQueue`, and `ConversationList` transition backgrounds smoothly without retaining harsh whites.
- Trigger a loading state in the `AuditLogViewer` or `MatchingProfileEditor` to see the primary-colored spinner.

Open risks / TODO:

- None identified.
