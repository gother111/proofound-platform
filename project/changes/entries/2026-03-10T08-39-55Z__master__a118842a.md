# Project Change Entry

- Date/time (UTC): 2026-03-10T08:39:55Z
- Branch: master
- Base commit: a118842a
  What changed:
- Added a new standalone canonical PRD section, `6.6 Public Verification Log and Algorithm Transparency Surface (MVP-safe)`, to `PRD_for_a_web_platform_MVP.master-latest.md`.
- Defined the MVP-safe public verification log, public vs internal transparency split, public matching-system algorithm changelog, privacy boundaries, edge cases, events and retention, and acceptance criteria.

Why:

- The canonical PRD already promised trust, provenance, explainability, and bias-aware behavior, but it did not yet unify those promises into one calm, privacy-safe public transparency surface.
- This block makes the public trust contract legible without exposing reviewer notes, raw scoring internals, or security-sensitive logic.

How to verify:

- `rg -n "Public Verification Log and Algorithm Transparency Surface|Why this exists|Algorithm changelog requirements|Events and retention" PRD_for_a_web_platform_MVP.master-latest.md`
- `npm run lint` -> PASS with 2 pre-existing `no-img-element` warnings in `src/components/ProofoundLanding.tsx` and `src/components/landing/sections/FooterSection.tsx`
- `npm run typecheck` -> PASS
- `npm run docs:freshness` -> PASS in warning mode with pre-existing registry / governance-pair warnings unrelated to this change

Open risks / TODO:

- The compatibility mirror `PRD_for_a_web_platform_MVP.md` does not yet include this new transparency block.
- `docs:freshness` still reports pre-existing warnings for `agent/runbooks/geo-audit.md` registry coverage and `Documentation.md` / `metrics.md` Last Verified mismatch with `project/Documentation.md`.
