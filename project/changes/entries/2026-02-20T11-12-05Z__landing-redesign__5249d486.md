# Project Change Entry

- Date/time (UTC): 2026-02-20T11:12:05Z
- Branch: landing-redesign
- Base commit: 5249d486
  What changed:
- Fixed landing internal anchor drift by updating footer persona links to `#personas`.
- Added landing-scoped magnetic CTA component and wired it into Hero, Personas, and Final CTA.
- Added SEO metadata hardening on the homepage:
  - `metadataBase` in root layout.
  - stronger title/description/keywords on `/`.
  - absolute OG/Twitter image URLs using site URL fallback logic.
  - JSON-LD (`Organization`, `WebSite`, `WebPage`) on `/`.
- Updated landing visual baseline snapshot after intentional UI/SEO-adjacent changes.

Why:

- Ship current landing redesign safely and improve homepage discoverability for brand + trust-platform intent.
- Remove broken in-page navigation and align metadata with production indexing/social sharing requirements.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test:e2e:landing`
- `npm run test:e2e:landing:visual`

Open risks / TODO:

- `src/app/sitemap.ts` and `src/app/robots.txt/route.ts` are prepared locally but should ship in a dedicated non-landing PR to avoid landing-scope guardrail conflicts.
