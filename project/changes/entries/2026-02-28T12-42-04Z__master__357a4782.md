# Project Change Entry

- Date/time (UTC): 2026-02-28T12:42:04Z
- Branch: master
- Base commit: 357a4782

What changed:

- Hardened skill verifier requester identity resolution in `src/app/api/verify/[token]/route.ts`:
  - Added profile lookup fallback when `profiles.email` is unavailable.
  - Added requester identity fallback chain using `requester_email_snapshot`.
- Added skill proof payload support in `GET /api/verify/[token]`:
  - Returns sanitized `proofs` array from `skill_proofs` for the requested skill and requester.
- Updated external verifier UI in `src/app/verify/[token]/page.tsx`:
  - Removed `skill_code` rendering under the skill name.
  - Added “Supporting proof(s)” section with proof type/title/description/date metadata.
  - Added link rendering and inline image preview for image proof URLs.
  - Added explicit empty state when no proofs are attached.
- Added regression coverage in `tests/api/verify-impact-token-route.test.ts`:
  - Skill flow display-name preservation when `profiles.email` column lookup fails.
  - Skill flow proof metadata returned in verification payload.
  - Updated test mocks to cover `skill_proofs` and `profiles` fallback selects.

Why:

- PRO-102 reported broken verifier UX: requester appeared as `Unknown`, internal numeric skill code was exposed, and certificate/proof context was absent.

How to verify:

- `npm run test -- tests/api/verify-impact-token-route.test.ts` (PASS)
- `npm run lint` (PASS with unrelated warnings only in landing image components)
- `npm run typecheck` (PASS)

Open risks / TODO:

- Proof links are shown directly on the verifier page. This is intentional for token holders but should continue to be reviewed as part of token-sharing risk posture.
- Inline preview currently supports common image URL extensions; non-image files render as metadata + link only.
