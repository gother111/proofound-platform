# Project Change Entry

- Date/time (UTC): 2026-05-21T16:44:19Z
- Branch: master
- Base commit: 2e6093deb
  What changed:
- Debug ingest now fails closed in production-like deploy runtimes by reusing `isProductionDeployRuntime`.
- Added focused unit coverage proving debug ingest still sends in local test/development contexts and does not send in production even when debug ingest env vars are configured.

Why:

- Debug telemetry is useful for local QA, but launch readiness should not include a public or server debug sink that can silently capture login, middleware, or email-path diagnostics in production.

How to verify:

- `npm run test -- tests/lib/debug-ingest.test.ts tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This does not replace final Browser/manual launch evidence. Production-candidate backup, restore, launch-status, perf-status, and final go/no-go proof are still required before launch readiness can be claimed.
