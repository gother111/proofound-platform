# Project Change Entry

- Date/time (UTC): 2026-06-19T14:03:35Z
- Branch: master
- Base commit: 2cf262aa4
  What changed:
- Kept the communications hub inside the mobile app shell's usable viewport instead of letting it extend under the fixed bottom navigation.
- Made the individual messages ready and loading panes inherit the available app-shell height.
- Added a communications hub regression check for mobile bottom-navigation clearance.

Why:

- Browser verification showed the individual communications message list could render conversation rows beneath the fixed mobile bottom nav on a 390px viewport.

How to verify:

- Browser: `/app/i/communications?section=messages` at 390x844 and 1280x720; messages/interviews load without horizontal overflow, and mobile message rows scroll above the bottom nav.
- `npm test -- tests/ui/communications-hub-mobile-targets.test.tsx tests/ui/deferred-messages-client.test.tsx tests/ui/conversation-list.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- `npm run docs:freshness` still reports existing orphan-artifact warnings in warning-only mode.
