# Project Change Entry

- Date/time (UTC): 2026-03-10T20:29:44Z
- Branch: master
- Base commit: 5a2984be

What changed:

- Narrowed the shipped MVP surface to the proof-first corridor across individual and org navigation, route metadata, launch copy, and individual home.
- Replaced legacy individual dashboard/workspace rendering with a calm overview and removed the deleted dashboard client test surface.
- Gated non-launch routes and APIs for projects, opportunities, analytics-like dashboards, evidence packs, assignment templates, goals, momentum, and public fairness reporting behind launch-safe notices or 410 responses.
- Reduced Zen to private optional check-ins/reflections, removed broader wellbeing wording, and kept expanded wellbeing endpoints disabled.
- Updated strict/unit tests to match launch behavior, including canonical org roles, intro-hold responses, and non-launch public fairness behavior.

Why:

- The repo still exposed deprecated MVP sprawl that conflicted with the canonical PRD corridor: proof-first, portfolio-first, privacy-first, and calm by design.
- Launch defaults needed to make non-canonical surfaces opt-in only, not silently public.

How to verify:

- Run `npm run lint`.
- Run `npm run typecheck`.
- Run `npx vitest run tests/api/feature-flags-route.test.ts tests/api/momentum-summary-cache-flag-route.test.ts tests/api/analytics-dashboard-route.test.ts tests/api/analytics-dashboard-load-time-route.test.ts tests/api/dashboard-layout-route.test.ts tests/assignment-templates.test.ts tests/ui/topbar-customize-visibility.test.tsx tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/settings-integrations-discoverability.test.tsx tests/ui/settings-privacy-visibility-placement.test.tsx tests/ui/public-fairness-note.test.tsx`.
- Run `npm run test`.
- Run `npm run build`.
- Run `npm run test:e2e:individual:strict`.
- Run `npm run test:e2e:org:strict` and confirm it completes without local Next dev-server `.next` ENOENT churn or transient Supabase DNS failures.

Open risks / TODO:

- `npm run test:e2e:org:strict` remained flaky in local verification because the Next dev server intermittently served `.next` ENOENT errors after repeated reruns, and one ranking run hit transient Supabase DNS resolution failure.
- `npm run docs:freshness` still reports existing orphan-doc warnings unrelated to this cleanup.
