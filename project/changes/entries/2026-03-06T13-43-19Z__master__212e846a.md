# Project Change Entry

- Date/time (UTC): 2026-03-06T13:43:19Z
- Branch: master
- Base commit: 212e846a

What changed:

- Patched `src/db/migrations/20260306113000_fix_symbol_skill_alias_normalization.sql` so it drops the legacy active-alias partial index before backfill, uses a deferred uniqueness constraint during updates, deprecates the conflicting `cplusplus` alias, flushes constraints, and then recreates the active-alias index.
- Applied that migration to the configured Supabase database.

Why:

- The migration could not apply cleanly against the live alias table because older uniqueness/index rules and equivalent symbol aliases caused transactional collisions.
- The patch preserves the intended symbol-aware normalization contract while making the migration executable on real data.

How to verify:

- `npm run db:audit:migrations`
- `npm run taxonomy:validate:aliases`
- Query `public.skills_taxonomy_aliases` for `C#`, `C++`, `cplusplus`, `Node.js`, and `NodeJS` and confirm the expected `alias_norm` and `status` values.

Open risks / TODO:

- `cplusplus` is now deprecated to satisfy the new normalization uniqueness contract; revisit only if product explicitly wants that textual alias reintroduced under a different search policy.
- `project/Documentation.md` still lacks the freshness metadata expected by `npm run docs:freshness`.
