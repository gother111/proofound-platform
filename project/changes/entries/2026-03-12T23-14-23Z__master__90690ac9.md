# Project Change Entry

- Date/time (UTC): 2026-03-12T23:14:23Z
- Branch: master
- Base commit: 90690ac9
  What changed:
- Archived and simplified selected admin surfaces, tightened launch and archive route handling, and updated navigation and command-palette exposure for the reduced MVP surface.
- Added launch hardening coverage for middleware, synthetic monitors, org ownership, match explainability, and launch smoke verification.
- Fixed Python CV proxy same-origin targeting so internal requests use the actual request origin instead of public-site fallback during local and test execution.

Why:

- This finishes the final landing block by removing stale admin entry points, hardening launch monitoring paths, and aligning tests with the archived route posture.
- The proxy fix prevents forwarded-host influence from changing the internal Python service target.

How to verify:

- `npm run test:launch:smoke`
- `npm test`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`
- `npm run build`

Open risks / TODO:

- `docs:freshness` still reports three existing audit markdown files that are not registered in `docs/DOCS_REGISTRY.md`.
- Two existing landing-page/footer `no-img-element` warnings remain in lint output.
