# Project Change Entry

- Date/time (UTC): 2026-03-06T12:39:17Z
- Branch: master
- Base commit: 835ae2cf
  What changed:
- Updated `src/app/api/cron/sla-enforcement/route.ts` to treat completed interviews with `decided_by IS NULL` as overdue, matching the live production schema.
- Hardened `src/lib/analytics/fairness.ts` to inspect `information_schema.columns` before demographic segmentation and emit a degraded zero-segment report when `wellbeing_opt_ins` lacks demographic fields.
- Added focused regression coverage for the SLA cron route, fairness cron route, and fairness analytics compatibility path.
- Recorded the compatibility behavior in `project/Architecture.md`.

Why:

- cron-job.org was reporting `sla-enforcement` and `fairness-report` failures because production schema lagged the repo schema.
- The Python internal worker failure was caused by production still serving an older commit that did not include `/api/cron/python-internal-worker`.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `curl -sS https://proofound.io/api/health` should show the deployed commit once production catches up.
- `curl -sS -H "Authorization: Bearer <secret>" https://proofound.io/api/cron/python-internal-worker`
- `curl -sS -H "Authorization: Bearer <secret>" https://proofound.io/api/cron/sla-enforcement`
- `curl -sS -H "Authorization: Bearer <secret>" https://proofound.io/api/cron/fairness-report`

Open risks / TODO:

- `fairness-report` still depends on future schema work if demographic segmentation is required instead of degraded baseline-only reporting.
- Production deploy sync still needs to complete before cron-job.org can turn the Python internal worker green.
