# Project Change Entry

- Date/time (UTC): 2026-05-21T15:47:09Z
- Branch: master
- Base commit: 67a921fed
  What changed:
- Routed legacy assignment builder, profile localStorage helper, and realtime read-receipt helper diagnostics through `proofound:client-diagnostic`.
- Added runtime debug-output guardrails for those low-level client helper console strings.
- Updated the MVP surface sweep artifact with the legacy helper diagnostics checkpoint.

Why:

- Keeps remaining client-side helper failures out of raw browser console output while preserving legacy assignment submission, profile storage fallback, and non-blocking realtime read-receipt behavior.

How to verify:

- `npm test -- --run tests/lib/runtime-debug-output-guardrails.test.ts`
- Targeted scan: `rg -n "console\\.(error|warn|log)" src/components/matching/AssignmentBuilder.tsx src/lib/profileStorage.ts src/lib/messaging/realtime.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- None known for this scoped diagnostics slice.
