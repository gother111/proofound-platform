# Project Change Entry

- Date/time (UTC): 2026-03-10T10:06:53Z
- Branch: master
- Base commit: 380761d5
  What changed:
- Rewrote `LAUNCH_RUNBOOK.md` into a canonical operator-facing runbook aligned to the master PRD launch appendices and `docs/launch-operations-mvp.md`, removing the stale beta-wave, TTFQI, and soft-delete playbook assumptions.
- Tightened `PRD_TECHNICAL_REQUIREMENTS.md` so its front matter, authorization model, privacy language, membership schema, upload constraints, deletion wording, and launch-readiness section now defer to the canonical master PRD instead of competing with it.

Why:

- The active technical requirements and runbook docs still described older marketplace, RBAC, visibility, upload, and deletion behavior that no longer matched the proof-first MVP or the March 2026 audit baseline.

How to verify:

- `npm run docs:freshness`
- `npm run lint`
- `npm run typecheck`
- `rg -n "org_owner|org_manager|org_reviewer|principal|membership|visibility matrix|reconciliation ledger|synthetic monitors|rollback" PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md`
- `rg -n "owner/admin/member/viewer|Soft delete|soft delete|public/network/private|50 invite-only users|TTFQI" PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md`
- `rg -n "Weeks 1-4|Weeks 5-8|Weeks 9-12|launch-blocking|early post-launch|later expansion corridor" LAUNCH_RUNBOOK.md`

Open risks / TODO:

- `docs:freshness` may still report the pre-existing orphan-file warnings for `agent/runbooks/geo-audit.md` and several `audit/*.md` files outside this doc pass.
- `lint` may still report the two pre-existing `@next/next/no-img-element` warnings in landing components unrelated to this docs-only change.
- Other archived or non-canonical docs still carry older launch wording and should continue to be treated as non-authoritative until narrowed in later passes.

Verification results:

- `npm run docs:freshness` -> PASS in warning mode; only pre-existing orphan-file warnings for `agent/runbooks/geo-audit.md` and `audit/*.md`.
- `npm run lint` -> PASS with 2 pre-existing `@next/next/no-img-element` warnings in `src/components/ProofoundLanding.tsx` and `src/components/landing/sections/FooterSection.tsx`.
- `npm run typecheck` -> PASS.
- `rg -n "org_owner|org_manager|org_reviewer|principal|membership|visibility matrix|reconciliation ledger|synthetic monitors|rollback" PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md` -> PASS, canonical terms present.
- `rg -n "owner/admin/member/viewer|Soft delete|soft delete|public/network/private|public directory|50 invite-only users|TTFQI|ATS|HRIS" PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md` -> PASS with intentional negative-scope hits only: `No ATS or HRIS integration`, `Do not treat soft delete as the user-facing recovery model`, and canonical `no public directory` exclusions.
- `rg -n "Weeks 1-4|Weeks 5-8|Weeks 9-12|launch-blocking|early post-launch|later expansion corridor" LAUNCH_RUNBOOK.md` -> PASS, rollout corridor aligned.
- `rg -n "soft delete|deleted_at" LAUNCH_RUNBOOK.md` -> PASS with one intentional negative rule only: `Do not treat soft delete as the user-facing recovery model`.
