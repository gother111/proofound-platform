# Project Change Entry

- Date/time (UTC): 2026-03-11T22:04:54Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Refactored duplicated rate-limit 429 response construction in `src/lib/rate-limit/middleware.ts`.
- Added `getRetryAfterSeconds` and `createRateLimitExceededResponse` helpers.
- Reused the shared helper in both `withRateLimit` and `requireRateLimit` without changing payload keys, status, or headers.

Why:

- Remove small duplication and keep rate-limit response formatting consistent across both middleware entry points.

How to verify:

- `npm run typecheck` (PASS)
- `npx eslint src/lib/rate-limit/middleware.ts` (PASS)

Open risks / TODO:

- Focused Vitest command currently fails in this local runtime with a Node/Vite module export mismatch (`node:fs/promises` constants export), so unit test verification for this run relied on typecheck + lint for the touched file.
