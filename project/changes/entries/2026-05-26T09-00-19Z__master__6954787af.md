# Project Change Entry

- Date/time (UTC): 2026-05-26T09:00:19Z
- Branch: master
- Base commit: 6954787af
  What changed:
- Replaced Vercel production `AI_MODEL_DEFAULT` with `gemini-3.1-flash-lite`.
- Refreshed `.artifacts/ai-provider-smoke.json` after a successful production-like Gemini provider smoke.
- Included `.artifacts/ai-provider-smoke.json` in the launch-status route's Vercel file tracing so production can read the model-matching smoke evidence.

Why:

- The previous production value used the rejected preview model identifier, which caused live provider smoke to fail with `model_error`.

How to verify:

- `npm run vercel:pull:production`, then confirm the pulled production snapshot reports `AI_MODEL_DEFAULT="gemini-3.1-flash-lite"`.
- `npm run ai:provider:smoke` with the pulled production env: passed with default model `gemini-3.1-flash-lite`.
- `npm run test:launch:ai`: passed 41 test files / 336 tests.
- Authenticated production `/api/monitoring/launch-status` should report `aiConfiguredModel="gemini-3.1-flash-lite"` and a non-null `aiLastSuccessfulProviderSmokeAt` after redeploy.

Open risks / TODO:

- Existing deployments may need a redeploy or runtime refresh before they read the updated Vercel environment variable.
