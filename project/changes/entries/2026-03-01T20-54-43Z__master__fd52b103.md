# Project Change Entry

- Date/time (UTC): 2026-03-01T20:54:43Z
- Branch: master
- Base commit: fd52b103

What changed:

- Added CV Import Gemini and OCR environment examples to `.env.example`.
- Expanded `docs/ENV_VARIABLES.md` with Gemini dual-key budget vars, model policy vars, parser limits, OCR toggles, OCR timeout vars, and updated feature/checklist tables.
- Updated `docs/features/cv-import.md` to document the Gemini runtime modes, supported endpoints, idempotency/request-id headers, metadata additions, stable error codes, and current limitations.
- Generated sharded governance logs via `npm run log:change` and `npm run log:session`.

Why:

- Align runtime docs and env contracts with the implemented CV Gemini pipeline and avoid drift between code and deployment configuration.
- Preserve repository governance policy by recording session/change details in sharded log files.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run docs:freshness`
- Inspect updated docs for:
- `docs/ENV_VARIABLES.md` CV Import + Gemini + OCR sections
- `docs/features/cv-import.md` metadata/error-code contract and endpoint list
- `.env.example` CV Import configuration block

Open risks / TODO:

- `docs/API_REFERENCE.md` is generated and currently lags new route inventory until regenerated.
- Commit and push were not performed in this session.
