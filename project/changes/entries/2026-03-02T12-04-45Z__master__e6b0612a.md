# Project Change Entry

- Date/time (UTC): 2026-03-02T12:04:45Z
- Branch: master
- Base commit: e6b0612a
  What changed:
- Hardened deployment artifact filtering in `.vercelignore` to drop non-runtime trees and generated outputs from Vercel source uploads.
- Updated Python function `excludeFiles` in `vercel.json` to a compact value under Vercel's 256-character limit while still excluding major non-runtime trees.

Why:

- Vercel builds were consistently succeeding but failing during `Deploying outputs` with an internal error.
- Python function packaging previously over-included repository files, increasing publish-time payload pressure and risk of platform-side deploy failure.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- Push to `master` and confirm next Vercel production deploy passes beyond `Deploying outputs`.

Open risks / TODO:

- If Vercel still fails after these reductions, next step is to remove Python runtime packaging from this project and run CV extraction fully through TypeScript fallback while provisioning a separate Python microservice.
