# Project Change Entry

- Date/time (UTC): 2026-02-25T09:43:31Z
- Branch: HEAD
- Base commit: 70020bd0
  What changed:
- Updated `src/components/expertise/CVJDAutoSuggest.tsx` to use `apiFetch` for `POST /api/expertise/auto-suggest` instead of raw `fetch`.
- Added focused regression test `tests/ui/cvjd-auto-suggest.test.tsx` to verify:
- Analyze path calls `apiFetch` with `POST`.
- Suggestions render on successful analyze response.
- Backend error messages are surfaced via toast on analyze failure.

Why:

- `PRO-31` reported CV analysis failures when skills already existed.
- Root cause was inconsistent request handling: analyze used raw `fetch` while other expertise mutations used CSRF-aware `apiFetch`.
- Middleware enforces CSRF on mutating `/api/*` routes, so analyze requests could fail without `x-csrf-token`.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/cvjd-auto-suggest.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS, with expected warnings about missing local `DATABASE_URL` and mock DB fallback message in this workspace)

Open risks / TODO:

- Other future POST calls added under expertise flows can regress if raw `fetch` is used instead of `apiFetch`.
- Consider a small lint guard or helper convention doc to enforce `apiFetch` for mutating same-origin API calls.
