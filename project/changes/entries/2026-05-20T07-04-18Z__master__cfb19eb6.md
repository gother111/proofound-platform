# Project Change Entry

- Date/time (UTC): 2026-05-20T07:04:18Z
- Branch: master
- Base commit: cfb19eb6
  What changed:
- Created a new `PublicProofPackList` client component in `src/components/public-profile/PublicProofPackList.tsx` that supports progressive disclosure of Proof Packs (collapsible details panel).
- Replaced the static selectedProofPacks mapping in `src/app/portfolio/[handle]/page.tsx` with `<PublicProofPackList />`.
- Cleaned up the trust badges to be rendered as a single-line consolidated string (`Verified evidence / Fresh / Owned`).
- Applied `font-display` (Crimson Pro) to the public display name on the portfolio page.

Why:

- To make public portfolio pages calm, highly scannable, and clean rather than overwhelming the viewer with nested card boundaries, multiple color chips, and heavy walls of text.

How to verify:

- Run typecheck: `npm run typecheck`
- Run lint: `npm run lint`
- View public profile pages to verify progressive disclosure and typographic styling.

Open risks / TODO:

- Apply similar progressive disclosure to the authenticated profile dashboard view if desired.
