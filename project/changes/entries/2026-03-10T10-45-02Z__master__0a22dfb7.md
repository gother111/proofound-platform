# Project Change Entry

- Date/time (UTC): 2026-03-10T10:45:02Z
- Branch: master
- Base commit: 0a22dfb7

What changed:

- Reconciled `PRD_for_a_web_platform_MVP.master-latest.md`, `PRD_TECHNICAL_REQUIREMENTS.md`, and `LAUNCH_RUNBOOK.md` around a single launch authority chain.
- Tightened PRD launch-facing sections for source precedence, proof-pack object model, visibility and reveal rules, trust and portability, launch NFRs, and later-corridor appendix framing.
- Cleaned technical requirements sections outside Section 7 so they no longer compete with the canonical launch contract, and added a launch-safe portability and trust subsection in Block 12.
- Removed remaining active JWT refresh-token wording from the technical launch sections while keeping Supabase-managed SSR cookies as the interactive web auth/session model.
- Adjusted the runbook only where needed so it clearly defers product behavior to the master PRD and technical hardening to Section 7.

Why:

- The active launch docs contained legacy wording that could be read as contradictory launch scope or architecture.
- The reconciliation keeps Proofound's launch corridor proof-first, privacy-first, bias-aware, explainable, and calm by design without widening scope.

How to verify:

- `npm run docs:freshness` -> PASS with existing warning-mode orphan-doc notices only.
- `npm run lint` -> PASS with two pre-existing `@next/next/no-img-element` warnings in landing components.
- `npm run typecheck` -> PASS.
- `rg -n "JWT|refresh token|SameSite-only|Redis cache|Redis for session|GraphQL|Datadog|LogDNA|Swedish runtime parity|bilingual|public-by-default|remote fetch|fetch-on-render" PRD_for_a_web_platform_MVP.master-latest.md PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md` -> legacy terms remain only in explicit archive or reconciliation notes.
- `rg -n "public_link_only|RLS plus server-side authorization|Supabase-managed SSR cookies|Postgres-backed queue|Sentry|Vercel Analytics|Supabase dashboard|uptime monitor|quarantine-first|private by default|narrowest" PRD_for_a_web_platform_MVP.master-latest.md PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md` -> canonical launch phrases present.
- `rg -n "proof_artifacts|proof_packs|proof_pack_items|verification_records|submissions|operator-facing execution summary|authoritative technical launch contract" PRD_for_a_web_platform_MVP.master-latest.md PRD_TECHNICAL_REQUIREMENTS.md LAUNCH_RUNBOOK.md` -> canonical proof object vocabulary and authority wording present.

Open risks / TODO:

- `PRD_for_a_web_platform_MVP.master-latest.md` now has a large text diff because two overbuilt sections were intentionally replaced with shorter launch-safe versions; future reviewers should review intent, not raw line count.
- `docs:freshness` warnings come from existing orphan registry gaps outside this task's scope.
