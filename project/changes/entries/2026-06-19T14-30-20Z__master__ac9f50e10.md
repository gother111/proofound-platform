# Project Change Entry

- Date/time (UTC): 2026-06-19T14:30:20Z
- Branch: master
- Base commit: ac9f50e10
  What changed:
- Moved the individual Public Page copy/download/share controls out of the top header action row and into the hero card after the public identity, introduction, and privacy context.
- Kept owner-only `Manage visibility` in the header, while public export/copy actions remain a single accessible action group.
- Added a public portfolio test assertion that the copy/share controls follow the public identity in DOM order.

Why:

- On narrow screens, the public page previously showed three stacked utility actions before the visitor saw the person and proof context. That weakened the MVP rule that the primary object and trust state should be obvious first.
- The new order keeps the page calm and proof-first: visitor sees the public identity, headline, privacy-safe bio, intro route, then public export/copy actions.

How to verify:

- Browser, `/portfolio/demo-proofound`, mobile emulation 390x844: name appears at y=133, intro at y=390, action group at y=538, traceable summary at y=719; no horizontal overflow, no small controls, no console warnings.
- Browser, `/portfolio/demo-proofound`, desktop 1280x900: action group stays inside the hero card right column at 311px wide; no page overflow, no small controls, no console warnings.
- Browser interaction: copy-share action shows the inline fallback when clipboard is blocked by the Browser environment; proof-context toggle expands and the DOM snapshot shows `Proof context`, `Verification scope`, and `Public-safe evidence`.
- `npm test -- tests/ui/public-portfolio-page.test.tsx`
- `npm test -- tests/ui/public-portfolio-page.test.tsx tests/ui/public-portfolio-actions.test.tsx tests/ui/public-portfolio-access-consistency.test.tsx tests/ui/public-snippet-page.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Browser screenshot capture timed out in this environment, so visual proof used Browser DOM snapshots, bounding boxes, console logs, and interaction checks.
- `docs:freshness` still reports the existing 84 warning-only orphan-artifact notices unrelated to this change.
