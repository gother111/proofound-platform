# Project Change Entry

- Date/time (UTC): 2026-04-14T23:00:40Z
- Branch: master
- Base commit: 1539dd00
  What changed:
- Hardened `/api/upload/cover` so organization cover uploads authorize `org_profile.update` before any privileged storage write and clean up uploaded objects/records if the later profile write fails.
- Hardened `/api/conversations` so it accepts only `matchId`, derives canonical participants from trusted match/org membership state, and reuses a shared conversation resolution helper.
- Removed request-derived Python internal service URL fallback and now require validated `PYTHON_CV_IMPORT_BASE_URL` in production-like runtimes, with only localhost fallback in local/test runtimes.
- Replaced plaintext work-email verification token storage with SHA-256 token hashes and added migrations/policies to invalidate legacy pending tokens and tighten conversation insert/update RLS integrity.
- Added focused regression coverage for upload authz, canonical conversation creation, Python internal service URL validation, work-email token hashing, and updated privacy fixture support for stricter conversation RLS.

Why:

- Close confirmed security gaps in upload authorization, conversation authorization, internal service SSRF exposure, and work-email token handling while keeping behavior changes narrow and MVP-compatible.

How to verify:

- `npm run db:migrate`
- `npm run lint`
- `npm run typecheck`
- `npx vitest run tests/api/upload-cover-route.test.ts tests/api/conversations-route.test.ts tests/lib/python-internal-service.test.ts tests/api/work-email-verification-send-route.test.ts tests/api/work-email-verification-verify-route.test.ts tests/api/verification-status-route.test.ts tests/api/mobile-verification-status-route.test.ts tests/api/org-match-review-route.test.ts`
- `npm run test:privacy`
- `npm run test:privacy:extended`
- `npm run build`

Open risks / TODO:

- `npm run test` still has unrelated baseline failures in existing UI and archive-heavy suites; this hardening work does not resolve those broader repo issues.
- A follow-up migration can drop the legacy `work_email_token` column after the rollout window closes.
