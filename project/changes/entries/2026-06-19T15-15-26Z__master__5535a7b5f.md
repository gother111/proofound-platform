# Project Change Entry

- Date/time (UTC): 2026-06-19T15:15:26Z
- Branch: master
- Base commit: 5535a7b5f
  What changed:
- Added mobile-visible launch-date context to the selected assignment header in the organization
  proof-submission review workspace.
- Reused one launch-date label helper so desktop and mobile render the same review-readiness
  context.
- Added a regression assertion for the mobile launch-date element.

Why:

- Browser verification showed the selected assignment's launch context was available on desktop but
  dropped out on mobile, weakening the review-readiness signal in a launch-critical org workflow.

How to verify:

- Browser: `/app/o/test-org/assignments?visualState=filled` as the org mock persona at 390px shows
  selected assignment title, launch date, and edit action in order with no overflow or console
  errors.
- Browser: the same route at desktop width shows one visible launch-date label and no duplicate
  desktop UI.
- `npm test -- tests/ui/matching-organization-view.test.tsx tests/ui/matching-organization-view-beta.test.tsx tests/ui/org-assignments-page-touch-targets.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- `docs:freshness` still reports existing warning-mode orphan artifact registry warnings unrelated
  to this UI change.
