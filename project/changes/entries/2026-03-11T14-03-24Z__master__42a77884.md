# Project Change Entry

- Date/time (UTC): 2026-03-11T14:03:24Z
- Branch: master
- Base commit: 42a77884

What changed:

- Added `toDateOrNull` to `src/lib/datetime/normalize.ts`.
- Replaced duplicated date parsing helpers in `src/lib/verification/policy.ts` with shared `toIsoOrNull` and `toTimestampOrNull`.
- Replaced duplicated date parsing helpers in `src/lib/proofs/canonical-pack.ts` with shared `toIsoOrNull` and `toDateOrNull`.

Why:

- Keep date normalization logic in one place and reduce drift risk across trust and verification code paths.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/lib/canonical-proof-pack.test.ts tests/lib/verification-policy.test.ts` -> PASS (8 tests)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` -> PASS
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` -> PASS with 2 pre-existing `@next/next/no-img-element` warnings in landing components

Open risks / TODO:

- Vitest logs a non-blocking `EPERM` websocket warning in this environment; tests still pass.
