# Project Change Entry

- Date/time (UTC): 2026-04-23T19:01:28Z
- Branch: master
- Base commit: a05d6fa6
  What changed:
- Completed PRO-175's first-proof modal approval pass in the launch UI.
- Updated the actual `FirstProofDialog` copy so the modal states the approved source policy: proof link, proof file upload, or both when they describe the same work.
- Approved the required modal elements in tests: skill or capability, real context, proof title, proof link, upload proof file, evidence note, date fields, and the `Save first proof` CTA.
- Added a focused submit test proving the modal saves the proof against the selected skill and real context without backend or schema changes.

Why:

- PRO-175 needed the required elements, labels, helper text, CTA copy, and upload-vs-link decision captured before QA.
- The first-proof modal existed, but its copy did not yet capture the approval decision clearly enough for QA handoff.

How to verify:

- `npm run test -- tests/ui/first-proof-dialog.test.tsx`
- Open the profile proof flow with `proof=first` and confirm the modal title, helper copy, proof-source inputs, and CTA match the approved first-proof flow.

Open risks / TODO:

- None for PRO-175. Existing upload handling and proof attach APIs are reused unchanged.
