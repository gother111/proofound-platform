# Project Change Entry

- Date/time (UTC): 2026-02-27T15:21:44Z
- Branch: master
- Base commit: a811e535
  What changed:
- Added canonical verification-tier model (`unverified`, `workplace_verified`, `identity_verified`) with source and LinkedIn-level fields in DB schema and migration.
- Updated LinkedIn initiate flow to auto-approve `IDENTITY` and `WORKPLACE` separately, keep pending manual path only when no official signal, and notify admins only for pending manual cases.
- Updated work-email and Veriff flows, status APIs, gates, admin review/queue UI, and user settings UI to reflect identity-vs-workplace semantics.
- Added backfill script (`scripts/backfill-verification-tiers.ts`) and executed it after migration to reclassify existing users.
- Added/updated tests for LinkedIn parsing, initiate route outcomes, status APIs, queue visibility, work-email behavior, and tier helper logic.

Why:

- Needed to distinguish identity-grade verification from workplace-grade verification and enforce identity badge/gate only for identity-tier users.
- Needed to remove hidden queue and messaging inconsistencies caused by previous pending-only LinkedIn behavior.
- Needed to migrate existing users to the canonical model so current data matches new runtime logic.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `npm run test -- tests/lib/linkedin-verified.test.ts tests/api/linkedin-verification-initiate-route.test.ts`
- `npm run test -- tests/api/verification-status-route.test.ts tests/ui/linkedin-verification.test.tsx tests/ui/verification-status-options.test.tsx`
- `npm run test`
- `npm run build`
- `npm run db:migrate`
- `npx tsx scripts/backfill-verification-tiers.ts --dry-run`
- `npx tsx scripts/backfill-verification-tiers.ts --apply`
- `npx tsx scripts/backfill-verification-tiers.ts --dry-run` (expected `updatesNeeded: 0` after apply)

Open risks / TODO:

- Backfill currently uses point-in-time LinkedIn payload interpretation; if legacy payload formats change, helper normalization may need expansion.
- `next build` still reports pre-existing `@next/next/no-img-element` warnings in landing components.
