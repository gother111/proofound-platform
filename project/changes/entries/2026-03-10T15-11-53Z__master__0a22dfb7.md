# Project Change Entry

- Date/time (UTC): 2026-03-10T15:11:53Z
- Branch: master
- Base commit: 0a22dfb7
  What changed:
- Hardened capability-token flows for candidate invites, custom verification, feedback, org invite redirect, assignment invite lookup, and the main verify route with hashed opaque token lookup, canonical state updates, regeneration revocation, replay tracking, and redeem-session nonce binding.
- Tightened upload ingest and proof attach so evidence stays private/quarantined by default, only canonical MIME types pass, 25 MB per-file limits are enforced, and proof attach requires an `uploaded_file_id` that has passed lifecycle checks.
- Added and updated focused tests for token replay, regeneration revocation, wrong-recipient neutral failure, upload rejection, quarantined attach blocking, and the updated proof upload payloads.

Why:

- The repo still had mixed legacy token handling and file attach paths that trusted raw tokens or file paths directly. This change moves the active invite and verification surfaces toward the canonical MVP security contract and closes the highest-risk replay and attach gaps.

How to verify:

- `npm run db:migrate`
- `npm run typecheck`
- `npm run lint`
- `npx vitest run tests/lib/capability-tokens.test.ts tests/api/candidate-invite-claim-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/lib/upload-validate-file.test.ts tests/api/org-candidate-invites-route.test.ts tests/ui/edit-skill-window-proofs.test.tsx`

Open risks / TODO:

- Full-suite and privacy-suite failures still exist outside this task area. The main remaining token migration gap is broader legacy verification/admin/assignment coverage and server-side aggregate Proof Pack batch limits.
