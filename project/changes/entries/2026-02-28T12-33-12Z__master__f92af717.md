# Project Change Entry

- Date/time (UTC): 2026-02-28T12:33:12Z
- Branch: master
- Base commit: f92af717
  What changed:
- Fixed CV PDF parsing initialization in `CVJDAutoSuggest` to support actual `pdfjs-dist/webpack.mjs` runtime shape (`getDocument` named export), with compatibility fallback for default export shapes.
- Added parser error normalization for `getDocument`/init failures so users see a friendly message instead of raw runtime errors.
- Added defensive response normalization for `/api/expertise/cv-import/suggest` to prevent UI crashes on malformed payloads.
- Hardened taxonomy manual mapping parsing to safely handle non-array/malformed `l4_skills` payloads.
- Added safe error body extraction for non-JSON API errors in the CV import component.
- Updated `pdfjs-dist/webpack.mjs` declaration file to named exports.
- Updated UI tests to mock named `getDocument` and added regressions for parser init failure and malformed analysis payload.

Why:

- Users were blocked by `Cannot read properties of undefined (reading 'getDocument')` when uploading CV PDFs.
- Current tests passed due a mismatched mock/type shape that did not reflect production runtime.
- Additional guardrails were required to prevent similar undefined dereference issues from malformed API responses.

How to verify:

- `npm run test -- tests/ui/cvjd-auto-suggest.test.tsx` (PASS)
- `npm run test -- tests/api/cv-import-suggest-route.test.ts tests/lib/cv-import-suggest.test.ts tests/lib/cv-import-suggest-quality.test.ts` (PASS)
- `npm run typecheck` (PASS)
- `npm run lint` (PASS with two pre-existing landing warnings)
- `npm run build` (PASS)

Open risks / TODO:

- Optional maintenance: update `baseline-browser-mapping` dev dependency to clear warning noise during tests/build.
