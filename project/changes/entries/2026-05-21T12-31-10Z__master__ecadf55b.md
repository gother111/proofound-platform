# Project Change Entry

- Date/time (UTC): 2026-05-21T12:31:10Z
- Branch: master
- Base commit: ecadf55b
  What changed:
- Replaced the raw live-refresh fallback diagnostic in `/api/monitoring/launch-status` with structured `launch_status.live_refresh_failed` logging.
- Added focused route coverage for the structured diagnostic and runtime guardrail coverage.
- Updated the active MVP surface sweep artifact with the launch-status verification record.

Why:

- Launch status is an internal launch-ops API and should keep operator diagnostics structured and non-leaky when live HTTP refresh fails.
- The route intentionally falls back to persisted status evidence, so the change preserves response semantics while making the fallback path observable.

How to verify:

- `npm test -- --run src/app/api/monitoring/__tests__/launch-status-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts src/lib/launch/__tests__/surface-policy.test.ts tests/api/launch-surface-inventory.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- src/app/api/monitoring/launch-status/route.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/app/api/monitoring/launch-status/route.ts -n`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment; the affected test runs passed.
- Broader raw diagnostics remain in unrelated active server, action, lib, and client surfaces and need separate scoped sweeps.
