# Project Change Entry

- Date/time (UTC): 2026-02-27T15:06:51Z
- Branch: master
- Base commit: 604eb559
  What changed:
- Updated `/app/i/verifications` data composition to merge skill and impact-story verification requests for both Incoming and Sent views.
- Added server-side admin-client fetch for incoming impact-story requests filtered by authenticated verifier email, with minimal selected fields and no token/claim exposure.
- Extended `VerificationsClient` request model and rendering to support `request_type: skill | impact_story`, read-only incoming impact rows, and status badge handling including `failed`.
- Added/updated UI tests:
  - `tests/ui/verifications-client.test.tsx`
  - `tests/ui/verifications-page.test.tsx`

Why:

- PRO-87 reported empty Verification tab even when profile verification was confirmed.
- Root cause was data-source mismatch: tab used only `skill_verification_requests`, while confirmed profile-level verification flow is also represented in `impact_story_verification_requests`.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/verifications-client.test.tsx tests/ui/verifications-page.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS with existing repo warnings about `<img>` in landing components)

Open risks / TODO:

- Incoming impact requests are intentionally read-only in this tab; response action remains through existing email-token flow (`/verify/[token]`).
