# Project Change Entry

- Date/time (UTC): 2026-02-26T08:28:09Z
- Branch: master
- Base commit: 4be09a08
  What changed:
- Fixed `GET /api/expertise/taxonomy?l3_id=...` to load L4 skills without FK-hint-dependent joins and enrich parent context via explicit taxonomy table reads.
- Updated add-skill client API to throw actionable errors for failed L3 L4 lookups instead of returning silent empty arrays.
- Updated add-skill drawer to surface L4 fetch failures with a destructive toast and reset stale local L4 options.
- Added migration `src/db/migrations/20260226102000_add_skills_taxonomy_parent_foreign_keys.sql` to create `skills_taxonomy_cat_id_fkey` and `skills_taxonomy_cat_id_subcat_id_fkey`.
- Added/updated tests:
  - `tests/api/expertise-taxonomy-route.test.ts` (new L3 mode coverage)
  - `src/app/app/i/expertise/components/add-skill/api.test.ts` (L3 fetch error propagation)

Why:

- PRO-77 reported that browse-categories flow returned no L4 results at Step 4 for any L3 selection.
- Runtime validation showed PostgREST relationship-hint joins failed with `PGRST200` because expected parent FK names were absent in schema cache.
- Silent client fallback (`[]`) hid the backend error and presented misleading “No matching skills found” UX.

How to verify:

- `npm run test -- tests/api/expertise-taxonomy-route.test.ts src/app/app/i/expertise/components/add-skill/api.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run db:drift-check`
- `npm run db:migrate`
- Validate runtime constraints and legacy join path:
  - check `pg_constraint` contains `skills_taxonomy_cat_id_fkey` and `skills_taxonomy_cat_id_subcat_id_fkey`
  - run a Supabase select using `skills_categories!skills_taxonomy_cat_id_fkey` and `skills_subcategories!skills_taxonomy_cat_id_subcat_id_fkey` and confirm no `PGRST200`

Open risks / TODO:

- Browse Step 4 still filters within the fetched L3 subset and remains capped by route limit.
- Migration runner should be executed sequentially; concurrent runs can race on ledger/DDL ordering.
