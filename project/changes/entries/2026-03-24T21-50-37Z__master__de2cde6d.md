# Project Change Entry

- Date/time (UTC): 2026-03-24T21:50:37Z
- Branch: master
- Base commit: de2cde6d

What changed:
- Added `src/db/migrations/20260324213000_add_feedback_tokens_id_compat.sql` to add and backfill `feedback_tokens.id`, preserve `token`, backfill `token_hash`, and reconnect legacy `feedback_tokens` and `capability_tokens` rows.
- Updated `src/lib/feedback/service.ts` so new feedback-token writes persist the legacy raw `token` alongside canonical `id`, `token_hash`, and `capability_token_id`.
- Added `tests/lib/feedback-invite-issuance.test.ts` to lock the compatibility write contract for feedback invite issuance.

Why:
- The connected database lagged behind current code. `feedback_tokens` still required `token` as the primary key and lacked `id`, while runtime code already wrote by `id`.
- Launch evidence showed the build/runtime blocker from the March 22 audit was stale, so the current highest-priority blocker became DB schema parity with the live code path.

How to verify:
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run db:migrate`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run db:audit:migrations`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run test -- tests/lib/feedback-service.test.ts tests/lib/feedback-invite-issuance.test.ts`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && CI=1 NEXT_TELEMETRY_DISABLED=1 npm run build`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && PORT=3101 CI=1 NEXT_TELEMETRY_DISABLED=1 npm run start`

Open risks / TODO:
- Strict org E2E still fails on duplicate `Messages` headings and missing `/role title/i` labeling in the assignment draft resume flow.
- Clean up or reconcile the historical extra migration row `20260317224741_canonicalize_org_role_constraints` only if strict ledger parity becomes a release requirement.
