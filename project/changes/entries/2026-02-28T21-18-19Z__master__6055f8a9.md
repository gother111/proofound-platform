# Project Change Entry

- Date/time (UTC): 2026-02-28T21:18:19Z
- Branch: master
- Base commit: 6055f8a9

What changed:

- Updated schedule guard in `src/app/api/interviews/schedule/route.ts` so only non-cancelled interviews block new scheduling for a match.
- Added compatibility fallback for legacy schemas missing `status`: preserve prior behavior by checking for any existing interview.
- Added regression test coverage in `tests/api/interviews-schedule-route.test.ts`:
- blocks when an active (`scheduled`) interview exists,
- allows progression when only `cancelled` interview exists (then fails later at integration as expected in mocked scenario).

Why:

- After cancelling an interview, organizations could not schedule a replacement because the API treated cancelled records as blocking duplicates.

How to verify:

- `npm run test -- tests/api/interviews-schedule-route.test.ts tests/api/interviews-edit-route.test.ts tests/api/interviews-cancel-route.test.ts tests/ui/organization-interviews-page-actions.test.tsx` (PASS)
- `npm run lint` (PASS with 2 existing warnings in landing image components)
- `npm run typecheck` (PASS)

Open risks / TODO:

- Full production build remains blocked by unrelated existing `pdfjs-dist/webpack.mjs` resolution errors in expertise components.
