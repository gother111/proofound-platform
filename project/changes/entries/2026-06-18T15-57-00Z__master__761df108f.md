# Project Change Entry

- Date/time (UTC): 2026-06-18T15:57:00Z
- Branch: master
- Base commit: 761df108f
  What changed:
- Hardened `.github/workflows/retry-vercel-deploy.yml` so the final prebuilt Vercel deploy step treats the known `api-deployments-free-per-day` rejection as a skipped retry with a GitHub notice instead of failing the whole workflow.
- Added launch-gate coverage that keeps the deploy-step quota recovery and job-summary field in place.

Why:

- Recent GitHub notifications showed repeated `Retry Vercel Deploy Until Synced` failures on `master`.
- The workflow already had a preflight quota check, but Vercel can still reject the actual deploy at publish time when the account is at the daily deployment limit.
- Known quota exhaustion should be visible in the job summary without creating another failed-run email; non-quota deploy failures still fail normally.

How to verify:

- `npm run ci:workflows`
- `npm run test -- tests/scripts/launch-gate-config.test.ts tests/scripts/vercel-deploy-quota-check.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- `npm run docs:freshness` still reports the existing orphan-file warnings in warning mode.
- A future remote workflow run is still needed to confirm the live Vercel quota branch reports the notice as expected when the daily quota is exhausted.
