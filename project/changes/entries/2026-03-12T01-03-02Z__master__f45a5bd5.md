# Project Change Entry

- Date/time (UTC): 2026-03-12T01:03:02Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Added a first-class `engagement_verifications` workflow with a separate state machine, additive SQL migration, and post-`hire` auto-creation from the decision workflow.
- Added `PATCH /api/engagement-verifications/[id]` plus org and individual interview surface updates that render engagement verification separately from the decision state.
- Normalized new engagement-type writes to the locked four-value corridor and added focused workflow, API, service, projection, and UI tests.

Why:

- Block 8 requires `hire` / `engage` to remain a decision outcome while engagement verification becomes a later trust state with independent confirmations and optional evidence.

How to verify:

- `npx vitest run tests/lib/workflow-contracts.test.ts tests/lib/engagement-verifications.test.ts tests/lib/interviews-process-state.test.ts tests/api/engagement-verifications-route.test.ts tests/api/decisions-route.test.ts tests/api/core-matching-profile-route.test.ts tests/ui/matching-profile-setup-focus.test.tsx tests/ui/organization-interviews-page-actions.test.tsx`
- `npm run lint`
- `npm run typecheck` currently fails on unrelated pre-existing errors in `src/actions/profile.ts`, `src/app/api/expertise/**`, `src/app/app/o/[slug]/profile/page.tsx`, `src/components/matching/AssignmentBuilder*.tsx`, and `src/lib/profileStorage.ts`.

Open risks / TODO:

- Phase 2: review whether historical `hire` decisions should be backfilled into engagement verification rows once duplicate-handling and ownership rules are agreed.
- `engagement_type` is nullable on auto-created rows until a candidate or org confirmation supplies the canonical value.
- `npm run db:migrate` is currently blocked by a pre-existing checksum mismatch on `20260309113000_add_residual_lifecycle_contracts`.
