# Project Change Entry

- Date/time (UTC): 2026-03-09T10:24:15Z
- Branch: master
- Base commit: 7b9cc13b
  What changed:
- Fixed `src/db/migrations/20260309113000_add_residual_lifecycle_contracts.sql` so the `matches` backfill joins `intro_workflows` via `mrs.match_id` and qualifies `m.shortlisted_at`.
- Applied `20260309113000_add_residual_lifecycle_contracts.sql` to the configured Supabase database.

Why:

- The pending production migration failed on PostgreSQL syntax/ambiguity errors and could not be applied until the SQL was corrected.

How to verify:

- `npm run db:backup:checkpoint`
- `npm run db:migrate`
- `npm run db:audit:migrations`
- `npx vitest run tests/lib/residual-lifecycle-contracts.test.ts`
- Query `public.app_migration_ledger` for `20260309113000_add_residual_lifecycle_contracts`.

Open risks / TODO:

- `npm run db:drift-check` still reports additional schema work in the working tree, so the next production DDL pass should start with another drift review.
