# Project Change Entry

- Date/time (UTC): 2026-03-06T09:42:33Z
- Branch: master
- Base commit: df793db1
  What changed:
- Added a versioned internal Python document-intelligence contract, including stricter TS-to-Python response validation and explicit internal service metadata.
- Added a Postgres-backed `python_internal_jobs` queue, an internal enqueue route, and a cron worker route that drains queued Python jobs with lease/retry semantics.
- Updated ops surfaces for the new Python layer: `vercel.json`, `.env.example`, `README.md`, `docs/ENV_VARIABLES.md`, `docs/API_REFERENCE.md`, `project/Architecture.md`, and local Python script runtime selection in `package.json`.
- Added `scripts/sync-cron-job-org.mjs` plus `npm run cron:sync`, removed the Hobby-incompatible Vercel sub-daily cron entry, and created cron-job.org job `7348941` for `/api/cron/python-internal-worker`.

Why:

- The platform now has a dedicated internal Python compute layer for document-intelligence and batch/reporting-style work without expanding Python into public business APIs.
- Queue-backed internal workers let TypeScript keep orchestration and public contracts while Python owns heavier document parsing, extraction, and reporting workloads.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/lib/python-cv-proxy.test.ts tests/api/cv-import-suggest-route.test.ts tests/api/cv-import-wizard-suggest-route.test.ts tests/api/internal-python-jobs-route.test.ts tests/api/cron-python-internal-worker-route.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test:python`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH node scripts/generate-api-reference.mjs`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run docs:freshness`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run cron:sync`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:migrate`

Open risks / TODO:

- The target migration `20260306103000_add_python_internal_jobs` is now applied on the configured remote database, but `npm run db:migrate` still exits non-zero because an unrelated pending local migration file (`20260306113000_fix_symbol_skill_alias_normalization.sql`) also exists in the dirty worktree and fails remotely.
- `docs:freshness` still warns about missing metadata in legacy `project/Documentation.md`; that warning predates this change and remains non-blocking.
- Build and lint still surface the existing `@next/next/no-img-element` warnings in landing components.
