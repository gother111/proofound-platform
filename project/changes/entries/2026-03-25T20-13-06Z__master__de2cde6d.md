# Project Change Entry

- Date/time (UTC): 2026-03-25T20:13:06Z
- Branch: master
- Base commit: de2cde6d
What changed:
- Tightened `tests/api/launch-surface-inventory.test.ts` so every compiled `src/app/api` route must classify as either `active_launch_path` or `internal_only_launch_ops`, while keeping the required active and internal-only route assertions.
- Generated a fresh `2026-03-25` launch-validation evidence bundle under `.artifacts/launch-validation-2026-03-25/`, refreshed `.artifacts/launch-smoke-report.json`, and rewrote `.artifacts/launch-readiness-summary.md` to match current evidence.

Why:
- The locked MVP launch corridor needs compiled-surface enforcement, not hand-maintained API allow/deny drift, and the go/no-go memo must reflect fresh live evidence instead of stale earlier summaries.

How to verify:
- `npm run test -- tests/api/launch-surface-inventory.test.ts tests/api/launch-page-inventory.test.ts src/lib/launch/__tests__/surface-policy.test.ts src/lib/__tests__/middleware-launch-archive.test.ts tests/ui/archived-mvp-routes.test.ts`
- `npm run test:privacy`
- `npm run test:privacy:extended`
- `PLAYWRIGHT_SERVER_MODE=prod BASE_URL=https://proofound.io npm run test:e2e:org-trust:smoke`
- `BASE_URL=https://proofound.io npm run test:launch:smoke`
- `BASE_URL=https://proofound.io npm run monitor:launch`
- `curl https://proofound.io/api/health`
- `curl https://proofound.io/api/monitoring/launch-status`

Open risks / TODO:
- The local prod build and boot pair is currently broken in this workspace, so the requested local prod reruns remain blocked.
- The repaired route-surface test now exposes `18` still-active routes outside the locked corridor.
- The authoritative live launch-status endpoint still reports `missing_smoke_artifact` even after the workspace smoke artifact refresh.
