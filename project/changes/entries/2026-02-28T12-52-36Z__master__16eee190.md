# Project Change Entry

- Date/time (UTC): 2026-02-28T12:52:36Z
- Branch: master
- Base commit: 16eee190

What changed:

- Refactored `src/components/dashboard/TasksCard.tsx` from static content to live, persona-aware tasks loaded from `/api/momentum/summary` (individual and organization support).
- Refactored `src/components/dashboard/ProjectsCard.tsx` to support both individual (`/api/projects`) and organization (`/api/organizations/[orgId]/projects`) data paths with persona-correct links.
- Updated dashboard wiring to pass persona context:
  - `src/components/dashboard/DraggableDashboard.tsx` now calls `TasksCard` with `persona="individual"`.
  - `src/app/app/o/[slug]/home/OrgDashboardClient.tsx` now passes org persona/context into `TasksCard` and `ProjectsCard`.
- Replaced settings placeholder dashboard rendering by making `src/components/dashboard/CustomizableDashboard.tsx` a thin wrapper around live `DraggableDashboard` for individual persona.
- Added/updated focused tests:
  - `tests/ui/tasks-card.test.tsx`
  - `tests/ui/projects-card.test.tsx`
  - `tests/ui/org-dashboard-client.test.tsx` (now verifies org context props passed to tasks/projects widgets)

Why:

- PRO-101 required dashboard tiles to show changing, persona-correct information instead of static or wrong-persona tile content.

How to verify:

- `npm run test -- tests/ui/tasks-card.test.tsx tests/ui/projects-card.test.tsx` (PASS)
- `npm run test -- tests/ui/org-dashboard-client.test.tsx tests/ui/draggable-dashboard-while-away-visibility.test.tsx` (PASS)
- `npm run lint` (PASS with existing unrelated warnings in landing image usage)
- `npm run typecheck` (PASS)

Open risks / TODO:

- Manual smoke checks on `/app/i/home`, `/app/o/<slug>/home`, and `/app/i/settings` still recommended for final UX validation.
- Vitest reported a non-blocking dev websocket port warning (`24678`) during one focused run; tests still passed.
