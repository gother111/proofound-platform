# Project Change Entry

- Date/time (UTC): 2026-05-21T08:56:04Z
- Branch: master
- Base commit: ddc15882
  What changed:
- Redesigned Individual Home page to display computed readiness steps and trust/privacy controls in a two-column bento layout.
- Added `hideHeader` prop to `DeferredOrgMessagesClient` and `OrgMessagesClient` to avoid dual H1 in the Communications hub.
- Updated Playwright mobile E2E spec expectations to match new headings and layout bounds.
- Replaced console error logs with standardized `log.error` calls in organization detail and visibility routes.

Why:

- Align the home experience with the private-first, proof-first corridor MVP requirements.
- Standardize headings to follow strict single H1 rule per route.
- Ensure automated UI/UX and mobile view E2E specs pass without regression.

How to verify:

- Run typecheck, lint, and E2E mobile suites:
  ```bash
  npm run typecheck
  npm run lint
  npm run test:e2e:mobile
  ```
- Perform production build:
  ```bash
  npm run build
  ```

Open risks / TODO:

- None.
