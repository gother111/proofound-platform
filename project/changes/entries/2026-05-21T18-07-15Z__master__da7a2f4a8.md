# Project Change Entry

- Date/time (UTC): 2026-05-21T18:07:15Z
- Branch: master
- Base commit: da7a2f4a8
  What changed:
- Aligned `npm run gates:mvp:strict` environment validation with the launch internal-auth fallback.
- The strict gate now accepts either `INTERNAL_API_SECRET` or `CRON_SECRET` for protected launch checks and redacts both values in command logs.
- Extended the launch-gate config guard to cover the strict runner's internal secret contract.

Why:

- Lower-level launch gates now support `INTERNAL_API_SECRET` first and `CRON_SECRET` second, but the consolidated strict runner still required `CRON_SECRET` specifically.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `npm run typecheck`
- `npm run lint`
- `git diff --check`

Open risks / TODO:

- Live production-candidate strict gates still require a real target, trusted origin, server-only internal secret, restore evidence, and final protected gate output.
