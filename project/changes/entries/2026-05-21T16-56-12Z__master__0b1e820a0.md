# Project Change Entry

- Date/time (UTC): 2026-05-21T16:56:12Z
- Branch: master
- Base commit: 0b1e820a0
  What changed:
- `scripts/vercel-preflight.mjs` now requires `KV_REST_API_URL` and `KV_REST_API_TOKEN` for production, preview, and development Vercel targets.
- Vercel env parity comparison now includes the KV rate-limit dependency keys.
- Added script guardrail coverage for required launch dependency keys in Vercel preflight.

Why:

- Rate limiting is a launch dependency in deploy-readiness and launch-status. Vercel preflight should catch missing KV env keys before a production-candidate deploy can appear ready.

How to verify:

- `npm run test -- tests/scripts/vercel-preflight.test.ts tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This verifies Vercel env key presence/parity, not live KV behavior. Final launch readiness still requires live target launch-status, perf-status, backup, restore, and go/no-go evidence.
