# Project Change Entry

- Date/time (UTC): 2026-05-21T05:59:07Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Reframed the `org_reviewer` role rule in the active technical requirements authority doc and legacy mirror from reviewing candidates to reviewing proof submissions.
- Strengthened launch-gate coverage for both the authority stack and legacy technical mirror so the stale candidate-review sentence cannot return.

Why:

- `PRD_TECHNICAL_REQUIREMENTS.aligned-rewrite.2026-03-11.md` is part of the active MVP authority stack, so role copy must match the locked proof-submission review corridor.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "locked MVP authority stack|legacy technical and runbook mirrors" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- Targeted scan for the stale and corrected `org_reviewer` role sentence in the active technical requirements docs.
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `listen EPERM 0.0.0.0:24678` warning before passing.
