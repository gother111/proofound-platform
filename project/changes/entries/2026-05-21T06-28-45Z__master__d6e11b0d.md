# Project Change Entry

- Date/time (UTC): 2026-05-21T06:28:45Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Replaced ad hoc `console.log` / `console.error` output in `/api/cron/performance-check` with structured `log.info`, `log.warn`, and `log.error` events.
- Replaced organization onboarding RLS-continuation success-path `console.log` messages with structured warnings carrying only the organization id context.
- Added runtime debug guardrail coverage so the old cron and onboarding console strings cannot return to the active paths.

Why:

- Active launch operations and onboarding paths should use the repo's structured, redacted logging surface rather than ad hoc console messages.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This slice only tightened the active cron/onboarding debug-output residue found in the scaffold scan; the broader MVP surface sweep remains in progress.
