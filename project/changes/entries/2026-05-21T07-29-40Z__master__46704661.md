# Project Change Entry

- Date/time (UTC): 2026-05-21T07:29:40Z
- Branch: master
- Base commit: 46704661
  What changed:
- Replaced raw console diagnostics in the verification request feed, `/api/verification/requests`, and `/api/verification/requests/email-hint` with structured `verification.request_feed.*`, `verification.requests.get_failed`, and `verification.email_hint.get_failed` events.
- Added focused coverage for request-feed route authorization/loading/failure behavior and email-hint route failure logging.
- Extended runtime debug-output guardrails so the old request-feed and email-hint console strings cannot return.

Why:

- Keeps active proof-verification request surfaces launch-observable without leaking auth or verifier emails into unstructured console output.
- Preserves safe public response bodies and non-blocking feed enrichment behavior while making backend failures easier to triage.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/custom-verification-routes.test.ts tests/api/verification-requests-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning, but the focused and launch-gate suites passed.
- Branch remains behind `origin/master` by one commit, so do not push this checkpoint until remote drift is reconciled and checks are rerun.
