# Project Change Entry

- Date/time (UTC): 2026-03-04T17:00:13Z
- Branch: master
- Base commit: 2de1154e

What changed:

- Added `html-to-design` MCP server entry to `mcp-config.json` with HTTP transport.
- Verified endpoint handshake and reported server/tool capabilities.

Why:

- Register the provided MCP endpoint in repo config so it can be used directly from this workspace.

How to verify:

- `node -e "JSON.parse(require('fs').readFileSync('mcp-config.json','utf8')); console.log('mcp-config.json: valid JSON')"` (PASS)
- MCP initialize check:
  `curl -sS --max-time 8 -X POST 'https://h2d-mcp.divriots.com/6540a2d3-51b2-4a42-996d-211f3031113c/mcp' ...` (PASS; server `html.to.design@0.0.2`)
- `npm run docs:freshness` (PASS with existing warning about `project/Documentation.md` metadata)
- `npm run lint` (PASS with existing landing `<img>` warnings)
- `npm run typecheck` (PASS)

Open risks / TODO:

- Existing lint warnings unrelated to this change remain in landing components.
