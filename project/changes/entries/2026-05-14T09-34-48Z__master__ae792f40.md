# Project Change Entry

- Date/time (UTC): 2026-05-14T09:34:48Z
- Branch: master
- Base commit: ae792f40
  What changed:
- Added a top-level superseded banner to `PRD_for_a_web_platform_MVP.master-latest.md`.
- Marked the file as historical and listed the active MVP authority stack.
- Updated `docs/DOCS_REGISTRY.md` so the file is classified as `historical`.

Why:

- Prevent older broad-platform PRD language from being mistaken for launch scope.
- Preserve the file in place because root repo instructions and the docs registry still reference it.

How to verify:

- `npm run docs:freshness` exits 0 in warning mode.
- `npm run lint` exits 0.
- `npm run typecheck` currently fails because tracked file `src/app/api/data-export/route.ts` is deleted in the existing workspace state.

Open risks / TODO:

- `PRD_for_a_web_platform_MVP.master-latest.txt` was requested as an example but is not present in the repo.
- `npm run docs:freshness` still reports two unrelated orphan-doc warnings: `.artifacts/mvp-ai-pilot-readiness-left-2026-05-11.md` and `supabase/migrations/README.md`.
