# Project Change Entry

- Date/time (UTC): 2026-05-21T06:37:35Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Replaced legacy analytics development console output with structured `analytics.legacy.event_tracked` debug logging.
- Replaced legacy analytics tracking console errors with structured `analytics.legacy.track_failed` errors.
- Extended runtime debug-output guardrails so the old analytics console prefixes cannot return.

Why:

- Even development-only active analytics diagnostics should use the structured logging surface and avoid ad hoc console prefixes with event context.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/actions/auth.test.ts tests/lib/runtime-debug-output-guardrails.test.ts tests/lib/api-observability-local-smoke.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- The broader MVP surface sweep remains active; logger implementation internals, CLI progress output, and client-only development diagnostics remain intentionally outside this server-side analytics slice.
