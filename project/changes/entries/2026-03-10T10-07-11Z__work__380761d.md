# Project Change Entry

- Date/time (UTC): 2026-03-10T10:07:11Z
- Branch: work
- Base commit: 380761d
  What changed:
- Redacted committed Supabase OAuth and SMTP secrets from both JSON auth-config artifacts under `artifacts/supabase-auth-config/` by replacing secret values with `REDACTED` placeholders.

Why:

- Remove live-looking credentials from version control to remediate a critical secret exposure vulnerability.

How to verify:

- `rg -n "external_google_secret|external_linkedin_oidc_secret|smtp_pass" artifacts/supabase-auth-config/*.json`
- Confirm matching values are `"REDACTED"`.

Open risks / TODO:

- Rotating any previously exposed real credentials should be handled out-of-band in the relevant provider consoles.
