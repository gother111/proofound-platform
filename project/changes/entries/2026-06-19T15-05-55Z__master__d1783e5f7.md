# Project Change Entry

- Date/time (UTC): 2026-06-19T15:05:55Z
- Branch: master
- Base commit: d1783e5f7
  What changed:
- Grouped the public portfolio introduction, share, export, and proof-summary actions into one
  uninterrupted action stack.
- Moved the privacy/safety explanation below the full action group so it supports the controls
  without splitting them on narrow screens.
- Added a UI regression assertion for the action order and safety-copy placement.

Why:

- Browser verification showed the public portfolio mobile header split the primary introduction
  action from the copy/export actions, making the launch-visible action area feel less coherent.

How to verify:

- Browser: `/portfolio/demo-proofound` at 390px width keeps all action controls ordered before the
  privacy explanation, with no horizontal overflow.
- Browser: `/portfolio/demo-proofound` at default desktop width keeps the same ordered action stack.
- `npm test -- tests/ui/public-portfolio-page.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- `docs:freshness` still reports existing warning-mode orphan artifact registry warnings unrelated
  to this UI change.
