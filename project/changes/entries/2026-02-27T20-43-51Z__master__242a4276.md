# Project Change Entry

- Date/time (UTC): 2026-02-27T20:43:51Z
- Branch: master
- Base commit: 242a4276

What changed:

- Refactored `src/app/portfolio/[handle]/page.tsx` to make the public individual portfolio route read-only.
- Removed owner editing CTAs (`Edit profile`, `Add proof`, `Request verification`) from header, hero, empty states, and bottom action area.
- Moved owner-only return navigation to top-left while preserving `returnTo` behavior.
- Removed sections `Credibility at a glance` and `Verification activity`.
- Reordered sections so `Proof-based summary` renders at the top and renamed `Open to` to `My next challenge`.
- Added `Mission & vision` tabbed section and owner-only rendering rules for location, mission, and vision.
- Updated featured proof logic to include only finalized `skill_proofs` (`verified === true`) and ignore impact stories/pending items.
- Renamed `Skills (with evidence)` to `Expertise snapshot` and replaced with blank placeholder content.
- Updated `tests/ui/public-portfolio-page.test.tsx` assertions to match the new read-only surface and featured-proof filtering behavior.

Why:

- Implement `PRO-97` acceptance criteria for public portfolio cleanup, owner-action removal, section realignment, and proof-quality constraints.

How to verify:

- `npm run test -- tests/ui/public-portfolio-page.test.tsx` (PASS)
- `npm run lint` (PASS with existing unrelated warnings in landing files)
- `npm run typecheck` (PASS)

Open risks / TODO:

- Product privacy semantics may evolve for location/mission/vision visibility and should be revalidated if policy changes.
- `Expertise snapshot` selection/population logic is intentionally deferred and currently placeholder-only.
