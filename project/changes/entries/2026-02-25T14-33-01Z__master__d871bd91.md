# Project Change Entry

- Date/time (UTC): 2026-02-25T14:33:01Z
- Branch: master
- Base commit: d871bd91
  What changed:
- Added proof upload constraints for proof documents only:
- Allowed proof formats are now `PNG/JPG/JPEG/HEIF/HEIC/PDF`.
- Enforced a 10MB proof upload limit and added shared proof constants/utilities in `src/lib/proofs/constants.ts`.
- Updated `/api/upload/document` to apply strict type and size checks when `category=proof`.
- Updated client-side `validateFile` to enforce the same rules for proof uploads while preserving existing artifact/certificate behavior.
- Enforced proof count cap per skill in `/api/expertise/user-skills/[id]/proofs` (max 5 proofs).
- Updated add/edit skill proof UI to show accepted formats, size limit, and proof-count guidance.
- Edit-skill proof add action is disabled once the proof cap is reached.
- Added/updated tests:
- `tests/api/expertise-user-skill-proofs-route.test.ts` (limit enforcement)
- `tests/ui/edit-skill-window-proofs.test.tsx` (UI lock at proof cap)
- `tests/lib/upload-validate-file.test.ts` (proof file validation)

Why:

- Follow-up requirement from PRO-37 review requested explicit proof file format support, size cap, and attach-count limit.

How to verify:

- `npm run test -- tests/api/expertise-user-skill-proofs-route.test.ts tests/ui/edit-skill-window-proofs.test.tsx tests/ui/add-skill-drawer-proof-verification.test.tsx tests/lib/upload-validate-file.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run build`
- `git add src/db/migrations/20260225170000_add_document_skill_proof_type.sql && npm run db:drift-check && git reset HEAD src/db/migrations/20260225170000_add_document_skill_proof_type.sql`

Open risks / TODO:

- Proof validation accepts either MIME or extension for HEIF/HEIC compatibility. If stronger content validation is needed, add server-side MIME sniffing.
- Existing branch has additional in-progress PRO-37 files beyond this follow-up delta.
