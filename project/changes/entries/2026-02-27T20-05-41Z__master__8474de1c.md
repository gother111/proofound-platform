# Project Change Entry

- Date/time (UTC): 2026-02-27T20:05:41Z
- Branch: master
- Base commit: 8474de1c
  What changed:
- Redesigned `src/app/portfolio/[handle]/page.tsx` to a proof-first IA with mode-aware actions, no public internal-nav leakage, and robust section empty states.
- Added reusable empty state primitive: `src/components/public-profile/PublicProfileEmptyState.tsx`.
- Added public-safe endpoints for shared-link actions:
  - `src/app/api/portfolio/public/[handle]/export/route.ts`
  - `src/app/api/portfolio/public/[handle]/summary/route.ts`
- Added public export data helper path in `src/lib/portfolio/export-data.ts`:
  - `fetchPublicTrustExportDataByHandle(...)`
- Updated action buttons:
  - `CopyTextButton` -> supports optional `endpoint` and label `Copy recruiter summary`
  - `DownloadPdfButton` -> supports optional `endpoint`
- Added/updated tests:
  - `tests/ui/public-portfolio-page.test.tsx`
  - `tests/api/public-portfolio-export-route.test.ts`
  - `tests/api/public-portfolio-summary-route.test.ts`

Why:

- PRO-93 required a public-viewer-first, proof-centric profile experience that still feels intentional when sparse, plus public-safe shared-link actions for PDF/summary export.

How to verify:

- Targeted:
  - `npm run test -- tests/ui/public-portfolio-page.test.tsx tests/api/public-portfolio-export-route.test.ts tests/api/public-portfolio-summary-route.test.ts tests/lib/public-profile-metadata.test.ts`
- Full:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test` (known unrelated failures in `tests/actions/create-impact-story.test.ts` and `tests/actions/profile.test.ts` around impact-story verification mocks)
  - `npm run build`

Open risks / TODO:

- Public proof/verification sections depend on existing RLS visibility; some profiles may show sparse blocks until more data is published.
- Existing unrelated test failures in impact-story action suites should be addressed in a dedicated follow-up.
