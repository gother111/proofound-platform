# Project Change Entry

- Date/time (UTC): 2026-03-13T14:46:10Z
- Branch: master
- Base commit: 4767ea77
  What changed:
- Tightened launch smoke freshness to a shared 60-minute threshold for smoke-backed launch monitors.
- Extended `/api/monitoring/launch-status` with explicit readiness plus smoke freshness metadata and changed stale or missing smoke evidence to return a blocked non-green response.
- Forced persisted fallback without a smoke artifact to synthesize failing smoke rows so summary counts remain conservative.
- Aligned `scripts/go-no-go-check.ts` to the same threshold and readiness contract.
- Added focused tests for fresh smoke, stale smoke, and missing smoke artifact behavior.

Why:

- The previous launch-status path could return `ok:true` while using stale smoke evidence, which violated the launch runbook and created a false-green operator signal.

How to verify:

- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run test -- tests/lib/launch-synthetic-monitors.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run typecheck`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run lint`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run docs:freshness`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run build`

Open risks / TODO:

- Live local runtime rerun of `/api/monitoring/launch-status` is still UNVERIFIED in this block because no app server was running on `127.0.0.1:33100` during close-out.
- Org corridor latency brittleness remains the next high-risk launch item.
