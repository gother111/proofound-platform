# Project Change Entry

- Date/time (UTC): 2026-05-21T08:43:54Z
- Branch: master
- Base commit: 2308f5fa
  What changed:
- Replaced raw console diagnostics in `/api/organizations/[orgId]/visibility` with structured `organization.visibility.*` server log events.
- Added a fail-closed PUT guard so non-`PGRST116` visibility read failures return a safe 500 instead of falling through to the create path.
- Added route tests for structured GET read, PUT read, and organization publication update failures, plus runtime guardrails for the old console strings.
- Updated `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md` with the organization visibility sweep checkpoint.

Why:

- Organization trust-page visibility and publication controls are launch-critical MVP surfaces; their failure diagnostics should be structured, privacy-conscious, and guardrailed against raw console output.

How to verify:

- `npm test -- --run tests/api/organization-visibility-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- 'src/app/api/organizations/[orgId]/visibility/route.ts' tests/api/organization-visibility-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`

Open risks / TODO:

- The broader MVP sweep is still active; remaining route/lib diagnostics outside the organization visibility surface should continue in separate scoped checkpoints.
- The worktree still has unrelated unstaged edits in `src/app/app/i/home/page.tsx` and `tsconfig.json`; they were not touched by this change.
