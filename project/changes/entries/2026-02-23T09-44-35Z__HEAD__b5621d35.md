# Project Change Entry

- Date/time (UTC): 2026-02-23T09:44:35Z
- Branch: HEAD
- Base commit: b5621d35
  What changed:
- Added BYOC candidate invite persistence with `org_candidate_invites` in `src/db/schema.ts` and migration `src/db/migrations/20260223110000_add_org_candidate_invites.sql`.
- Added invite RLS policy definitions in `src/db/policies.sql`.
- Added org invite management APIs:
  - `src/app/api/organizations/[orgId]/candidate-invites/route.ts`
  - `src/app/api/organizations/[orgId]/candidate-invites/[inviteId]/route.ts`
- Added candidate token lifecycle APIs:
  - `src/app/api/candidate-invites/[token]/route.ts`
  - `src/app/api/candidate-invites/[token]/claim/route.ts`
  - `src/app/api/candidate-invites/[token]/proof-card/route.ts`
- Added candidate invite UX:
  - `src/app/candidate-invite/[token]/page.tsx`
  - `src/app/candidate-invite/[token]/CandidateInviteClient.tsx`
- Added organization review UX:
  - `src/components/organization/OrgCandidatesWorkspace.tsx`
  - `src/components/organization/OrgCandidateInvitesPanel.tsx`
  - `src/app/app/o/[slug]/candidates/page.tsx`
- Added candidate invite email template/sender in `emails/CandidateInvite.tsx` and `src/lib/email.ts`.
- Fixed org invite URL routing mismatch via `src/app/accept-invite/page.tsx` and updated sender call path in `src/actions/org.ts`.
- Added auth `next` return-path support for invite journey in:
  - `src/actions/auth.ts`
  - `src/components/auth/SignIn.tsx`
  - `src/components/auth/SignupForm.tsx`
  - `src/app/(auth)/login/page.tsx`
- Added invite analytics events in `src/lib/analytics/constants.ts`.
- Added tests for BYOC API behavior:
  - `tests/api/org-candidate-invites-route.test.ts`
  - `tests/api/candidate-invite-claim-route.test.ts`
- Added shared client-safe constants in `src/lib/candidate-invites-shared.ts` and kept crypto helpers in `src/lib/candidate-invites.ts` to fix build bundling.
- Updated API reference with new endpoints in `docs/API_REFERENCE.md`.

Why:

- Deliver MVP workflow for organizations to invite existing candidates, let candidates submit Proof Cards, and review structured evidence in-app.
- Reuse existing profile snippet artifact (`/p/[token]`) without coupling BYOC invites to matching score ingestion.

How to verify:

- `npm run lint` (pass)
- `npm run typecheck` (pass)
- `npm run test -- tests/api/profile-snippet-route.test.ts tests/api/org-candidate-invites-route.test.ts tests/api/candidate-invite-claim-route.test.ts tests/ui/share-profile-dialog.test.tsx` (pass)
- `npm run test` (pass)
- `npm run build` (pass)
- Manual checks:
  - Org admin can create, resend, revoke invites.
  - Candidate token page enforces auth + invited email match.
  - Candidate can generate snippet and submit proof card.
  - Org invited-candidates tab shows submitted cards with proof link.

Open risks / TODO:

- `npm run build` and tests run with missing local `DATABASE_URL` and use fallback mock DB warnings in this environment.
- No background expiry job was added; invites are marked expired on read/mutation paths.
- No automated e2e coverage yet for candidate invite claim + submit browser flow.
