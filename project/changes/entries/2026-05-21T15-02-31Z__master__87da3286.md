# Project Change Entry

- Date/time (UTC): 2026-05-21T15:02:31Z
- Branch: master
- Base commit: 87da3286
  What changed:
- Replaced raw realtime messaging console diagnostics with local `proofound:client-diagnostic` events.
- Covered presence join/leave, typing indicator failures, single-message read failures, mark-all-read failures, conversation refresh failures, and realtime-thread send failures.
- Redacted presence diagnostics to self/other plus count instead of raw presence payloads.
- Added runtime debug-output guardrails and appended the slice to `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md`.

Why:

- Keeps active messaging diagnostics structured and privacy-aware without exposing raw user/presence payloads or errors through browser console output.
- Continues launch-surface diagnostic hygiene across the individual and organization conversations workflow.

How to verify:

- `npm test -- --run tests/routes/organization-messages-page.test.tsx tests/ui/deferred-messages-client.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/hooks/useRealtimeMessages.ts src/components/messaging/RealtimeMessageThread.tsx tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment, but the suites passed.
- Other active MVP surfaces still have raw client diagnostics and need follow-up slices before the full goal can be completed.
