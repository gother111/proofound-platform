# Project Change Entry

- Date/time (UTC): 2026-03-10T23:07:05Z
- Branch: master
- Base commit: 09fe129d
  What changed:
- Gated the remaining legacy MVP APIs behind `FF_LEGACY_MVP_SURFACES`, including assignment templates, evidence pack, goals, momentum summary, and org dashboard endpoints.
- Aligned interview authorization labels, private check-in copy, fairness admin access, and launch-corridor UI text across the remaining touched surfaces.
- Updated the related Vitest expectations and retained the strict and PRD e2e spec updates in the rollout batch.

Why:

- Finish the launch-corridor rollout with consistent non-launch API behavior and copy after the lighter page replacements landed.
- Keep the deployable surface coherent so the reduced-build version of the app does not expose legacy routes with stale wording or mismatched tests.

How to verify:

- Run `npx vitest run tests/api/momentum-summary-cache-flag-route.test.ts tests/assignment-templates.test.ts`.
- Run `npm run typecheck`.
- Run `VERCEL=1 npm run build`.

Open risks / TODO:

- The touched Playwright specs were not executed in this session, so end-to-end coverage for those flows still depends on the next CI or manual browser pass.
