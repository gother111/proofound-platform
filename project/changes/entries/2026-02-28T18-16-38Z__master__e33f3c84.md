# Project Change Entry

- Date/time (UTC): 2026-02-28T18:16:38Z
- Branch: master
- Base commit: e33f3c84

What changed:

- Hardened `src/app/api/verify/[token]/route.ts` skill lookup to retry compatibility selects on both relation ambiguity and missing-column schema drift.
- Added `normalizeSkillVerificationRecord` defaults for legacy rows missing integrity/auth columns.
- Updated skill GET/POST token error handling so `404` is only for true not-found token lookups and non-not-found lookup failures return `500`.
- Added POST fallback update retry with a legacy-safe payload when newer response/integrity columns are unavailable.
- Added regressions in `tests/api/verify-impact-token-route.test.ts` for:
  - GET missing `requester_email_snapshot` compatibility fallback.
  - POST missing integrity columns plus legacy update fallback.
  - GET/POST returning `500` for non-not-found lookup errors.

Why:

- PRO-102 follow-up reported verifier submit/load failures in partially migrated environments where newer `skill_verification_requests` columns may be absent.

How to verify:

- `npm run test -- tests/api/verify-impact-token-route.test.ts` (PASS, 23 tests)
- `npm run lint` (PASS, 2 pre-existing warnings in landing image components)
- `npm run typecheck` (PASS)
- `npm run db:audit:migrations` (PASS, 53 files and DB rows aligned)
- `npm run db:migrate` (PASS, 0 applied / 53 skipped)

Open risks / TODO:

- Compatibility mode intentionally drops writing newer integrity response columns when they are missing, so old schemas receive reduced telemetry until migrations are fully applied.
