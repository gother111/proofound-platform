# Project Change Entry

- Date/time (UTC): 2026-05-21T14:59:20Z
- Branch: master
- Base commit: fc0f411f
  What changed:
- Replaced raw settings privacy/publication `console.error` diagnostics with local `proofound:client-diagnostic` events.
- Covered Privacy Overview visibility-summary load and export failures plus Public Page visibility card load/save/privacy-preflight failures.
- Added runtime debug-output guardrails and appended the slice to `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md`.

Why:

- Keeps active settings privacy and publication controls from leaking raw browser-console diagnostics while preserving the existing user-facing alerts and fallback copy.
- Continues the launch-surface diagnostic hygiene sweep across active MVP settings flows.

How to verify:

- `npm test -- --run tests/ui/privacy-overview-copy.test.tsx tests/ui/portfolio-visibility-card-ai.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/components/settings/PrivacyOverview.tsx src/components/settings/PortfolioVisibilityCard.tsx tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment, but the suites passed.
- Other active MVP surfaces still have raw client diagnostics and need follow-up slices before the full goal can be completed.
