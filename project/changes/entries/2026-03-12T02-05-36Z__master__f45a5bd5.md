# Project Change Entry

- Date/time (UTC): 2026-03-12T02:05:36Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Refactored `src/lib/surveys/sus-calculator.ts` to compute item contributions from `SUS_QUESTIONS` metadata via `scoreQuestionContribution(...)` instead of hardcoded `q1..q10` scoring lines.
- Added `tests/lib/sus-calculator.test.ts` with coverage for a maximum score case, neutral-score case, and out-of-range validation.

Why:

- Remove duplicate source-of-truth for item polarity and make SUS scoring easier to maintain without changing behavior.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/lib/sus-calculator.test.ts` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (FAIL due unrelated pre-existing errors in `src/actions/profile.ts` and `src/lib/profileStorage.ts`)

Open risks / TODO:

- Full-repo typecheck currently fails outside this refactor; resolve unrelated errors before treating global typecheck as green.
