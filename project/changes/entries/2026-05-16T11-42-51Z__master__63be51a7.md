# Project Change Entry

- Date/time (UTC): 2026-05-16T11:42:51Z
- Branch: master
- Base commit: 63be51a7

What changed:

- Made `npm run ai:provider:smoke` use the same value-free preflight gate as launch readiness before it writes `success: true` or exits `0`.
- Exported `isAiProviderSmokePreflightSatisfied` from `src/lib/ai/provider-smoke-artifact.ts` and covered it in `tests/lib/ai-provider-smoke-artifact.test.ts`.
- Refreshed `.artifacts/ai-provider-smoke.json` with the current fail-closed local smoke result.
- Updated `audit/ai-features-verification-2026-05-16.md` with the provider-smoke preflight hardening evidence.

Why:

- The artifact resolver already rejected under-configured smoke evidence, but the smoke command itself could still have reported success from model acceptance alone. The command should not give operators a false green signal when launch prerequisites are incomplete.

How to verify:

- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run ai:provider:smoke` exits `1` in this environment with `Preflight gate: blocked`.
- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test:launch:ai`
- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test -- tests/lib/ai-provider-smoke-artifact.test.ts tests/lib/ai-launch-guardrails.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts`

Open risks / TODO:

- Live Gemini provider behavior remains unverified until server-only provider credentials and hard-cap env are available.
