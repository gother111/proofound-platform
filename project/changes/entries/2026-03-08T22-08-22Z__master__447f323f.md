# Project Change Entry

- Date/time (UTC): 2026-03-08T22:08:22Z
- Branch: master
- Base commit: 447f323f
  What changed:
- Added Block 8 schema contracts in `src/db/migrations/20260308180000_add_block8_security_lifecycle_contracts.sql` for capability tokens, upload lifecycle records, and lifecycle reconciliation operations/targets.
- Added shared services in `src/lib/security/capability-tokens.ts`, `src/lib/uploads/lifecycle.ts`, `src/lib/lifecycle/reconciliation.ts`, and `src/lib/security/lifecycle-jobs.ts`.
- Migrated invite, snippet/share, verification, feedback, upload, delete, and export flows onto the new token/upload/lifecycle contracts.
- Added lifecycle and upload status endpoints plus focused API test updates for the new capability-token behavior.

Why:

- Remove raw bearer-token persistence from app-owned flows.
- Prevent unvalidated uploads from becoming public before validation and promotion.
- Make delete/export operations operationally honest across DB, storage, public surfaces, and audit state.

How to verify:

- `npm run db:migrate`
- `npx vitest run tests/api/profile-snippet-route.test.ts tests/api/feedback-schema.test.ts tests/api/expertise-skill-verification-request-route.test.ts`
- `npm run lint`
- `npm run test:privacy`
- `npm run test:privacy:extended`

Open risks / TODO:

- `npm run typecheck` and `npm run build` are still blocked by pre-existing unrelated type errors in assignment and expertise verification routes.
- `src/db/schema.ts` was not fully synchronized with the additive SQL migration because the worktree already contains overlapping user changes there; current Block 8 services use raw SQL.
- Some async job helpers are implemented as MVP-safe orchestration stubs and still need scheduler wiring plus runbook coverage.
- `npm test` is still red across unrelated baseline suites and adjacent verification test mocks; Block 8-focused suites passed, but the repo-wide suite is not fully reconciled.
