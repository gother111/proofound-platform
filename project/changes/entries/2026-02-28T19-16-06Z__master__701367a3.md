# Project Change Entry

- Date/time (UTC): 2026-02-28T19:16:06Z
- Branch: master
- Base commit: 701367a3
  What changed:
- Added PRO-115 schema + migration support:
  - `profiles.is_beta_testing`
  - `matches.is_test_match`
  - `org_candidate_invites` flow and acceptance linkage fields (`flow_type`, `assignment_id`, `accepted_*`, `match_id`, `conversation_id`)
- Added migration: `src/db/migrations/20260228192000_add_beta_testing_and_test_match_invites.sql` with beta backfill for:
  - `p.samoshko97@icloud.com`
  - `pavlo@edgeoftalent.com`
- Extended candidate invite APIs:
  - org invite create/list now support `flowType=test_match` + assignment validation + beta gating
  - invite read returns flow metadata and assignment context
  - invite claim now has transactional test-match accept path (match upsert + conversation create/link)
  - proof-card submission route now blocks non-proof-card flows
- Added new test-match read APIs:
  - `GET /api/organizations/[orgId]/test-matches`
  - `GET /api/match/test`
- UI changes:
  - left nav shows `Beta testing` marker for beta-flagged users
  - individual and org layouts pass `isBetaTesting`
  - org matching view adds beta-only `Initiate test` modal and assignment-scoped test-match list with message + schedule actions
  - candidate invite page branches for `test_match` vs `proof_card`
  - individual matching page surfaces test matches independent of regular matchability gating
- Added/extended targeted tests for API and UI coverage around PRO-115.

Why:

- Enable a standardized beta-account flag and implement deterministic assignment-linked test-match flow, while preserving existing Proof Card invite behavior.

How to verify:

- `npm run db:migrate` (PASS)
- `npm run lint` (PASS with 2 pre-existing image warnings unrelated to PRO-115)
- `npm run typecheck` (PASS)
- `npm run test -- tests/api/org-candidate-invites-route.test.ts tests/api/candidate-invite-claim-route.test.ts tests/api/organization-test-matches-route.test.ts tests/api/match-test-route.test.ts tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/matching-organization-view-beta.test.tsx tests/ui/candidate-invite-client.test.tsx` (PASS)
- `npm run test:e2e:org:strict` (FAIL in this environment: missing `/opt/homebrew/opt/node@20/bin/node` used by repo Node20 wrapper script)

Open risks / TODO:

- E2E strict org suite is currently blocked locally by missing Node20 binary path expected by wrapper scripts.
- Existing invite email template copy still references Proof Card wording for all invite flows.
