# Project Change Entry

- Date/time (UTC): 2026-02-28T17:47:09Z
- Branch: master
- Base commit: d120d859
  What changed:
- Removed the `Values` step from the individual matching profile setup wizard.
- Rewired setup flow to `Atlas Skills -> Focus & Weights -> Work -> Review`.
- Switched purpose readiness progress signal to server eligibility (`/api/matching-profile`) instead of local wizard tags.
- Added backend fallback in `PUT /api/core/matching/matching-profile` to hydrate missing `valuesTags`/`causeTags` from `individual_profiles.values` and `individual_profiles.causes`.
- Updated focused UI and API tests for the new flow and fallback behavior.

Why:

- The setup wizard duplicated purpose data that already lives in the individual profile.
- Removing the duplicate step simplifies setup while preserving PAC inputs through server-side fallback.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/matching-profile-setup-focus.test.tsx tests/api/core-matching-profile-route.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`

Open risks / TODO:

- If profile purpose fields are empty, fallback cannot populate `valuesTags`/`causeTags`.
- Existing React act() warnings remain in the matching setup UI test (non-blocking, pre-existing pattern).
