# Project Change Entry

- Date/time (UTC): 2026-03-11T20:04:12Z
- Branch: master
- Base commit: 42a77884
  What changed:
- Added a shared LinkedIn OAuth context helper in `src/lib/integrations/linkedin-oauth-context.ts`.
- Refactored `src/app/api/auth/linkedin/route.ts` and `src/app/api/auth/linkedin/callback/route.ts` to use the shared parser/type instead of duplicated local implementations.

Why:

- Remove duplicated `LinkedInOAuthContext` parsing logic while preserving route behavior and API signatures.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/linkedin-oauth-redirects.test.ts` (pass)
- `npm run lint` (pass, 2 pre-existing warnings in landing image usage)
- `npm run typecheck` (pass)

Open risks / TODO:

- Running `npm run test` without Node 20 path pin can fail in this environment due Vite/Node mismatch (`node:fs/promises` export issue).
