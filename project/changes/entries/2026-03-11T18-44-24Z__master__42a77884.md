# Project Change Entry

- Date/time (UTC): 2026-03-11T18:44:24Z
- Branch: master
- Base commit: 42a77884
  What changed:
- Reconciled the remaining non-archived docs that still used pre-lock MVP language.
- Rewrote `EMAIL_SUPPORT_SETUP.md`, `docs/LINKEDIN_VERIFICATION_SETUP.md`, `docs/DATA_PROCESSING_AGREEMENTS.md`, and `docs/SUPABASE_MCP_SETUP.md` to keep support and operational guidance aligned with the locked MVP scope.
- Added a non-canonical banner and normalized trust wording in `Proofound_Core_User_Flows_v1.md`.
- Tightened obvious legacy examples in `SYSTEM_ARCHITECTURE_COMPREHENSIVE.md` and `SYSTEM_ARCHITECTURE_SUPPLEMENT.md`.
- Added the remaining orphan doc entries to `docs/DOCS_REGISTRY.md`.

Why:

- The first reconciliation pass fixed the canonical chain, but a second pass was needed to remove stale launch wording from adjacent operational and reference docs and to clear `docs:freshness` orphan warnings.

How to verify:

- Run `npm run docs:freshness`.
- Run `npm run lint`.
- Run `npm run typecheck`.
- Run `rg -n "at least 5 skills|Activate Profile|Verified badge|verified badge|staged identity reveal|Veriff" --glob '*.md' --glob '!docs/archive/**'`.
- Run `rg -n "ATS|HRIS|marketplace|government" --glob '*.md' --glob '!docs/archive/**'`.

Open risks / TODO:

- Historical/governance log entries still contain some of the old terms because they record past work; that is acceptable but should not be treated as product truth.
- Unrelated dirty `src/` changes were left untouched.
