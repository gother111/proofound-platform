# Project Change Entry

- Date/time (UTC): 2026-05-21T18:00:24Z
- Branch: master
- Base commit: 93e1767c4
  What changed:
- Hardened final launch validation so protected gates are not executed against untrusted `BASE_URL` origins.
- Added tests covering the trusted-origin requirement for `monitor:launch`, `launch:status`, and `go:no-go` inside `npm run launch:validate`.
- Updated testing strategy docs to state that protected final validation gates require localhost, canonical proofound.io, or an exact `LAUNCH_TRUSTED_BASE_URLS` origin.

Why:

- The protected launch scripts already guard secret delivery, but the final validation runner should fail closed before dispatching protected gates to an untrusted target.

How to verify:

- `npm run test -- src/lib/launch/__tests__/final-launch-validation-runner.test.ts src/lib/launch/__tests__/trusted-internal-auth.test.ts tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `npm run typecheck`
- `npm run lint`
- `git diff --check`

Open risks / TODO:

- A real production-candidate run still needs the exact trusted origin configured, `CRON_SECRET`, restore evidence, and final target-specific go/no-go output.
