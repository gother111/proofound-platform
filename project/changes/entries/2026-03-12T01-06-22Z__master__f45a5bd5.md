# Project Change Entry

- Date/time (UTC): 2026-03-12T01:06:22Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Narrowed the org trust profile surface to mission, why the work matters, working context, hiring-process clarity, and website.
- Added `working_context` and `hiring_process_summary` to the organization schema plus a phased SQL migration.
- Reworked the org assignment builder into one lean corridor and stopped new legacy `weights` writes from this path.
- Tightened publish validation to require concrete work summaries and proof expectations, and to block vague generic assignment copy.
- Added focused UI and API coverage for the lean org trust page, assignment builder flow, and publish-block reasons.

Why:

- Block 9 requires the org side to stay a calm trust page plus one strong assignment corridor instead of drifting into broader org-suite surfaces.

How to verify:

- `npm run test -- tests/ui/organization-trust-profile-page.test.tsx tests/ui/public-org-portfolio-page.test.tsx tests/ui/assignment-builder-mode-entry.test.tsx tests/lib/assignment-publish-validation.test.ts tests/api/assignments-publish-route.test.ts tests/api/organizations-route.test.ts`
- `npm run lint`
- Confirm the org profile page no longer shows culture or values editing and the assignment builder routes to internal review after the four authored steps.

Open risks / TODO:

- `npm run typecheck` still fails on unrelated pre-existing errors in `src/actions/profile.ts` and `src/lib/profileStorage.ts`.
- `npm run db:migrate` is currently blocked by a pre-existing checksum mismatch on `20260309113000_add_residual_lifecycle_contracts`.
