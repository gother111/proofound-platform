# Project Change Entry

- Date/time (UTC): 2026-03-06T13:57:21Z
- Branch: master
- Base commit: a099096a
  What changed:
- Replaced the legacy fairness note generator with a compatibility-safe shared helper that stores an insufficient-data note instead of throwing.
- Rewired account-deletion-workflow, fairness-note cron, generate-fairness-note cron, and admin fairness-note generation to use the shared helper.
- Expanded `npm run cron:sync` to reconcile the full managed cron-job.org job set and disabled overlapping legacy external jobs.
- Updated README, ENV docs, and architecture notes to reflect cron ownership and sync behavior.

Why:

- Production `account-deletion-workflow` returned success with `fairnessNote.status: "error"` because the fairness note path diverged from the schema-tolerant cron implementation.
- cron-job.org still had duplicate daily jobs enabled outside the repo-defined ownership model.

How to verify:

- `npm run typecheck`
- `npm run lint`
- `npm run test -- tests/lib/fairness-note-generator.test.ts tests/api/cron-account-deletion-workflow-route.test.ts tests/api/cron-fairness-report-route.test.ts`
- `npm run test`
- `npm run docs:freshness`
- `npm run vercel:preflight`
- `npm run cron:sync`

Open risks / TODO:

- Local `npm run build` failed in the dirty worktree while prerendering `/`, which is outside this change set and needs validation after pushing the scoped commit.
- Production still needs post-deploy validation for `/api/cron/account-deletion-workflow`, `/api/cron/fairness-note`, `/api/cron/sla-enforcement`, and `/api/cron/python-internal-worker`.
