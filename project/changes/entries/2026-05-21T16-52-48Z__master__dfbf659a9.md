# Project Change Entry

- Date/time (UTC): 2026-05-21T16:52:48Z
- Branch: master
- Base commit: dfbf659a9
  What changed:
- `scripts/check-deploy-readiness.mjs` now requires `RESEND_API_KEY` whenever strict deploy readiness applies.
- `tests/scripts/check-deploy-readiness.test.ts` now includes `RESEND_API_KEY` in the valid baseline and covers the missing-provider failure path.

Why:

- Launch-status and Resend docs already treat transactional email provider configuration as required. Deploy readiness should fail earlier instead of allowing a build or strict gate to advance to a later launch-status block.

How to verify:

- `npm run test -- tests/scripts/check-deploy-readiness.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This proves provider configuration is present, not that a live email was delivered. Final launch still needs target-specific live email validation plus production-candidate backup, restore, launch-status, perf-status, and go/no-go evidence.
