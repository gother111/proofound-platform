# Project Change Entry

- Date/time (UTC): 2026-02-27T22:09:25Z
- Branch: master
- Base commit: f39d97de
  What changed:
- Added deterministic CV import suggestion service with strict contracts and evidence-gated candidates in `src/lib/expertise/cv-import-suggest.ts`.
- Added dedicated multi-document API route `src/app/api/expertise/cv-import/suggest/route.ts` with auth checks, payload limits, and timeout handling.
- Migrated `src/app/api/expertise/auto-suggest/route.ts` to use the deterministic path for backward-compatible legacy callers.
- Reworked `src/components/expertise/CVJDAutoSuggest.tsx` to support multi-PDF client parsing, per-document review, manual mapping, and strict skill_id-only export/save allowlist.
- Updated PDF parsing to stay local by disabling PDF.js worker fetches in V1 (no CDN worker dependency).
- Added test coverage for new route and deterministic service behavior in `tests/api/cv-import-suggest-route.test.ts` and `tests/lib/cv-import-suggest.test.ts`.
- Expanded UI tests in `tests/ui/cvjd-auto-suggest.test.tsx` to cover multi-file upload and structured request payload generation.
- Added notebook scaffolds for CV matcher analysis in `output/jupyter-notebook/`.
- Added `pdfjs-dist` dependency in `package.json` and lockfile for browser-side PDF text extraction.

Why:

- Implement a free-tier-safe CV import workflow without external LLM runtime dependencies, while preserving taxonomy-only export guarantees and fallback-safe matching behavior.

How to verify:

- `npm run lint` (passes with existing non-blocking `no-img-element` warnings in landing files)
- `npm run typecheck`
- `npm run test -- tests/lib/cv-import-suggest.test.ts tests/ui/cvjd-auto-suggest.test.tsx tests/api/cv-import-suggest-route.test.ts`
- `npm run build`

Open risks / TODO:

- Existing full-suite failure unrelated to this change remains in impact-story/profile action tests (`rows.filter is not a function` / `db.select is not a function` in `src/actions/profile.ts` paths).
- PDF parsing depends on file text layers. Scanned PDFs without text layer remain unsupported in V1 by design.
