# 2026-03-01T15-12-43Z

**Context:** Duplicated UI elements sweep and fix (Issue: Visually unattractive duplicates on platform).

**Changes:**

1. Global `TopBar`: Removed the `Customize` button from the global header which was rendering redundantly alongside the dashboard's own customization controls. Dashboard customization is now solely triggered by the dashboards themselves.
2. Individual Profile (`ProfileHeroSection`): Adjusted the profile name (`h1`) to be hidden on extra-small mobile screens where the `MobileProfileHeader` sticky nav already elegantly renders the user's name, preventing visual duplication during initial load.

**Verification:**

- `npm run lint` - Passed
- `npm run typecheck` - Passed
- E2E Tests running to ensure nothing broke.

**Follow-ups:**

- Monitor for any edge-case layouts on intermediate tablet sizes if the hero name truncation feels aggressive.
