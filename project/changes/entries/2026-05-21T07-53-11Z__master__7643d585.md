# Project Change Entry

- Date/time (UTC): 2026-05-21T07:53:11Z
- Branch: master
- Base commit: 7643d585
  What changed:
- Replaced raw diagnostics in `/api/verify/[token]` with structured `verify_token.*` warning/error events.
- Added focused route coverage for canonical skill lookup failures and non-blocking skill notification email failures.
- Extended runtime debug-output guardrails so the old public verify-token console strings cannot return.

Why:

- Keeps the public verifier token surface observable through privacy-safe structured logging without changing neutral token errors, impact-to-skill fallback behavior, or non-blocking email/analytics behavior.
- Reduces launch risk on a public MVP verification route that can see external verifier failures.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/verify-impact-token-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while the suites pass.
- This slice is scoped to diagnostics only and does not change verifier token route behavior.
