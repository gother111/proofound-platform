# Project Change Entry

- Date/time (UTC): 2026-05-21T16:41:30Z
- Branch: master
- Base commit: 400b86189
  What changed:
- Launch-status email dependency checks now fail closed when `PROOFOUND_SKIP_TRANSACTIONAL_EMAIL_DELIVERY` is enabled, even if `RESEND_API_KEY` is present.
- The protected launch-status response now exposes `deliverySkipped` on the email-provider dependency object for internal operator diagnosis.
- Added route coverage for the explicit transactional-email skip state.

Why:

- Live launch readiness must not go green while transactional email delivery is intentionally disabled. The skip switch is acceptable for dry-run/local smoke paths, but it cannot count as live email readiness.

How to verify:

- `npm run test -- src/app/api/monitoring/__tests__/launch-status-route.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This does not send a real email. Final launch still needs target-specific live transactional email validation plus the production-candidate backup, restore, perf-status, launch-status, and go/no-go evidence bundle.
