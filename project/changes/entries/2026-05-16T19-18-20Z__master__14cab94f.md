# Project Change Entry

- Date/time (UTC): 2026-05-16T19:18:20Z
- Branch: master
- Base commit: 14cab94f
  What changed:
- Added `/auth/logout` GET and POST handlers that create a cookie-writing Supabase server client, call `auth.signOut()`, revalidate the app layout, and redirect home.
- Updated the app profile menu logout control to navigate to the server logout endpoint instead of relying on a dropdown-embedded server-action form submit.
- Added focused route and menu tests for the logout path.

Why:

- PRO-211 reported that selecting sign out did nothing, preventing account switching. The command palette already routed to `/auth/logout`, but that route did not exist, and the profile menu control now uses the same stable endpoint.

How to verify:

- `npm run test -- tests/api/auth-logout-route.test.ts tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/topbar-customize-visibility.test.tsx tests/ui/command-palette-archived-links.test.tsx`
- `npm run lint`
- `npm run typecheck`

Open risks / TODO:

- Full real-auth browser logout was not run in this pass because it requires the credentialed E2E environment.
