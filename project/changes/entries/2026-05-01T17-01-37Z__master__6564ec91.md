# Project Change Entry

- Date/time (UTC): 2026-05-01T17:01:37Z
- Branch: master
- Base commit: 6564ec91
  What changed:
- Added production and all-scope npm audit scripts with a high-severity threshold.
- Wired both dependency audit scripts into the strict MVP gate after clean install.
- Added a targeted override for `@typescript-eslint/typescript-estree` so its `minimatch` dependency resolves outside the vulnerable advisory range.
- Recorded the dependency triage in `audit/dependency-vulnerability-triage-2026-05-01.md`.

Why:

- Production audit was clean, but all-scope audit reported six high dev-only findings through the TypeScript-ESLint parser chain.
- The vulnerable path is CI/local lint tooling, not runtime code, but build-tool advisories can affect CI availability and should fail the release gate.

How to verify:

- `npm audit --omit=dev --json`
- `npm audit --json`
- `npm run audit:prod`
- `npm run audit:all`
- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test`

Open risks / TODO:

- `npm run docs:freshness` still reports the pre-existing warning for orphan `.artifacts/proofound-platform-left-to-do-2026-04-29.md`; it remains warning-only.
