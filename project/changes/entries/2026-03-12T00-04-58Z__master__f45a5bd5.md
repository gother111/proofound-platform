# Project Change Entry

- Date/time (UTC): 2026-03-12T00:04:58Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Added `src/lib/analytics/request-context.ts` to centralize analytics request context resolution (trusted-internal handling, auth gate, consent gate, and org membership check).
- Refactored `src/app/api/analytics/events/route.ts` and `src/app/api/analytics/track/route.ts` to use the shared helper instead of duplicating the same control flow.

Why:

- Reduce duplicated logic in two API handlers while preserving response semantics and status codes.

How to verify:

- `npm run typecheck`
- `npx eslint src/lib/analytics/request-context.ts src/app/api/analytics/events/route.ts src/app/api/analytics/track/route.ts`

Open risks / TODO:

- Existing dirty worktree files were intentionally left untouched (`src/lib/rate-limit/middleware.ts` and pre-existing untracked docs/log files).
