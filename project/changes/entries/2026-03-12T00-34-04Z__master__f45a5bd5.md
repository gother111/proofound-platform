# Project Change Entry

- Date/time (UTC): 2026-03-12T00:34:04Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Archived non-MVP product routes with `notFound()`: `app/i/zen`, `app/i/settings/fairness`, `app/o/[slug]/analytics/fairness`, `app/i/expertise`, and `app/i/skill-gaps`.
- Removed launch-visible nav and CTA links to archived surfaces from left nav, command palette, home, matching guidance, profile guidance, dashboard prompts, and docs quick links.
- Retired `api/wellbeing/**`, `api/mobile/**`, and non-MVP admin API families in middleware with a consistent archived `410` response.
- Narrowed admin UI to internal ops only by archiving fairness, performance, AI spend, users, org management, and cron pages while keeping verification and audit support visible.

Why:

- The locked MVP corridor excludes these live surfaces, and leaving them reachable created launch-surface drift against the source of truth.
- Middleware retirement keeps the diff smaller and stops new legacy API reads or writes without risky handler-by-handler edits.

How to verify:

- Visit the archived page routes and confirm they resolve as 404s.
- Open left nav and command palette and confirm they no longer expose Zen or Expertise links.
- Call archived APIs such as `/api/wellbeing/checkin`, `/api/mobile/v1/bootstrap`, or `/api/admin/analytics/overview` and confirm they return the archived `410` JSON shape.
- Confirm preserved internal ops endpoints such as `/api/admin/verification/linkedin/queue` and `/api/admin/organizations/:orgId/audit` still pass middleware.
- Run the focused vitest suite plus `npm run lint`, `npm run typecheck`, and `npm run docs:freshness`.

Open risks / TODO:

- Full repo `typecheck` still fails on unrelated pre-existing profile and portfolio typing issues outside this block.
- Legacy non-launch docs and e2e coverage still reference archived Expertise/Zen routes and should be cleaned up in a later pass.
