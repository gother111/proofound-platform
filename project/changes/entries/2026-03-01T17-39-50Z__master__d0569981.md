# Project Change Entry

- Date/time (UTC): 2026-03-01T17:39:50Z
- Branch: master
- Base commit: d0569981
  What changed:
- Added CV import runtime control metadata and engine mode wiring (`CV_IMPORT_ENGINE_MODE`) in suggest/wizard routes with JSON-to-Python support.
- Added matching throughput improvements:
  - ANN hybrid candidate pool for profile matching with response `meta.candidatePoolSource|candidatePoolSize|twoStageEnabled`.
  - Bulk upsert path for match writes.
  - Near-match scan cap via `MATCHING_NEAR_SCAN_LIMIT`.
- Added durable match refresh queue:
  - Migration `20260301173000_add_matching_refresh_job_queue.sql`.
  - Drizzle schema/table/types for `matching_refresh_jobs`.
  - Queue service `src/lib/matching/refresh-queue.ts`.
  - Enqueue-only cron route and new worker route `/api/cron/refresh-matches-worker`.
  - Vercel cron schedules for enqueue + worker.
- Unified observability API latency behavior:
  - Canonical `api_latency` event type.
  - Direct emitter logging with `duration_ms` (+ legacy `duration` fallback).
  - Health/perf-status/perf-budget scripts aligned to `PERF_API_P95_BUDGET_MS`.
- Added/updated tests for matching metadata and API latency logging, and updated docs (`README.md`, `docs/ENV_VARIABLES.md`, `docs/CRON_SETUP.md`).

Why:

- Improve CV import control and transparency over runtime engine use.
- Reduce matching compute cost while keeping recall-safe fallback behavior.
- Move match refresh to a retryable durable queue instead of synchronous cron loops.
- Standardize latency telemetry and thresholding across runtime, health checks, and CI budgets.

How to verify:

- `npm run lint` (pass, warnings only on existing `<img>` usage)
- `npm run typecheck` (pass)
- `npm run test -- tests/api/cv-import-suggest-route.test.ts tests/api/cv-import-wizard-suggest-route.test.ts` (pass)
- `npm run test -- tests/api/core-matching-gating-routes.test.ts tests/api/core-matching-profile-performance.test.ts` (pass)
- `npm run test -- src/app/api/monitoring/__tests__/perf-status-route.test.ts tests/lib/api-latency-log.test.ts` (pass)
- `npm run test:python` (fails in system Python due missing `pytest`), equivalent run succeeded via `.venv311`:
  - `PYTHONPATH=. ./.venv311/bin/python -m pytest tests/python -q` (18 passed)
- `npm run db:migrate` (pass, applied `20260301173000_add_matching_refresh_job_queue`)
- `npm run test:privacy` (20 passed)
- `npm run test:privacy:extended` (22 passed)
- `npm run build` (pass)
- `npm run docs:freshness` (warning-only; no hard fail)

Open risks / TODO:

- Docs freshness warnings are dominated by staged `.venv311` files and missing metadata in `project/Documentation.md`.
- Queue worker performance/backlog behavior should be monitored after deployment and worker cadence tuned if pending jobs accumulate.
