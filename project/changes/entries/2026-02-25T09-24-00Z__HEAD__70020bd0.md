# Project Change Entry

- Date/time (UTC): 2026-02-25T09:24:00Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Added `src/lib/navigation/sanitize-return-path.ts` to strictly allow internal `returnTo` paths under `/app/*` with safe fallback handling.
- Updated `src/app/app/i/portfolio/page.tsx` and `src/app/app/o/[slug]/portfolio/page.tsx` redirect targets to preserve context with encoded `returnTo`.
- Updated `src/app/portfolio/[handle]/page.tsx` and `src/app/portfolio/org/[slug]/page.tsx` to accept `searchParams.returnTo`, sanitize it, and render a return button (`Return to menu` or `Return home`).
- Kept existing portfolio share URL behavior unchanged (canonical `/portfolio/...` URLs, no `returnTo` propagation).
- Updated `tests/routes/portfolio-shortcuts.test.ts`.
- Added `tests/lib/sanitize-return-path.test.ts`.
- Reworked `tests/ui/public-org-portfolio-page.test.tsx` to mock Supabase and verify return-link behavior.

Why:

- Fix Linear bug `PRO-34` where users could open public portfolio from app navigation but had no direct path back to app menu context.
- Prevent open-redirect style misuse through strict `returnTo` sanitization.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/routes/portfolio-shortcuts.test.ts tests/lib/sanitize-return-path.test.ts tests/ui/public-org-portfolio-page.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS, with expected local env warning about missing `DATABASE_URL`)

Open risks / TODO:

- Local shell defaults to Node `v16.14.0`; this repo requires Node `20.20.0`. Commands should continue using Node 20 path or `nvm use 20.20.0`.
- Build emits repeated `DATABASE_URL` missing warnings in this environment; production/local env configuration should be validated separately.
