# Project Change Entry

- Date/time (UTC): 2026-03-13T02:03:25Z
- Branch: master
- Base commit: 4767ea77
  What changed:
- Added shared cron authorization helper [`src/lib/api/cron-auth.ts`](/Users/yuriibakurov/proofound/src/lib/api/cron-auth.ts) for bearer validation against `CRON_SECRET`, `CRON_SECRET_PREVIEW`, and `NEXT_PUBLIC_CRON_SECRET`.
- Replaced duplicated inline `isAuthorized` logic in:
  - [`src/app/api/cron/fairness-note/route.ts`](/Users/yuriibakurov/proofound/src/app/api/cron/fairness-note/route.ts)
  - [`src/app/api/cron/fairness-report/route.ts`](/Users/yuriibakurov/proofound/src/app/api/cron/fairness-report/route.ts)

Why:

- Remove local duplication in two cron routes while preserving identical auth behavior.
- Keep future cron-secret updates centralized for these fairness endpoints.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/cron-fairness-note-route.test.ts tests/api/cron-fairness-report-route.test.ts` (PASS)
- `npm run typecheck` (PASS)

Open risks / TODO:

- This refactor is intentionally scoped to fairness cron routes only. Other cron routes still have local auth checks and can be standardized in a separate small pass.
