# Project Change Entry

- Date/time (UTC): 2026-03-08T21:08:07Z
- Branch: master
- Base commit: 675c3b79
  What changed:
- Added the missing portfolio slug helper modules `src/lib/portfolio/slug-history.ts` and `src/lib/portfolio/slug-policy.ts`.
- Added migration `src/db/migrations/20260308203000_add_public_portfolio_launch_contract.sql` that provisions public portfolio state columns and slug history tables referenced by onboarding.
- Created this follow-up after Vercel failed to resolve the slug helper imports from `src/actions/onboarding.ts`.

Why:

- The previous pushed commit compiled against local untracked slug helper files, but those files were never committed, so Vercel failed with `Module not found` during `next build`.
- The migration belongs with the slug reservation helpers so the onboarding flow does not hit missing table errors after deployment.

How to verify:

- `git show 675c3b79:src/actions/onboarding.ts | rg "slug-history|slug-policy"`
- Confirm `src/lib/portfolio/slug-history.ts` and `src/lib/portfolio/slug-policy.ts` exist in the pushed commit.
- Re-run the Vercel deployment for the updated `master` commit.

Open risks / TODO:

- Local `npm run build` and `npm run typecheck` are not clean in this workspace because of unrelated local-only wellbeing, analytics, and Zen Hub changes that were intentionally excluded from this follow-up commit.
- The migration is committed, but production database migration execution remains a separate operational step if it has not already been run in the target environment.
