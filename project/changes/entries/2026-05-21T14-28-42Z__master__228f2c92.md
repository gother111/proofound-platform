# Project Change Entry

- Date/time (UTC): 2026-05-21T14:28:42Z
- Branch: master
- Base commit: 228f2c92
  What changed:
- Converted assignment-builder client failure diagnostics from raw console output to shared `proofound:client-diagnostic` events.
- Covered draft hydration, autosave, and review-save failure paths.
- Extended runtime debug-output guardrails and appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc client diagnostics from the organization assignment-builder workflow while preserving save and navigation behavior.

How to verify:

- `npm test -- --run tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" 'src/app/app/o/[slug]/assignments/new/AssignmentBuilderClient.tsx' tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
