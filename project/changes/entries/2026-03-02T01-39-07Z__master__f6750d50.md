# Project Change Entry

- Date/time (UTC): 2026-03-02T01:39:07Z
- Branch: master
- Base commit: f6750d50
  What changed:
- Updated `e2e/strict/organization.strict.spec.ts` to stabilize strict organization flows under CI constraints.
- Replaced brittle org-home text visibility assertion with deterministic URL + `main` visibility assertions.
- Added required `manualMeetingProvider: 'google_meet'` for manual interview scheduling payload in O-13..O-16 flow.

Why:

- `Strict Quality` run `22557585785` failed in organization strict suite due missing required payload field for manual interview scheduling.
- The org home display-name assertion was flaky under CI rendering/timing and triggered unnecessary retries.

How to verify:

- `npm run test:e2e:org:strict` (PASS, `6 passed`)
- `gh run view 22557585785 --log-failed` (used for failure triage before patch)

Open risks / TODO:

- `Retry Vercel Deploy Until Synced` remains red while `VERCEL_DEPLOY_HOOK_URL` secret is not configured.
