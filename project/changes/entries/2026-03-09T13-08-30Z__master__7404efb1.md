# Project Change Entry

- Date/time (UTC): 2026-03-09T13:08:30Z
- Branch: master
- Base commit: 7404efb1
  What changed:
- Added the audited lifecycle/submissions production slice: canonical `submissions` and `submission_artifacts`, `verification_log_entries`, lifecycle reconciliation helpers, upload status route, lifecycle lookup route, and the related runtime/test updates.
- Updated invite, verification, upload, account export/deletion, portfolio, snippet, sitemap, and workflow surfaces so they resolve through the same verified lifecycle and canonical submission model.
- Included migration `src/db/migrations/20260309020000_add_submission_and_verification_log_entities.sql` with the matching `src/db/schema.ts` changes.

Why:

- The remaining local worktree contained a mixed feature bundle. Production only needed the launch-blocking runtime slice for lifecycle state, submission persistence, verification logging, and public portfolio consistency.
- Shipping the audited slice together avoids partial API/schema drift and keeps unrelated tooling, docs, and scripts out of `master`.

How to verify:

- `npm run db:migrate`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/expertise-skill-verification-request-route.test.ts tests/api/portfolio-view-route.test.ts tests/api/profile-snippet-route.test.ts tests/canonical-domain.test.ts tests/portfolio-visibility.test.ts tests/lib/residual-lifecycle-contracts.test.ts tests/lib/verification-log-entries.test.ts tests/ui/public-org-portfolio-page.test.tsx tests/ui/public-portfolio-page.test.tsx --reporter=dot`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vercel build --prod --yes`

Open risks / TODO:

- Local migration verification showed `20260309113000_add_residual_lifecycle_contracts` was already applied in the dev database. Keep migration ordering in mind if another schema slice is prepared next.
- Main worktree still contains unrelated local edits that were intentionally excluded from this production commit.
