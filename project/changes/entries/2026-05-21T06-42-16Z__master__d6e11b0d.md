# Project Change Entry

- Date/time (UTC): 2026-05-21T06:42:16Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Replaced remaining raw server-side console diagnostics in performance API monitoring, performance alert notifications, API latency logging, and the expertise taxonomy API with structured `log.warn` / `log.error` events.
- Removed raw taxonomy exception stack logging from the active API catch path while preserving the same 500 response contract.
- Extended runtime debug-output guardrails and API latency tests so these active launch/ops paths stay structured and best-effort.

Why:

- Launch operations need queryable, privacy-safe structured diagnostics; raw console messages make failures harder to classify and can expose more exception detail than needed.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/expertise-taxonomy-route.test.ts tests/lib/api-latency-log.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Client-only performance debug traces remain for a later browser/runtime pass; this slice intentionally focused on active server-side launch and monitoring paths.
