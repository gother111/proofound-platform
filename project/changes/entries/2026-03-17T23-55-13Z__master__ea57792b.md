# Project Change Entry

- Date/time (UTC): 2026-03-17T23:55:13Z
- Branch: master
- Base commit: ea57792b
  What changed:
- Removed broad platform scope sections (`StewardOwnershipSection`, `ProductsSection`, `FinalQuoteSection`) from the homepage.
- Reordered the `ProofoundLanding` sections to present employer value (Personas) earlier.
- Replaced the 10-step scrolling narrative in `HowItWorksSection` with a concise 3-step proof corridor.
- Tightened copy in `HeroSection` and `ProblemSection` to focus directly on portable proof, biased evaluation, and the obsolescence of CVs.
- Cleaned up the `FooterSection` by removing placeholder social links.
- Adjusted the `CookieBanner` position.

Why:

- To align the homepage purely with the locked Proofound MVP and eliminate broader conceptual platform messaging.

How to verify:

- Run `npm run dev` and navigate to visual changes on the root `/`.
- Tests run: `npm run typecheck`, `npm run lint`, and `npm run test:e2e:landing`.

Open risks / TODO:

- Pre-existing type check errors in `src/app/api/decisions/route.ts` and `src/app/app/i/interviews/page.tsx`, and an ESLint error in `tests/routes/portfolio-shortcuts.test.ts`.
