# Project Change Entry

- Date/time (UTC): 2026-03-08T13:56:28Z
- Branch: master
- Base commit: e892d730
  What changed:
- Added canonical Drizzle schema and SQL migration support for `proof_artifacts`, `proof_packs`, `proof_pack_items`, `verification_records`, typed `profile_snippets`, and persisted match audit fields on `matches`.
- Added canonical TypeScript/Zod contracts plus repository helpers for visibility mapping, token hashing, proof/pack upserts, verification record upserts, and deterministic match audit hashes and reason codes.
- Dual-wrote active proof, profile snippet, custom verification, and skill verification response flows into the canonical layer, surfaced additive canonical IDs, persisted match audit metadata in web and mobile matching flows, and exposed those audit fields in explain APIs.
- Tightened organization ownership routes so reads require active org membership and writes require active `owner` or `admin` membership.

Why:

- Proofound needed one coherent proof-first and privacy-first persistence model for proof artifacts, portable proof packs, verification history, and durable explainable match scoring without breaking current MVP behavior.
- The canonical layer creates additive migration safety while keeping legacy tables and onboarding flows operational during rollout.

How to verify:

- Run `npm run db:migrate` and confirm `20260308153000_add_canonical_proof_contracts` applies successfully.
- Run `npm run typecheck` and confirm there are no TypeScript errors.
- Run `npm run lint` and confirm there are no lint errors beyond the pre-existing `no-img-element` warnings in landing components.
- Create a skill proof, a profile snippet, and a custom verification request, then verify the corresponding canonical rows are present in `proof_artifacts`, `proof_packs`, and `verification_records`.

Open risks / TODO:

- `vitest` and `next build` are blocked in this shell because the runtime is Node 16 while the repo requires Node 20+.
- Public snippet rendering still resolves tokens from legacy `profile_snippets`; canonical `proof_packs` is the new persistence layer but not yet the direct public-read path.
- Legacy visibility APIs still speak legacy enums at their edges; canonical normalization is applied in the new contract layer and write paths, but full API vocabulary consolidation is still follow-up work.
