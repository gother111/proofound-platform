# Project Change Entry

- Date/time (UTC): 2026-03-13T18:04:13Z
- Branch: master
- Base commit: 4767ea77
  What changed:
- Replaced per-profile shortlist discoverability checks with a batched portfolio-readiness map in `src/lib/readiness/individual-state.ts`, `src/lib/matching/eligibility.ts`, and `src/lib/core/matching/assignmentMatcher.ts`.
- Added explicit interview hot-path indexes in `src/db/schema.ts` and `src/db/migrations/20260313185000_add_interview_hot_path_indexes.sql`.
- Shortened `/api/interviews/schedule` response time by logging step timings and moving workflow registration, analytics emission, and interview messaging off the response path after insert success.
- Added targeted regression coverage for the batched matcher path and the async interview-scheduling side effects.

Why:

- The org corridor audit and fresh reruns showed `/api/match/assignment` taking about `73s` to `78s` because readiness was recomputed once per candidate profile.
- Interview scheduling was keeping non-critical follow-up work on the response path, which made the corridor brittle and inflated strict-suite timing.
- The launch block needed fresh before/after evidence, not just code changes.

How to verify:

- Run `npm run test -- src/lib/__tests__/matching-eligibility.test.ts`.
- Run `npm run test -- tests/api/interviews-schedule-route.test.ts`.
- Run `node run-migrations.mjs`.
- Run `node ./scripts/playwright-node20.mjs test e2e/strict/organization.strict.spec.ts --project=chromium --reporter=line --workers=1`.
- Optional direct benchmark: run the local TSX harness used in Block 3 against assignment `aaaa1111-2222-4333-8444-aaaaaaaaaa01` and confirm the matcher is in the low-single-digit seconds instead of about `76s`.

Open risks / TODO:

- Live launch monitoring still needs a fresh smoke artifact before `/api/monitoring/launch-status` and `npm run go:no-go` can show green evidence.
- Proof Pack anchor nullability and mixed verification semantics remain open drift outside this block.
