# Project Change Entry

- Date/time (UTC): 2026-02-28T15:27:38Z
- Branch: master
- Base commit: a1fa76f9
  What changed:
- Hardened custom artifacts aggregation to fail soft per source in `src/app/api/expertise/verifications/custom/artifacts/route.ts`.
- API now returns available artifacts when one or more sources fail and returns `500` only when all six artifact sources fail.
- Preserved skill filtering intent while making accepted-skill lookup failure non-blocking.
- Added regression tests in `tests/api/custom-verification-routes.test.ts` for partial success, accepted-skill lookup failure tolerance, and all-sources failure.

Why:

- PRO-106 reported that deleting pending requests caused "Custom verification request" to show no artifacts with a load error. The previous fail-fast API behavior allowed one query failure to blank the full selector.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/custom-verification-routes.test.ts tests/ui/verifications-client.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS, 2 pre-existing warnings in landing files)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)

Open risks / TODO:

- Partial responses can mask a failing artifact source in UI. Server-side error logging is retained per source for observability.
