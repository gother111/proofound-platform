# Project Change Entry

- Date/time (UTC): 2026-02-28T17:30:57Z
- Branch: master
- Base commit: c5ab34b4
  What changed:
- Updated matching profile Work-step UX in `src/components/matching/MatchingProfileSetup.tsx` to clarify hours as a desired range, allow transient zero/empty editing, and block progression/save when final hours are not above zero.
- Extended compensation input in `src/components/matching/CompensationInput.tsx` with a period selector (`annual`, `monthly`, `hourly`).
- Added `matching_profiles.comp_period` persistence via migration `src/db/migrations/20260228173000_add_matching_profile_comp_period.sql`, Drizzle schema update in `src/db/schema.ts`, and API schema support in `src/app/api/core/matching/matching-profile/route.ts`.
- Added period-aware annual normalization helper in `src/lib/matching/compensation.ts` and wired it into all compensation scoring paths:
- `src/lib/core/matching/assignmentMatcher.ts`
- `src/lib/matching/generate-matches-for-assignment.ts`
- `src/app/api/core/matching/profile/route.ts`
- `src/app/api/core/matching/near-matches/route.ts`
- Added/updated tests in:
- `tests/ui/matching-profile-setup-focus.test.tsx`
- `tests/api/core-matching-profile-route.test.ts`
- `tests/lib/compensation-normalization.test.ts`

Why:

- PRO-109 requires clearer hours-range inputs, edit-friendly hour typing behavior, save/progression guards for invalid final hours, and compensation period support without breaking annualized matching comparisons.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:migrate`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:drift-check`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/matching-profile-setup-focus.test.tsx tests/api/core-matching-profile-route.test.ts tests/lib/compensation-normalization.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`

Open risks / TODO:

- Full `npm run test` currently fails in existing unrelated test suites (`tests/actions/create-impact-story.test.ts`, `tests/actions/profile.test.ts`) with `db.select is not a function` / `rows.filter is not a function`.
- Existing Next.js lint warnings for `<img>` usage in landing components remain unchanged.
