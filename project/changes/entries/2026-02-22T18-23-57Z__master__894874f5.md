# Project Change Entry

- Date/time (UTC): 2026-02-22T18:23:57Z
- Branch: master
- Base commit: 894874f5
  What changed:
- Removed route-level opacity animation gating in `src/components/ui/transition-provider.tsx` by replacing `AnimatePresence` route transitions with a static wrapper (`<div className="w-full min-h-screen">`).
- Ensured profile routes no longer depend on first-paint animation state to become visible after client-side sidebar navigation.

Why:

- Sidebar navigation to profile intermittently resulted in a white/blank screen because the route transition wrapper could remain at `opacity: 0` while profile content was already rendered.

How to verify:

- Run `npm run test:e2e -- e2e/profile/profile-nav-no-blank.spec.ts --project=chromium --reporter=line`.
- Run `npm run lint`.
- Run `npm run typecheck`.
- Run `npm run test`.
- Manual: navigate `/app/i/home` -> sidebar `Profile` and `/app/o/<slug>/home` -> sidebar `Org Profile`; confirm visible content without hard refresh.

Open risks / TODO:

- Entrance animation polish is reduced on route changes because profile reliability was prioritized over transition effects.
