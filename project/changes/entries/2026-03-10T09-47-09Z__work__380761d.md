# Project Change Entry

- Date/time (UTC): 2026-03-10T09:47:09Z
- Branch: work
- Base commit: 380761d
  What changed:
- Hardened OAuth callback HTML generation by escaping inline script JSON payloads before embedding `postMessageType` and `redirectPath`.
- Added regression coverage asserting script-breaking payloads (e.g. `</script>`) are escaped in callback HTML.

Why:

- Prevent reflected XSS via attacker-controlled `returnTo` values reaching inline `<script>` in OAuth callback responses.

How to verify:

- `npm run test -- src/lib/integrations/__tests__/oauth-helpers.test.ts`
- `npm run lint`
- `npm run typecheck`

Open risks / TODO:

- None.
