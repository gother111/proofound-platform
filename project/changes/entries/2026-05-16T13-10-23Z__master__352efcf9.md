# Project Change Entry

- Date/time (UTC): 2026-05-16T13:10:23Z
- Branch: master
- Base commit: 352efcf9

What changed:

- Added the direct archived-handler route proof to `npm run test:launch:ai`.
- Updated `tests/lib/ai-launch-guardrails.test.ts` so the AI launch-command drift sentinel requires that archive proof.
- Updated `audit/ai-features-verification-2026-05-16.md` with the legacy CV-import archive coverage gap and current AI gate counts.

Why:

- The Gemini-era CV-import wizard handlers are intentionally archived, but the AI launch gate did not directly prove they still return launch-safe `410` responses. The gate should catch accidental reactivation of those handlers.

How to verify:

- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test -- tests/api/archived-api-handlers-route.test.ts tests/lib/ai-launch-guardrails.test.ts`
- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test:launch:ai`

Open risks / TODO:

- Live Gemini provider behavior remains unverified until server-only provider credentials and hard-cap env are available.
