# Project Change Entry

- Date/time (UTC): 2026-02-25T09:56:48Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Updated `src/components/dashboard/WhileAwayCard.tsx` to remove `/api/momentum/summary` fallback logic.
- `WhileAwayCard` now loads only `/api/updates` and reports visibility through `onVisibilityChange` after fetch resolution.
- Updated `src/components/dashboard/DraggableDashboard.tsx` to honor `while-away` visibility callbacks.
- Updated `src/app/app/o/[slug]/home/OrgDashboardClient.tsx` to honor `while-away` visibility callbacks.
- Added regression tests:
- Added `tests/ui/while-away-card.test.tsx`.
- Added `tests/ui/draggable-dashboard-while-away-visibility.test.tsx`.
- Extended `tests/ui/org-dashboard-client.test.tsx` with hidden-widget coverage.

Why:

- Linear `PRO-35` reported semantic mismatch: "While you were away" was showing profile-improvement suggestions instead of factual updates.
- Product decision for this fix: keep card updates-only and hide the widget when no updates exist.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/while-away-card.test.tsx tests/ui/draggable-dashboard-while-away-visibility.test.tsx tests/ui/org-dashboard-client.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS; readiness warnings about missing local env vars in this workspace)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test` (FAIL, unrelated baseline failure in `tests/ui/public-org-portfolio-page.test.tsx` with Next request-scope cookies error)

Open risks / TODO:

- Hiding the widget can cause visible tile reflow when activity is empty, by design.
- Full suite has unrelated failing tests in `tests/ui/public-org-portfolio-page.test.tsx`; this task does not modify that path.
