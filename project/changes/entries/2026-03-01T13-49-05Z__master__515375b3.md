# Project Change Entry

- Date/time (UTC): 2026-03-01T13:49:05Z
- Branch: master
- Base commit: 515375b3
  What changed:
- Standardized geometric alignment across core form UI components (`input.tsx`, `textarea.tsx`, `checkbox.tsx`, `badge.tsx`).
- Normalized focus ring offsets to `ring-offset-2` globally for exact visual rhythm.
- Replaced straggling non-semantic `border-proofound-stone` borders with dynamic `border-border` classes in `checkbox` and `textarea`.

Why:

- To achieve top-class aesthetic perfection and geometric alignment. Buttons and inputs now share identical 44px bounds (`h-11`), radius (`rounded-xl`), and focus ring geometries, ensuring the platform feels meticulously refined.

How to verify:

- Focus an `Input` or `Textarea` using the keyboard (`Tab`). Observe the focus ring now matches the exact 2px offset distance as the `Button` focus ring.
- Check `npm run lint` and `npm run typecheck` for regression status.

Open risks / TODO:

- N/A
