# Project Change Entry

- Date/time (UTC): 2026-05-21T06:24:19Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Reframed active backlog Phase 3 labels from `Hiring corridor` to `Assignment-review corridor`.
- Updated `docs/backlog/README.md`, `docs/backlog/dependency-map.md`, and `docs/backlog/phase-3-hiring-corridor.md`, including `Last Verified` dates and `docs/DOCS_REGISTRY.md` rows.
- Extended launch-gate coverage in the active route-count/backlog evidence test so the old Phase 3 label cannot return.

Why:

- The backlog pack is active launch guidance and should use the current assignment-review corridor vocabulary even while the existing filename remains stable for links.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "active route-count evidence" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- Targeted Phase 3 backlog wording and registry-date scans.
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- The file path `docs/backlog/phase-3-hiring-corridor.md` remains unchanged to preserve existing links; only displayed active wording was updated.
