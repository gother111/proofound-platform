# Project Change Entry

- Date/time (UTC): 2026-03-09T09:49:53Z
- Branch: master
- Base commit: f8e82fc9
  What changed:
- Verified the production database target from `.vercel/.env.production.local` without printing secrets.
- Attempted a production safety backup, but `npm run db:backup:checkpoint` failed because the `DIRECT_URL` host was not resolvable from the local environment.
- Applied the migration runner against production with pooled `DATABASE_URL` after unsetting `DIRECT_URL`; the run succeeded and reported `20260309003000_add_match_score_contract_v1` already applied.

Why:

- The Block 10 rollout required an explicit production migration check before committing and pushing.
- The repo also needs an audit trail for why the migration command was run with pooled connectivity instead of the direct host.

How to verify:

- `set -a; source .vercel/.env.production.local; unset DIRECT_URL; PATH=/opt/homebrew/opt/node@20/bin:$PATH PGOPTIONS='-c search_path=public' npm run db:migrate`
- Confirm the migration output skips `20260309003000_add_match_score_contract_v1` as already applied.

Open risks / TODO:

- `npm run db:backup:checkpoint` still needs a reachable direct Postgres host or an updated fallback strategy.
- Local `pg_dump` is version 14 while the production server is 17.6, so manual SQL backups from this machine remain blocked until the client is upgraded.
