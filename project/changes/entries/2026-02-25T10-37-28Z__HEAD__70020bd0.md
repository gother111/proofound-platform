# Project Change Entry

- Date/time (UTC): 2026-02-25T10:37:28Z
- Branch: HEAD
- Base commit: 70020bd0
  What changed:
- Removed the skills block from the active individual profile sidebar by deleting `SkillsCard` usage from `src/components/profile/editable-profile/ProfileSidebar.tsx`.
- Removed the skills block from legacy `ProfileView` by deleting `SkillsCard` usage from `src/components/profile/ProfileView.tsx`.
- Added focused UI regression coverage in `tests/ui/profile-skills-removal.test.tsx` to ensure profile UIs no longer render skills-card markers while still rendering mission/values/causes sections.

Why:

- Linear issue `PRO-43` requires profile-page skills UI removal because skills are managed in Expertise Hub.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/profile-skills-removal.test.tsx`

Open risks / TODO:

- `SkillsCard` still exists in the codebase for other potential use cases; if this component is no longer needed anywhere, a separate cleanup ticket can remove it.
