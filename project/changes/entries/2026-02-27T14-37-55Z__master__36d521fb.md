# Project Change Entry

- Date/time (UTC): 2026-02-27T14:37:55Z
- Branch: master
- Base commit: 36d521fb
  What changed:
- Hardened `src/app/api/verify/[token]/route.ts` skill token lookup to handle PostgREST relation ambiguity (`PGRST200`/`PGRST201`) caused by duplicate FKs.
- Added fallback lookup path: retry `skill_verification_requests` without embeds, then hydrate `skills` in a second query.
- Switched skill embeds to explicit relationship hints:
  - `skills!skill_verification_requests_skill_id_fkey`
  - `taxonomy:skills_taxonomy!skills_skill_code_fkey`
- Added regression tests in `tests/api/verify-impact-token-route.test.ts` for GET/POST skill flow under `PGRST201` ambiguity.

Why:

- Production links for valid skill verification tokens were returning `404` due ambiguous relationship embedding, not missing token rows.
- Fix keeps external API contract unchanged while making lookup robust across drifted DB FK states.

How to verify:

- `npm run db:migrate` (expect no new applies)
- `npm run test -- tests/api/verify-impact-token-route.test.ts`
- `npm run test -- tests/api/expertise-skill-verification-request-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/api/expertise-verification-respond-route.test.ts tests/api/verify-impact-token-route.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test`
- `npm run test:privacy`
- `npm run test:privacy:extended`

Open risks / TODO:

- DB still contains duplicate FK constraints; this patch mitigates the runtime impact in `/api/verify/[token]` but does not deduplicate schema constraints globally.
