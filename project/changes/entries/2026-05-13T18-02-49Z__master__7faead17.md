# Project Change Entry

- Date/time (UTC): 2026-05-13T18:02:49Z
- Branch: master
- Base commit: 7faead17
  What changed:
- Deferred root-only client enhancements out of the initial layout path via `RootClientEnhancements`.
- Moved heavier command palette and spotlight overlay code behind lazy/dynamic boundaries.
- Removed Framer Motion from the mobile left-nav active indicator.
- Deferred chat widget hydration until idle on app routes.
- Lazy-loaded Sentry only after an error boundary catches an error.
- Stopped middleware from issuing `csrf_token` cookies on public page GETs and safe API methods.
- Skipped local Vercel analytics/speed-insights mounting on `localhost`, `127.0.0.1`, and `::1` while preserving consent-gated production telemetry.
- Deferred the largest individual app client islands behind route-level shells: profile editor, verifications view, matching client, and messages client.
- Deferred realtime message-thread code until a conversation is selected.
- Deferred matching card dialog/modal code for explainers, PAC details, snooze, and consent until those surfaces are actually opened.
- Replaced the top-bar profile Radix dropdown/avatar path with a lighter trigger and lazy-loaded menu.

Why:

- Production page loads were slow on common public and app paths. Baseline build output showed heavy first-load JS on app pages, and middleware was doing avoidable per-user cookie work on safe page/API reads.
- Local perf baselines before the final patch: desktop TTI 5438.58 ms, mobile TTI 4676.91 ms, API p95 671.67 ms.
- Final completion-audit local production perf budget result: desktop TTI 5087.26 ms, mobile TTI 4311.84 ms, API p95 672.29 ms. That is about 351 ms faster desktop TTI and 365 ms faster mobile TTI than the initial baseline, with API p95 effectively unchanged.
- Final Next route table moved the main app bottlenecks substantially:
  - `/app/i/profile`: 473 kB first-load JS to 254 kB.
  - `/app/i/verifications`: 471 kB to 254 kB.
  - `/app/i/matching`: 428 kB to 263 kB.
  - `/app/i/messages`: 401 kB to 262 kB.
  - `/app/o/[slug]/matching`: 400 kB to 327 kB.
  - `/app/o/[slug]/messages`: 400 kB to 326 kB.
  - `/app/o/[slug]/assignments`: 400 kB to 327 kB.

How to verify:

- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run test -- src/lib/__tests__/middleware-csrf.test.ts src/lib/__tests__/csrf.test.ts src/lib/__tests__/security-headers.test.ts`
- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run test -- tests/ui/topbar-customize-visibility.test.tsx tests/ui/launch-discoverability.test.tsx src/lib/__tests__/middleware-csrf.test.ts`
- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run lint`
- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run typecheck`
- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run build`
- Start production server with Node 20.20.0, then run `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run perf:budgets`.
- Header smoke: `GET /` and `GET /api/health` no longer return `Set-Cookie`.
- Browser smoke: `http://127.0.0.1:3000/` loads with title `Proofound | Proof Behind the Claim` and no captured console errors after reload.

Open risks / TODO:

- Route-level first-load JS is much lower on the targeted app pages, but the shared baseline remains about 254 kB. The next high-impact bundle task is reducing the global shared chunk and then splitting settings/auth/verify pages that still sit above 300 kB.
- Public `/` still returns `Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate`, likely due the existing dynamic security/header path. CSRF cookie removal helps per-user cache fragmentation but does not make the homepage CDN-cacheable yet.

Completion audit:

- Read the required repo source-of-truth set: `AGENTS.md`, `package.json`, `agent/checklists/verification.md`, `docs/performance-testing.md`, `next.config.js`, `src/middleware.ts`, root/app layouts, and affected app routes/components.
- Baseline was established before source edits: initial build route table and perf budget run captured public TTI, mobile TTI, API p95, middleware size, shared JS, and heavy app route first-load JS.
- Public/logged-out path covered by root enhancement deferral, telemetry local guard, middleware CSRF-cookie narrowing, local production perf budget, header smoke, and browser smoke.
- Individual app paths covered by route-level deferral for profile, verifications, matching, and messages, plus matching-card and realtime-thread splitting.
- Organization app paths covered by matching-card splitting and org message realtime-thread deferral; org matching/messages/assignments route table improved.
- Shared layout/middleware/auth-sensitive path covered without changing production data, billing, auth rules, permissions, schema, RLS, CSRF enforcement, monitoring gates, or cloud resources.
- Required checks completed in the final audit: `npm run build`, `npm run lint`, `npm run typecheck`, focused UI/security tests, `npm run docs:freshness`, production `npm run perf:budgets`, header smoke, and Browser smoke.
- Known non-failing warning: `docs:freshness` still reports two existing orphan registry warnings for `.artifacts/mvp-ai-pilot-readiness-left-2026-05-11.md` and `supabase/migrations/README.md`.

Continuation audit, app-page focus:

- User clarified the priority is less the landing page and more individual and organization user pages. A landing dynamic split was tested, but Browser caught strict-CSP nonce failures on emitted chunk preloads, so that split was reverted before final verification.
- Added route-level deferred shells for:
  - `/app/o/[slug]/matching`
  - `/app/o/[slug]/assignments`
  - `/app/i/settings`
  - `/app/i/settings/privacy`
- Split browser-safe custom verification labels into `src/lib/verification/custom-verification-labels.ts` so verify pages and verification dialogs no longer import Node `crypto` through `custom-verification.ts`.
- Lazy-loaded `sonner` inside `GlobalErrorHandler` only when a global error/rejection is actually reported.
- Simplified `ErrorBoundary` fallback markup so the normal root path no longer pays for card/button/icon UI in the boundary component.
- Final route table after the continuation pass:
  - `/app/i/settings`: 357 kB to 263 kB first-load JS.
  - `/app/i/settings/privacy`: 368 kB to 262 kB.
  - `/app/o/[slug]/assignments`: 327 kB to 265 kB.
  - `/app/o/[slug]/matching`: 327 kB to 263 kB.
  - `/verify/[token]`: 413 kB to 290 kB.
  - `/verify/custom/[token]`: 408 kB to 285 kB.
  - `/app/o/[slug]/messages` remains 326 kB and is the next org-page target.
- Final perf budget after the continuation pass:
  - Desktop TTI: 4927.29 ms.
  - Desktop CLS: 0.000276.
  - Mobile TTI: 4224.80 ms.
  - Mobile CLS: 0.041747.
  - API p95: 671.81 ms.
  - Result: pass.
- Additional continuation checks:
  - `npm run test -- tests/api/custom-verification-routes.test.ts tests/ui/custom-verification-request-dialog.test.tsx tests/ui/verifications-client.test.tsx` passed, 19 tests.
  - `npm run build` passed after the CSP-safe landing revert.
  - `npm run lint` passed.
  - `npm run typecheck` passed.
  - `npm run docs:freshness` passed in warning mode with the same two existing orphan-file warnings.
  - `git diff --check` passed.
  - Browser smoke on `http://127.0.0.1:3000/` loaded with title `Proofound | Proof Behind the Claim` and 0 console errors after the landing revert.
