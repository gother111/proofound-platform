# Project Change Entry

- Date/time (UTC): 2026-02-23T12:04:34Z
- Branch: HEAD
- Base commit: b5621d35
  What changed:
- Switched matching API gating from hard-fail to soft-gate for ineligible individuals in `POST /api/core/matching/profile` and `POST /api/core/matching/near-matches` by returning `200` with empty `items`, `eligibility`, `topActions`, and `meta.softGated`.
- Added matching profile bootstrap in `GET /api/core/matching/matching-profile` to auto-create a baseline row if missing, plus idempotent activation event emission guard.
- Updated matching page UI to remove hard-block behavior and display a non-blocking “Get match-ready in 4 quick steps” checklist with actionable links while still allowing normal page usage.
- Simplified individual setup friction in `MatchingProfileSetup` by removing mandatory values validation and reframing completion as optional/iterative.
- Updated dashboard and organization onboarding copy to emphasize immediate activation and deferred completion.
- Updated tests for the new soft-gating contract and added API coverage for matching profile bootstrap.

Why:

- Product direction requires fast Lite activation without homework while preserving quality signals and making activation non-blocking.
- Users should be able to start using matching immediately and complete setup over time.

How to verify:

- `npm run lint` (PASS)
- `npm run typecheck` (PASS)
- `npm run test -- tests/api/core-matching-gating-routes.test.ts tests/ui/matching-page-gated.test.tsx src/lib/__tests__/matching-eligibility.test.ts tests/ui/matching-profile-setup-focus.test.tsx tests/api/core-matching-profile-route.test.ts` (PASS)
- `npm run test` (PASS)

Open risks / TODO:

- Activation event dedupe currently checks by query + process-local cache and should still be monitored for edge duplication under very high concurrent writes.
- Console warnings in tests about missing `DATABASE_URL` and mock DB fallback remain existing environment behavior.
