# Project Change Entry

- Date/time (UTC): 2026-05-16T13:21:34Z
- Branch: master
- Base commit: 4da5423e

What changed:

- Added route-level tests for the active `/api/expertise/jd-to-l4` AI-adjacent endpoint.
- Wired both `tests/api/jd-to-l4-route.test.ts` and the existing `tests/api/portfolio-visibility-route.test.ts` into `npm run test:launch:ai`.
- Updated the AI launch-command drift sentinel and audit report with the active non-`/api/ai` route coverage evidence.

Why:

- The JD-to-L4 route and portfolio visibility privacy-preflight route are active launch surfaces that exercise AI-adjacent behavior outside `/api/ai/*`; the AI launch gate should prove them directly.

How to verify:

- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test -- tests/api/jd-to-l4-route.test.ts tests/api/portfolio-visibility-route.test.ts tests/lib/ai-launch-guardrails.test.ts`
- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test:launch:ai`

Open risks / TODO:

- Live Gemini provider behavior remains unverified until server-only provider credentials and hard-cap env are available.
