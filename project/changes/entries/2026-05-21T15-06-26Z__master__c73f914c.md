# Project Change Entry

- Date/time (UTC): 2026-05-21T15:06:26Z
- Branch: master
- Base commit: c73f914c
  What changed:
- Replaced raw matching interaction console diagnostics with local `proofound:client-diagnostic` events.
- Covered match explanation, visible-fields, gate-check, paused/hidden match load, warm-refresh, restore, snooze, consent, and organization matching explanation failures.
- Added runtime debug-output guardrails and appended the slice to `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md`.

Why:

- Keeps active individual and organization matching failure diagnostics structured without leaking raw browser-console errors.
- Continues launch-surface diagnostic hygiene across the proof-first matching corridor while preserving existing toasts, rollbacks, and fallback behavior.

How to verify:

- `npm test -- --run tests/ui/match-result-card.test.tsx tests/ui/matching-paused-hidden-manager.test.tsx tests/ui/matching-organization-view-beta.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/components/matching/MatchResultCard.tsx src/components/matching/SnoozedMatchesList.tsx src/components/matching/HiddenMatchesList.tsx src/components/matching/SnoozeDialog.tsx src/components/matching/ConsentToShareDialog.tsx src/components/matching/MatchingOrganizationView.tsx tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment, but the suites passed.
- Other active MVP surfaces still have raw client diagnostics and need follow-up slices before the full goal can be completed.
