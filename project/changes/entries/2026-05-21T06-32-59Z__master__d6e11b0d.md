# Project Change Entry

- Date/time (UTC): 2026-05-21T06:32:59Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Removed the raw `Owner-only Proof Pack ID` UUID entry from the submission invite UI.
- The invite now lets a user choose an owned Proof Pack returned by the API, or asks them to create one and return before review/submission.
- Updated proof-card API validation copy, UI/API tests, and launch-gate coverage so the raw ID entry and placeholder UUID cannot return.

Why:

- Active submission-invite UX should be a guided proof-submission flow, not a launch-facing raw identifier form.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/candidate-invite-proof-card-route.test.ts tests/ui/candidate-invite-client.test.tsx --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "active submission invite failure copy" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- The broader MVP surface sweep remains active; this slice only tightened the raw Proof Pack ID placeholder path in submission invites.
