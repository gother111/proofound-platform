# Project Change Entry

- Date/time (UTC): 2026-02-27T17:49:50Z
- Branch: master
- Base commit: ff4337cb

What changed:

- Added migration `src/db/migrations/20260227182000_prevent_duplicate_active_verification_requests.sql` to normalize verifier emails, collapse active duplicate skill and impact verification requests, repoint impact response links, and enforce partial unique indexes for active requests.
- Added skill verification route duplicate guardrails in `src/app/api/expertise/user-skills/[id]/verification-request/route.ts`: precheck for active duplicates, explicit `409` response with `code: DUPLICATE_VERIFICATION_REQUEST`, and race-safe unique-violation handling.
- Added impact verification duplicate guardrails in `src/actions/profile.ts`: precheck for active duplicates and reuse existing request payload on duplicate submissions, including unique-violation race handling.
- Added custom verification linked-skill conflict handling in `src/app/api/expertise/verifications/custom/request/route.ts` to return explicit `409` duplicate response instead of generic `500`.
- Extended targeted tests in:
  - `tests/api/expertise-skill-verification-request-route.test.ts`
  - `tests/actions/profile-impact-story-create.test.ts`
  - `tests/api/custom-verification-routes.test.ts`

Why:

- Prevent duplicate active verification records from repeated submissions so verification tab and DB remain consistent.
- Add database-level integrity to guarantee dedupe behavior under concurrent requests.
- Make duplicate behavior explicit and predictable at API/action layers.

How to verify:

- `npm run db:migrate`
- `npm run test -- tests/api/expertise-skill-verification-request-route.test.ts tests/actions/profile-impact-story-create.test.ts tests/api/custom-verification-routes.test.ts`
- `npm run typecheck`
- `npm run test -- tests/ui/custom-verification-request-dialog.test.tsx tests/ui/edit-skill-window-proofs.test.tsx tests/ui/add-skill-drawer-proof-verification.test.tsx`
- `npm run lint` (passes with existing unrelated landing image warnings)

Open risks / TODO:

- Existing user-visible warning text for duplicate impact verification reuses a generic message and may need product copy review.
- Migration intentionally dedupes only active statuses (`pending`, `accepted`); declined/expired/failed records are preserved.
