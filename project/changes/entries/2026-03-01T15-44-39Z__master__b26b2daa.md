# Project Change Entry

- Date/time (UTC): 2026-03-01T15:44:39Z
- Branch: master
- Base commit: b26b2daa
  What changed:
- Added Python CV import runtime on Vercel under `api/python/cv_import.py` with deterministic PDF extraction (`pdfplumber`), rule-based entity extraction, and taxonomy-constrained skill matching (`rapidfuzz`).
- Added reusable Python domain modules in `python_cv/` for extraction, normalization, matching, contracts, skill DB loading, and export formatting.
- Added canonical skills DB builder (`python_cv/scripts/build_skills_db.py`) and generated `python_cv/data/skills_db.json` from repo fallback taxonomy source.
- Integrated Next.js CV routes to proxy multipart analyze requests to Python runtime while preserving existing endpoint contracts:
  - `src/app/api/expertise/cv-import/wizard-suggest/route.ts`
  - `src/app/api/expertise/cv-import/suggest/route.ts`
  - `src/lib/expertise/python-cv-proxy.ts`
- Updated CV wizard UI to submit uploaded PDFs as `multipart/form-data` to backend analyze route instead of client-side PDF parsing.
- Added "Show more suggestions" behavior and taxonomy-only approval guard continuity in CV import UIs.
- Added Python unit test suite under `tests/python/` and updated API/UI tests for multipart proxy and server-driven parse errors.
- Added Vercel Python function config and Python requirements for local/runtime parity.

Why:

- Remove client-side pdf.js parser-init fragility in production CV analyze flow.
- Provide deterministic zero-LLM extraction/matching pipeline aligned to current taxonomy contracts.
- Keep UX contract stable while changing extraction backend implementation.

How to verify:

- `source .venv311/bin/activate && PYTHONPATH=. python -m pytest tests/python -q`
- `npm run test -- tests/ui/cvjd-auto-suggest.test.tsx tests/ui/cv-import-wizard.test.tsx`
- `npm run test -- tests/api/cv-import-wizard-suggest-route.test.ts tests/api/cv-import-suggest-route.test.ts`
- `npm run test -- tests/lib/cv-import-wizard-extractor.test.ts tests/lib/cv-import-wizard-quality.test.ts`
- `npm run test -- tests/lib/pdf-client-extractor.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run build 2>&1 | tee /tmp/pro100-build.log`
- `! rg -q "Critical dependency: the request of a dependency is an expression" /tmp/pro100-build.log`

Open risks / TODO:

- Python runtime endpoint auth currently relies on optional `CV_IMPORT_PROXY_INTERNAL_SECRET` for internal-only access hardening.
- Manual production smoke is still required across browsers for final sign-off.
