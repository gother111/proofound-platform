# Project Change Entry

- Date/time (UTC): 2026-03-09T11:18:31Z
- Branch: master
- Base commit: d70641a5
  What changed:
- Added a lazy client loader boundary in `src/lib/expertise/cv-import-client-loaders.ts` so the CV import wizard no longer statically pulls `ocr-client` and `pdf-client-extractor` into the default compile graph.
- Extracted lightweight OCR config and PDF parse error helpers into `src/lib/expertise/ocr-client-config.ts` and `src/lib/expertise/pdf-client-errors.ts`, then re-exported them from the existing heavy modules to preserve behavior.
- Updated `src/components/expertise/cv-import/CvImportWizard.tsx` and `tests/ui/cv-import-wizard.test.tsx` to use the new loader surface.
- Added production Vercel env vars `VERCEL_BUILD_SYSTEM_REPORT=1` and `NODE_OPTIONS=--max_old_space_size=6144`, and dispatched the `Retry Vercel Deploy Until Synced` workflow for current `master`.
- Triggered a direct `vercel deploy --prod` for the linked `proofound-platform` project because the GitHub retry workflow is queued behind older runs.

Why:

- Recent Vercel production builds for the CV import surface were being killed by OOM during `next build`.
- The wizard only needs OCR and browser-side PDF parsing on specific user-triggered paths, so eager imports were inflating the client compile graph unnecessarily.
- Future production build failures need full build-system reporting, and the retry workflow must target the latest `master` SHA instead of leaving production pinned to an older failed deploy.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `npx vitest run tests/ui/cv-import-wizard.test.tsx tests/lib/pdf-client-extractor.test.ts --reporter=dot`
- `PYTHONPATH=. ./.venv311/bin/python -m pytest tests/python/test_pdf_extract.py -q`
- `npm run vercel:preflight`
- `npm run build`
- `npx vercel build --prod --yes`
- `gh run list --workflow ".github/workflows/retry-vercel-deploy.yml" --limit 10 --json databaseId,createdAt,status,headSha`
- `npx vercel deploy --prod --yes --token "$VERCEL_TOKEN"`
- `npx vercel inspect https://proofound-platform-ahdwklsh8-pavlo-samoshkos-projects.vercel.app --token "$VERCEL_TOKEN"`
- `curl -sS https://proofound.io/api/health`

Open risks / TODO:

- GitHub already has several older queued retry runs; monitor the newest run for SHA `d70641a578c81ee73d7b00235d9305d66cc589dd` until production advances.
- The direct production deploy `dpl_CQF3kLHTVkrTxVaDNfeFJBh6FcMW` is still queued in Vercel, so `curl https://proofound.io/api/health` continues to report the older live SHA until build scheduling and deployment finish.
- Local `vercel build` used Node `v25.4.0` from the installed CLI environment and emitted an engine warning, but the build completed successfully with Vercel-mode settings.
