# Project Change Entry

- Date/time (UTC): 2026-03-09T14:13:19Z
- Branch: master
- Base commit: a099e10c
  What changed:
- Restored CV PDF import to a Python-only async path.
- Added best-effort worker wake-ups after enqueue and for stale queued jobs during status polling.
- Removed browser-side PDF extraction and OCR fallback from the CV import wizard.
- Updated focused API/UI tests plus CV import and cron docs to match the new behavior.

Why:

- Inline extraction inside the status poll route caused user-facing `FUNCTION_INVOCATION_TIMEOUT` failures.
- The product requirement is that CV uploads must use the Python extraction algorithm reliably, not local browser parsing.

How to verify:

- `npm run test -- tests/ui/cv-import-wizard.test.tsx tests/api/cv-import-wizard-extract-route.test.ts tests/api/cv-import-wizard-extract-status-route.test.ts tests/api/cron-python-internal-worker-route.test.ts tests/lib/python-internal-trigger.test.ts` -> PASS
- `npm run lint` -> PASS with 2 pre-existing `no-img-element` warnings in landing components
- `npm run typecheck` -> PASS
- `npm run docs:freshness` -> WARN only for pre-existing `agent/runbooks/geo-audit.md` registry drift and missing metadata in `project/Documentation.md`

Open risks / TODO:

- Worker wake-up is best-effort. Durable scheduling still depends on cron-job.org for the minute-level safety net.
- Large or scanned PDFs still fail as Python parse errors in this flow because browser OCR fallback was intentionally removed.
