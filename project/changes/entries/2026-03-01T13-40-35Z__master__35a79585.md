# Project Change Entry

- Date/time (UTC): 2026-03-01T13:40:35Z
- Branch: master
- Base commit: 35a79585

What changed:

- Updated `src/components/dashboard/chipStyles.ts` to enforce non-wrapping dashboard status chips (`shrink-0 whitespace-nowrap leading-none`), removing multiline wrap tokens.
- Extended `tests/ui/dashboard-status-chip-style.test.tsx` to lock no-wrap behavior and added coverage for `InterviewsFeedbackCard` `On track` status badge.

Why:

- PRO-108 follow-up screenshots (2026-03-01) showed residual wrapped dashboard status badges after the first fix landed.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS, 2 pre-existing landing image warnings).
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS).
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/dashboard-status-chip-style.test.tsx tests/ui/metric-strip-chip-style.test.tsx` (PASS).
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/dashboard-client.test.tsx tests/ui/draggable-dashboard-while-away-visibility.test.tsx` (PASS).

Open risks / TODO:

- Very narrow card headers could still cause horizontal pressure; confirm visual QA on `/app/i/home` authenticated dashboard.
