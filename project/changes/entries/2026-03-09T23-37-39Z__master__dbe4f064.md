# Project Change Entry

- Date/time (UTC): 2026-03-09T23:37:39Z
- Branch: master
- Base commit: dbe4f064
  What changed:
- Added a new canonical PRD block, `5.4A Proof Pack Canonical Schema & Portability (MVP)`, to `PRD_for_a_web_platform_MVP.master-latest.md`.
- Specified the MVP Proof Pack object, JSON-LD-shaped export model, portability hash rules, signature placeholders, freshness rules, provenance rules, import and export behavior, visibility rules, public portfolio behavior, API and data model implications, event tracking, open questions, and acceptance criteria.
- Updated the canonical analytics taxonomy in the same PRD to add `proof_pack_updated`, `proof_pack_exported`, `proof_pack_import_validated`, `proof_pack_import_completed`, `proof_pack_freshness_changed`, and `proof_pack_signature_status_changed`.
- Reconciled later analytics text so `proof_pack_exported` is no longer described as optional or post-launch only.
- Added a compact aligned mirror section to `PRD_for_a_web_platform_MVP.md` and refreshed `Last Verified` metadata in both PRD files.

Why:

- The MVP PRD already treated Proof Packs as canonical, but it still left the export schema, provenance, signature posture, portability hash, and import behavior ambiguous.
- This change closes that gap with an MVP-safe contract that is explicit enough for implementation without drifting into heavy credential frameworks or non-MVP standards work.

How to verify:

- Review `PRD_for_a_web_platform_MVP.master-latest.md` after `5.4 Proof Pack Verification and Freshness` and confirm the new `5.4A Proof Pack Canonical Schema & Portability (MVP)` block is present with all required subheads.
- Review `PRD_for_a_web_platform_MVP.master-latest.md` Section `7.3 Canonical Event Taxonomy` and confirm the new Proof Pack events are listed and `proof_pack_freshness_changed` is the canonical pack-level freshness event.
- Review `PRD_for_a_web_platform_MVP.md` and confirm the condensed mirror section matches the canonical Proof Pack portability contract.
- Run `npm run lint`.
- Run `npm run typecheck`.
- Run `npm run docs:freshness`.

Open risks / TODO:

- `docs:freshness` still reports a pre-existing warning that `agent/runbooks/geo-audit.md` is missing from the docs registry.
- Repo code currently emits mostly artifact-level freshness events, so the future implementation will need pack-level events or a documented projection to satisfy the updated PRD.
