# Project Change Entry

- Date/time (UTC): 2026-03-10T08:41:42Z
- Branch: master
- Base commit: a118842a

What changed:

- Added `6.6 Capability Band Rubrics by Domain (MVP)` to `PRD_for_a_web_platform_MVP.master-latest.md`.
- Defined the shared four-band ladder, lean rubric schema, launch domains, provenance states, matching and proof rules, reviewer guidance, change-management rules, sample rubric tables, and acceptance scenarios.
- Added a concise governance index entry in `project/Documentation.md`.

Why:

- Explainable matching already referenced capability signals, but the canonical PRD still needed a buildable MVP rubric contract that avoided generic years-of-experience shortcuts and avoided a giant ontology.

How to verify:

- `npm run docs:freshness`
- `rg -n "Capability Band Rubrics by Domain|years-of-experience|rubric_version|Band 4|run-of-show execution" PRD_for_a_web_platform_MVP.master-latest.md`

Open risks / TODO:

- The PRD now defines the framework, but the first canonical curated `rubric_version = v1` capability row set per domain still needs later product curation before implementation.
