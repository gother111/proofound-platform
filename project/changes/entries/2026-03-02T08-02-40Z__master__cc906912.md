# Project Change Entry

- Date/time (UTC): 2026-03-02T08:02:40Z
- Branch: master
- Base commit: cc906912
  What changed:
- Increased wizard timeout robustness in `src/app/api/expertise/cv-import/wizard-suggest/route.ts`:
  - Added `export const maxDuration = 60`.
  - Added timeout precedence resolver:
    1. `CV_IMPORT_WIZARD_TIMEOUT_MS`
    2. `CV_IMPORT_SERVER_TIMEOUT_MS`
    3. default `45000`.
  - Added structured timeout code for 408 responses: `CV_IMPORT_WIZARD_TIMEOUT`.
- Added structured timeout code parity in `src/app/api/expertise/cv-import/suggest/route.ts` for 408 responses: `CV_IMPORT_SUGGEST_TIMEOUT`.
- Extended wizard client auto-recovery in `src/components/expertise/cv-import/CvImportWizard.tsx`:
  - Detect timeout failures via status `408`, `code=CV_IMPORT_WIZARD_TIMEOUT`, or timeout message.
  - Auto-retry with local text extraction and deterministic endpoint (`engine=typescript`).
  - Keep timeout fallback silent on successful recovery.
- Updated env contract:
  - `.env.example` now includes `CV_IMPORT_WIZARD_TIMEOUT_MS=45000`.
- Updated env docs:
  - `docs/ENV_VARIABLES.md` adds `CV_IMPORT_WIZARD_TIMEOUT_MS / CV_IMPORT_SERVER_TIMEOUT_MS` section with precedence and defaults.
  - Added timeout vars to feature matrix and checklist.
- Updated tests:
  - `tests/api/cv-import-wizard-suggest-route.test.ts`
  - `tests/api/cv-import-suggest-route.test.ts`
  - `tests/ui/cv-import-wizard.test.tsx`

Why:

- Single-CV wizard flows were timing out too early under the previous 15s limit and surfaced blocking errors instead of returning fallback results.
- The change ensures longer Gemini wait budget and deterministic recovery when timeout still occurs.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/cv-import-wizard-suggest-route.test.ts tests/ui/cv-import-wizard.test.tsx tests/api/cv-import-suggest-route.test.ts tests/lib/python-cv-proxy.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test:python`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`

Open risks / TODO:

- Lint still reports 2 pre-existing landing warnings for raw `<img>` usage; unrelated to this change.
- If request volume spikes, longer timeout windows can increase concurrent server occupancy; monitor route latency and fallback rate after deploy.
