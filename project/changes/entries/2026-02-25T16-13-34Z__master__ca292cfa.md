# Project Change Entry

- Date/time (UTC): 2026-02-25T16:13:34Z
- Branch: master
- Base commit: ca292cfa
  What changed:
- Added schema-drift compatibility to skill verification request creation route:
  - Primary insert uses `verification_token`.
  - Fallback retries insert without `verification_token` when the column is missing.
  - Legacy fallback links now use persisted request `id` token.
- Added token lookup compatibility in `/api/verify/[token]`:
  - Primary lookup by `verification_token`.
  - Fallback lookup by `id` when token is UUID and token-column lookup is missing/not found.
- Fixed expertise verification respond route drift:
  - Replaced `requester_id` references with `requester_profile_id`.
  - Normalized verifier-email authorization comparison to lowercase.
  - Removed unused profile lookup.
- Added/updated tests:
  - `tests/api/expertise-skill-verification-request-route.test.ts`
  - `tests/api/verify-impact-token-route.test.ts`
  - `tests/api/expertise-verification-respond-route.test.ts`

Why:

- Latest PRO-29 comment reported runtime send-flow failure in production-like usage.
- Active routes still had schema drift risks across environments where the token migration may lag.
- Respond route had column mismatch that could break notifications after response.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/expertise-skill-verification-request-route.test.ts tests/api/verify-impact-token-route.test.ts tests/api/expertise-verification-respond-route.test.ts` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build` (PASS; readiness warning for missing deploy env vars is expected in local dev)

Open risks / TODO:

- Legacy route family under `/api/verification/skill/*` still uses old columns (`requester_id`, `token_expires_at`) and remains out of scope here.
- Manual smoke in deployed environment still recommended for add-skill and edit-skill send flow.
