# Project Change Entry

- Date/time (UTC): 2026-05-16T11:04:16Z
- Branch: master
- Base commit: eb8de16b
  What changed:
- Changed provider-smoke artifacts to schema v2 with value-free prerequisite booleans for database URL presence, server-only provider-key presence, monthly hard-cap presence, and production-like runtime.
- Hardened launch-readiness smoke resolution so successful artifacts without database/provider prerequisites cannot count, and production-like readiness requires a production-like smoke artifact with monthly hard-cap proof.
- Fixed `scripts/ai-provider-smoke.ts` so dotenv files load before provider/config modules are imported, preventing false local `DATABASE_URL` diagnostics.
- Refreshed `.artifacts/ai-provider-smoke.json` with current fail-closed v2 evidence.
- Updated AI env docs and the AI verification audit with the current smoke, AI test, and privacy test evidence.

Why:

- A smoke artifact should prove the runtime prerequisites needed for the actual AI-assisted product path, not only that a model call once succeeded.
- The previous static import order let app modules observe env before `.env.local` was loaded, which made smoke diagnostics less trustworthy.

How to verify:

- `npm run ai:provider:smoke` (expected fail-closed locally without server-only provider credentials and AI spend-cap env)
- `npm run test -- tests/lib/ai-provider-smoke-artifact.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts`
- `npm run test:launch:ai`
- `npm run test:launch:privacy`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Live Gemini provider smoke remains unverified until server-only provider credentials and AI spend-cap env are available.
- Production-only provider/account state was not touched.
