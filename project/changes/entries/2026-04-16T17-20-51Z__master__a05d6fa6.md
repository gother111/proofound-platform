# Project Change Entry

- Date/time (UTC): 2026-04-16T17:20:51Z
- Branch: master
- Base commit: a05d6fa6

What changed:

- Tightened the default Vitest surface to exclude archived and intentionally non-MVP route/page tests that no longer belong to the locked launch corridor.
- Updated active tests to match current production behavior across CSRF coverage, account-deletion copy, canonical proof-pack writes, assignment publish validation, cron ownership, proof-backed verification UI, and launch-era tab/status labels.
- Cleaned up proof/verification UI tests so they seed valid proof context and target current CTA labels without ambiguous DOM queries.

Why:

- `npm run test` was failing because stale archived/non-MVP tests were still part of the default suite and several active tests had drifted from current contracts.
- The launch corridor needs a trustworthy default verification signal before broader refactors are safe.

How to verify:

- Run `npm run test`.
- Run `npm run lint`.
- Run `npm run typecheck`.
- Run `npm run docs:freshness`.

Open risks / TODO:

- The default test exclusions should be revisited if any archived/non-MVP surfaces are reactivated.
- Lint still reports one existing warning in `tests/ui/pilot-packaging-guardrails.test.tsx` for raw `<img>` usage.
