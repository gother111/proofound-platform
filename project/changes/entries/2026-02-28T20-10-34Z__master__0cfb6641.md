# Project Change Entry

- Date/time (UTC): 2026-02-28T20:10:34Z
- Branch: master
- Base commit: 0cfb6641

What changed:

- Hardened OAuth redirect URI resolution for Google and Zoom integration routes to prefer request origin when redirect env vars are relative or unset.
- Added legacy callback warning log in `/api/auth/google/callback` pointing to canonical `/api/integrations/google/callback`.
- Added deploy-readiness warnings for missing Google/Zoom redirect env vars and legacy Google callback path usage.
- Added regression tests for Google and Zoom connect/callback redirect URI behavior and state mismatch cookie clearing.
- Updated OAuth/env docs and setup runbook to recommend relative `GOOGLE_REDIRECT_URI` and `ZOOM_REDIRECT_URI` plus fully-qualified provider allowlist entries per host.
- Updated local `.env.local` redirect values to canonical relative integration callback paths.

Why:

- Fix `redirect_uri_mismatch` and prevent host drift between production, localhost, and stable preview domains.
- Ensure callback token exchange uses the same redirect URI strategy as connect initiation.

How to verify:

- `npm run test -- src/lib/integrations/__tests__/oauth-helpers.test.ts tests/api/google-integration-oauth-redirects.test.ts tests/api/zoom-integration-oauth-redirects.test.ts tests/api/video-integrations-auth-route.test.ts` (pass)
- `npm run lint` (pass with existing unrelated warnings only)
- `npm run typecheck` (pass)

Open risks / TODO:

- Real OAuth callback flow still requires manual browser validation on production, localhost, and preview hosts because external provider authorization is not fully automatable in CI.
- Vercel environment variables should be updated to relative callback paths for preview/local safety.
