# Project Change Entry

- Date/time (UTC): 2026-03-13T08:03:53Z
- Branch: master
- Base commit: 4767ea77
  What changed:
- Added a new shared helper at `src/lib/expertise/candidate-dedupe.ts`.
- Refactored `src/lib/expertise/skill-candidate-fusion.ts` and `src/lib/expertise/gemini/reranker.ts` to use the shared helper instead of duplicating dedupe-key fallback logic.

Why:

- Remove duplicated logic for candidate dedupe key generation while preserving behavior.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `. "$HOME/.nvm/nvm.sh" && nvm use 20.20.0 >/dev/null && npm run test -- tests/lib/gemini-reranker.test.ts tests/lib/skill-candidate-fusion.test.ts`

Open risks / TODO:

- Test execution depends on Node 20.20.0 for Vitest and Vite compatibility in this repo.
