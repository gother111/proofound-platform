# Project Change Entry

- Date/time (UTC): 2026-03-09T20:06:37Z
- Branch: master
- Base commit: 197fcb37
  What changed:
- Rewrote `docs/verification-policy-mvp.md` into a launch-safe trust ladder with explicit meanings, limits, freshness rules, downgrade behavior, surface rules, edge cases, and event requirements.
- Updated `src/lib/verification/policy.ts` so public portfolio badges only expose active positive trust signals and renamed the workplace trust label to `Workplace-verified`.
- Aligned public portfolio and settings copy with the narrower trust language in `src/app/portfolio/[handle]/page.tsx`, `src/app/portfolio/org/[slug]/page.tsx`, `src/components/settings/VerificationStatus.tsx`, `src/components/settings/SettingsContent.tsx`, `src/app/verify-work-email/VerifyWorkEmailContent.tsx`, and `src/lib/reports/evidence-pack-generator.ts`.
- Added and updated verification policy and UI tests.

Why:

- The prior verification language still overclaimed trust and allowed expired or under-review states to leak onto public surfaces.
- MVP needs a narrow trust model that states exactly what each badge does and does not prove.

How to verify:

- `npm run test -- tests/lib/verification-policy.test.ts tests/ui/verification-status-options.test.tsx tests/ui/public-portfolio-page.test.tsx tests/ui/public-org-portfolio-page.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run docs:freshness`

Open risks / TODO:

- `docs:freshness` still reports pre-existing warnings for `agent/runbooks/geo-audit.md` missing from the registry and missing freshness metadata in `project/Documentation.md`.
- The canonical PRD files were already dirty in the worktree, so this task updated the active verification policy doc instead of editing those in-place.
