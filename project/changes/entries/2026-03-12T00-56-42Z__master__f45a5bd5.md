# Project Change Entry

- Date/time (UTC): 2026-03-12T00:56:42Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Canonical org-role enforcement is now the active source of truth in authz and route-layer checks.
- `src/lib/authz/policy.ts` now types `OrgRole` as canonical-only and limits decision finalization plus org configuration actions to `org_owner`.
- `src/lib/api/auth.ts`, `src/lib/api/mobile/auth.ts`, and `src/lib/authz/membership.ts` retain legacy label compatibility only through normalization.
- Route-level authorization in decisions, decision window, match explain, ownership, org content/config CRUD, shortlist/coverage, and related mobile routes now uses only canonical role arrays or normalized canonical roles.
- Added focused tests for canonical policy enforcement, legacy-role normalization, owner-only decision rights, canonical match-explain access, reviewer review access, and ownership-route authorization.

Why:

- The locked MVP defines only `org_owner`, `org_manager`, and `org_reviewer` as valid org roles.
- Legacy `owner/admin/member/viewer` labels were still shaping live enforcement behavior in multiple API handlers, which weakened the MVP role model and made final-decision/config permissions ambiguous.

How to verify:

- `npm run test -- tests/lib/authz-policy.test.ts tests/lib/membership-normalization.test.ts tests/api/organizations-route.test.ts tests/api/org-candidate-invites-route.test.ts tests/api/organization-visibility-route.test.ts tests/api/organization-test-matches-route.test.ts tests/api/org-match-review-route.test.ts tests/api/interviews-cancel-route.test.ts tests/api/interviews-edit-route.test.ts tests/api/decisions-route.test.ts tests/api/decisions-window-route.test.ts tests/api/match-explain-route.test.ts tests/api/organizations-ownership-route.test.ts src/app/api/metrics/__tests__/route.test.ts`
- `npm run lint`
- `npm run typecheck` currently fails on unrelated pre-existing repo issues in `src/actions/profile.ts`, `src/app/app/o/[slug]/assignments/new/page.tsx`, `src/app/app/o/[slug]/profile/page.tsx`, `src/components/matching/AssignmentBuilder.tsx`, `src/components/matching/AssignmentBuilderV2.tsx`, and `src/lib/profileStorage.ts`.

Open risks / TODO:

- Phase 2 should reconcile `src/db/policies.sql` and schema/reporting surfaces that still use legacy role labels for analytics or presentation.
- `src/app/api/moderation/report/route.ts` still selects owners for notifications; it is not an enforcement path, but should eventually align with canonical role naming if that surface remains in scope.
