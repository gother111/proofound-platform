# Project Change Entry

- Date/time (UTC): 2026-05-21T08:48:28Z
- Branch: master
- Base commit: e28b0ce9
  What changed:
- Replaced raw console diagnostics in `/api/match/hide` and `/api/match/snoozed` with structured `match.hide.*` and `match.snoozed.*` server log events.
- Added route coverage for hidden-match list failures, hide/unhide update failures, and paused-match list failures.
- Extended runtime debug-output guardrails so the old matching console strings cannot return.
- Updated `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md` with the matching sweep checkpoint.

Why:

- Hidden and paused match surfaces sit inside the MVP's privacy-safe matching corridor, so failure diagnostics should stay structured while responses continue to avoid raw score artifacts.

How to verify:

- `npm test -- --run tests/api/match-hide-route.test.ts tests/api/match-snoozed-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- 'src/app/api/match/hide/route.ts' 'src/app/api/match/snoozed/route.ts' tests/api/match-hide-route.test.ts tests/api/match-snoozed-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`

Open risks / TODO:

- The broader MVP sweep is still active; `src/app/api/match/explain/[matchId]/route.ts` and `src/app/api/core/matching/profile/handler.ts` remain separate matching diagnostics follow-ups.
- The worktree still has unrelated unstaged edits outside this checkpoint; they were not staged or committed here.
