# Project Change Entry

- Date/time (UTC): 2026-02-28T19:38:16Z
- Branch: master
- Base commit: 1a876aef
  What changed:
- Added organization settings support for video integrations:
  - New page: `src/app/app/o/[slug]/settings/integrations/page.tsx` (owner/admin gated).
  - Added Integrations card/link in org settings hub (`src/app/app/o/[slug]/settings/page.tsx`).
- Extended the shared video integrations manager to support persona-aware callback cleanup and OAuth start paths:
  - `VideoIntegrationsManager` now accepts optional `returnTo`.
  - OAuth init request now includes `returnTo` query.
- Added safe return-path plumbing across video OAuth routes:
  - New helper: `resolveIntegrationReturnPath` in `src/lib/integrations/oauth-helpers.ts`.
  - `GET /api/integrations/video/[provider]/auth` now returns provider connect URL with sanitized `returnTo`.
  - Zoom/Google connect routes persist sanitized return target in httpOnly cookies.
  - Zoom/Google callback routes read return target cookie, redirect back to initiator page, and clear OAuth cookies.
- Added/updated tests:
  - `src/lib/integrations/__tests__/oauth-helpers.test.ts`
  - `tests/api/video-integrations-auth-route.test.ts`
  - `tests/ui/organization-settings-integrations.test.tsx`

Why:

- Org users could not connect Zoom/Google from org settings because no org settings entry/page existed.
- OAuth callbacks defaulted to individual settings, so org-initiated connections redirected to the wrong page.
- Interview scheduling depends on `user_video_integrations`; this change ensures both personas can reliably configure providers.

How to verify:

- Focused:
  - `npx vitest run src/lib/integrations/__tests__/oauth-helpers.test.ts tests/api/video-integrations-auth-route.test.ts tests/ui/organization-settings-integrations.test.tsx tests/ui/settings-integrations-client.test.tsx` (PASS)
- Quality gates:
  - `npm run lint` (PASS with existing unrelated warnings in landing components for `<img>` usage)
  - `npm run typecheck` (PASS)
- Broader checks attempted:
  - `npm run test` (FAIL, existing unrelated failures: missing `pdfjs-dist/webpack.mjs` imports and one matching-page-gated assertion mismatch)
  - `npm run build` (FAIL, existing unrelated missing `pdfjs-dist/webpack.mjs` module)

Open risks / TODO:

- Global repo baseline currently has unresolved `pdfjs-dist/webpack.mjs` import issues that block full test/build verification.
- This implementation keeps provider connections user-scoped (host-level) for both individual and org contexts by design.
