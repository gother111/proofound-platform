# Project Change Entry

- Date/time (UTC): 2026-06-20T07:58:40Z
- Branch: master
- Base commit: e4e543978

What changed:

- Added mobile safe-area bottom clearance to the assignment review workspace in `src/components/matching/MatchingOrganizationView.tsx`, with the reset aligned to the `md` breakpoint where the fixed mobile nav disappears.
- Added focused test coverage in `tests/ui/matching-organization-view.test.tsx` for the mobile assignment review bottom clearance contract.
- Restructured the actions header layout in `src/app/portfolio/org/[slug]/page.tsx` to position "Copy share link" and optional "Website" actions before "Return home" / "Return to menu".
- Changed "Return home" button variant from `variant="outline"` to `variant="ghost"` and added text/hover styling to make it visually quieter/secondary.
- Restyled the external "Website" link to match the custom stone border, rounded-xl, and glass styling used by `ShareLinkButton` and `DownloadOrganizationPdfButton`.
- Added `aria-hidden="true"` to Lucide icons inside the action components (`ShareLinkButton`, `CopyTextButton`, `DownloadPdfButton`, `DownloadOrganizationPdfButton`, and the main page).
- Added an descriptive `aria-label` to the external "Website" link warning users that it opens in a new tab.
- Added corresponding unit tests in `tests/ui/public-org-portfolio-page.test.tsx` verifying that the button reordering, classes, and accessibility labels behave correctly.

Why:

- To keep mobile proof-review cards and the selected-review jump/detail region clear of the fixed mobile nav.
- To correct the visual hierarchy so the public trust page feels like a shareable proof surface rather than an exit/landing-bound redirect.
- To improve accessibility following WCAG guidelines (aria-hidden on redundant/decorative icons and warning on links opening new tabs).

How to verify:

- Run focused vitest tests: `npm test -- tests/ui/matching-organization-view.test.tsx tests/ui/public-org-portfolio-page.test.tsx tests/ui/public-portfolio-actions.test.tsx`.
- Run typecheck, linting, docs freshness, and diff whitespace checks: `npm run typecheck`, `npm run lint`, `npm run docs:freshness`, and `git diff --check`.

Open risks / TODO:

- `npm run docs:freshness` still reports pre-existing orphan artifact warnings in warning mode.
