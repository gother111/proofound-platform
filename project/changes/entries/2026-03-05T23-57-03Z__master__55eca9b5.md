# Project Change Entry

- Date/time (UTC): 2026-03-05T23:57:03Z
- Branch: master
- Base commit: 55eca9b5
  What changed:
- Hardened CV skill precision with disagreement suppression between local and Gemini mappings, stricter fallback filtering, and ambiguous short-token handling in `src/lib/expertise/*` and `src/lib/ai/nlp-extractor.ts`.
- Simplified unresolved review actions in the CV import UI so users can explicitly accept, replace, keep unmapped, or mark a candidate as not a skill, with weak-match confirmation and safe telemetry in `src/components/expertise/*`.
- Added curated taxonomy alias fixes for `C++` and `Node.js`, blocked unsafe short-token taxonomy search, added review telemetry reporting, and expanded regression coverage across taxonomy mismatch, UI, and API tests.

Why:

- Wrong taxonomy matches were still leaking through weak fuzzy and ambiguous-token paths, especially when local and Gemini ranking disagreed.
- Review flow needed a clearer escape hatch for uncertain candidates so users were not pushed toward low-confidence taxonomy choices.
- The feature needed measurable correction signals without storing CV text.

How to verify:

- `npm run test -- tests/lib/skill-confidence.test.ts tests/lib/skill-candidate-fusion.test.ts tests/lib/taxonomy-mismatch-regressions.test.ts tests/lib/cv-import-suggest-quality.test.ts tests/lib/cv-import-wizard-quality.test.ts`
- `npm run test -- tests/ui/cv-import-wizard.test.tsx tests/ui/cvjd-auto-suggest.test.tsx tests/api/cv-import-wizard-suggest-route.test.ts tests/api/expertise-taxonomy-route.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run taxonomy:validate:aliases`
- `npm run taxonomy:report:cv-import-review`
- `npm run build`

Open risks / TODO:

- `src/db/migrations/20260306005500_fix_cv_import_taxonomy_alias_precision.sql` was generated but not applied locally because `.env.local` points `DATABASE_URL` at a remote Supabase pooler instead of a local dev database.
- If a safe local Postgres target becomes available, apply the new migration there and rerun alias validation against that migrated state before any remote rollout.
