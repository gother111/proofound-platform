# Project Change Entry

- Date/time (UTC): 2026-03-02T12:58:21Z
- Branch: master
- Base commit: 90206924
  What changed:
- Fixed `.vercelignore` patterns to root-anchored rules so Vercel no longer excludes `src/data/**`.
- Removed overly broad extension ignores (`*.md`, `*.png`, `*.jpg`, `*.jpeg`) that could drop runtime/static assets unexpectedly.

Why:

- Vercel build failed with `Module not found: Can't resolve '@/data/zen'` because `src/data/zen.ts` was excluded from deployment input by the unanchored `data` rule.

How to verify:

- Trigger a Vercel production deploy and confirm build passes the previous `@/data/zen` module resolution step.
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`

Open risks / TODO:

- Deploy can still fail for unrelated platform-side issues, but this specific module-resolution blocker is removed.
