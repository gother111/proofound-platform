# Project Change Entry

- Date/time (UTC): 2026-02-23T09:37:00Z
- Branch: HEAD
- Base commit: b5621d35
  What changed:
- Updated `/src/app/app/o/[slug]/assignments/new/page.tsx` to keep organization assignment creation in Basic mode by default and gate Advanced mode behind explicit opt-in.
- Added `advancedModeUnlocked` state and opt-in CTA (`Need extra control? Enable Advanced mode`) so the Basic/Advanced segmented toggle is only shown after explicit user action.
- Updated template application behavior so advanced-recommended templates no longer auto-switch mode unless Advanced is already unlocked; added informational toast guidance.
- Preserved switch-back behavior from Advanced to Basic and step jump behavior (step 3 -> step 4 when returning to Basic).
- Added focused UI tests in `/tests/ui/assignment-builder-mode-entry.test.tsx` covering:
  - default Basic entry with hidden advanced toggle
  - explicit Advanced opt-in reveal
  - advanced template application without opt-in
  - existing advanced draft hydration behavior

Why:

- Align company posting flow with product direction: start fast in Basic mode and expose Advanced controls only on demand to reduce cognitive load and accidental strict-mode publishing requirements.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `npm run test -- tests/ui/assignment-builder-mode-entry.test.tsx tests/api/assignments.test.ts`
- `npm run test`
- `npm run build` (passes with expected local warning about missing `DATABASE_URL` and mock DB fallback)

Open risks / TODO:

- Local environment required Node `20.20.0`; default shell was on Node `16.14.0` and needed explicit `nvm use 20.20.0` for checks.
- Advanced template metadata currently informs via toast only when mode is not unlocked; evaluate whether an inline non-toast affordance is needed after rollout feedback.
