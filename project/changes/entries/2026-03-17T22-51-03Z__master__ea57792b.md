# Project Change Entry

- Date/time (UTC): 2026-03-17T22:51:03Z
- Branch: master
- Base commit: ea57792b
  What changed:
- Simplified individual onboarding so the first-session context step only collects anchor fields.
- Replaced the Proof Pack recap step with a real mini-editor for claim, ownership, and outcome plus a live preview card.
- Updated onboarding persistence to map the lighter payload into one anchor context, one proof artifact, and one canonical Proof Pack without a migration.
- Expanded onboarding UI and action tests to cover the lighter flow and structured Proof Pack mapping.

Why:

- Reduce first-session friction so the dominant value feels like adding proof, not completing a résumé-style profile.
- Keep the live onboarding corridor aligned with the locked MVP authority and Proof Pack canonicality rules.

How to verify:

- Run `npm run test -- tests/ui/individual-setup-proof-first.test.tsx tests/lib/proof-pack-anchor.test.ts tests/actions/onboarding.test.ts` in a Node runtime new enough for the current Vitest/Vite toolchain.
- Run `npm run typecheck`.
- Manually walk `/onboarding` as an individual user and confirm one anchor context plus one Proof Pack can reach the publish step in one session.

Open risks / TODO:

- The current local environment uses Node `v16.14.0`, which blocks the targeted Vitest run because the installed Vite/Vitest toolchain expects a newer Node runtime.
- Onboarding still maps the lighter first-session fields into older required context columns, so a later cleanup can reduce duplication once the broader profile schema is simplified.
