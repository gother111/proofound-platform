# Project Change Entry

- Date/time (UTC): 2026-03-08T20:06:01Z
- Branch: master
- Base commit: 1352ef4e
  What changed:
- Added Block 3 schema contracts for `match_review_states`, `reveal_events`, `match_reason_ledger`, `fairness_evaluations`, and `fairness_remediation_events`, plus new match audit/versioning columns.
- Added a canonical review-contract service for blind projection, reason rendering, reveal logging, review mutations, and fairness gating.
- Moved org shortlist APIs and UI to review-state-backed shortlist records with named-no-contact progressive reveal and fairness-safe rank bands.
- Updated match persistence, explanation routes, mutual reveal hooks, and candidate audit export to use the new durable contracts.

Why:

- Progressive reveal, explainability, and fairness needed to be enforced as stored backend contracts instead of UI-only behavior or prose-only explanations.

How to verify:

- Run `npm run typecheck`.
- Run `npx vitest run tests/lib/matching-review-contract.test.ts tests/canonical-domain.test.ts tests/api/mobile-practical-routes.test.ts`.
- Run `npm run db:migrate`.
- Load `/api/org/:slug/shortlist`, `/api/match/assignment`, and `/api/match/explain/:matchId` with org auth and verify blind shortlist behavior plus rank-band degradation.

Open risks / TODO:

- Fairness evaluation currently degrades to `unavailable` when demographic columns or opt-in cohort support are missing; richer breach detection still needs live demographic schema support.
- Org assignment matching now persists snapshots inline; if this route becomes hot, persistence/evaluation may need to move behind a background job.
