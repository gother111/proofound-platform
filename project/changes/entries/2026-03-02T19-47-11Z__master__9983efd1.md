# Project Change Entry

- Date/time (UTC): 2026-03-02T19:47:11Z
- Branch: master
- Base commit: 9983efd1
  What changed:
- Added shared CV import existing-skill filtering in `src/lib/expertise/cv-import-existing-skills-filter.ts` and wired it into both suggest routes.
- Extended CV import candidate schema with optional `already_in_profile` and propagated it through route responses, including proxied/passthrough success payloads.
- Updated CV import UI flows to parse/render `already_in_profile`, suppress false "Needs mapping" for duplicate-only candidates, and clear the flag when users manually map a skill.
- Added API and UI tests covering duplicate-only candidate tagging and non-regression behavior.

Why:

- Prevent duplicate skill suggestions during CV import before apply-time upsert, while preserving transparency by showing duplicate-only candidates as already present in profile.

How to verify:

- `npm run test -- tests/api/cv-import-suggest-route.test.ts tests/api/cv-import-wizard-suggest-route.test.ts tests/ui/cv-import-wizard.test.tsx`
- `npm run lint` (expect existing non-blocking `@next/next/no-img-element` warnings in landing components)
- `npm run typecheck`

Open risks / TODO:

- Existing landing page lint warnings are unrelated and left unchanged.
