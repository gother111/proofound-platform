# Project Change Entry

- Date/time (UTC): 2026-05-21T17:30:34Z
- Branch: master
- Base commit: 262e45440
  What changed:
- Made `scripts/launch-smoke-runner.ts` resolve command scenario env through a tested helper.
- Live launch smoke commands now strip local-only bypass flags, including `PROOFOUND_SKIP_TRANSACTIONAL_EMAIL_DELIVERY`, and restore the intended live `BASE_URL` when a local command scenario carries an empty override.
- Added focused test coverage for local versus live launch smoke env behavior.

Why:

- The full organization corridor smoke scenario needs local email-skip behavior for local Playwright parity, but production-candidate smoke evidence must not be generated with transactional email delivery skipped or local smoke fallbacks enabled.

How to verify:

- `npm run test -- tests/scripts/launch-smoke-runner-env.test.ts tests/lib/launch-hardening-contract.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This prevents live smoke command env contamination. It does not replace running production-candidate smoke, perf-status, restore verification, or final go/no-go against the intended target.
