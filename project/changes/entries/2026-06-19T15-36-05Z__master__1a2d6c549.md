# Project Change Entry

- Date/time (UTC): 2026-06-19T15:36:05Z
- Branch: master
- Base commit: 1a2d6c549
  What changed:
- Adjusted the public individual portfolio header so the Proofound label and direct-link badge stack cleanly on narrow screens and return to a compact row on larger screens.
- Added a focused UI regression assertion for the public-page header label and badge no-wrap behavior.

Why:

- The public portfolio first viewport was functionally healthy, but the mobile header looked cramped because the product label and direct-link badge competed for one row and wrapped awkwardly.
- The public page is a launch-facing proof/trust surface, so its first viewport should read calm and deliberate before the viewer reaches the proof content.

How to verify:

- `npm test -- tests/ui/public-portfolio-page.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`
- Browser: `/portfolio/demo-proofound` at 320, 375, 390, 414, 768, and desktop widths with no horizontal overflow, no framework overlay, no console warnings/errors, no sub-44px visible controls, and a successful proof-context disclosure interaction.

Open risks / TODO:

- `docs:freshness` still reports warning-mode orphan registry entries unrelated to this UI change.
