# Project Change Entry

- Date/time (UTC): 2026-03-01T14:59:29Z
- Branch: master
- Base commit: e1388bad
  What changed:
- Paused Zoom integrations as coming soon in active OAuth entrypoints and integration UI surfaces (`/api/integrations/video/[provider]/auth`, `/api/integrations/zoom/connect`, `/api/integrations/[provider]/connect`, `VideoIntegrationsManager`).
- Kept Google Meet auto flow active for scheduling while manual scheduling now requires explicit manual provider selection (`teams`, `zoom`, `google_meet`, `other`).
- Added interview persistence for manual provider via new DB column and migration (`interviews.manual_meeting_provider`) plus compatibility fallback for legacy DBs missing the column.
- Updated interview display surfaces to show manual provider labels and manual-safe platform rendering (`InterviewConfirmation`, `InterviewCard`).
- Extended API/UI tests for Zoom coming-soon rejection and manual provider requirement.

Why:

- Zoom OAuth redirect instability blocked reliable integration; product direction is to pause Zoom while keeping Google and manual scheduling available.
- Manual links now require provider attribution for clearer context in interview operations and reporting.

How to verify:

- `npm run db:migrate` (PASS; applied `20260301150000_add_interviews_manual_meeting_provider`).
- `npm run lint` (PASS with 2 pre-existing `@next/next/no-img-element` warnings in landing files).
- `npm run typecheck` (PASS).
- `npm run test -- tests/api/interviews-schedule-route.test.ts tests/ui/schedule-interview-modal.test.tsx` (PASS; 12 tests).
- `npm run build` (FAIL due pre-existing unrelated `pdfjs-dist/webpack.mjs` module resolution errors in expertise import paths).

Open risks / TODO:

- Full production build is currently blocked by unrelated existing `pdfjs-dist/webpack.mjs` dependency issue in expertise components.
- Optional follow-up: remove/expire legacy Zoom tokens in `user_video_integrations` if product wants hard cleanup beyond connect/schedule blocking.
