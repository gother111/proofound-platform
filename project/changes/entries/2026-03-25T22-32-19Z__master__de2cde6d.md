# Project Change Entry

- Date/time (UTC): 2026-03-25T22:32:19Z
- Branch: master
- Base commit: de2cde6d
What changed:
- Narrowed the active launch surface so the compiled route inventory matches the locked MVP corridor again, including reclassifying only launch-critical internal ops routes and removing stale non-MVP API routes.
- Moved the kept core matching implementations behind non-route handler files so legacy duplicate compiled routes no longer widen the launch allowlist.
- Fixed the strict org prod corridor expectation to match the current hard-gated org settings behavior.
- Removed raw `original_filename` exposure from the user export manifest and replaced it with owner-safe display names derived from sanitized filename metadata.
- Generated a fresh local launch smoke artifact and confirmed local synthetic monitors plus local `/api/monitoring/launch-status` now report ready.

Why:
- Actions 1 to 4 were blocked by route-surface breadth rather than a current `/_document` build failure.
- Action 8 required the legacy `/api/match/decision` transport to be removed from the active corridor.
- Action 9 required the export surface to stop leaking raw source filenames in portability output.
- Action 14 required fresh local smoke evidence after the corridor fixes so the local launch-status contract could be revalidated.

How to verify:
- `npm run build`
- `npm run test -- tests/api/launch-surface-inventory.test.ts tests/api/launch-page-inventory.test.ts src/lib/launch/__tests__/surface-policy.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts`
- `npm run test -- tests/api/core-matching-gating-routes.test.ts tests/api/core-matching-assignment-route.test.ts tests/api/core-matching-profile-route.test.ts tests/api/match-interest-route.test.ts tests/api/core-matching-profile-performance.test.ts`
- `PLAYWRIGHT_SERVER_MODE=prod npm run test:e2e:org:strict`
- `npm run test -- tests/api/organizations-team-route.test.ts tests/lib/authz-policy.test.ts`
- `npm run test -- tests/api/verification-status-route.test.ts tests/ui/verification-status-options.test.tsx tests/api/assignments-publish-route.test.ts tests/api/org-audit-export-routes.test.ts tests/api/decisions-route.test.ts tests/api/engagement-verifications-route.test.ts`
- `npm run test -- tests/lib/uploads-export.test.ts tests/lib/uploads-privacy.test.ts`
- `npm run test:privacy`
- `npm run test:privacy:extended`
- Start prod server locally, then run `BASE_URL=http://127.0.0.1:3100 LAUNCH_SMOKE_ARTIFACT_PATH=.artifacts/launch-smoke-report.json npm run test:launch:smoke`
- `BASE_URL=http://127.0.0.1:3100 LAUNCH_SMOKE_ARTIFACT_PATH=.artifacts/launch-smoke-report.json npm run monitor:launch`
- `curl -sS http://127.0.0.1:3100/api/monitoring/launch-status`

Open risks / TODO:
- Live `https://proofound.io/api/monitoring/launch-status` is still blocked by `missing_smoke_artifact`, so the deployed runtime still cannot see the required smoke evidence even though the workspace is green.
- Actions 15 to 20 remain intentionally deferred until the live launch-status blocker is cleared.
