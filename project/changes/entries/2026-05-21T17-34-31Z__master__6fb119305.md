# Project Change Entry

- Date/time (UTC): 2026-05-21T17:34:31Z
- Branch: master
- Base commit: 6fb119305
  What changed:
- Updated the main, mock a11y, and strict a11y Playwright configs so an external `BASE_URL` reuses the existing target even when `CI=true`.
- Added launch-gate config coverage to keep external production-candidate Playwright runs from starting local web servers.

Why:

- Production-candidate smoke and a11y evidence must run against the intended target. A CI run with `BASE_URL=https://...` should not start a local `next start` or `next dev` process with local/test env prefixes while also claiming to validate that external target.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This protects Playwright target selection. Final launch readiness still requires actually running production-candidate smoke, a11y where applicable, perf-status, restore verification, and go/no-go against the intended target.
