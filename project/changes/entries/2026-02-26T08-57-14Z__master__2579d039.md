# Project Change Entry

- Date/time (UTC): 2026-02-26T08:57:14Z
- Branch: master
- Base commit: 2579d039
  What changed:
- Reworked migration audit script to align with canonical migration flow:
  - `scripts/audit-migration-ledger.mjs`
  - default mode now audits `src/db/migrations/*.sql` plus supplemental policy/trigger versions against `public.app_migration_ledger`
  - added optional mode: `--mode legacy-supabase`
  - added optional strict extra-row enforcement: `--fail-on-extra`
- Updated migration docs to match canonical behavior:
  - `README.md`
  - `agent/runbooks/setup.md`
  - `agent/checklists/verification.md`
  - `RUN_MIGRATIONS_GUIDE.md`
  - `APPLY_MIGRATIONS_MANUAL.md`

Why:

- Previous `db:audit:migrations` compared `supabase/migrations` to `supabase_migrations.schema_migrations`, which produced persistent historical drift noise and blocked actionable migration verification.
- The migration runner of record (`db:migrate`) uses `src/db/migrations` and `public.app_migration_ledger`, so audit behavior needed to match runtime truth.

How to verify:

- `npm run db:audit:migrations` (expected pass unless local canonical files are unapplied)
- `npm run db:audit:migrations -- --mode legacy-supabase` (expected to surface legacy drift in current DB)
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Canonical ledger still has one extra historical version in current DB not present locally:
  - `20260225143000_expand_skill_taxonomy_language_and_common_terms`
- Legacy Supabase migration table remains heavily divergent from local `supabase/migrations` and should be handled as a separate reconciliation task if strict parity is required.
