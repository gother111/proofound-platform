# Project Change Entry

- Date/time (UTC): 2026-02-28T18:07:22Z
- Branch: master
- Base commit: f297b144

What changed:

- Shifted impact story outcomes to change-first semantics across form, action serialization, profile rendering, and verifier claim parsing.
- Added `change` as canonical outcome text while keeping backward-compatible `label`; metric fields now persist as nullable/optional supporting data.
- Added migration `src/db/migrations/20260228184000_reframe_impact_story_outcomes_change_first.sql` to backfill `measured_outcomes[*].change` with directional/neutral heuristic and recompute legacy `outcomes` summary text.
- Updated targeted unit tests for form behavior, create/update action serialization, and verify route claim labels.

Why:

- Outcome UX and verification labels previously emphasized metrics first, which did not match product intent for impact stories.
- Existing JSONB outcome data needed one-time content migration to avoid mixed old/new phrasing after rollout.

How to verify:

- `npm run typecheck` (PASS)
- `npm run lint` (PASS, with 2 pre-existing landing image warnings)
- `npm run db:migrate` (PASS, migration applied and idempotent on rerun)
- `npm run db:drift-check` (PASS)
- `npm run test -- tests/ui/impact-story-form.test.tsx tests/actions/create-impact-story.test.ts tests/actions/profile-impact-story-create.test.ts tests/actions/profile.test.ts tests/api/verify-impact-token-route.test.ts` (PASS)

Open risks / TODO:

- Heuristic change-text rewrite can still sound awkward for some legacy labels; fallback safety remains via preserved `label`.
- Verification route test output still logs expected audit insert errors from mocked `audit_logs` table surface.
