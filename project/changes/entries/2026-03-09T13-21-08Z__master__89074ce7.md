# Project Change Entry

- Date/time (UTC): 2026-03-09T13:21:08Z
- Branch: master
- Base commit: 89074ce7
  What changed:
- Added the missing public portfolio SEO dependency files: `src/components/seo/JsonLdScripts.tsx`, `src/lib/seo/json-ld.ts`, and the focused coverage in `tests/lib/public-json-ld.test.ts`.
- Kept the follow-up narrow so the lifecycle/submissions release stays as a two-commit production sequence instead of folding unrelated extraction-queue work into `master`.

Why:

- Clean post-commit verification of `89074ce7` failed because the public portfolio pages import the new JSON-LD helpers directly.
- Production needed this follow-up so `typecheck`, `next build`, and `vercel build` all pass on the exact release SHA.

How to verify:

- `npm run db:migrate`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/expertise-skill-verification-request-route.test.ts tests/api/portfolio-view-route.test.ts tests/api/profile-snippet-route.test.ts tests/canonical-domain.test.ts tests/portfolio-visibility.test.ts tests/lib/public-json-ld.test.ts tests/lib/residual-lifecycle-contracts.test.ts tests/lib/verification-log-entries.test.ts tests/ui/public-org-portfolio-page.test.tsx tests/ui/public-portfolio-page.test.tsx --reporter=dot`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run build`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vercel build --prod --yes`

Open risks / TODO:

- The main worktree still contains many unrelated local edits that remain intentionally excluded from this release.
- `npm run lint` still reports the 2 pre-existing `@next/next/no-img-element` warnings in landing components.
