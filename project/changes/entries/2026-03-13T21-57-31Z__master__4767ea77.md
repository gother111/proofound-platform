# Project Change Entry

- Date/time (UTC): 2026-03-13T21:57:31Z
- Branch: master
- Base commit: 4767ea77
  What changed:
- Added `src/lib/launch/surface-policy.ts` and moved middleware archive classification for `/api/mobile/**`, `/api/wellbeing/**`, and non-allowlisted `/api/admin/**` onto a single shared policy.
- Retargeted active admin dashboard, performance alert, and fairness-note links away from archived admin pages and into the preserved internal-ops corridor.
- Added focused launch-posture tests for the shared policy, middleware archive behavior, and admin dashboard quick links.
- Documented a repo-specific verification rule in `agent/checklists/verification.md`: run `npm run typecheck` and `npm run build` sequentially because both touch `.next/types`.

Why:

- Block 6 required the active launch surface to match the locked MVP corridor while leaving future mobile, wellbeing, and broader admin code safely isolated instead of deleted.
- The verification checklist needed one practical note captured from fresh evidence so future blocks do not lose time to a false `.next/types` race.

How to verify:

- `export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH" && npm run test -- src/lib/launch/__tests__/surface-policy.test.ts src/lib/__tests__/middleware-launch-archive.test.ts tests/ui/admin-dashboard-launch-links.test.tsx tests/ui/archived-mvp-routes.test.ts tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/command-palette-archived-links.test.tsx tests/ui/organization-settings-integrations.test.tsx`
- `export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH" && npm run lint`
- `export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH" && npm run typecheck`
- `export PATH="$HOME/.nvm/versions/node/v20.20.0/bin:$PATH" && npm run build`

Open risks / TODO:

- `tests/ui/verifications-page.test.tsx` still expects the older request-feed shape and should be reconciled in a follow-up block.
- Live launch smoke evidence is still stale, so launch-status and `npm run go:no-go` cannot yet prove a fresh green state.
