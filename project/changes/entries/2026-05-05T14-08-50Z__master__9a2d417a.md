# Project Change Entry

- Date/time (UTC): 2026-05-05T14:08:50Z
- Branch: master
- Base commit: 9a2d417a
  What changed:
- Kept production GCP OCR fail-closed by setting Vercel production `GCP_CV_OCR_ENABLED=false` after the production retry workflow showed OCR enabled without budget-alert and Cloud Run max-instance governance.
- Added local/mock-mode fallbacks for selected assignment, verification, dashboard, and proof flows so strict/local launch checks can exercise MVP surfaces without broadening production behavior.
- Tightened cookie/telemetry consent behavior and defaulted the assistive AI UI hook to closed until the feature-flag API confirms availability.
- Refreshed launch smoke evidence and added the GCP OCR deployment runbook to the docs registry.

Why:

- Production deploy readiness was correctly blocking because OCR beta provider configuration was partially enabled without the required cost controls.
- The final readiness pass needed current evidence for AI assist, Proof Artifact OCR beta fail-closed behavior, Start from CV, route boundaries, privacy, security, cost controls, release flags, and launch smoke.

How to verify:

- `npm ci`
- `npm run audit:prod`
- `npm run audit:all`
- `npm run deploy:readiness:strict`
- `npm run db:audit:migrations`
- production-env simulation of `scripts/check-deploy-readiness.mjs` using `.vercel/.env.production.local`
- `npm run test -- tests/api/start-from-cv-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/ui/cookie-banner.test.tsx tests/lib/gcp-cv-ocr-config.test.ts tests/lib/proof-artifact-text-extraction.test.ts`
- `npm run test:launch:ai`
- `npm run test:launch:routes`
- `npm run test:launch:upload`
- `npm run test:launch:privacy`
- `npm run test:e2e:landing`
- `npm run test:e2e:landing:visual`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`
- `npm test`
- `npm run build`
- `npm run test:launch:smoke`
- `npm run vercel:build:production`

Open risks / TODO:

- OCR beta remains intentionally disabled in production until the real Google Cloud budget alert, hard cap, private Cloud Run invocation, max-instance setting, and log-safety evidence are verified.
- `npm run db:audit:migrations` still reports one non-blocking historical DB-only migration row: `20260317224741_canonicalize_org_role_constraints`.
