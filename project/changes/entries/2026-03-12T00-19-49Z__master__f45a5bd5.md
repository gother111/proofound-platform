# Project Change Entry

- Date/time (UTC): 2026-03-12T00:19:49Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Added a shared handle-level public portfolio access resolver in `src/lib/portfolio/public-projection.ts`.
- Switched the individual public page, public summary route, public export route, and `fetchPublicTrustExportDataByHandle` to fail closed unless the shared resolver returns `accessible`.
- Added focused regression coverage for page, summary, export, cross-surface consistency, and the hardened export-data helper.

Why:

- The public portfolio page already blocked unavailable portfolios, but the recruiter summary and PDF export routes still leaked `exportData` for unavailable handles.
- This block closes a P0 privacy bypass so all public portfolio surfaces now obey the same availability gate.

How to verify:

- `npm run test -- tests/api/public-portfolio-summary-route.test.ts tests/api/public-portfolio-export-route.test.ts tests/ui/public-portfolio-page.test.tsx tests/ui/public-portfolio-access-consistency.test.tsx tests/lib/public-trust-export-data.test.ts`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- `npm run build` still fails in an existing unrelated path while collecting page data for `/api/verification/linkedin/initiate` because Playwright cannot resolve `./screenshotter`.
- Organization public export routes were intentionally left out of scope for this block.
