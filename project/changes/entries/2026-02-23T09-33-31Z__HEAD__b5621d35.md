# Project Change Entry

- Date/time (UTC): 2026-02-23T09:33:31Z
- Branch: HEAD
- Base commit: b5621d35

What changed:

- Added first-10-minute activation instrumentation events:
  - onboarding completion: `individual_onboarding_completed`, `organization_onboarding_completed`
  - individual actions: `portfolio_share_link_copied`, `portfolio_pdf_export_succeeded`
  - company actions: `assignment_template_applied`, `assignment_publish_succeeded`
- Wired event emission to the relevant product actions:
  - onboarding actions, portfolio share copy, portfolio PDF export route, assignment template apply, and assignment publish route.
- Added first-10-minute KPI calculation in `src/lib/analytics/metrics.ts`:
  - individual success = share-link copy + PDF export within 10 minutes of individual onboarding completion
  - company success = template apply + publish success within 10 minutes of organization onboarding completion
  - boundary handling is inclusive (`<= 10 minutes`).
- Exposed new KPI outputs in `/api/admin/metrics/rollout` response:
  - `individualFirst10MinuteActivationRate`
  - `companyFirst10MinuteActivationRate`
  - detailed `firstTenMinuteActivation` block with numerators/denominators.

Why:

- Implement the approved “First 10 Minutes Success Definition” as measurable analytics and admin-visible KPIs.

How to verify:

- `npm run typecheck` -> PASS
- `npm run lint` -> PASS
- `npm run test -- tests/api/analytics-track-route.test.ts tests/api/assignment-publish.test.ts tests/api/assignments-publish-route.test.ts tests/portfolio-pdf.test.ts` -> PASS
- `npm run build` -> PASS (with expected missing-env warnings for `DATABASE_URL` and other local env vars)
- `npm run docs:freshness` -> PASS with warnings (non-blocking warning mode)

Open risks / TODO:

- Share-link tracking currently goes through `/api/analytics/track`, so analytics-consent gating can exclude some users from numerator counts.
- Existing historical onboarding users will not be included until they emit new onboarding-completion events under this instrumentation.
