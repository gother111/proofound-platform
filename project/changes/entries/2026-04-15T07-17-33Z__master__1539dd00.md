# Project Change Entry

- Date/time (UTC): 2026-04-15T07:17:33Z
- Branch: master
- Base commit: 1539dd00
  What changed:
- Added an explicit repo-only launch smoke selection path via `getLaunchSmokeMatrix('repo')`.
- Updated `scripts/launch-smoke-runner.ts` to accept `--scope repo|full` and emit repo-scoped smoke output when requested.
- Updated `src/lib/launch/repo-ready-validation.ts` to run repo smoke into a separate `repo-ready-launch-smoke-report.json` artifact instead of the canonical full-launch smoke artifact.
- Added contract coverage in `tests/lib/launch-hardening-contract.test.ts` so repo-ready smoke stays distinct from the strict full-launch matrix.

Why:

- Repo-ready validation was failing on timeout because it reused the full strict org corridor smoke, which is intentionally a multi-minute full-launch proof.
- Repo-only readiness needed an explicit smoke pack that preserves full-launch strictness without blocking local engineering truth on the slow end-to-end org corridor.

How to verify:

- Run `npm run test -- tests/lib/launch-hardening-contract.test.ts tests/lib/launch-synthetic-monitors.test.ts src/lib/launch/__tests__/final-launch-checklist.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts tests/actions/onboarding-private-context-scaffolding.test.ts`
- Run `npm run typecheck`
- Run `npm run lint`
- Run `npm run docs:freshness`
- Run `npm run launch:checklist`
- Confirm `.artifacts/launch-validation-2026-04-15/final-launch-checklist-status.json` reports `scope: "repo"` and `verdict: "READY"` with no `trueBlockers`.

Open risks / TODO:

- Full-launch smoke still includes `full_org_corridor_review_to_engagement_verification` and remains the stricter contract for final go/no-go.
