# Project Change Entry

- Date/time (UTC): 2026-05-21T18:12:26Z
- Branch: master
- Base commit: ef4c9e991
  What changed:
- Documented that protected launch-status and go/no-go command examples may use either `CRON_SECRET=<secret>` or `INTERNAL_API_SECRET=<secret>` across active launch/readiness docs.
- Added regression coverage in `tests/scripts/launch-gate-config.test.ts` for the internal launch secret fallback wording.

Why:

- The launch gate scripts now prefer `INTERNAL_API_SECRET` and fall back to `CRON_SECRET`; active operator docs needed to reflect that current server-only auth behavior.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `git diff --check`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Production-candidate launch gates still require real target execution with current secrets and fresh restore evidence before any final go/no-go claim.
