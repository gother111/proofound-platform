# Project Change Entry

- Date/time (UTC): 2026-03-01T21:07:40Z
- Branch: master
- Base commit: 5b21dd79

What changed:

- Added `src/lib/wellbeing/client.ts` with CSRF-safe client helpers that route mutating Zen Hub calls through `apiFetch`:
  - `setWellbeingOptIn(...)`
  - `saveWorkSchedule(...)`
  - `saveSelfAssessment(...)`
- Updated Zen Hub opt-in page flow to use helper-based POST and `apiFetch` GET for `/api/wellbeing/opt-in`:
  - `src/app/app/i/zen/page.tsx`
- Updated work schedule editor to use `apiFetch` GET and helper-based POST for `/api/wellbeing/work-schedule`:
  - `src/components/wellbeing/WorkScheduleEditor.tsx`
- Updated self-assessment dialog to use helper-based POST for `/api/wellbeing/self-assessment`:
  - `src/components/wellbeing/SelfAssessmentDialog.tsx`
- Added focused unit tests that assert helper endpoint/method/payload wiring:
  - `tests/lib/wellbeing-client.test.ts`

Why:

- Zen Hub mutating calls were inconsistent with repository CSRF policy. Some critical actions used plain `fetch` and could fail on missing CSRF token headers, blocking enablement/usability.
- Standardizing these flows on `apiFetch` removes that mismatch without changing server route contracts.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/lib/wellbeing-client.test.ts` (PASS)
- `npm run lint` (FAIL, unrelated pre-existing merge conflict markers in CV import files)
- `npm run typecheck` (FAIL, unrelated pre-existing merge conflict markers in CV import files)
- `rg -n "fetch\\('/api/wellbeing/(opt-in|work-schedule|self-assessment)'" src` (no matches)

Open risks / TODO:

- Global repo lint/typecheck remain blocked by unrelated pre-existing conflict markers in non-ZenHub files.
- No dedicated authenticated Zen Hub E2E was added in this ticket; this change relies on deterministic unit coverage plus callsite audit.
