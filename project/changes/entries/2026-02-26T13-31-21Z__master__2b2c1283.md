# Project Change Entry

- Date/time (UTC): 2026-02-26T13:31:21Z
- Branch: master
- Base commit: 2b2c1283

What changed:

- Added shared public metadata helper (`src/lib/seo/public-metadata.ts`) and applied it to marketing/legal pages (`/about`, `/careers`, `/contact`, `/manifesto`, `/cookies`, `/cookies/settings`, `/terms`, `/privacy`, `/support`) for canonical, OG, Twitter, and longer title/description coverage.
- Added new `/support` route and linked it from footer and marketing/legal trust navigation.
- Updated sitemap to include `/support` and `/cookies/settings`; added `public/favicon.svg` and wired favicon/icon metadata in root layout.
- Fixed landing accessibility blockers: removed `aria-hidden` from interactive regions and corrected footer heading levels from `h4` to `h3`.
- Replaced generic cookie-settings back link text with descriptive text and expanded legal/info page content for thin-content remediation.
- Optimized above-the-fold media handling (`hero-shape` quality/sizes, eager logo loading) and ensured local render reflects non-lazy logo usage.

Why:

- Implement the production audit remediation plan with focus on P0 issues first (broken internal support route, accessibility blockers, and security triage evidence path), then SEO/legal/performance baseline improvements.

How to verify:

- `npm run lint` (PASS with warnings only for `<img>` usage in landing components).
- `npm run typecheck` (PASS).
- `npm run test` (PASS, 141 files and 585 tests).
- `npm run build` (PASS).
- `npm run test:e2e:landing` (PASS, 14 tests).
- `npm run test:e2e:landing:visual` (PASS, 1 test).
- `npm run docs:freshness` (PASS with non-blocking existing warnings).
- Local squirrel audits against `http://localhost:3000`: surface `9c84c18d`, full `47f3443d`, diff shows no regressions (`added=0 removed=0 changed=0`).
- Production squirrel audits against `https://proofound.io`: surface `6d424a12`, full `74269d27`, diff `--diff 6d424a12` reports no score change (still `59/F`) before deployment.

Open risks / TODO:

- Production remains on pre-remediation build (score `59/F`, unresolved `/support` 404 and legacy metadata/accessibility findings) until this branch is deployed.
- Production still needs deploy before production-domain audit acceptance can be confirmed.
- `security/leaked-secrets` remains scanner-detected in local chunk `9764-30c47b113ba419f7.js`; manual inspection indicates false positives from generic text (`password=%filtered%`, `visibilitychange`) but production bundle must be revalidated after deploy.
- Local audits include expected environment artifacts (`security/https`, `sitemap-domain`, `sitemap-coverage`) because local host is `http://localhost:3000` while canonical/sitemap target `https://proofound.io`.
- `docs:freshness` reports existing repo warnings unrelated to this remediation (`PRD_for_a_web_platform_MVP.master-latest.md`, `agent/runbooks/expertise-taxonomy-recovery.md`, `docs/mobile/IOS_PARITY_MATRIX.md`, legacy domain string in `docs/ENV_VARIABLES.md`).
