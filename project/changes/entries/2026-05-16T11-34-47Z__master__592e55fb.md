# Project Change Entry

- Date/time (UTC): 2026-05-16T11:34:47Z
- Branch: master
- Base commit: 592e55fb

What changed:

- Moved the pure local AI redaction test from `tests/privacy/` to `tests/lib/` so the AI launch gate can run it under the default Vitest config while `test:privacy` still covers it.
- Expanded `npm run test:launch:ai` again so it now covers local NLP extraction, excluded Gemini CV-import helper guardrails, Start-from-CV first-proof onboarding visibility, archived CV-import spend analytics, AI redaction, and the archived admin AI spend page through the archived config.
- Added a launch-command drift check in `tests/lib/ai-launch-guardrails.test.ts` so default-excluded admin AI and edge AI guardrail suites cannot be silently dropped from the AI gate.
- Updated `audit/ai-features-verification-2026-05-16.md` with the stronger composite gate evidence.

Why:

- A fresh AI surface inventory found relevant AI/privacy guardrail tests that were outside `test:launch:ai`, including one suite that the default Vitest config intentionally excludes and one local redaction suite that was living under the privacy-only excluded folder.

How to verify:

- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test:launch:ai`

Open risks / TODO:

- Live Gemini provider behavior remains unverified until server-only provider credentials and hard-cap env are available.
