# Project Change Entry

- Date/time (UTC): 2026-02-25T10:45:29Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Implemented PRO-42 education/volunteering skill input redesign from free-text to picker-only based on existing Expertise Atlas skills.
- Added reusable profile skill picker and legacy skill normalization utilities:
  - `src/components/profile/forms/ProfileSkillPicker.tsx`
  - `src/components/profile/forms/skill-selection-utils.ts`
- Added full edit workflow for education and volunteering:
  - Edit buttons in `LearningTab` and `ServiceTab`
  - Edit target state and open/close routing in `EditableProfileView`
  - Conditional add vs update dispatch in `ProfileDialogs`
- Added server update actions and optimistic hook handlers:
  - `updateEducation`, `updateVolunteering` in `src/actions/profile.ts`
  - `updateEducation`, `updateVolunteering` in `src/hooks/useProfileData.ts`
- Added/updated test coverage:
  - `tests/ui/education-form-skill-picker.test.tsx`
  - `tests/ui/volunteer-form-skill-picker.test.tsx`
  - `tests/ui/profile-dialogs-edit-routing.test.tsx`
  - `tests/actions/profile.test.ts`
- Updated Linear issue `PRO-42` status to **In Progress** and posted implementation + verification comment.

Why:

- Ticket requirement: skills in education and volunteering should be selected from user L-skillset (Expertise Atlas), not entered as arbitrary text.
- Ticket scope included editing workflow, not only add/delete.
- Existing legacy text data needed deterministic migration behavior during edits (drop unmatched values).

How to verify:

- `npm run lint` (PASS)
- `npm run typecheck` (PASS)
- `npm run test -- tests/actions/profile.test.ts tests/ui/education-form-skill-picker.test.tsx tests/ui/volunteer-form-skill-picker.test.tsx tests/ui/profile-dialogs-edit-routing.test.tsx` (PASS)
- `npm run test` (FAIL, unrelated existing failure in `tests/ui/public-org-portfolio-page.test.tsx` about Next `cookies` request scope)

Open risks / TODO:

- Full suite still has unrelated pre-existing failure in public org portfolio page tests.
- Form behavior depends on available skills being loaded in profile payload; if skill payload is missing, save is intentionally blocked.
