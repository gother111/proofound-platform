# Project Change Entry

- Date/time (UTC): 2026-05-21T18:04:28Z
- Branch: master
- Base commit: 4e6f291dd
  What changed:
- Aligned launch CLI probes and final validation helpers with the server-only internal auth contract.
- `launch:status`, `go:no-go`, full launch validation fetches, final checklist fetches, and `launch:validate` now accept `INTERNAL_API_SECRET` first, then `CRON_SECRET`.
- Added tests guarding the internal-secret fallback across scripts and final launch validation.

Why:

- The protected monitoring routes accept server-only internal secrets, but CLI launch gates were still hardcoded to `CRON_SECRET`, creating a docs/runtime mismatch for production-candidate launch evidence.

How to verify:

- `npm run test -- src/lib/launch/__tests__/final-launch-validation-runner.test.ts src/lib/launch/__tests__/trusted-internal-auth.test.ts src/lib/launch/__tests__/full-launch-validation.test.ts src/lib/launch/__tests__/final-launch-checklist.test.ts tests/scripts/launch-gate-config.test.ts tests/api/cron-auth.test.ts`
- `npm run docs:freshness`
- `npm run typecheck`
- `npm run lint`
- `git diff --check`

Open risks / TODO:

- Live launch readiness still requires a trusted production-candidate target, a configured server-only internal secret, fresh restore evidence, and final protected gate output.
