# Project Change Entry

- Date/time (UTC): 2026-05-21T07:47:42Z
- Branch: master
- Base commit: cb284e2e
  What changed:
- Replaced verifier contradiction reconciliation raw console diagnostics with structured `verification.contradiction.*` log events.
- Added focused failure-path coverage for admin-client fallback/catch-all failures and canonical verification record scan failures.
- Extended runtime debug-output guardrails so the old contradiction console strings cannot return.

Why:

- Keeps verification reconciliation failures observable through the privacy-safe structured logger while preserving the helper's best-effort empty-result behavior.
- Prevents raw verifier emails from leaking through contradiction failure diagnostics.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/verification-contradiction-logging.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while the suites pass.
- This slice is scoped to structured diagnostics and does not change contradiction reconciliation rules.
