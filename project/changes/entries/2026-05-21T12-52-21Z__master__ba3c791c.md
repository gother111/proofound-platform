# Project Change Entry

- Date/time (UTC): 2026-05-21T12:52:21Z
- Branch: master
- Base commit: ba3c791c
  What changed:
- Reused the shared `HeroResumeStack` visual for the mobile landing hero story and removed the duplicate mobile-only proof-dossier mock.
- Added the small `Results note` layer to the shared resume-stack visual.
- Refreshed the mobile landing visual baseline and saved Browser screenshot evidence for mobile and desktop landing views.
- Updated the active MVP surface sweep artifact with the landing verification record.

Why:

- The public landing hero should make the proof-first object concrete across desktop and mobile instead of using separate visual metaphors.
- Reusing the same resume-stack visual reduces duplicate UI and keeps the first viewport aligned with the current proof-first landing story.

How to verify:

- `npm test -- --run tests/ui/landing-copy-guardrails.test.tsx tests/ui/landing-hero-headline-semantics.test.tsx tests/scripts/launch-gate-config.test.ts`
- `node ./scripts/playwright-node24.mjs test e2e/landing-visual.spec.ts --project=chromium --reporter=line`
- Browser check at `http://localhost:33180/` on `390x844` and `1440x1000`: H1 and hero images visible, no horizontal overflow, no console warnings/errors.
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- src/components/landing/sections/ScrollytellingSection.tsx e2e/landing-visual.spec.ts-snapshots/landing-home-mobile-390x844-linux-chromium.png`

Open risks / TODO:

- The first visual-test attempt failed because sandbox/server ownership conflicted with a manual dev server; after stopping that server, the configured Playwright visual run passed.
- Broader raw diagnostics remain in unrelated active action, lib, and client surfaces and need separate scoped sweeps.
