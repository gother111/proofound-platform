# Project Change Entry

- Date/time (UTC): 2026-03-01T21:05:30Z
- Branch: master
- Base commit: b26b2daa
  What changed:
- Added shared industry taxonomy module with exact 19 labels, stable keys, alias mapping, and canonicalization helpers.
- Added migration `20260301173000_standardize_industry_taxonomy` with new canonical/legacy columns and backfill logic for organizations, experiences, and matching profiles.
- Updated Drizzle schema and profile/auth/org data plumbing to include canonical industry fields.
- Replaced organization and experience industry free-text handling with shared taxonomy-backed key/label persistence.
- Added preferred/avoid industry multi-select wiring in matching setup UI and focus section.
- Updated matching profile APIs (canonical + legacy adapters) to dual-write/read canonical preferred/avoid fields while preserving `desiredIndustries` compatibility.
- Updated matching focus scoring to use canonical industry keys and added hard exclusion for avoided industries in main and near-match routes.
- Updated tests for organization API/editor, experience form, matching profile routes, matching setup focus payloads, and focus scoring logic.

Why:

- Standardize all Industry inputs to one shared taxonomy and support deterministic matching behavior with stable keys.
- Preserve backward compatibility for legacy clients/routes while introducing canonical storage for new features.
- Implement avoid-industry hard exclusion and migration fallback semantics required by PRO-116.

How to verify:

- `npm run typecheck`
- `npm run lint`
- `npm run test -- tests/api/organizations-route.test.ts tests/api/core-matching-profile-route.test.ts tests/api/matching-profile-compat-route.test.ts tests/ui/organization-basic-info-editor.test.tsx tests/ui/experience-form.test.tsx tests/ui/matching-profile-setup-focus.test.tsx src/lib/__tests__/matching-focus.test.ts`
- `npm run build`
- `npm run db:migrate`
- `npm run test` (currently has pre-existing failures outside this scope in video integration/topbar suites)

Open risks / TODO:

- Full `npm run test` currently fails in existing unrelated suites:
  - `tests/api/video-integrations-auth-route.test.ts`
  - `tests/api/zoom-integration-oauth-redirects.test.ts`
  - `tests/ui/topbar-customize-visibility.test.tsx`
  - `tests/ui/video-provider-selector-connect-route.test.tsx`
- Next step: confirm these failures are baseline and not regressions before merge/release.
