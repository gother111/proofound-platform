# Project Change Entry

- Date/time (UTC): 2026-05-21T12:58:06Z
- Branch: master
- Base commit: 727d2a0c
  What changed:
- Replaced raw auth-action console diagnostics with structured `auth.*` server logs in `src/actions/auth.ts`.
- Added focused auth tests and runtime debug-output guardrails for the structured logging events.
- Updated the MVP surface sweep artifact with the auth diagnostics checkpoint.

Why:

- Active auth, signup, password reset, verification resend, fallback email, consent, and OAuth failure paths should stay observable without ad hoc console strings or accidental form-payload logging.

How to verify:

- `rg "console\\.(error|warn|log)" src/actions/auth.ts -n`
- `npm test -- --run tests/actions/auth.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still emits the known sandbox Vite websocket `EPERM` warning, but the focused suites pass.
