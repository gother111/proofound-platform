# Project Change Entry

- Date/time (UTC): 2026-05-21T14:14:54Z
- Branch: master
- Base commit: 532a7add
  What changed:
- Converted `apiFetch` CSRF token initialization diagnostics from raw console output to local `proofound:api-diagnostic` browser events.
- Added focused API-fetch coverage and a runtime debug-output guardrail for the client CSRF diagnostic path.
- Appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc diagnostics from shared launch-relevant client infrastructure while preserving fail-closed CSRF behavior.

How to verify:

- `npm test -- --run src/lib/__tests__/api-fetch.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" src/lib/api/fetch.ts tests/lib/runtime-debug-output-guardrails.test.ts src/lib/__tests__/api-fetch.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
