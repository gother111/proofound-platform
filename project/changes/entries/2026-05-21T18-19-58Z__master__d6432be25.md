# Project Change Entry

- Date/time (UTC): 2026-05-21T18:19:58Z
- Branch: master
- Base commit: d6432be25
  What changed:
- Aligned the restore drill and master launch checklist with the current server-only launch secret contract.
- Documented `INTERNAL_API_SECRET=<secret>` as an accepted go/no-go replacement for `CRON_SECRET=<secret>` in the restore drill.
- Updated final `launch:validate` examples in the master launch checklist to include `INTERNAL_API_SECRET=<secret>` because production-candidate validation includes protected gates.
- Added launch-gate regression assertions for these docs.

Why:

- Final launch documentation should not imply that protected launch validation can run against a production-candidate target without a server-only launch secret.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `git diff --check`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- This does not produce live production-candidate evidence; final readiness still requires fresh restore evidence and target-specific go/no-go output.
