# Project Change Entry

- Date/time (UTC): 2026-03-08T21:01:33Z
- Branch: master
- Base commit: 12b353de
  What changed:
- Added canonical workflow persistence for assignment, intro, interview, decision, verification, consent obligations, and workflow async jobs in `src/db/schema.ts`.
- Added migration `src/db/migrations/20260308213000_add_workflow_state_machines_and_async_jobs.sql` to create new workflow tables, extend existing lifecycle tables, migrate assignment `paused` to `hold`, and backfill intro, decision, consent, and transition history records.
- Added `src/lib/workflow/contracts.ts`, `src/lib/workflow/service.ts`, `src/lib/workflow/queue.ts`, and `src/lib/workflow/processor.ts` to centralize transition guards, canonical state writes, append-only audit logs, and async job processing.
- Rewired matching, decision, interview, verification, and consent APIs to write canonical workflow state and expose workflow metadata in responses.
- Added defensive UUID guards in verification routes so canonical writes do not turn legacy or partial verification requests into 500s.
- Added cron entrypoint `src/app/api/cron/workflow-jobs/route.ts` and interview no-show route `src/app/api/interviews/no-show/route.ts`.

Why:

- Block 4 required one durable workflow contract across schema, APIs, reminders, analytics, and UI instead of split lifecycle ownership and ad hoc status inference.
- The product needs explicit expiry, hold, withdrawal, no-show, duplicate-candidate, and consent-expiry behavior with durable audit trails and operable async jobs.

How to verify:

- `npm run db:migrate`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `npm run test -- tests/api/match-interest-route.test.ts tests/api/interviews-cancel-route.test.ts tests/api/interviews-schedule-route.test.ts tests/api/verification-status-route.test.ts`
- `npm run test -- tests/api/expertise-verification-respond-route.test.ts tests/api/work-email-verification-verify-route.test.ts`
- Optionally call `POST /api/cron/workflow-jobs` with `CRON_SECRET` to process queued workflow jobs.

Open risks / TODO:

- Full `npm run test` still has unrelated failures outside Block 4, including expertise verification, profile snippet, work-email verification, onboarding, CVJD auto-suggest, and public portfolio UI tests.
- `workflow_fanout`, `proof_freshness_nudge`, and `portfolio_index_refresh` currently complete as observable MVP no-op handlers; downstream delivery behavior can be expanded later without changing the job contract.
- `npm run db:drift-check` was not reliable in this session because the migration file started untracked and the script compares tracked/staged drift only.
