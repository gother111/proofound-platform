# Project Change Entry

- Date/time (UTC): 2026-02-25T11:01:24Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Scoped the global top-right `Customize` button and `CustomizeModal` in `src/components/app/TopBar.tsx` to dashboard routes only (`/home` in pathname).
- Added route-change safety in `TopBar`: if the modal is open and user navigates away from dashboard, the modal is closed.
- Added targeted UI regression coverage in `tests/ui/topbar-customize-visibility.test.tsx`:
  - visible on `/app/i/home`
  - hidden on `/app/i/profile`
  - visible on `/app/o/acme/home`
  - hidden on `/app/o/acme/settings`
  - modal state reset across dashboard -> non-dashboard -> dashboard navigation.

Why:

- Implements Linear issue `PRO-49`: the top-right `Customize` action should only be present on dashboard tabs and not across unrelated pages.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/topbar-customize-visibility.test.tsx` (pass)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (pass)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (pass)

Open risks / TODO:

- Route detection currently keys on `/home`; if a future dashboard alias route is added, `isDashboardTab` logic in `TopBar` must be updated.
