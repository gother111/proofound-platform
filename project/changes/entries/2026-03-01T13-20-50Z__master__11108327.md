# Project Change Entry

- Date/time (UTC): 2026-03-01T13:20:50Z
- Branch: master
- Base commit: 11108327
  What changed:
- Implemented platform-wide loading parity sweep for core authenticated routes under `src/app/app/i/*` and `src/app/app/o/[slug]/*` to replace generic text/gray-block loaders with shared structural skeleton primitives.
- Added `src/components/skeletons/CoreLoadingPrimitives.tsx` and `src/components/skeletons/OrganizationHomeSkeleton.tsx`, then migrated dashboard/home, projects, opportunities, interviews, notifications, matching, and org team/assignments loaders to use them.
- Refactored `src/components/dashboard/WidgetGridSkeleton.tsx` to support explicit variants (`individualDashboard`, `organizationDashboard`) and switched dashboard loading branches/fallbacks to variant-aware skeletons.
- Fixed dashboard "Take a Tour" target mapping in `src/app/app/i/home/DashboardClient.tsx` to use current widget IDs, filter missing targets, and fallback safely to `main-content`.
- Added regression coverage in `tests/ui/dashboard-client.test.tsx` and `tests/ui/widget-grid-skeleton.test.tsx` for tour-target behavior and skeleton variant rendering.

Why:

- PRO-120 scope was expanded from dashboard-only to core app routes; users observed visible layout/style jumps during loading transitions.
- Matching loading structures to final cards/tiles reduces perceived instability and prevents empty-tile flashes.

How to verify:

- `export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 20.20.0`
- `npm run test -- tests/ui/dashboard-client.test.tsx tests/ui/draggable-dashboard-while-away-visibility.test.tsx tests/ui/org-dashboard-client.test.tsx tests/ui/organization-interviews-page-actions.test.tsx tests/ui/widget-grid-skeleton.test.tsx`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Existing lint warnings in landing components (`<img>` usage in `ProofoundLanding` and `FooterSection`) remain unrelated and unchanged.
- Visual QA should still confirm all updated loaders feel natural across breakpoint transitions, especially on org matching/assignments dense grids.
