# Project Change Entry

- Date/time (UTC): 2026-02-26T07:18:10Z
- Branch: master
- Base commit: 66677bd9
  What changed:
- Fixed PRO-70 by adding schema-compat fallback in `src/app/api/verification/status/route.ts` when `work_email_verified_at` / `work_email_reverify_due_at` columns are missing.
- Fixed PRO-71 by adding schema-compat fallback in `src/actions/profile.ts` for values/causes saves when `mission_links` / `vision_links` are unavailable.
- Updated `ValuesEditor` and `CausesEditor` to await async saves and keep dialogs open on save failure, with pending-state UX.
- Fixed PRO-72 by hiding empty dashboard cards in `src/app/app/i/expertise/ExpertiseAtlasClient.tsx` and removing empty L1 rows in `CoverageHeatmap`.
- Added regression tests:
- `tests/api/verification-status-route.test.ts` (legacy-column fallback)
- `tests/actions/profile.test.ts` (values-only fallback)
- `tests/ui/expertise-atlas-widget-visibility.test.tsx`
- `tests/ui/coverage-heatmap-visibility.test.tsx`
- Added shared helper `src/lib/db/schemaCompatibility.ts`.

Why:

- Production environments can temporarily run older schemas; these fallbacks prevent hard failures and data-loss symptoms while migrations catch up.
- Atlas dashboard should show only meaningful sections, not empty placeholders.

How to verify:

- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run lint`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run typecheck`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run test -- tests/api/verification-status-route.test.ts tests/actions/profile.test.ts`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run test -- tests/ui/expertise-atlas-widget-visibility.test.tsx tests/ui/coverage-heatmap-visibility.test.tsx`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run test`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run build`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run db:backup:checkpoint`
- `source ~/.nvm/nvm.sh && nvm use >/dev/null && npm run db:migrate`

Open risks / TODO:

- `npm run db:audit:migrations` currently reports ledger drift (missing local files / unapplied local files) and should be reconciled separately.
