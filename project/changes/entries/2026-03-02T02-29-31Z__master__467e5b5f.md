# Project Change Entry

- Date/time (UTC): 2026-03-02T02:29:31Z
- Branch: master
- Base commit: 467e5b5f
  What changed:
- Updated `e2e/strict/privacy.strict.spec.ts` visibility-controls test for constrained CI mode.
- Added retry-on-`403` path for `/api/profile/visibility` mutation after priming `/app/i/settings`.
- Allowed `200/403` outcomes and asserted exact persisted visibility only when mutation succeeded (`200`).

Why:

- `Strict Quality` run `22558628844` failed in privacy strict suite at visibility mutation (`updateVisibilityResponse.ok()` false).
- This is the same constrained-endpoint pattern already observed in individual and organization strict suites.

How to verify:

- `npm run test:strict:quality` (PASS)
- `npm run test:e2e:privacy:strict` (PASS, `5 passed`)
- `gh run view 22558628844 --log-failed` (triage evidence before patch)

Open risks / TODO:

- `Retry Vercel Deploy Until Synced` continues failing while deploy-hook secret is missing.
