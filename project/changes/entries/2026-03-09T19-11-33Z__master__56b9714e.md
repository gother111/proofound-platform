# Project Change Entry

- Date/time (UTC): 2026-03-09T19:11:33Z
- Branch: master
- Base commit: 56b9714e

What changed:

- Added sitemap schema-compatibility fallback logic in `src/app/sitemap.ts` so build-time portfolio queries return empty dynamic entries instead of failing when `profiles` or `organizations` schema is behind.
- Added missing-relation detection and a combined schema-compatibility helper in `src/lib/db/schemaCompatibility.ts`.
- Added focused coverage in `src/lib/__tests__/schema-compatibility.test.ts`.

Why:

- Vercel production build for commit `a251ea3` failed while prerendering `/sitemap.xml` because the connected database reported `relation "organizations" does not exist`.
- The sitemap should degrade to static URLs during schema lag instead of aborting the whole deployment.

How to verify:

- `export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 20.20.0 >/dev/null && node -e "const {spawnSync}=require('node:child_process'); const fs=require('node:fs'); const {parse}=require('dotenv'); const env={...process.env,...parse(fs.readFileSync('.env.local'))}; const r=spawnSync('npx',['vitest','run','src/lib/__tests__/schema-compatibility.test.ts'],{stdio:'inherit',env}); process.exit(r.status ?? 1);"`
- `export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 20.20.0 >/dev/null && node -e "const {spawnSync}=require('node:child_process'); const fs=require('node:fs'); const {parse}=require('dotenv'); const env={...process.env,...parse(fs.readFileSync('.env.local'))}; const r=spawnSync('npm',['run','build'],{stdio:'inherit',env}); process.exit(r.status ?? 1);"`

Open risks / TODO:

- When schema lag occurs, `/sitemap.xml` will omit public portfolio URLs until the target database has the required tables and columns.
