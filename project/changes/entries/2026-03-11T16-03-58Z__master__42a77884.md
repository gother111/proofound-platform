# Project Change Entry

- Date/time (UTC): 2026-03-11T16:03:58Z
- Branch: master
- Base commit: 42a77884
  What changed:
- Extracted duplicated purpose-link helper logic from `MissionEditor` and `VisionEditor` into `src/components/profile/purpose-links-utils.ts`.
- Updated both editors to import `normalizeLabels`, `normalizeLinks`, and `toggleItem` from the new shared utility.

Why:

- Remove local duplication while preserving behavior, making future changes to purpose-link normalization and chip toggling consistent.

How to verify:

- `npm run lint` (passes with existing unrelated warnings in landing image usage)
- `npm run typecheck` (pass)
- `npm run test` (fails in unrelated baseline tests and sandbox IPC restrictions; no failures linked to mission/vision editor refactor)

Open risks / TODO:

- Full test suite currently has existing failures unrelated to this change (`python-cv-proxy`, cron reminder route, and tsx IPC permissions in this environment).
