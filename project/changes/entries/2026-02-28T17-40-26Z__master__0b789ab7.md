# Project Change Entry

- Date/time (UTC): 2026-02-28T17:40:26Z
- Branch: master
- Base commit: 0b789ab7

What changed:

- Removed the Languages step from matching setup in `src/components/matching/MatchingProfileSetup.tsx` including tab UI, related state/actions, review row, and submission payload field.
- Added shared Atlas language resolution utility in `src/lib/core/matching/language-resolution.ts` for:
- CEFR mapping (`0..5 -> A1..C2`)
- Atlas natural-language skill extraction from taxonomy metadata
- Legacy `matching_profiles.languages` parsing
- Atlas-first with legacy fallback level resolution
- Updated language scoring paths to use the shared resolver:
- `src/app/api/core/matching/profile/route.ts`
- `src/app/api/core/matching/near-matches/route.ts`
- `src/lib/core/matching/assignmentMatcher.ts`
- `src/lib/matching/generate-matches-for-assignment.ts`
- Updated UI test `tests/ui/matching-profile-setup-focus.test.tsx` for the removed Languages step and payload assertions.
- Added unit tests in `src/lib/core/matching/__tests__/language-resolution.test.ts` for Atlas extraction, CEFR conversion, fallback, and false-positive prevention.

Why:

- PRO-110 requires removing redundant language collection from the setup wizard while preserving assignment language matching via Atlas skill data.

How to verify:

- `npm run test -- tests/ui/matching-profile-setup-focus.test.tsx src/lib/core/matching/__tests__/language-resolution.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run build`

Open risks / TODO:

- Full `npm run test` fails in pre-existing unrelated suites:
- `tests/actions/create-impact-story.test.ts`
- `tests/actions/profile.test.ts`
- Error signatures: `db.select is not a function` and `rows.filter is not a function` in profile impact-verification helpers.
