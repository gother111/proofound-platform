# Project Change Entry

- Date/time (UTC): 2026-03-30T11:06:02Z
- Branch: master
- Base commit: 8d7193d3
  What changed:
- Hard-disabled weekly digest delivery in `src/lib/notifications/weekly-digest.ts` so the feature now returns unavailable regardless of `ENABLE_WEEKLY_DIGEST`.
- Updated weekly digest cron tests to assert both cron entry points always return `skipped` and never invoke the digest processor.
- Updated `README.md`, `docs/CRON_SETUP.md`, and `docs/API_REFERENCE.md` so repo docs no longer advertise active weekly digest delivery.

Why:

- Users were still receiving the weekly digest email even though a prior change entry documented it as disabled by default behind an env gate.
- A hard code-level shutdown is the safest temporary stop because it prevents both email sends and in-app weekly digest notification creation without changing cron wiring or notification preference schema.

How to verify:

- `npm run test -- src/app/api/cron/decision-reminders/__tests__/route.test.ts src/app/api/cron/weekly-digest/__tests__/route.test.ts src/lib/notifications/__tests__/weekly-digest.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Production configuration may still contain `ENABLE_WEEKLY_DIGEST=true`, but this code path now ignores it; if the feature is restored later, the product and ops teams should reconcile env state before re-enabling.
- Notification settings surfaces and stored weekly digest preferences still exist for future restoration and are intentionally unchanged in this temporary shutdown.
