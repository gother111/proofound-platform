# Project Change Entry

- Date/time (UTC): 2026-02-25T09:37:59Z
- Branch: HEAD
- Base commit: 70020bd0
  What changed:
- Added DB-backed work experience timeline support for PRO-40:
- Added migration `src/db/migrations/20260225104500_add_experience_timeline_dates.sql` to add `experiences.start_date`, `experiences.end_date`, backfill common legacy `duration` formats, and enforce date-order constraint.
- Updated `src/db/schema.ts` and `src/types/profile.ts` to include structured experience date fields.
- Added shared timeline utility `src/lib/profile/experience-timeline.ts` for month input conversion, canonical duration formatting, and legacy parsing.
- Updated profile actions/import/export paths to persist and emit structured timeline dates while preserving compatibility `duration` text.
- Replaced free-text duration in `src/components/profile/forms/ExperienceForm.tsx` with month pickers and validation.
- Added tests:
- `tests/lib/experience-timeline.test.ts`
- `tests/ui/experience-form.test.tsx`

Why:

- PRO-40 requires date selection for work experience timelines instead of free-text duration.
- DB-backed dates are needed for reliable timeline data while keeping existing display behavior stable.

How to verify:

- `npm run db:drift-check`
- `npm run typecheck`
- `npm run lint`
- `npm run test -- tests/lib/experience-timeline.test.ts tests/ui/experience-form.test.tsx`

Open risks / TODO:

- Full `npm run test` currently fails due existing unrelated `tests/ui/public-org-portfolio-page.test.tsx` request-scope cookies issue; this task does not touch that area.
- Follow-up can add structured timeline support to additional downstream consumers if they need direct date fields (current behavior remains duration-compatible).
