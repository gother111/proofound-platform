# Project Change Entry

- Date/time (UTC): 2026-03-02T00:01:19Z
- Branch: master
- Base commit: d2edc229

What changed:

- Hardened strict individual E2E flow for expertise add-skill API timing by retrying the call once when an initial `403` occurs:
  - `e2e/strict/individual.strict.spec.ts`
- Updated strict interview scheduling payload to satisfy current API schema for manual platform:
  - Added `manualMeetingProvider: 'google_meet'` to `/api/interviews/schedule` request in the same strict spec.

Why:

- GitHub `Strict Quality` failed on the individual strict suite due to transient `403` on skill add and schema validation requiring `manualMeetingProvider` for manual interviews.

How to verify:

- `export NVM_DIR="$HOME/.nvm"; . "$NVM_DIR/nvm.sh"; nvm use 20.20.0`
- `npm run test:e2e:individual:strict`
- `npm run lint`
- `npm run typecheck`

Open risks / TODO:

- The optional `Retry Vercel Deploy Until Synced` workflow still fails due missing deploy-hook secret configuration; core deploy path is unaffected.
