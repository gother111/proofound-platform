# Project Change Entry

- Date/time (UTC): 2026-05-21T06:47:37Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Replaced client web-vitals console diagnostics with a local `proofound:web-vital` browser event.
- Replaced client performance tracker catch-path `console.debug` calls with local `proofound:performance-diagnostic` events.
- Added focused coverage for local-only web-vitals instrumentation and client performance failure diagnostics, and extended runtime debug-output guardrails.

Why:

- The launch posture keeps broad analytics transports archived/local-only, so client performance instrumentation should not leave browser console noise or drift back toward archived API posting.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/client-performance-tracker.test.ts tests/lib/web-vitals-local.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This slice intentionally leaves broader pre-existing application error logging for later classification; it only addresses active client performance/web-vitals diagnostics.
