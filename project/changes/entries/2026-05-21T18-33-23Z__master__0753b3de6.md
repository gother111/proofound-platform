# Project Change Entry

- Date/time (UTC): 2026-05-21T18:33:23Z
- Branch: master
- Base commit: 0753b3de6
  What changed:
- Added a `missing_target` launch-smoke artifact evaluation state for artifacts that lack `targetBaseUrl` when target binding is required.
- Hardened `scripts/go-no-go-check.ts` so non-local production-candidate go/no-go runs require target-bound launch smoke artifacts.
- Added focused regression coverage in `tests/lib/launch-smoke-artifact.test.ts` and `tests/scripts/launch-gate-config.test.ts`.

Why:

- Legacy smoke artifacts without a target binding should not prove production-candidate readiness for an arbitrary `BASE_URL`.

How to verify:

- `npm run test -- tests/lib/launch-smoke-artifact.test.ts tests/scripts/launch-gate-config.test.ts`
- `npm run docs:freshness`
- `git diff --check`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Existing historical artifacts without `targetBaseUrl` remain useful as historical/local evidence only; final launch readiness still requires fresh target-specific production-candidate evidence.
