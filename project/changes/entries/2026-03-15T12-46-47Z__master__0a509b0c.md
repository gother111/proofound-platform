# Project Change Entry

- Date/time (UTC): 2026-03-15T12:46:47Z
- Branch: master
- Base commit: 0a509b0c
  What changed:
- Added canonical token lookup helpers for skill and impact verification requests so launch-path resolution now starts and ends in `verification_records`.
- Refactored `/api/verify/[token]` to use those helpers and removed active-route fallback behavior tied to legacy request-table resolution.
- Added a migration to normalize canonical request-transport rows away from legacy `source_*_table` values.
- Added helper-level token-resolution tests and updated the token-route tests to mock the new canonical helper boundary.

Why:

- The MVP launch corridor needs verification request feed and token resolution to stay canonical, claim-scoped, and explicit, without depending on legacy request tables.

How to verify:

- `npm run test -- tests/api/verify-impact-token-route.test.ts tests/lib/canonical-verification-request-token-resolution.test.ts`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Migration behavior against a real local database is UNVERIFIED in this session.
- The repo already has unrelated dirty-worktree changes outside this task; they were not modified or normalized here.
