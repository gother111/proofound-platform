# Project Change Entry

- Date/time (UTC): 2026-05-21T07:13:21Z
- Branch: master
- Base commit: 1545b157
  What changed:
- Replaced organization-side submission-invite raw console diagnostics with structured `log` events for list, create, update/resend, and non-blocking email-send failures.
- Added focused GET/POST/PATCH failure tests in `tests/api/org-candidate-invites-route.test.ts`.
- Extended runtime debug-output guardrails so old org submission-invite console strings cannot return.
- Updated the MVP surface sweep artifact with this slice's evidence.

Why:

- Keeps active organization submission-invite APIs launch-safe: public errors remain generic, email delivery remains non-blocking where intended, and server diagnostics are structured and scrubbed.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/org-candidate-invites-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `listen EPERM 0.0.0.0:24678` warning while the suites pass.
- The larger MVP surface sweep goal remains active; this entry covers only the organization submission-invite diagnostics slice.
