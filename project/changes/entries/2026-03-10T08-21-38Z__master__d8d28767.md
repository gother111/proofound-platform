# Project Change Entry

- Date/time (UTC): 2026-03-10T08:21:38Z
- Branch: master
- Base commit: d8d28767

What changed:

- Rewrote `docs/verification-policy-mvp.md` so its user-facing trust language now matches the canonical PRD badge family and evidence-layer model.
- Replaced older ladder labels such as `Identity checked`, `Workplace-verified`, `Attested`, and `Artifact-verified` with the canonical badge family: `self-claimed`, `peer-attested`, `org-verified`, `human-reviewed`, and `auto-checks-passed`.
- Aligned source rules, freshness windows, display rules, contract verification, edge cases, and event tracking with the canonical PRD's `5.5 Verification, Provenance, Badges, and Proof Ladder (MVP)` section.

Why:

- The canonical PRD and the active verification policy doc had drifted into two different trust vocabularies, which would create ambiguity for product, design, engineering, and QA.
- This reconciliation keeps the PRD and the active policy doc on one trust-language contract without expanding MVP into hard verification or enterprise reviewer ops.

How to verify:

- Read `docs/verification-policy-mvp.md` and confirm it uses the same five badge labels as the canonical PRD.
- Run `rg -n "Identity checked|Workplace-verified|Attested|Artifact-verified" docs/verification-policy-mvp.md` and expect no matches.
- Run `rg -n "self-claimed|peer-attested|org-verified|human-reviewed|auto-checks-passed" docs/verification-policy-mvp.md PRD_for_a_web_platform_MVP.master-latest.md` and confirm both files use the same badge family.
- Run `npm run lint` and confirm only the existing `@next/next/no-img-element` warnings remain in the landing image components.
- Run `npm run typecheck`.
- Run `npm run docs:freshness` and confirm it remains warning-only, with the known orphaned `agent/runbooks/geo-audit.md` plus the existing `Documentation.md` / `metrics.md` governance-pair date mismatches against `project/Documentation.md`.

Open risks / TODO:

- `docs:freshness` still reports governance-pair date mismatch warnings between `project/Documentation.md` and its mirrors. This task did not change those files.
- The repo still contains unrelated untracked sharded log files from earlier work. They were intentionally left untouched.
