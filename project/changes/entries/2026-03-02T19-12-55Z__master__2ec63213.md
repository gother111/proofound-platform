# Project Change Entry

- Date/time (UTC): 2026-03-02T19:12:55Z
- Branch: master
- Base commit: 2ec63213

What changed:

- Updated `Card` to use responsive padding (`p-4` on mobile, `p-6` on sm).
- Updated `Button` component's `sm` size to enforce an ergonomic touch-target of `h-10` on mobile breakpoints.
- Updated `DashboardGrid` backings to use responsive gaps (`gap-4` on mobile, `gap-6` on large bounds).
- Refactored `Header.tsx` to use the V3 `Sheet` component for its mobile menu, transitioning away from the manually animated dropdown.
- Fixed layout squishing and updated button scales in `ProfileActivationCard.tsx`.

Why:

- To align the mobile UX with V3 product characteristics, offering a native, premium feel with larger, standard touch targets and reduced lateral padding to reclaim screen real estate on smaller devices.
- **Mobile Constraints**: Converted to `Sheet` primitive (side: 'bottom') offering native feel, gesture support, and an explicit safe zone instead of a hovering `absolute` box.
- **`src/components/ui/dialog.tsx`** & **`src/components/ui/alert-dialog.tsx`**
  - **Mobile Padding & Width**: Reduced internal padding from hardcoded `p-6` to responsive `p-4 sm:p-6`. Replaced absolute `w-full` behavior with safe margins `w-[calc(100%-2rem)] sm:w-full` on mobile.

- **`src/components/ui/sonner.tsx`** & **`src/components/ui/toaster.tsx`**
  - **Mobile Navigation Safety**: Added explicit mobile-only bottom margins (`mb-16 sm:mb-0`) so global system toasts clear the new sticky `/app` bottom navigation bar.

How to verify:

- Resize the browser or use a mobile emulator. Verify the marketing landing page menu is now a bottom sheet, card padding dynamically adjusts to free up horizontal space, and touch targets such as Action links and `sm` Buttons are reasonably sized (>= ~40-44px). Tests (`typecheck`, `lint`) have passed.

Open risks / TODO:

- Mobile touch-target sweeps may require minor adjustments in complex forms if horizontal space was rigidly coded elsewhere. None found explicitly so far.
