# Project Change Entry

- Date/time (UTC): 2026-03-10T09:05:07Z
- Branch: work
- Base commit: 9cca051
  What changed:
- Patched `src/db/migrations/20260309113000_add_residual_lifecycle_contracts.sql` to enable RLS on the newly added lifecycle/data portability tables.
- Added authenticated self-access policies (`SELECT` and `INSERT` with ownership checks) for `data_portability_exports`, `data_portability_imports`, and `profile_deletion_requests`.
- Left `residual_lifecycle_transitions` with RLS enabled and no user policy so only privileged/service access can interact with it.

Why:

- A security scan reported new public tables were created without RLS/policies, creating an access-control regression in Supabase/PostgREST.

How to verify:

- Run `rg -n "ENABLE ROW LEVEL SECURITY|CREATE POLICY" src/db/migrations/20260309113000_add_residual_lifecycle_contracts.sql`.
- Run `npm run lint`.
- Run `npm run typecheck`.

Open risks / TODO:

- `npm run docs:freshness` reports existing repository warning-mode orphan docs unrelated to this patch.
