# Project Change Entry

- Date/time (UTC): 2026-03-10T17:34:48Z
- Branch: master
- Base commit: 0a22dfb7
  What changed:
- Finished the remaining canonical intro corridor gap by removing auto-conversation opening from candidate and mobile mutual-interest routes.
- Updated the org review `request_intro` path to approve intros canonically from Stage 2 when reciprocal candidate interest exists, unlock full identity, create or reveal the conversation, and open the intro workflow.
- Updated candidate matching UI copy to describe intro approval instead of immediate mutual-interest messaging.
- Added and updated route tests for pending mutual interest and org-side intro approval.

Why:

- Matching needed one canonical approval point so blind-stage mutual interest could not bypass reveal protections or open messaging before intro approval.
- User-facing copy and notifications needed to match the actual state machine and reveal contract.

How to verify:

- `npx vitest run tests/lib/matching-review-contract.test.ts tests/lib/workflow-contracts.test.ts tests/lib/feedback-service.test.ts tests/api/interviews-schedule-route.test.ts tests/api/org-match-review-route.test.ts tests/api/cron-sla-enforcement-route.test.ts tests/api/match-interest-route.test.ts`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Interview recovery lineage is still tracked in metadata rather than a dedicated interview-to-interview foreign key.
- Mobile interest flow now respects intro approval semantics, but it still does not reuse the full qualification and policy checks from the core web route.
