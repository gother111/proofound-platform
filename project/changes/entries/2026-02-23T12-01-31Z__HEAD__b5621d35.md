# Project Change Entry

- Date/time (UTC): 2026-02-23T12:01:31Z
- Branch: HEAD
- Base commit: b5621d35

What changed:

- Added shared recovery-action helper with deterministic 3-action sets for individual and organization empty/incomplete states.
- Updated critical individual surfaces to use the shared helper:
  - matching empty/blocked states and filtered-zero reset flow
  - dashboard matching results empty state
  - dashboard explore empty state
  - profile activation next-step fallback
  - expertise empty state
  - proofs-empty state in skill editor
- Updated organization matching surfaces to use the shared helper:
  - organization matching empty state
  - assignment-level no-matches state in matching view
- Added unit tests for recovery-action helper behavior and URL uniqueness guarantees.

Why:

- Remove dead-end empty states and enforce always-available recovery actions.
- Ensure both personas receive clear next steps when data is missing or results are empty.
- Centralize recovery action logic to prevent drift across components.

How to verify:

- `npm run lint` (pass)
- `npm run typecheck` (pass)
- `npm run test` (pass)
- `npm run build` (pass; warns that `DATABASE_URL` is missing in local env but completes)
- Manual checks:
  - individual matching blocked/empty state shows 3 actions
  - filtered-zero state shows `Reset filters` then 3 actions
  - expertise empty and proofs-empty show 3 actions
  - dashboard explore/matching empty states show 3 actions
  - org matching empty and assignment no-matches show 3 actions including `Turn on candidate matching`

Open risks / TODO:

- Several actions intentionally route with `focus` query params for unique destinations; follow-up can consume these params in-page for deeper context-specific behavior.
- Build/test logs still emit mock DB warnings when `DATABASE_URL` is not set locally.
