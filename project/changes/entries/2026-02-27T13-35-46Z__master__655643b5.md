# Project Change Entry

- Date/time (UTC): 2026-02-27T13:35:46Z
- Branch: master
- Base commit: 655643b5
  What changed:
- Added requester-name snapshot support when creating impact verification claim snapshots.
  - `src/actions/profile.ts`
- Hardened impact verifier requester-profile lookup to retry without `email` column when schema/environment drift makes that column unavailable.
  - `src/app/api/verify/[token]/route.ts`
- Updated requester identity resolution to prefer snapshot full name from claim context before local-part fallbacks.
  - `src/app/api/verify/[token]/route.ts`
- Added regression coverage:
  - `tests/api/verify-impact-token-route.test.ts` (email-column drift fallback keeps full display name)
  - `tests/actions/profile.test.ts` (request snapshot stores requester full name in claim context)

Why:

- Latest PRO-63 feedback reported verifier UI still showing local-part style fallback instead of full name even when profile name is correct.
- Existing fallback logic could lose profile `display_name` if profile select failed due optional column drift.

How to verify:

- `npm run test -- tests/api/verify-impact-token-route.test.ts tests/actions/profile.test.ts`
- `npm run test -- tests/ui/impact-story-form.test.tsx tests/actions/create-impact-story.test.ts tests/actions/profile-impact-story-create.test.ts tests/ui/profile-dialogs-edit-routing.test.tsx`
- `npx eslint src/actions/profile.ts "src/app/api/verify/[token]/route.ts" tests/actions/profile.test.ts tests/api/verify-impact-token-route.test.ts`

Open risks / TODO:

- Existing requests created before requester-name snapshot support still rely on profile lookup and email fallbacks only.
- Full repo `npm run lint`/`npm run typecheck` remain blocked by unrelated pre-existing issues outside PRO-63 scope.
