# Project Change Entry

- Date/time (UTC): 2026-05-16T19:57:37Z
- Branch: master
- Base commit: 2dbc6aed
  What changed:
- Allowed the exact `/auth/logout` route through the launch page surface policy.
- Added regression coverage proving `/auth/logout` is classified active and middleware passes it through instead of rendering the archived-page response.

Why:

- PRO-211 still showed the locked-corridor Not found page on sign-out because middleware classified `/auth/logout` under the fallback archived `/auth` page family before the logout route could run.

How to verify:

- `npm run test -- src/lib/launch/__tests__/surface-policy.test.ts src/lib/__tests__/middleware-launch-archive.test.ts tests/api/auth-logout-route.test.ts tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/command-palette-archived-links.test.tsx`
- `npm run lint`
- `npm run typecheck`
- Browser check: visiting `http://localhost:3000/auth/logout` redirects to `http://localhost:3000/` and does not show the locked-corridor Not found page.

Open risks / TODO:

- Production deployment remains dependent on the existing release workflow gate being configured with required provider E2E credentials.
