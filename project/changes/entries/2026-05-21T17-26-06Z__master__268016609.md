# Project Change Entry

- Date/time (UTC): 2026-05-21T17:26:06Z
- Branch: master
- Base commit: 268016609
  What changed:
- Updated `scripts/perf-budgets.mjs` so the perf budget gate measures `/api/assignments` latency in addition to `/api/health`.
- The assignments probe accepts the expected unauthenticated `401` or `403` response while still enforcing the shared API P95 budget.
- Added launch-gate config coverage to keep the perf budget script tied to the launch-critical assignments route.

Why:

- Current launch docs require fresh `/api/assignments` latency/perf evidence before final signoff. The perf budget gate previously measured only `/api/health`, which was too narrow for the documented launch evidence requirement.

How to verify:

- `npm run test -- tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This improves the local/scripted evidence gate but does not replace a real production-candidate perf run, authenticated `/api/monitoring/perf-status`, restore proof, or final go/no-go.
