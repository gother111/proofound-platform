# Project Change Entry

- Date/time (UTC): 2026-04-23T19:22:55Z
- Branch: master
- Base commit: a05d6fa6
  What changed:
- Completed PRO-177 by retiring the standalone individual Portfolio workspace as a primary surface.
- Kept `/app/i/portfolio` as a compatibility shortcut into profile-owned tabs:
  - ready profiles go to `Visibility / Portfolio`
  - locked profiles go to `Proof Packs` with lock context preserved
- Removed the old `PortfolioWorkspaceClient` implementation and the workspace-specific UI test.
- Redirected live CTAs, onboarding/tour recovery links, matching/profile setup links, readiness actions, and dashboard cards to the profile Proof Packs or Visibility / Portfolio tabs.
- Documented ownership: Proof Packs live in the profile Proof Packs tab, Verification lives in `/app/i/verifications`, and Visibility / Portfolio lives in the profile Visibility / Portfolio tab plus the public `/portfolio/[handle]` output.

Why:

- The locked MVP makes Proof Pack the canonical proof object. The standalone Portfolio workspace duplicated profile tab behavior and made Proof Packs, verification, and publication ownership less clear.

How to verify:

- `npm run test -- tests/routes/portfolio-shortcuts.test.tsx tests/ui/first-proof-dialog.test.tsx tests/ui/left-nav-portfolio-gating.test.tsx tests/ui/profile-artifact-edit-actions.test.tsx tests/ui/command-palette-archived-links.test.tsx tests/ui/education-form-skill-picker.test.tsx tests/ui/volunteer-form-skill-picker.test.tsx tests/ui/matching-page-gated.test.tsx src/lib/__tests__/recovery-actions.test.ts src/lib/__tests__/matching-eligibility.test.ts tests/api/core-matching-gating-routes.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- Browser-use check: open `http://localhost:3001/app/i/portfolio`; unauthenticated browser sessions land on `/login`, while the route tests verify authenticated users are sent to the profile Proof Packs or Visibility / Portfolio tab.

Open risks / TODO:

- `/app/i/portfolio` remains intentionally available as a shortcut so older links do not break. Remove the route entirely only after analytics/support confirm no active dependency.
