# Project Change Entry

- Date/time (UTC): 2026-02-25T11:12:46Z
- Branch: HEAD
- Base commit: 70020bd0

What changed:

- Added individual-profile gating so mission/vision editing requires at least one value and one cause first.
- Added server-side guard in `src/actions/profile.ts` to reject mission/vision updates when values/causes prerequisites are missing.
- Updated profile action error handling in `src/hooks/useProfileData.ts` to surface concrete server error messages.
- Added focused tests:
  - `tests/actions/profile.test.ts` for prerequisite pass/fail behavior.
  - `tests/ui/editable-profile-purpose-gating.test.tsx` for UI gating behavior.
- Added sharded logs for this task (change + session entries).

Why:

- Implements `PRO-44` requirement that mission/vision writing and editing must be connected to core values and supported causes, with values and causes filled first.
- Prevents bypass via direct server action calls and keeps UI + server behavior aligned.

How to verify:

- `npm run lint` (PASS)
- `npm run typecheck` (PASS)
- `npm run test -- tests/actions/profile.test.ts tests/ui/editable-profile-purpose-gating.test.tsx` (PASS)
- `npm run test` (FAIL: unrelated pre-existing failure in `tests/ui/public-org-portfolio-page.test.tsx` due Next request-scope `cookies()` context)
- `npm run build` (PASS; prebuild warns about missing env vars in local environment)

Open risks / TODO:

- Organization scope is intentionally unchanged for `PRO-44`; org profile currently lacks a clearly defined first-class core values editing surface in the active flow.
- Follow-up spec needed to define what qualifies as org "core values" input for analogous gating.
