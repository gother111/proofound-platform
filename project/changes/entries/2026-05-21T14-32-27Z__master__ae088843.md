# Project Change Entry

- Date/time (UTC): 2026-05-21T14:32:27Z
- Branch: master
- Base commit: ae088843
  What changed:
- Converted top-level verification center sent-request delete/resend failure diagnostics from raw console output to shared `proofound:client-diagnostic` events.
- Extended runtime debug-output guardrails and appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc client diagnostics from the individual verification center while preserving request action behavior.

How to verify:

- `npm test -- --run tests/ui/verifications-client.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" src/app/app/i/verifications/VerificationsClient.tsx tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
