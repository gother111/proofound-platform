# Project Change Entry

- Date/time (UTC): 2026-03-08T21:22:46Z
- Branch: master
- Base commit: 3bac1846

What changed:

- Removed accidental imports from the readiness rollout that pointed at excluded modules: `slug-history`, `slug-policy`, `review-contract`, and `workflow/service`.
- Restored `src/actions/onboarding.ts` to local handle/slug validation and removed writes tied to the excluded public-portfolio migration fields.
- Reverted `src/app/api/core/matching/interest/route.ts` and `src/app/api/core/matching/profile/route.ts` to the pre-workflow persistence shape while keeping the readiness soft-gating and `qualified_intro_ready` checks.
- Fixed the assignment status union in `src/app/api/core/matching/profile/route.ts` to accept `paused`, which surfaced during clean verification.

Why:

- Vercel failed to build commit `2e6110e` because the scoped readiness commit still referenced modules and schema fields from unrelated work that had been intentionally excluded from the push.
- The follow-up needed to preserve the readiness/onboarding reframe while removing those cross-feature dependencies so the deploy could compile and run on the existing production contract.

How to verify:

- `npm run vercel:preflight`
- In a clean worktree at commit `2e6110e`, apply this patch and run `npm run typecheck`
- In the same clean worktree, run a prod-like build with Node 20 and env loaded: `PATH="/opt/homebrew/opt/node@20/bin:/opt/homebrew/bin:$PATH" npm run build`

Open risks / TODO:

- The main workspace still contains unrelated local modifications outside this fix; use a clean checkout or CI/Vercel to validate production behavior.
- Sourcing `.env.local` in shell emitted benign `command not found` lines for bare config entries before the clean worktree build; prefer a dotenv loader next time if this becomes a recurring workflow.
