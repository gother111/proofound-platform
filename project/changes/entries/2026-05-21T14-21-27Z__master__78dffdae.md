# Project Change Entry

- Date/time (UTC): 2026-05-21T14:21:27Z
- Branch: master
- Base commit: 78dffdae
  What changed:
- Converted individual and organization messages client failure diagnostics from raw console output to shared `proofound:client-diagnostic` events.
- Covered conversation-list load, thread load, and message-send failure paths with persona-specific event names.
- Extended runtime debug-output guardrails and appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc client diagnostics from launch-relevant communications surfaces while preserving messaging behavior.

How to verify:

- `npm test -- --run tests/routes/organization-messages-page.test.tsx tests/ui/deferred-messages-client.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" 'src/app/app/i/messages/MessagesClient.tsx' 'src/app/app/o/[slug]/messages/OrgMessagesClient.tsx' tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
