# Project Change Entry

- Date/time (UTC): 2026-02-27T13:04:25Z
- Branch: master
- Base commit: d519ef31
  What changed:
- Updated `src/app/api/verify/[token]/route.ts` so skill verification token read/write paths use `createAdminClient()` instead of anon request-scoped client.
- Kept optional auth identity checks via `createClient()` for `requires_authenticated_verifier` behavior.
- Preserved legacy UUID fallback lookup (`id`) and response contracts.
- Extended `tests/api/verify-impact-token-route.test.ts` with skill-flow regressions:
  - signed-out GET token resolution through admin client
  - unauthenticated POST allowed when `requires_authenticated_verifier=false`
  - `AUTH_REQUIRED` on POST when `requires_authenticated_verifier=true`
  - updated legacy fallback test for admin-backed skill lookup.

Why:

- PRO-74 follow-up regression: verification emails were sent but opened links failed with `Verification request not found or invalid token`.
- Root cause: anon client could not read/update `skill_verification_requests` rows under current strict RLS policies.

How to verify:

- `npm run db:migrate` (pass, applied 0, skipped 47)
- `npm run test -- tests/api/verify-impact-token-route.test.ts` (pass)
- `npm run test -- tests/api/expertise-skill-verification-request-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/api/expertise-verification-respond-route.test.ts tests/api/verify-impact-token-route.test.ts` (pass)
- `npm run lint` (pass with existing warnings)
- `npm run typecheck` (pass)
- `npm run build` (fails in current dirty workspace with `PageNotFoundError: Cannot find module for page: /_document`)
- `npm run test` (pass)
- `npm run test:privacy` (pass)
- `npm run test:privacy:extended` (pass)

Open risks / TODO:

- Manual browser validation for affected users (`y.bakurov@icloud.com`, `p.samoshko97@icloud.com`) remains pending.
- Build failure appears unrelated to this diff and likely tied to existing unrelated workspace changes.
