# Project Change Entry

- Date/time (UTC): 2026-03-08T21:39:37Z
- Branch: master
- Base commit: c26d175d
  What changed:
- Extended `src/components/matching/assignment-steps/Step1BusinessValue.tsx` to accept `templateAccessEnabled`.
- Hid the template button and launch-path copy behind that flag so the assignment builder prop contract matches the page usage on `master`.

Why:

- `master` already contained the earlier deploy-fix commits for missing modules, but a clean verification build still failed because the assignment builder page passed `templateAccessEnabled` to `Step1BusinessValue` without the prop existing in the component type.

How to verify:

- `git worktree add /tmp/proofound-verify-current c26d175d`
- `ln -s /Users/yuriibakurov/proofound/node_modules /tmp/proofound-verify-current/node_modules`
- `PATH=/Users/yuriibakurov/.nvm/versions/node/v20.20.0/bin:$PATH npm run build`
- Expect a successful build with only the existing `no-img-element` warnings and local missing-env warnings when `DATABASE_URL` is not set.

Open risks / TODO:

- Local `npm run typecheck` in a fresh worktree requires `.next/types` generation first, so use `npm run build` for first-pass parity.
- Vercel production still needs its real env vars. The clean local build used the repository fallback warnings for missing local envs.
