# Project Change Entry

- Date/time (UTC): 2026-02-27T08:38:02Z
- Branch: master
- Base commit: a8e6f035
  What changed:
- Added standalone impact-story verification request server action and extracted shared verification insert/email logic from `createImpactStory` into reusable helper code in `src/actions/profile.ts`.
- Added auto-save-first verification request support and latest verification summary hydration for impact stories (`verificationRequestStatus`, requested timestamp, verifier email, email delivery metadata).
- Updated profile hook and dialog wiring to support independent `Send Request` behavior without coupling to `Save Changes`:
  - `src/hooks/useProfileData.ts`
  - `src/components/profile/EditableProfileView.tsx`
  - `src/components/profile/editable-profile/ProfileDialogs.tsx`
- Reworked `src/components/profile/forms/ImpactStoryForm.tsx` to:
  - expose separate `Send Request` action
  - support auto-save-first for unsaved stories
  - keep editing open after sending
  - route subsequent saves to update mode using persisted story ID.
- Added verification request status rendering in impact cards:
  - `src/components/profile/editable-profile/ImpactTab.tsx`
  - `src/components/profile/ProfileView.tsx`
- Extended shared profile types in `src/types/profile.ts` for dispatch params/results and verification summary fields.
- Added/updated tests for the new flow:
  - `tests/ui/impact-story-form.test.tsx`
  - `tests/actions/profile.test.ts`
  - `tests/ui/profile-dialogs-edit-routing.test.tsx`
  - regression coverage confirmed in existing create-impact tests.

Why:

- Users could only request impact-story verification during save, and edit-mode saves ignored verification request payloads.
- Receiver email delivery issues for impact-story verification in edit workflows were caused by missing standalone request path, not just SMTP config.
- Product requirement needed explicit separate request UX and visible status persistence while continuing to edit.

How to verify:

- `npm run test -- tests/ui/impact-story-form.test.tsx tests/actions/create-impact-story.test.ts tests/actions/profile-impact-story-create.test.ts tests/actions/profile.test.ts tests/ui/profile-dialogs-edit-routing.test.tsx`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Existing lint warnings in unrelated landing files remain (`@next/next/no-img-element`).
- Manual runtime validation still recommended for affected real accounts on `/app/i/profile` with real email delivery and request status refresh.
