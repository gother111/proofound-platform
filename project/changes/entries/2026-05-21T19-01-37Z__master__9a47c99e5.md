# Project Change Entry

- Date/time (UTC): 2026-05-21T19:01:37Z
- Branch: master
- Base commit: 9a47c99e5
  What changed:
- Archived the generic `/api/assignments/[id]/pipeline` route as a non-launch API surface.
- Replaced the route's database-backed pipeline read/write behavior with a `410` launch-corridor response.
- Updated launch surface policy, inventory tests, archived-handler tests, and API reference status for the route.

Why:

- The locked MVP corridor uses assignment drafting, internal review, publish readiness, intro, reveal, interview, and decision routes. A generic assignment pipeline API is broader workflow/ATS-style drift and should not remain active in the launch surface.

How to verify:

- `npm run test -- tests/api/archived-api-handlers-route.test.ts tests/api/launch-surface-inventory.test.ts tests/api/assignment-mutation-json-boundary.test.ts src/lib/launch/__tests__/surface-policy.test.ts`
- `npm run test:launch:routes`
- `git diff --check`
- `npm run docs:freshness`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Existing database compatibility objects for legacy assignment pipeline state remain in migrations/history, but the active API route no longer exposes the generic pipeline surface.
