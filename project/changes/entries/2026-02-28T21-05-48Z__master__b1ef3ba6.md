# Project Change Entry

- Date/time (UTC): 2026-02-28T21:05:48Z
- Branch: master
- Base commit: b1ef3ba6

What changed:

- Added interview messaging helper in `src/lib/interviews/messaging.ts` to:
- ensure conversation existence by `match_id` (auto-create when missing),
- check org admin authorization context for interview actions,
- post structured interview update messages (`scheduled`/`edited`/`cancelled`) as best-effort.
- Added `POST /api/interviews/edit` route in `src/app/api/interviews/edit/route.ts` with org `owner/admin` authorization, scheduled-only + future-time validation, and best-effort conversation messaging.
- Updated `POST /api/interviews/cancel` in `src/app/api/interviews/cancel/route.ts` to enforce org `owner/admin` authorization, scheduled-only cancellation, optional reason append to notes, and best-effort conversation messaging.
- Updated `POST /api/interviews/schedule` in `src/app/api/interviews/schedule/route.ts` to post structured “scheduled” interview messages after successful interview creation.
- Updated org interviews UI (`src/app/app/o/[slug]/interviews/page.tsx`) to replace reschedule-only action with explicit `Edit Interview` dialog and `Cancel Interview` action.
- Added tests:
- `tests/api/interviews-edit-route.test.ts`
- `tests/api/interviews-cancel-route.test.ts`
- `tests/ui/organization-interviews-page-actions.test.tsx`

Why:

- Org admins need direct control to edit/cancel scheduled interviews from org profile workflows.
- Interview lifecycle changes must notify the other party via the platform messaging system, even when no conversation exists yet.

How to verify:

- `npm run test -- tests/api/interviews-edit-route.test.ts tests/api/interviews-cancel-route.test.ts tests/ui/organization-interviews-page-actions.test.tsx tests/ui/schedule-interview-modal.test.tsx` (PASS)
- `npm run lint` (PASS with 2 existing warnings in landing image components)
- `npm run typecheck` (PASS)
- `npm run build` (FAIL due to unrelated existing `pdfjs-dist/webpack.mjs` module resolution errors in expertise components)

Open risks / TODO:

- Build currently fails on unrelated existing expertise imports:
- `src/components/expertise/CVJDAutoSuggest.tsx`
- `src/components/expertise/cv-import/CvImportWizard.tsx`
