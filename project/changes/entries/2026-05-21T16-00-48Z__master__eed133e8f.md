# Project Change Entry

- Date/time (UTC): 2026-05-21T16:00:48Z
- Branch: master
- Base commit: eed133e8f
  What changed:
- Polished active launch/user-facing copy that still exposed unfinished release wording:
  - OCR API errors now say "Text extraction" instead of "OCR beta".
  - Start from CV and candidate-invite API denials now use product/early-access language instead of "beta testing".
  - Visual-fixture match APIs no longer return "Mock match not found".
  - Organization collaborator invite and admin queue copy no longer expose "launch role" or "stuck MVP handoff" wording.
  - Launch-surface policy labels now use API names without "Beta".
- Added a runtime source guardrail for the exact active-copy regressions.

Why:

- Active MVP surfaces should not expose mock/beta/MVP implementation terms as user-facing or API-facing copy unless the locked corridor explicitly requires it.

How to verify:

- `npm test -- --run tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm run test:launch:routes`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This is repo/local verification only. Browser rendering was not required for the copy-only/API-message changes, and production/staging was not mutated.
