# Project Change Entry

- Date/time (UTC): 2026-03-10T16:32:02Z
- Branch: master
- Base commit: 0a22dfb7
  What changed:
- Added canonical reveal and corridor helpers to `src/lib/matching/review-contract.ts`, including progressive reveal stages, fallback mapping, and visibility-safe reason summaries.
- Updated matching and shortlist routes to return canonical corridor fields and keep Stage 2 contextual reveal anonymous.
- Tightened workflow transitions so `withdrawn` and `no_show` are terminal on the same record, while intro recovery creates a new intro record linked back through existing metadata.
- Added explicit feedback follow-up SLA helpers and exposed due or breached follow-up state from interview completion, feedback token lookup, feedback submission, and SLA cron reporting.
- Updated interview scheduling to treat Google Meet and manual links as primary, keep Zoom in coming-soon fallback mode, and allow new interview creation after a prior `no_show`.

Why:

- The matching-to-feedback corridor needed one canonical contract across blind review, intro gating, interview coordination, and the 48-hour feedback follow-up window.
- Stage 2 contextual reveal must stay identity-safe even when shortlist visibility increases.
- The canonical workflow docs require terminal no-show and withdrawn handling on the same record and fairness-safe rank-band fallbacks.

How to verify:

- Run `npx vitest run tests/lib/matching-review-contract.test.ts tests/lib/workflow-contracts.test.ts tests/lib/feedback-service.test.ts tests/api/interviews-schedule-route.test.ts tests/api/org-match-review-route.test.ts tests/api/cron-sla-enforcement-route.test.ts`.
- Run `npm run typecheck`.
- Run `npm run lint`.
- Hit `POST /api/org/[id]/matches/[matchId]/review` with `action: "request_intro"` before Stage 2 and verify it returns `409` with `corridorState: "intro_hold"`.
- Complete an interview and verify the response includes `feedbackFollowUp.dueAt`, reminder checkpoints, and that `/api/cron/sla-enforcement` reports `feedbackDue` and `feedbackBreached`.

Open risks / TODO:

- Interview recovery lineage is currently carried in scheduling metadata instead of a first-class interview table foreign key.
- Core matching interest flows still use the existing mutual-interest path and could be aligned further with the new canonical intro request surface.
