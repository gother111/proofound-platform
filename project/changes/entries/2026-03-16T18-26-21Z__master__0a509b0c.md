# Project Change Entry

- Date/time (UTC): 2026-03-16T18:26:21Z
- Branch: master
- Base commit: 0a509b0c
  What changed:
- Deleted the non-MVP org coverage analytics route at `src/app/api/org/[id]/coverage/route.ts`.
- Deleted the unused `src/components/organization/TeamCoverageMatrix.tsx` component that only depended on the removed coverage API.
- Deleted the archived legacy verification route handlers under `src/app/api/expertise/**` and `src/app/api/verification/skill/**` so those non-launch surfaces no longer compile at all.
- Extended `src/lib/launch/surface-policy.ts` and related tests so `/api/org/[id]/coverage` archives through middleware with the standard non-launch `410` response.
- Simplified `tests/api/launch-surface-inventory.test.ts` so it now fails whenever any compiled API route is classified as `archived` by `classifyLaunchApiPath(...)`, removing the need for a hand-maintained denylist.
- Narrowed the `docs/API_REFERENCE.md` org and verification sections and added a staleness note to `docs/proofound-hard-audit-2026-03-16-rerun.md` so repo docs stop claiming the removed route surface is still compiled.

Why:

- The locked MVP excludes team coverage analytics and broader non-MVP org/dashboard analytics surfaces.
- The current workspace had already pruned most of that route family, so this finishes the remaining org coverage edge and keeps docs aligned with the code.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH /opt/homebrew/opt/node@20/bin/node /opt/homebrew/opt/node@20/lib/node_modules/npm/bin/npm-cli.js run test -- tests/api/launch-surface-inventory.test.ts tests/ui/archived-mvp-routes.test.ts tests/api/archived-legacy-verification-route-handlers.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH /opt/homebrew/opt/node@20/bin/node /opt/homebrew/opt/node@20/lib/node_modules/npm/bin/npm-cli.js run test -- src/lib/__tests__/middleware-launch-archive.test.ts src/lib/launch/__tests__/surface-policy.test.ts tests/routes/portfolio-shortcuts.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH /opt/homebrew/opt/node@20/bin/node /opt/homebrew/opt/node@20/lib/node_modules/npm/bin/npm-cli.js run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH /opt/homebrew/opt/node@20/bin/node /opt/homebrew/opt/node@20/lib/node_modules/npm/bin/npm-cli.js run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH /opt/homebrew/opt/node@20/bin/node /opt/homebrew/opt/node@20/lib/node_modules/npm/bin/npm-cli.js run build`
- After `build`, confirm the route inventory no longer lists `/api/org/[id]/coverage`, `/api/expertise/**/verification-request`, `/api/expertise/verifications/**`, or `/api/verification/skill/{request,respond}`.

Open risks / TODO:

- `docs/API_REFERENCE.md` still contains other stale non-MVP org route entries outside the small org section updated here.
- Archived page-level shortcuts such as `/app/o/[slug]/team/coverage` still compile as compatibility redirects by design.
