# Project Change Entry

- Date/time (UTC): 2026-02-27T12:53:19Z
- Branch: master
- Base commit: cbba917f
  What changed:
- Updated impact-story verification send flow to auto-save existing-story draft before dispatch:
  - `src/components/profile/forms/ImpactStoryForm.tsx`
  - `src/actions/profile.ts` (`requestImpactStoryVerification`)
- Added requester identity fallback chain and legacy outcome-claim fallback in verifier token route:
  - `src/app/api/verify/[token]/route.ts`
- Added/updated regression coverage:
  - `tests/ui/impact-story-form.test.tsx`
  - `tests/actions/profile.test.ts`
  - `tests/api/verify-impact-token-route.test.ts`

Why:

- PRO-63 remained reproducible in production-like behavior: requester rendered as `Unknown` and claims could be empty for legacy/partial impact story data.
- Existing-story send path used last saved snapshot only, so unsaved measured outcomes were not included in verification requests.

How to verify:

- `npm run test -- tests/ui/impact-story-form.test.tsx tests/actions/profile.test.ts tests/api/verify-impact-token-route.test.ts`
- `npm run test -- tests/actions/create-impact-story.test.ts tests/actions/profile-impact-story-create.test.ts tests/ui/profile-dialogs-edit-routing.test.tsx`
- `npx eslint src/components/profile/forms/ImpactStoryForm.tsx src/actions/profile.ts "src/app/api/verify/[token]/route.ts" tests/ui/impact-story-form.test.tsx tests/actions/profile.test.ts tests/api/verify-impact-token-route.test.ts`
- Full repo checks currently blocked by unrelated pre-existing issues in local tree:
  - `npm run lint`
  - `npm run typecheck`

Open risks / TODO:

- Auto-save-on-send updates existing story content before request creation; UX copy was updated, but this behavior should be smoke-tested in browser against real impact stories.
- Unrelated repo errors (outside PRO-63 files) prevent full lint/typecheck pass in this workspace.
