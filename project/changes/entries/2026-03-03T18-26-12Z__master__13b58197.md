# Project Change Entry

- Date/time (UTC): 2026-03-03T18:26:12Z
- Branch: master
- Base commit: 13b58197

What changed:

- Updated Gemini CV import default model in runtime config to `gemini-3.1-flash-lite`.
- Updated pricing resolution to use model-family matching (`flash-lite` vs `flash`) so `gemini-3.1-flash-lite` resolves to Lite pricing instead of Flash fallback pricing.
- Updated `.env.example` and `docs/ENV_VARIABLES.md` to document the new default model.
- Added `tests/lib/gemini-pricing.test.ts` for family mapping and override coverage.
- Extended `tests/lib/gemini-config.test.ts` with default-model assertion and updated one stale adaptive-token expectation to current behavior.
- Updated API metadata fixture model string in `tests/api/cv-import-auto-suggest-route.test.ts`.

Why:

- Ensure default CV import model uses Gemini 3.1 Flash Lite and avoid pricing misclassification that could overstate cost when new Flash Lite model versions are used.

How to verify:

- `npm run test -- tests/lib/gemini-config.test.ts tests/lib/gemini-pricing.test.ts tests/api/cv-import-auto-suggest-route.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npx vercel@latest env ls --token "$VERCEL_TOKEN" | rg "CV_IMPORT_GEMINI_MODEL_DEFAULT|CV_IMPORT_GEMINI_MODEL_FALLBACK"`

Open risks / TODO:

- Preview environment variable updates in Vercel can prompt for git branch; the applied config was explicitly set with an empty branch argument for all preview branches.
