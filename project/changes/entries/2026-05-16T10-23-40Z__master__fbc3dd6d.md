# Project Change Entry

- Date/time (UTC): 2026-05-16T10:23:40Z
- Branch: master
- Base commit: fbc3dd6d
  What changed:
- Hardened AI provider-smoke readiness so stale artifacts for a different configured model do not satisfy launch evidence.
- Updated AI model/env docs and added an AI verification report with rendered evidence.
- Cleaned an async assistive-AI UI test warning and removed stale port-specific generated type includes from `tsconfig.json`.
- Hardened the local Next dev wrapper so port-specific generated dev output is marked CommonJS inside the repo's module package.
- Verified the stable `gemini-3.1-flash-lite` model code against the official Gemini API model docs.

Why:

- Keep the assistive AI layer aligned to the locked proof-first MVP corridor and prevent false launch readiness from outdated provider smoke evidence.

How to verify:

- `npm run test:launch:ai`
- focused AI UI Vitest suites listed in `audit/ai-features-verification-2026-05-16.md`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`
- `npm run build`
- `npm run test:privacy:extended`
- `npm run ai:provider:smoke` (expected fail-closed locally without DB/provider credentials)

Open risks / TODO:

- Live Gemini provider smoke remains unverified until `DATABASE_URL`, server-only provider credentials, and AI spend-cap env are available.
- Requested Codex in-app Browser validation remains unverified because the in-app Browser backend was unavailable; Playwright fallback evidence is saved in the report.
