# Project Change Entry

- Date/time (UTC): 2026-05-21T05:56:17Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Reframed lower `DATA_SECURITY_PRIVACY_ARCHITECTURE.md` RLS and multi-org reference snippets from legacy `owner` / `steward` / `recruiter` / `viewer` role checks to canonical `org_owner` / `org_manager` / `org_reviewer` roles.
- Strengthened the root privacy architecture launch-gate guard to require the canonical role examples and reject the old role lists.

Why:

- The file is reference material, but its security examples should not teach stale org role names when current migrations and policy truth use canonical org roles.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "root privacy architecture" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- Targeted scans for old and canonical role examples in `DATA_SECURITY_PRIVACY_ARCHITECTURE.md`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `listen EPERM 0.0.0.0:24678` warning before passing.
