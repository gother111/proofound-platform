# Project Change Entry

- Date/time (UTC): 2026-05-21T06:21:14Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Reframed the locked MVP exclusion from `open candidate search index` to `open people search index`.
- Reframed the reference-only system architecture notification from notifying the `hiring manager` to notifying the `assignment-review owner`.
- Strengthened launch-gate coverage in the active authority stack and broad reference spec guard.

Why:

- These root surfaces should use the current assignment-review vocabulary while preserving the same non-goal and reference-only semantics.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts -t "locked MVP authority stack|broad reference specs" --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- Targeted scans for `open candidate search index`, `open people search index`, `notify hiring manager`, and `notify assignment-review owner`.
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Remaining hits are mostly explicit AI prohibitions, archived/audit history, migration/database field names, tests asserting stale copy is absent, and historical change logs. Continue reviewing by source class before editing.
