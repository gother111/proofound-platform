# Project Change Entry

- Date/time (UTC): 2026-03-09T13:15:09Z
- Branch: master
- Base commit: 89074ce7
  What changed:
- Added a shared Python internal worker helper and a claim-by-id queue helper so the CV import status route can execute a queued extract job inline.
- Updated the CV import wizard to stop indefinite queued polling after a bounded wait and recover through the existing local PDF extraction path when files are still available.
- Added coverage for inline queue recovery, stalled queue fallback, and shared worker execution behavior.
- Reconciled `docs/CRON_SETUP.md` with the intended every-minute cron-job.org cadence for `/api/cron/python-internal-worker`.

Why:

- Users could upload a CV, press Analyze, and remain in `Queued for extraction` indefinitely whenever cron-job.org did not pick up the job quickly enough.
- The wizard needed a deterministic recovery path that preserved the async queue design while avoiding a stuck loading state in the UI.

How to verify:

- `npm run test -- tests/ui/cv-import-wizard.test.tsx tests/api/cv-import-wizard-extract-route.test.ts tests/api/cv-import-wizard-extract-status-route.test.ts tests/api/cron-python-internal-worker-route.test.ts tests/lib/python-internal-worker.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Inline recovery only covers the currently polled extract job. Broader queue health still depends on the cron worker for the rest of `python_internal_jobs`.
- `npm run docs:freshness` still reports the pre-existing warning that `project/Documentation.md` is missing freshness metadata near the top.
