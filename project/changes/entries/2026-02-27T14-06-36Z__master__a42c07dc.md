# Project Change Entry

- Date/time (UTC): 2026-02-27T14:06:36Z
- Branch: master
- Base commit: a42c07dc

What changed:

- Added persistent, informational `Current location` trail to Add Skill drawer browse flow in `BrowseModePanel` with Domain/Category/Subcategory chips and `Not selected` fallbacks.
- Passed `selectedL3` through `AddSkillDrawer` -> `AddSkillDrawerView` -> `BrowseModePanel` to render complete context at all browse steps.
- Updated Step 3 helper copy to include parent path context and Step 4 helper copy to reference the location trail.
- Added focused UI test coverage in `tests/ui/add-skill-drawer-browse-location.test.tsx` for step 2/3/4 context visibility and partial-context placeholders.

Why:

- PRO-83 reported user confusion in browse flow because current taxonomy location was not visible in later steps.
- The change keeps location visible through the flow, including step 4 reached from search when parent taxonomy context is available.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/add-skill-drawer-browse-location.test.tsx` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS, 2 pre-existing warnings outside task scope)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test` (PASS)

Open risks / TODO:

- None identified for this scope.
