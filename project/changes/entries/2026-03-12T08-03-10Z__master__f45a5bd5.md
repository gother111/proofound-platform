# Project Change Entry

- Date/time (UTC): 2026-03-12T08:03:10Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Refactored `src/lib/profile/normalizePurposeLinks.ts` to extract reusable internal helpers `normalizeLabel` and `extractValueLabel`.
- Reused the new helper in both `normalizeIndividualValues` and `normalizeIndividualValueLabels` to remove duplicated label parsing and trimming logic.

Why:

- Reduce duplication in profile purpose normalization while preserving output behavior.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/normalize-individual-purpose-links.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx eslint src/lib/profile/normalizePurposeLinks.ts`

Open risks / TODO:

- File was already modified in the working tree before this run; verify with the focused test when merging broader branch changes.
