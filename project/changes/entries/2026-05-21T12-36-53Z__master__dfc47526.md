# Project Change Entry

- Date/time (UTC): 2026-05-21T12:36:53Z
- Branch: master
- Base commit: dfc47526
  What changed:
- Replaced the raw KV provider-error diagnostic in the shared rate-limit helper with structured `rate_limit.provider_check_failed` logging.
- Added focused provider-error coverage for strict auth rate-limit profiles and runtime guardrail coverage.
- Updated the active MVP surface sweep artifact with the rate-limit verification record.

Why:

- Rate limiting is a launch-safety control, and provider failures need structured, non-leaky observability without weakening fail-closed behavior.
- The change preserves missing-configuration handling, local smoke fallback behavior, strict profile denial, and provider-error metadata.

How to verify:

- `npm test -- --run src/lib/__tests__/rate-limit.test.ts src/lib/__tests__/middleware-fail-closed.test.ts src/app/api/monitoring/__tests__/launch-status-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- src/lib/rate-limit/index.ts src/lib/__tests__/rate-limit.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/lib/rate-limit/index.ts -n`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment; the affected test runs passed.
- Middleware fail-closed tests intentionally print simulated failure stderr while passing.
- Broader raw diagnostics remain in unrelated active server, action, lib, and client surfaces and need separate scoped sweeps.
