# Project Change Entry

- Date/time (UTC): 2026-02-28T08:53:59Z
- Branch: master
- Base commit: 73383837
  What changed:
- Fixed verification data loading queries in `/app/i/verifications` and `/api/expertise/verifications/incoming` to use explicit PostgREST relation paths and schema-correct column aliases.
- Added `router.refresh()` wiring after successful custom verification request creation so the verifications page updates without manual reload.
- Added regression coverage:
  - Updated `tests/ui/verifications-page.test.tsx` to assert disambiguated query syntax.
  - Updated `tests/ui/verifications-client.test.tsx` to verify refresh behavior after custom request creation.
  - Updated `tests/ui/custom-verification-request-dialog.test.tsx` to assert `onCreated` callback execution.
  - Added `tests/api/expertise-verifications-incoming-route.test.ts` for incoming route query shape and normalized email filtering.

Why:

- Pending verification rows existed in DB but were not visible in UI because runtime queries failed with ambiguous relation embeds (`PGRST201`) and outdated selected columns (`42703`).
- Users needed immediate visibility of newly created requests without manually refreshing the page.

How to verify:

- `npm run test -- tests/ui/verifications-page.test.tsx tests/ui/verifications-client.test.tsx tests/ui/custom-verification-request-dialog.test.tsx tests/api/expertise-verifications-incoming-route.test.ts tests/api/custom-verification-routes.test.ts` (PASS)
- `npm run lint` (PASS with existing warnings unrelated to this change)
- `npm run typecheck` (FAIL due existing unrelated typing issue in `src/components/expertise/CVJDAutoSuggest.tsx` for `pdfjs-dist/webpack.mjs`)

Open risks / TODO:

- TypeScript baseline is currently red in this repo due unrelated `pdfjs-dist/webpack.mjs` declaration gap; full repo typecheck remains blocked until that is addressed.
