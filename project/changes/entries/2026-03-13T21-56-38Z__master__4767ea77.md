# Project Change Entry

- Date/time (UTC): 2026-03-13T21:56:38Z
- Branch: master
- Base commit: 4767ea77

What changed:

- Added a shared canonical verification status contract that returns `summary`, `workflow`, and `channels`, then moved both launch and mobile status routes onto it.
- Removed the launch status-route schema-lag fallback query and dropped active launch reliance on top-level compatibility trust fields.
- Extracted a shared claim-scoped verification request feed, added canonical `/api/verification/requests/...` route aliases, and moved launch verification clients and the skill editor off expertise-prefixed transport paths.
- Removed unused compatibility-era verification fields from proof-trust snapshot computation and added a contradicted workplace policy assertion.

Why:

- The locked MVP requires claim-scoped verification semantics where transport signals like work email and LinkedIn do not inflate broad trust.
- Active launch clients were still tied to legacy request transport and compatibility fields, which kept the audit findings alive even though the policy layer was already conservative.

How to verify:

- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run test -- tests/api/verification-status-route.test.ts tests/api/mobile-verification-status-route.test.ts tests/ui/verification-status-options.test.tsx tests/ui/verifications-client.test.tsx tests/ui/verifications-page.test.tsx tests/ui/custom-verification-request-dialog.test.tsx tests/ui/edit-skill-window-proofs.test.tsx tests/lib/verification-policy.test.ts tests/portfolio-trust-signals.test.ts tests/lib/public-trust-export-data.test.ts tests/lib/public-portfolio-projection.test.ts tests/api/public-portfolio-export-route.test.ts tests/api/portfolio-export-route.test.ts`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run typecheck`
- `source ~/.nvm/nvm.sh && nvm use 20.20.0 >/dev/null && npm run lint`
- `rg -n "/api/expertise/verifications|/api/expertise/verification" src/app/app/i src/components/settings`

Open risks / TODO:

- `/api/verify/[token]` and `/api/verify/custom/[token]` are still legacy-compatible public link responders and should be narrowed separately once compatibility evidence is refreshed.
