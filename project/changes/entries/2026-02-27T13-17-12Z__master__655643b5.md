# Project Change Entry

- Date/time (UTC): 2026-02-27T13:17:12Z
- Branch: master
- Base commit: 655643b5
  What changed:
- Applied V2 AppSurface component with feature-flagged routing logic to phase 4 Organization Application Routes.
- Specifically refactored the following Organization Application routes: `home`, `profile`, `assignments` (and nested), `matching`, `candidates`, `shortlist`, `messages`, `interviews`, `opportunities`, `members`, `team` (and nested: `coverage`), `projects`, `analytics/fairness`, `portfolio`, `invitations/[token]`, `settings` and nested settings routes.

Why:

- The UI refactor V2 requires migrating all individual and organization apps into the new standardized layout models (such as `AppSurface`) to reduce style debt and present a modern application aesthetic. Phase 4 targeted the organization routes, successfully closing out the route migration.

How to verify:

- `npm run typecheck`
- `npm run test`
- `npm run test:e2e:individual:strict`
- `npm run test:e2e:org:strict`
- `npm run test:a11y:strict`

Open risks / TODO:

- Refactoring settings required changing various styles in complex layout. Tests did not show any regression, but some aesthetic discrepancies might surface going forward since some inline styles were swapped to V2.
