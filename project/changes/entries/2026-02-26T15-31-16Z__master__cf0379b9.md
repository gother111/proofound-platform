# Project Change Entry

- Date/time (UTC): 2026-02-26T15:31:16Z
- Branch: master
- Base commit: cf0379b9
  What changed:
- Added migration `20260226161000_fix_skill_verification_requests_policy_auth_users.sql` to drop/recreate verifier SELECT/UPDATE RLS policies on `skill_verification_requests` without `auth.users` table references.
- Updated `src/app/api/expertise/user-skills/[id]/verification-request/route.ts` POST flow to create verification requests via insert-only write path (no readback dependency), while preserving legacy `verification_token` fallback and existing API response shape.
- Expanded `tests/api/expertise-skill-verification-request-route.test.ts` for insert-only behavior and auth/error-path compatibility.

Why:

- PRO-74 verification requests were still failing for individual accounts because RLS policy evaluation touched `auth.users`, causing `42501 permission denied for table users` during authenticated SELECT/INSERT RETURNING.
- Insert flow hardening prevents request creation from depending on post-insert SELECT visibility.

How to verify:

- `npm run lint` (pass with existing warnings only)
- `npm run typecheck` (pass)
- `npm run test -- tests/api/expertise-skill-verification-request-route.test.ts tests/api/expertise-user-skill-proofs-route.test.ts tests/api/expertise-verification-respond-route.test.ts tests/api/verify-impact-token-route.test.ts` (pass)
- `npm run test` (pass)
- `npm run build` (pass)
- `npm run db:backup:checkpoint` (pass)
- `npm run db:migrate` (applied: `20260226161000_fix_skill_verification_requests_policy_auth_users`)
- Authenticated-role DB simulation in rollback transaction: `SELECT` and `INSERT ... RETURNING` on `skill_verification_requests` both succeed.

Open risks / TODO:

- Manual UI validation for affected accounts (`y.bakurov@icloud.com`, `p.samoshko97@icloud.com`) still required in runtime environment with real sessions.
