# Project Change Entry

- Date/time (UTC): 2026-02-26T09:35:13Z
- Branch: master
- Base commit: c0d7d064
  What changed:
- Added canonical backfill marker migration:
  - `src/db/migrations/20260225143000_expand_skill_taxonomy_language_and_common_terms.sql`
- Added strict legacy baseline snapshot:
  - `supabase/ledger-baseline/schema_migrations.current-db.json`
- Extended migration audit modes:
  - `scripts/audit-migration-ledger.mjs`
  - new mode: `legacy-supabase-baseline`
  - new flag: `--baseline <path>`
- Updated migration documentation/checklists:
  - `README.md`
  - `RUN_MIGRATIONS_GUIDE.md`
  - `APPLY_MIGRATIONS_MANUAL.md`
  - `agent/runbooks/setup.md`
  - `agent/checklists/verification.md`
- Reconciled checksum for canonical orphan version in DB ledger:
  - `public.app_migration_ledger.version = 20260225143000_expand_skill_taxonomy_language_and_common_terms`

Why:

- Enforce strict zero drift for both:
  - canonical ledger parity (`src/db/migrations` + supplemental versions vs `public.app_migration_ledger`)
  - legacy Supabase history parity against a committed DB-derived baseline snapshot

How to verify:

- `npm run db:backup:checkpoint`
- `npm run db:audit:migrations -- --mode canonical --fail-on-extra`
- `npm run db:audit:migrations -- --mode legacy-supabase-baseline --baseline supabase/ledger-baseline/schema_migrations.current-db.json`
- `npm run db:migrate`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Canonical backfill migration is intentionally no-op because original SQL body was not recoverable from repo history.
- Legacy baseline is environment-specific (`current-configured-database-url`) and should be regenerated intentionally if source environment changes.
