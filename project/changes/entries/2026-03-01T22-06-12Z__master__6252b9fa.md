# Project Change Entry

- Date/time (UTC): 2026-03-01T22:06:12Z
- Branch: master
- Base commit: 6252b9fa
  What changed:
- Added a reusable staged progress component for CV analysis flows: `src/components/expertise/cv-import/AnalyzeProgressPanel.tsx`.
- Wired staged progress state and messages into `CvImportWizard` (PDF upload analyze flow), including retry-aware messaging and 4-second success auto-collapse.
- Wired the same staged progress behavior into `CVJDAutoSuggest` text analyze flow (job description and general text contexts).
- Extended UI tests in `tests/ui/cv-import-wizard.test.tsx` and `tests/ui/cvjd-auto-suggest.test.tsx` to assert running state, completion state, and timer-based collapse.

Why:

- Users had no on-page feedback during analyze requests and could interpret the UI as stalled.
- The change provides explicit phase visibility across request, extraction, mapping, and finalization while preserving existing API contracts and toast behavior.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/cv-import-wizard.test.tsx tests/ui/cvjd-auto-suggest.test.tsx`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`

Open risks / TODO:

- Progress percentages are deterministic UX markers, not backend-streamed progress.
- Existing lint warnings in landing files remain unrelated to this change.
