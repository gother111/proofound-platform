# Project Change Entry

- Date/time (UTC): 2026-05-21T14:49:39Z
- Branch: master
- Base commit: 110e4f5a
  What changed:
- Replaced privacy settings client fetch/save failure `console.error` calls with local `proofound:client-diagnostic` events via `dispatchClientErrorDiagnostic`.
- Added runtime debug-output guardrails for `privacy_settings.client.visibility_fetch_failed` and `privacy_settings.client.visibility_save_failed`.
- Appended the privacy settings diagnostics slice to `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md`.

Why:

- Keeps active individual privacy settings failure diagnostics structured and local to the browser event channel without leaking raw errors to routine console output.
- Preserves the existing user-facing privacy settings behavior while tightening launch-surface diagnostic hygiene.

How to verify:

- `npm test -- --run tests/ui/deferred-settings-loaders.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" 'src/app/app/i/settings/privacy/PrivacySettingsClient.tsx' tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment, but the suites passed.
