# Project Change Entry

- Date/time (UTC): 2026-03-10T10:14:45Z
- Branch: work
- Base commit: 380761d
  What changed:
- Hardened `fetchLinkedInIdentityMe` URL normalization to only accept LinkedIn hostnames before storing/using profile URLs.
- Added regression coverage to ensure non-LinkedIn `identityMe` profile URLs are rejected and fallback uses `publicIdentifier`.

Why:

- Prevent SSRF by blocking attacker-controlled non-LinkedIn URLs from being passed to server-side scraping/enrichment.

How to verify:

- `npm run test -- tests/lib/linkedin-verified.test.ts`
- `npm run typecheck`

Open risks / TODO:

- Consider adding route-level warning telemetry when `identityMe` returns a rejected profile URL.
