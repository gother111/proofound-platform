# Project Change Entry

- Date/time (UTC): 2026-03-02T16:14:13Z
- Branch: master
- Base commit: ae2fd22b

What changed:

- Completed CV Import Wizard V3 implementation wiring for skills-first review and apply-first UX across wizard and text flows.
- Added reusable V3 review components: `AnalyzeProgressPanel`, `ImportActionBanner`, `SkillReviewPanel`, `SkillMatchPicker`, and `EntitySummaryCard` integration in wizard/text components.
- Updated wizard apply flow to support quick skills-only apply and full-review apply with explicit confirmation counts.
- Improved deterministic extractor quality in `cv-import-wizard-extractor.ts` by reducing field cloning and enforcing concise summary/evidence output.
- Tuned Gemini extraction defaults/prompts for balanced quality and moderate budget increase.
- Added optional metadata/review hints in suggest/wizard route responses and shared types.
- Updated `tests/ui/cv-import-wizard.test.tsx` to match V3 UX labels/flow (`Skills to review`, `Review All Extracted Sections`, `Apply Recommended Skills`).

Why:

- Existing post-analysis UI was technically correct but unclear for end users, and review/apply intent was ambiguous.
- V3 requirements prioritized skills-first usability, explicit mapping actions, and clearer apply semantics without breaking fallback reliability.
- Test alignment was required so regression coverage reflects new UX contracts instead of legacy labels.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/ui/cv-import-wizard.test.tsx tests/ui/cvjd-auto-suggest.test.tsx tests/lib/cv-import-wizard-extractor.test.ts tests/lib/cv-import-wizard-quality.test.ts tests/lib/gemini-reranker.test.ts tests/api/cv-import-wizard-suggest-route.test.ts tests/api/cv-import-suggest-route.test.ts` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck` (PASS)
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint` (PASS with pre-existing `@next/next/no-img-element` warnings outside this scope)
- Manual smoke: upload CV PDF -> Analyze -> review skills -> use Find picker -> Apply Recommended Skills -> confirm apply call payload.

Open risks / TODO:

- Python test suite and full repository test matrix were not rerun in this session; run before release cut.
- Existing untracked `pyproject.toml` and `uv.lock` remain in workspace and were intentionally untouched.
- Final commit/push was not executed yet in this session.
