# Project Change Entry

- Date/time (UTC): 2026-05-21T08:03:56Z
- Branch: master
- Base commit: 9484261c
  What changed:
- Replaced raw diagnostics in `src/lib/messaging/identity-reveal.ts` with structured `messaging.identity_reveal.*` logger events.
- Added focused helper tests for unresolved reveal email context and reveal-status lookup failures.
- Extended runtime debug-output guardrails so the old identity-reveal console strings cannot return.

Why:

- Keeps identity reveal failure handling observable through privacy-safe structured logging without changing reveal state updates, match unlock behavior, or non-blocking email behavior.
- Protects a launch-critical privacy boundary from leaking conversation or participant identifiers through helper failure logs.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/lib/messaging-identity-reveal-logging.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while the suites pass.
- This slice is scoped to diagnostics only and does not change identity reveal behavior.
