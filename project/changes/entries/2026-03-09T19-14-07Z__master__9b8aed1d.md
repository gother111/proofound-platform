# Project Change Entry

- Date/time (UTC): 2026-03-09T19:14:07Z
- Branch: master
- Base commit: 9b8aed1d

What changed:

- Ran the canonical production migration safety sequence against the configured pooled `DATABASE_URL`: checkpoint backup, migration-ledger audit, and migration runner apply.
- Pulled Vercel production env metadata and compared database host/port metadata to the local workspace config to investigate the earlier sitemap build failure.
- Verified the pooled production database already contains `public.profiles`, `public.organizations`, and both `public_portfolio_state` columns.

Why:

- The earlier Vercel build failure reported `relation "profiles" does not exist` during `/sitemap.xml` prerender, so production schema parity needed to be confirmed before making further deployment assumptions.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:backup:checkpoint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:audit:migrations`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run db:migrate`
- Query the pooled production database for `to_regclass('public.profiles')` and `information_schema.columns` entries for `public_portfolio_state`.

Open risks / TODO:

- Canonical migration ledger reports no pending migrations on the pooled production database, so the earlier Vercel build error likely came from a different connection path, stale deployment env, or transient database visibility issue.
- Vercel `DIRECT_URL` host resolution failed from this workstation (`ENOTFOUND`), so direct-path verification was not completed here.
