# Project Change Entry

- Date/time (UTC): 2026-02-26T15:49:01Z
- Branch: master
- Base commit: e183997d

What changed:

- Updated `src/app/api/expertise/user-skills/[id]/route.ts` to accept `last_used_at` as either ISO datetime or `YYYY-MM-DD`, and normalize date-only values to `T00:00:00.000Z`.
- Updated `src/app/app/i/expertise/components/EditSkillWindow.tsx` to normalize `last_used_at` payloads and trim/lowercase verifier email before verification requests.
- Updated `src/app/api/expertise/user-skills/[id]/verification-request/route.ts` to trim verifier email before validation.
- Added `tests/api/expertise-user-skill-route.test.ts` for PATCH `last_used_at` regression coverage.
- Extended `tests/ui/edit-skill-window-proofs.test.tsx` and `tests/api/expertise-skill-verification-request-route.test.ts` with normalization-focused cases.

Why:

- Skill edits in Expertise Atlas failed when the UI sent date-only `last_used_at` values that the PATCH API rejected.
- Verification requests from edit flow accepted raw email input, which could fail validation when users entered surrounding whitespace.

How to verify:

- `npm run test -- tests/ui/edit-skill-window-proofs.test.tsx tests/api/expertise-user-skill-route.test.ts tests/api/expertise-skill-verification-request-route.test.ts`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Lint still reports existing unrelated `@next/next/no-img-element` warnings in landing components.
- Date normalization uses UTC midnight for date-only values by design; if product later requires locale-aware conversion, this path should be revisited.
