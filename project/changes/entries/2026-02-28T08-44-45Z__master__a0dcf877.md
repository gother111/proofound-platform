# Project Change Entry

- Date/time (UTC): 2026-02-28T08:44:45Z
- Branch: master
- Base commit: a0dcf877
  What changed:
- Fixed PDF parsing initialization in `src/components/expertise/CVJDAutoSuggest.tsx`:
  - switched dynamic import from `pdfjs-dist/legacy/build/pdf.mjs` to `pdfjs-dist/webpack.mjs`
  - removed deprecated/ignored `disableWorker: true` option from `getDocument(...)`
- Added PDF parse error normalization:
  - maps `GlobalWorkerOptions.workerSrc` runtime failure to user-friendly message:
    - `PDF parser could not start. Please refresh and re-upload the file.`
  - keeps existing OCR/empty-text handling unchanged
- Updated UI tests in `tests/ui/cvjd-auto-suggest.test.tsx`:
  - updated pdfjs mock path to `pdfjs-dist/webpack.mjs`
  - added regression test for worker init failure and friendly error rendering

Why:

- CV upload in Expertise Atlas failed with `No "GlobalWorkerOptions.workerSrc" specified.` during parsing.
- `pdfjs-dist` v4 requires proper worker initialization; legacy import plus `disableWorker` no longer works as intended.

How to verify:

- `npm run test -- tests/ui/cvjd-auto-suggest.test.tsx`
- `npm run test -- tests/api/cv-import-suggest-route.test.ts tests/lib/cv-import-suggest.test.ts tests/lib/cv-import-suggest-quality.test.ts`
- `npm run lint`
- Manual smoke:
  - upload a text PDF in CV mode and confirm no `workerSrc` error is shown

Open risks / TODO:

- `pdfjs-dist/webpack.mjs` is expected to be stable in Next client runtime; verify once on deployed environment as well.
- Lint still reports two pre-existing unrelated landing warnings (`<img>` usage).
