# Project Change Entry

- Date/time (UTC): 2026-05-21T12:23:46Z
- Branch: master
- Base commit: 8f4f5488
  What changed:
- Replaced the remaining raw first-match analytics failure diagnostic in `/api/core/matching/profile` with structured `match.profile.first_match_event_failed` logging.
- Added a runtime debug-output guardrail so the legacy first-match analytics console string cannot return.
- Updated the active MVP surface sweep artifact with the profile matching analytics verification record.

Why:

- The matching profile route is part of the active MVP matching surface and should use structured, non-leaky server diagnostics for launch-safe observability.
- The first-match analytics event is intentionally best-effort, so the change preserves response behavior while making the failure path operationally visible.

How to verify:

- `npm test -- --run tests/api/core-matching-profile-route.test.ts tests/api/core-matching-profile-performance.test.ts tests/api/core-matching-gating-routes.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- src/app/api/core/matching/profile/handler.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/app/api/core/matching/profile/handler.ts -n`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment; the affected test runs passed.
- Broader raw diagnostics remain in unrelated surfaces and need separate scoped sweeps.
