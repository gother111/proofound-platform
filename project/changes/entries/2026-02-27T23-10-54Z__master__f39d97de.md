# Project Change Entry

- Date/time (UTC): 2026-02-27T23:10:54Z
- Branch: master
- Base commit: f39d97de
  What changed:
- Hardened deterministic fuzzy matching in `src/lib/expertise/cv-import-suggest.ts`:
  - substring overlaps now require token-sequence containment for high fuzzy scores
  - short alias collisions like `py` in `jupyter` and `ts` in `environments` no longer score as strong matches
- Re-validated CV import flows after the matcher fix:
  - strict E2E bundle for individual, org, privacy, and providers
  - repeated 1000-case benchmark run plus focused API/UI/import suites
- Confirmed DB migration state with `npm run db:migrate` (no pending migrations).

Why:

- User requested maximum-hardness validation and tuning for CV autosuggestions on free-tier-safe deterministic logic.
- Benchmark diagnostics showed remaining false-positive hotspots from substring alias matching that needed elimination.

How to verify:

- `npm run test:e2e:strict:all`
- `npm run test -- tests/lib/cv-import-suggest.test.ts tests/lib/cv-import-suggest-quality.test.ts tests/lib/cv-import-suggest-1000-benchmark.test.ts`
- `npm run test -- tests/api/cv-import-suggest-route.test.ts tests/ui/cvjd-auto-suggest.test.tsx`
- `npm run test -- tests/lib/cv-import-suggest-1000-benchmark.test.ts`
- `npm run db:migrate`
- `npm run lint`
- `npm run typecheck`
- `npm run build`
- `npm run test` (known unrelated failures remain in impact-story tests)

Open risks / TODO:

- Full-unit-suite failures are currently unrelated to CV import (`tests/actions/create-impact-story.test.ts`, `tests/actions/profile.test.ts`).
- Benchmark corpus is synthetic and deterministic; production quality should continue to be monitored against real anonymized CV samples.
