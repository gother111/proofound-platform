# Project Change Entry

- Date/time (UTC): 2026-03-15T13:35:15Z
- Branch: master
- Base commit: 0a509b0c
  What changed:
- Cut the active verification response path over to canonical `verification_records`, including canonical bundle load/respond/cancel/resend handling for active `/api/verification/**` and `/api/verify/**` launch routes.
- Replaced active skill and impact sent-request handlers with canonical-only helpers instead of legacy expertise transport re-exports.
- Archived legacy verification transport routes at the launch-surface policy layer and retired `/verify-skill` to `notFound()`.
- Updated verification, middleware, and archived-route tests to cover the canonical launch corridor and explicit archive behavior.

Why:

- The launch corridor still depended on legacy verification request-table transport, which violated the MVP cutover requirement that active verification state live only on canonical verification records.
- Archived legacy routes must return explicit non-launch responses so dead endpoints do not remain callable from launch surfaces.

How to verify:

- `/opt/homebrew/bin/node ./node_modules/vitest/vitest.mjs run tests/api/custom-verification-routes.test.ts tests/api/expertise-verification-respond-route.test.ts tests/api/expertise-verifications-custom-route.test.ts tests/api/expertise-verifications-sent-delete-route.test.ts src/lib/__tests__/middleware-launch-archive.test.ts src/lib/launch/__tests__/surface-policy.test.ts tests/ui/archived-mvp-routes.test.ts`
- `/opt/homebrew/bin/node /opt/homebrew/bin/npm run lint`
- `/opt/homebrew/bin/node /opt/homebrew/bin/npm run typecheck`
- `PATH=/opt/homebrew/bin:$PATH npm run test`
- `PATH=/opt/homebrew/bin:$PATH npm run build`
- `/opt/homebrew/bin/node /opt/homebrew/bin/npm run docs:freshness`

Open risks / TODO:

- `PATH=/opt/homebrew/bin:$PATH npm run test` still fails in `tests/actions/profile-impact-story-create.test.ts` because that suite now hits the stricter canonical impact verification UUID guard; this remains outside the active verification transport cutover and should be resolved separately.
- Local default `node` is `v16.14.0`; verification and build commands need `/opt/homebrew/bin` first on `PATH` to run the current Vitest/Vite toolchain.
