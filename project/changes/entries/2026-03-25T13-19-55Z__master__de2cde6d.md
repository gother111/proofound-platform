# Project Change Entry

- Date/time (UTC): 2026-03-25T13:19:55Z
- Branch: master
- Base commit: de2cde6d
What changed:
- Added the pilot manual-ops SOP bundle under `docs/internal-ops/` and updated the registry, launch ops note, API reference, and LinkedIn setup doc to point at the current internal queue surface.
- Added `GET /api/admin/internal-ops/queues` and `PATCH /api/admin/internal-ops/queues/[id]`, queue mutation support, audit logging for queue actions, and risky-upload queue creation for `manual_review` evidence uploads.
- Switched the admin verification dashboard to the generic queue API, added minimal queue action controls, and archived the stale LinkedIn-flavored admin queue/review lane.

Why:
- The locked MVP needs narrow, explicit manual ops procedures and a minimal internal queue surface that supports privacy-safe review, verification handling, and pilot follow-through without carrying broader legacy admin workflows.

How to verify:
- `npm run test -- tests/lib/internal-ops-queue.test.ts tests/lib/uploads-lifecycle-queue.test.ts tests/api/admin-internal-ops-queue-route.test.ts tests/ui/admin-dashboard-launch-links.test.tsx tests/ui/admin-verification-dashboard.test.tsx src/lib/launch/__tests__/surface-policy.test.ts src/lib/__tests__/middleware-launch-archive.test.ts`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`
- `npm run build`

Open risks / TODO:
- `docs:freshness` still reports the pre-existing orphan-doc warning set outside this change.
- The broader workspace still contains unrelated user changes and route-archive work that were intentionally left untouched.
