# Project Change Entry

- Date/time (UTC): 2026-02-28T18:32:12Z
- Branch: master
- Base commit: 2db38c9e
  What changed:
- Added structured experience fields end-to-end: new DB columns (`organization_name`, org metadata fields, `measured_outcomes` JSONB, `project_entries` JSONB), Drizzle schema updates, and migration `20260228201000_add_structured_experience_fields.sql`.
- Refactored Add/Edit Experience UI to support private organization name, org metadata, ongoing timeline toggle, repeatable outcome rows, and repeatable project rows.
- Removed colleagues/achievements from journey rendering and updated empty-state journey copy.
- Updated profile actions to read/write new columns, normalize structured arrays, derive legacy text summaries, and keep public `orgDescription` non-sensitive.
- Updated compatibility surfaces: data export now prioritizes private `organizationName`, data import and CV wizard store org name privately while keeping public description sanitized.
- Added shared option constants for org type, employee amount, industry presets, and participation capacity.
- Updated UI tests for new experience form payload and behavior.

Why:

- Implement PRO-114 to modernize the Add Experience workflow while preserving backward compatibility and preventing external exposure of raw organization names.

How to verify:

- `npm run db:migrate`
- `npm run test -- tests/ui/experience-form.test.tsx tests/ui/profile-dialogs-edit-routing.test.tsx tests/ui/profile-artifact-edit-actions.test.tsx tests/actions/profile.test.ts`
- `npm run typecheck`
- `npm run lint`

Open risks / TODO:

- Legacy imported rows without org metadata will show generic public org description until users enrich org type/industry/employee amount.
- Lint warnings in landing files (`<img>` usage) remain unrelated and pre-existing.
