# Project Change Entry

- Date/time (UTC): 2026-03-20T12:58:45Z
- Branch: master
- Base commit: b29e922b
  What changed:
- Finished the canonical org-role migration across auth helpers, the org team route, DB policy truth, and the migration runner path used for privacy verification.
- Added forward-only SQL migrations to reconcile canonical org-role backfills, restore the temporary compatibility helper only when older unapplied migrations still need it, and reapply owner-only collaborator invites plus owner-or-manager candidate invites in RLS.
- Centralized owner/manager/reviewer UI labels behind the canonical org-role map and extended the team-route test coverage to reject legacy-role authorization.

Why:

- The live privacy test database had drifted below the TypeScript layer: managers could still create collaborator invites and could not create candidate invites.
- The team route still authorized through a raw membership lookup, which meant a legacy active row could grant access even though the response payload filtered legacy roles out.
- Updating `src/db/policies.sql` without a forward migration and runner support made `db:migrate` unusable on already-migrated environments because the supplemental ledger checksum was strict.

How to verify:

- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run test -- tests/lib/authz-policy.test.ts tests/lib/api-auth.test.ts tests/actions/org-canonical-membership.test.ts tests/actions/org-invitations.test.ts tests/api/organizations-team-route.test.ts tests/lib/membership-normalization.test.ts`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && node --input-type=module -e "import { config } from 'dotenv'; config({ path: '.env.test' }); await import('./run-migrations.mjs');"`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run test:privacy`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run test:privacy:extended`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run lint`
- `source ~/.nvm/nvm.sh >/dev/null 2>&1 && nvm use 20.20.0 >/dev/null && npm run typecheck`

Open risks / TODO:

- `npm run lint` still reports one pre-existing warning in `src/components/ProofoundLanding.tsx` for a raw `<img>` tag outside this scope.
- The migration runner now refreshes checksums for the legacy supplemental baseline files only; ordered SQL migrations remain checksum-strict.
