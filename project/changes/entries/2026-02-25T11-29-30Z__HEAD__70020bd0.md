# Project Change Entry

- Date/time (UTC): 2026-02-25T11:29:30Z
- Branch: HEAD
- Base commit: 70020bd0
  What changed:
- Implemented PRO-48 structured impact stories end-to-end:
- Added migration `src/db/migrations/20260225112000_add_structured_impact_stories_and_verification.sql` with additive `impact_stories` columns and new verification tables.
- Updated Drizzle schema in `src/db/schema.ts` and policy bootstrap in `src/db/policies.sql` for impact verification request/response tables.
- Extended impact story domain types in `src/types/profile.ts`.
- Extended write/read flows in `src/actions/profile.ts` and `src/hooks/useProfileData.ts` for structured payloads, optional verification request creation, and warning handling when email send fails.
- Replaced legacy-only form with structured `ImpactStoryForm` UI, including outcomes, role/scope, cause tags, artifact URL/file upload, and optional verifier request.
- Added polymorphic verify token API + UI support for impact claim checkbox confirmation in:
- `src/app/api/verify/[token]/route.ts`
- `src/app/verify/[token]/page.tsx`
- Updated impact rendering with structured-first + legacy fallback in:
- `src/components/profile/editable-profile/ImpactTab.tsx`
- `src/components/profile/ProfileView.tsx`
- Added tests:
- `tests/api/verify-impact-token-route.test.ts`
- `tests/ui/impact-story-form.test.tsx`
- Fixed selector ambiguity in the new UI test to avoid false failures.
- Updated `tests/ui/public-org-portfolio-page.test.tsx` to mock the current Supabase server-client path used by `src/app/portfolio/org/[slug]/page.tsx`, replacing stale legacy mocks.

Why:

- PRO-48 requires claim-based impact verification and structured impact evidence while preserving backward compatibility for existing legacy impact stories.

How to verify:

- `npm run lint` (pass)
- `npm run typecheck` (pass)
- `npm run test -- tests/ui/impact-story-form.test.tsx tests/api/verify-impact-token-route.test.ts` (pass)
- `npm run test -- tests/ui/public-org-portfolio-page.test.tsx` (pass)
- `npm run test` (pass)
- `npm run build` (pass, with expected local warning about missing `DATABASE_URL`)
- `npm run db:migrate` (blocked locally: missing `DIRECT_URL` or `DATABASE_URL`)
- `npm run test:privacy` (blocked locally: missing Supabase test env vars in `.env.test`)
- `npm run test:privacy:extended` (blocked locally: missing Supabase test env vars in `.env.test`)

Open risks / TODO:

- Migration and privacy verification require DB/Supabase credentials in local env.
- Node runtime in this workspace is `v25.4.0`, while repo targets Node `20.20.0`; rerun checks on Node 20 for strict parity.
