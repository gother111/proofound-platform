# Project Change Entry

- Date/time (UTC): 2026-03-01T13:45:08Z
- Branch: master
- Base commit: 4c100d70
  What changed:
- Swept the remaining secondary UI components (`select.tsx`, `dropdown-menu.tsx`, `glass-card.tsx`, `calendar.tsx`, `skeleton.tsx`, `v2/SectionCard.tsx`) to replace hardcoded hex colors and arbitrary styles with standard V3 semantic Tailwind classes (e.g., `dark:bg-popover`, `dark:border-border`, `dark:bg-background/80`).

Why:

- To achieve 100% compliance with the V3 design system across the entire `/src/components/ui/` directory, eliminating any remaining technical UX debt.

How to verify:

- Visually interact with the components (e.g., expanding a Select or DropdownMenu in dark mode) to ensure the background and borders match the standardized global dark theme.
- Re-run `npm run lint` and `npm run typecheck` to confirm.

Open risks / TODO:

- N/A
