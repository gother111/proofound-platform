# Project Change Entry

- Date/time (UTC): 2026-02-25T20:20:35Z
- Branch: master
- Base commit: 0e5c335e

What changed:

- Successfully synced branded auth email templates to Supabase Auth config on project `cjpfrgmsxwxhuomnvciq` using `npm run email:auth:templates:sync`.
- Performed live SMTP path validation by triggering real Supabase Auth signup and password-reset sends for a smoke recipient.
- Verified Resend email log API contains both auth messages with sender `"Proofound" <no-reply@proofound.io>` and expected subjects.

Why:

- Complete end-to-end validation that auth email transport is working through the required path:
  `App -> Supabase Auth -> Resend SMTP -> delivery pipeline`.

How to verify:

- `npm run email:auth:templates:dry-run` returns supported kinds and patch summary.
- `npm run email:auth:templates:sync` completes with `Templates were updated in Supabase Auth config.`
- Supabase config endpoint reflects branded template subjects/content fields.
- Resend `/emails` list shows auth messages for smoke recipient:
  - `Verify your email - Proofound`
  - `Reset your password - Proofound`

Open risks / TODO:

- Smoke recipient used in this run is non-human and produced bounce events; run the same flow against a real monitored inbox for final non-bounce deliverability confirmation.
- Linear state transition to `In Review` could not be automated in this environment because Linear MCP returned `Tool update_issue not found`; manual state move may be required.
