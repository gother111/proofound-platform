# Project Change Entry

- Date/time (UTC): 2026-05-21T13:20:44Z
- Branch: master
- Base commit: a84529d8
  What changed:
- Replaced raw privacy helper console diagnostics with structured `privacy.*` server logs.
- Added focused tests for policy-consent fallback and matched-relationship visibility fallback.
- Extended runtime debug-output guardrails and updated the MVP surface sweep artifact.

Why:

- Consent checks and `match_only` visibility checks are active privacy controls; their failure paths should remain observable while failing closed.

How to verify:

- `rg "console\\.(error|warn|log)" src/lib/privacy/policy-versions.ts src/lib/privacy/visibility.ts -n`
- `npm test -- --run tests/lib/privacy-helpers-logging.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still emits the known sandbox Vite websocket `EPERM` warning, but the focused suites pass.
