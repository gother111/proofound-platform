# Project Change Entry

- Date/time (UTC): 2026-03-09T09:58:27Z
- Branch: master
- Base commit: 7b9cc13b
  What changed:
- Added canonical `submissions`, `submission_artifacts`, and `verification_log_entries` entities to the Drizzle schema and introduced the SQL migration `src/db/migrations/20260309020000_add_submission_and_verification_log_entities.sql`.
- Added canonical contracts plus helper services for submission dual-write and append-only verification audit logging.
- Dual-wrote assignment invite submissions and candidate proof-card submissions into the canonical `submissions` layer.
- Enriched `/api/user/export` to include canonical proof artifacts, proof packs, submissions, verification records, and verification log entries.
- Added focused tests for the new canonical contract shapes and verification log entry type mapping.

Why:

- The proof-first canonical model still lacked a first-class proof delivery record and a unified append-only verification audit ledger.
- Existing `assignment_submissions` and split verification audit tables were not sufficient for canonical provenance, export integrity, or chronological audit replay.

How to verify:

- `npm run typecheck`
- `npm run lint`
- `npx vitest run tests/canonical-domain.test.ts tests/lib/verification-log-entries.test.ts`
- Apply `src/db/migrations/20260309020000_add_submission_and_verification_log_entities.sql` and confirm legacy assignment submissions, proof-card submissions, and verification history backfill into the new canonical tables.

Open risks / TODO:

- Public proof-history read APIs still expose legacy-first payloads unless the caller uses `/api/user/export`; broader read-surface adoption is a follow-up.
- `submitted_by_user_id` is nullable in canonical submissions to support external stakeholder assignment-invite submissions without inventing a fake user principal.
