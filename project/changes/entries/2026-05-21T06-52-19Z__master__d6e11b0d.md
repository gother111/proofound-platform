# Project Change Entry

- Date/time (UTC): 2026-05-21T06:52:19Z
- Branch: master
- Base commit: d6e11b0d
  What changed:
- Replaced raw console errors in `/api/feature-flags`, `/api/proof-artifacts/text-extraction/status`, `/api/profile`, and `/api/organizations` with structured `log.error` events.
- Added failure-path tests proving those routes keep safe public error responses while logging structured diagnostics.
- Extended runtime debug-output guardrails for these active compatibility API surfaces.

Why:

- These routes are part of active launch/runtime compatibility and feature/status plumbing; structured diagnostics make failures safer and easier to classify without changing user-facing contracts.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/api/feature-flags-route.test.ts tests/api/profile-route.test.ts tests/api/organizations-list-route.test.ts tests/api/proof-artifact-text-extraction-routes.test.ts tests/lib/runtime-debug-output-guardrails.test.ts --reporter=verbose`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/scripts/launch-gate-config.test.ts --reporter=verbose`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Many older API and library error paths still use raw console output and need separate classification; this slice intentionally covered a compact active compatibility/status API cluster.
