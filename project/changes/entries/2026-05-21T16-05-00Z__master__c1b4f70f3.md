# Project Change Entry

- Date/time (UTC): 2026-05-21T16:05:00Z
- Branch: master
- Base commit: c1b4f70f3
  What changed:
- Corrected the README post-deployment health check to match the current public `/api/health` contract.
- Added a launch-gate guardrail that ties README/deployment/checklist guidance to the minimal health payload and requires commit verification to stay in Vercel metadata or prebuilt workflow summaries.

Why:

- The public health route intentionally exposes only `status` and `timestamp`; asking operators to verify deployed commit SHA from `/api/health` contradicted the current route and privacy/minimal-diagnostics tests.

How to verify:

- `npm test -- --run tests/scripts/launch-gate-config.test.ts tests/api/public-health-route.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- This is repo-local docs/guardrail alignment only. Production deployment metadata was not queried or changed.
