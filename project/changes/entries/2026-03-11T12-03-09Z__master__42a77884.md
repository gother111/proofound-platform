# Project Change Entry

- Date/time (UTC): 2026-03-11T12:03:09Z
- Branch: master
- Base commit: 42a77884
  What changed:
- Added shared datetime normalization helper at `src/lib/datetime/normalize.ts` with `toTimestampOrNull` and `toIsoOrNull`.
- Replaced duplicated local `toIso` helpers in `src/lib/workflow/service.ts` and `src/lib/interviews/messaging.ts` with imports from the new helper.
- Replaced duplicated local `toTimestamp` helper in `src/lib/verification/work-email-validity.ts` with the shared import.

Why:

- Remove repeated date coercion logic and keep null/invalid-date behavior consistent across workflow, interview messaging, and verification freshness logic.

How to verify:

- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run test -- tests/api/interviews-schedule-route.test.ts tests/api/interviews-edit-route.test.ts tests/api/interviews-cancel-route.test.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`

Open risks / TODO:

- Other similar date helpers still exist in `src/lib/verification/policy.ts`, `src/lib/proofs/canonical-pack.ts`, and `src/lib/readiness/individual-state.ts`; these can be consolidated in a later small refactor.
