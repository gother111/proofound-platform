# Project Change Entry

- Date/time (UTC): 2026-03-10T08:41:36Z
- Branch: master
- Base commit: a118842a
  What changed:
- Replaced the canonical `6.5` section in `PRD_for_a_web_platform_MVP.master-latest.md` with `Explainable Matching Reason Codes, Overrides, and Fairness Operations (MVP)`.
- Locked the canonical explainability frame to six ordered score factors: `capability_band_fit`, `tool_fluency`, `mission_values_alignment`, `freshness`, `availability_deadline_fit`, and `language_timezone_fit`.
- Split reason taxonomy into separate example enums for automatic reasons, manual review reasons, and post-review override reasons.
- Added exact counterpart-facing behavior for individuals, organizations, and admins or auditors, plus explicit privacy boundaries for hidden notes, protected-signal leakage, and non-shareable proofs.
- Reframed fairness from a passive ledger concept into an operations model with required audit data, snapshot cadence, canonical events, example payloads, and compatibility alias tables to current repo events and enums.

Why:

- The previous block already covered explainability and overrides, but it did not fully encode the master-brief scoring frame, the required automatic-versus-manual reason split, or a complete fairness-operations contract.
- The repo already has partial enum, ledger, and lifecycle-event support, so the canonical PRD needed a stricter normative layer plus explicit compatibility mapping rather than another overlapping spec.

How to verify:

- `rg -n "Explainable Matching Reason Codes, Overrides, and Fairness Operations|Facts & Decisions|Open Questions|Acceptance Criteria" PRD_for_a_web_platform_MVP.master-latest.md`
  - PASS
- `rg -n "match_scored|reason_code_assigned|shortlist_decision_logged|override_applied|override_revoked|fairness_snapshot_created|capability_band_fit|tool_fluency|mission_values_alignment|availability_deadline_fit|language_timezone_fit" PRD_for_a_web_platform_MVP.master-latest.md PRD_TECHNICAL_REQUIREMENTS.md`
  - PASS
- `npm run docs:freshness`
  - PASS in warning mode with 3 pre-existing warnings:
    - `agent/runbooks/geo-audit.md` missing from docs registry
    - `Documentation.md` Last Verified mismatch versus `project/Documentation.md`
    - `metrics.md` Last Verified mismatch versus `project/Documentation.md`
- `npm run lint`
  - PASS with 2 pre-existing warnings for raw `<img>` usage in `src/components/ProofoundLanding.tsx` and `src/components/landing/sections/FooterSection.tsx`
- `npm run typecheck`
  - PASS

Open risks / TODO:

- The compatibility mirror PRD was not updated in this task, so the canonical PRD is now more specific than the mirror by design.
- The canonical taxonomy now leads current repo enums in some areas, especially `tool_fluency` and the broader blocker taxonomy, so any future implementation work should treat the compatibility tables in this section as the migration bridge.
