# Project Change Entry

- Date/time (UTC): 2026-06-19T14:18:17Z
- Branch: master
- Base commit: d12843adc
  What changed:
- Reworked the top privacy settings card so the shield icon and "Your Privacy Controls" heading share a centered header row.
- Kept the explanatory copy and primary privacy actions aligned under the title column on larger screens while preserving full-width stacked actions on mobile.
- Updated the privacy overview regression test to lock the paired icon/title layout and action grouping.

Why:

- Browser review showed the primary privacy tile felt asymmetrical because the icon was pinned into a separate grid column from the heading/copy block.

How to verify:

- Browser: `/app/i/settings/privacy` at 1440x1354 and 390x844; icon and heading centers align, primary actions stay touch-safe, no horizontal overflow, no console warnings.
- Browser interaction: the primary "Review field visibility" action focuses the privacy visibility section and field controls remain present.
- `npm test -- tests/ui/privacy-overview-copy.test.tsx tests/ui/field-visibility-controls.test.tsx tests/ui/settings-account-history-mobile-clarity.test.tsx`
- `npm run lint`
- `npm run typecheck`
- `npm run docs:freshness`

Open risks / TODO:

- Browser screenshot capture timed out in this environment, so verification used DOM measurements, console logs, and interaction state instead.
- `npm run docs:freshness` still reports existing orphan-artifact warnings in warning-only mode.
