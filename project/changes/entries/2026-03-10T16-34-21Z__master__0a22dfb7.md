# Project Change Entry

- Date/time (UTC): 2026-03-10T16:34:21Z
- Branch: master
- Base commit: 0a22dfb7

What changed:

- Added canonical org trust tier storage, transition audit logging, and assignment policy enforcement fields in `src/db/schema.ts` with matching SQL in `src/db/migrations/20260310223000_finish_mvp_trust_layer.sql`.
- Added `src/lib/assignments/policy.ts` and extended publish, intro, feedback, and verification gate flows so restricted orgs, sensitive-domain holds, unpaid commercial work, sponsor-required work, and cross-border restrictions are enforced operationally.
- Tightened public trust rendering and admin trust-tier management across `src/lib/verification/policy.ts`, `src/lib/portfolio/trust-signals.ts`, `src/app/api/admin/organizations/[orgId]/verify/route.ts`, and related tests.
- Added `src/lib/candidate-invite-policy.ts` and enforced the same canonical org and assignment policy checks across candidate invite creation, preview, claim, and proof-card submission so pre-issued invites cannot bypass restricted or on-hold states.

Why:

- The MVP trust layer required canonical, audited enforcement rather than copy-only policy states and legacy fail-open verification checks.
- Public trust surfaces needed coarse-only semantics that do not overstate confidence or expose verifier identity.

How to verify:

- Run `npx vitest run tests/lib/verification-policy.test.ts tests/lib/verification-gates.test.ts tests/lib/assignment-publish-validation.test.ts tests/portfolio-trust-signals.test.ts tests/api/assignments-publish-route.test.ts tests/api/match-interest-route.test.ts tests/api/admin-organizations-verify-route.test.ts tests/api/expertise-verifications-sent-delete-route.test.ts tests/api/verify-impact-token-route.test.ts`.
- Run `npx vitest run tests/api/org-candidate-invites-route.test.ts tests/api/candidate-invite-claim-route.test.ts tests/api/candidate-invite-proof-card-route.test.ts`.
- Run `npm run lint`, `npm run typecheck`, `npm run docs:freshness`, `npm run db:migrate`, `npm run test:privacy`, and `npm run test:privacy:extended`.

Open risks / TODO:

- Candidate invite acceptance and proof-card submission now read canonical policy state. If a separate non-invite assignment acceptance path exists and becomes user-reachable before launch, it should read the same canonical policy helper.
- `docs:freshness` still reports existing registry warnings for unrelated audit docs and `agent/runbooks/geo-audit.md`.
