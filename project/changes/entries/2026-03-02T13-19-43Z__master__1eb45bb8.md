# Project Change Entry

- Date/time (UTC): 2026-03-02T13:19:43Z
- Branch: master
- Base commit: 1eb45bb8
  What changed:
- Trimmed GitHub Actions triggers to reduce automatic minute usage:
- `ci.yml`: removed push + pull_request_target; now `pull_request` on `master` + `workflow_dispatch`.
- `accessibility.yml`: removed push + pull_request_target; now `pull_request` on `master` + `workflow_dispatch`.
- `playwright.yml`: manual-only (`workflow_dispatch`).
- `strict-quality.yml`: manual-only (`workflow_dispatch`).
- `retry-vercel-deploy.yml`: manual-only (`workflow_dispatch`).
- `auto-update-pr-branch.yml`: removed `push` trigger; bulk update job now runs on manual dispatch.

Why:

- GitHub Actions minutes were exhausted on the free plan and recurring automatic workflows were burning quota quickly.

How to verify:

- `python3` YAML parse over all files in `.github/workflows/*.yml` succeeds.
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run typecheck`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npm run docs:freshness`
- In GitHub UI, confirm only the intended workflows auto-run on PRs, and heavy suites are manual.

Open risks / TODO:

- Required check configuration in branch protection may need adjustment if it expected push-triggered runs.
- Strict/Playwright/deploy-retry workflows now require manual dispatch when needed.
