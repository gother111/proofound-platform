# Project Change Entry

- Date/time (UTC): 2026-03-08T15:40:53Z
- Branch: master
- Base commit: 1352ef4e
  What changed:
- Replaced user-facing Lite/Strong readiness framing with shared `portfolio_ready`, `browse_ready`, and `qualified_intro_ready` derivation plus legacy compatibility mapping.
- Reworked individual onboarding, dashboard cards, matching soft-gating, and intro protection around portfolio-first activation and explicit qualified intro thresholds.
- Added milestone analytics, readiness presentation helpers, and updated tests for the new copy and gating behavior.

Why:

- The MVP should optimize for publishing a credible public portfolio first, allow low-friction browsing second, and keep qualified introductions behind stronger proof and constraint requirements.

How to verify:

- Run `npm run typecheck`.
- Run `npx vitest run tests/actions/onboarding.test.ts tests/api/core-matching-gating-routes.test.ts tests/api/match-interest-route.test.ts tests/lib/profile-completion-flow.test.ts tests/ui/dashboard-status-chip-style.test.tsx tests/ui/editable-profile-purpose-gating.test.tsx tests/ui/matching-page-gated.test.tsx src/lib/__tests__/matching-eligibility.test.ts tests/lib/verification-integrity-alignment.test.ts`.
- Inspect `/app/onboarding`, `/app/i/home`, `/app/i/matching`, and `/portfolio/[handle]` for portfolio-first copy and soft-gated browse behavior.

Open risks / TODO:

- Full `npm test` still has unrelated failures in canonical snippet/proof verification flows, `tests/ui/cvjd-auto-suggest.test.tsx`, and `tests/scripts/cron-scheduling.test.ts`.
