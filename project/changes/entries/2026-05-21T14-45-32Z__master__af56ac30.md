# Project Change Entry

- Date/time (UTC): 2026-05-21T14:45:32Z
- Branch: master
- Base commit: af56ac30
  What changed:
- Converted individual matching client failure diagnostics from raw console output to shared `proofound:client-diagnostic` events.
- Covered profile loading, match loading, timeout, restore refresh, and hide failure paths.
- Extended runtime debug-output guardrails and appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc client diagnostics from the individual matching workflow while preserving loading, soft-gate, and hide behavior.

How to verify:

- `npm test -- --run tests/ui/deferred-matching-client.test.tsx tests/api/match-hide-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" src/app/app/i/matching/MatchingClient.tsx tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
