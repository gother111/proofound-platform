# Project Change Entry

- Date/time (UTC): 2026-02-26T08:49:17Z
- Branch: master
- Base commit: 903781be
  What changed:
- Added legacy JSON string normalization support for individual profile purpose fields in:
  - `src/lib/profile/normalizePurposeLinks.ts`
  - `src/actions/profile.ts`
- Added migration:
  - `src/db/migrations/20260226083000_normalize_individual_profile_purpose_jsonb_shapes.sql`
  - Includes safe JSON parse helper and table-existence guard.
- Added regression tests:
  - `tests/lib/normalize-individual-purpose-links.test.ts`
  - `tests/actions/profile.test.ts` (`getProfileData` mixed-shape compatibility case)

Why:

- Fix PRO-75 where legacy JSONB string payloads in `individual_profiles.values`, `mission_links`, and `vision_links` caused inconsistent runtime shapes and user-specific regressions.

How to verify:

- `npm run lint`
- `npm run typecheck`
- `npm run test -- tests/lib/normalize-individual-purpose-links.test.ts tests/actions/profile.test.ts`
- `npm run test`
- `npm run build`
- `npm run db:backup:checkpoint`
- `npm run db:audit:migrations` (known ledger drift warnings in current DB environment)
- `npm run db:migrate`
- Verify post-migration SQL:
  - `select count(*) from public.individual_profiles where jsonb_typeof(values)='string';`
  - `select count(*) from public.individual_profiles where jsonb_typeof(mission_links)='string';`
  - `select count(*) from public.individual_profiles where jsonb_typeof(vision_links)='string';`

Open risks / TODO:

- DB migration ledger drift exists in current environment and is pre-existing (`db:audit:migrations`).
- Migration intentionally no-ops when `public.individual_profiles` is absent in target DB.
