# Project Change Entry

- Date/time (UTC): 2026-03-11T08:17:47Z
- Branch: master
- Base commit: 217d1a2b
  What changed:
- Added `scripts/vercel-ensure-project.mjs` to create or update a child Vercel project with the expected root directory and inherited runtime settings.
- Added `scripts/vercel-sync-project-env.mjs` to upsert env vars from `proofound-platform` into `proofound-platform-api`, excluding `NODE_OPTIONS` and `VERCEL_*`.
- Added package scripts for `vercel:api:ensure`, `vercel:api:sync-env`, and `vercel:api:bootstrap`.
- Added `agent/runbooks/vercel-api-project-bootstrap.md` and updated `agent/checklists/verification.md` with the new API-project bootstrap and verification flow.
- Bootstraped the live `proofound-platform-api` Vercel project, set `apps/api` as its root directory, synced 46 env vars, and connected the GitHub repo.

Why:

- The API extraction needs a dedicated Vercel project before same-domain `/api/*` rewrites can be cut over.
- Repo-native automation reduces manual Vercel setup drift and makes future product and marketing project splits repeatable.

How to verify:

- `npm run build:api`
- `npm run vercel:api:ensure`
- `npm run vercel:api:sync-env`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `npx -y vercel@latest project inspect proofound-platform-api --token "$VERCEL_TOKEN"`

Open risks / TODO:

- `proofound-platform-api` is Git-connected but still has `productionBranch=main`; the Vercel API rejected branch updates, so `master` must be set in the dashboard before relying on automatic production sync.
- Same-domain `/api/*` rewrites are not configured yet, so public traffic still goes to the current primary app.
- The child project preview/protection flow still needs an authenticated Vercel session or bypass token for direct HTTP smoke checks against protected deployments.
