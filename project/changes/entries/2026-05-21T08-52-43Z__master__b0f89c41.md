# Project Change Entry

- Date/time (UTC): 2026-05-21T08:52:43Z
- Branch: master
- Base commit: b0f89c41
  What changed:
- Replaced raw console diagnostics in `/api/organizations/[orgId]` and `/api/profile/visibility` with structured `organization.detail.*` and `profile.visibility.*` server log events.
- Added route coverage for organization fetch/update failures and profile visibility fetch/update failures.
- Extended runtime debug-output guardrails so the old organization/profile visibility console strings cannot return.
- Updated `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md` with the organization detail/profile visibility sweep checkpoint.

Why:

- Organization trust-page metadata and individual visibility settings are active MVP privacy/trust surfaces; failure diagnostics should stay structured without changing safe response contracts.

How to verify:

- `npm test -- --run tests/api/organizations-route.test.ts tests/api/profile-visibility-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check -- 'src/app/api/organizations/[orgId]/route.ts' 'src/app/api/profile/visibility/route.ts' tests/api/organizations-route.test.ts tests/api/profile-visibility-route.test.ts tests/lib/runtime-debug-output-guardrails.test.ts`

Open risks / TODO:

- The broader MVP sweep is still active; remaining raw diagnostics in admin, auth, matching explanation/profile, and shared library helpers should continue in separate scoped checkpoints.
- The worktree still has unrelated unstaged edits outside this checkpoint; they were not staged or committed here.
