# Project Change Entry

- Date/time (UTC): 2026-03-12T14:34:09Z
- Branch: master
- Base commit: f45a5bd5
  What changed:
- Extracted `prepareImpactStoryPersistenceFields()` in `src/actions/profile.ts` so `createImpactStory()` and `updateImpactStory()` share the same normalization and legacy field preparation logic.

Why:

- Reduce duplicate persistence preparation code in one file without changing impact story write behavior.

How to verify:

- `npm run lint`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx eslint src/actions/profile.ts`
- `PATH=/opt/homebrew/opt/node@20/bin:$PATH npx vitest run tests/ui/impact-story-form.test.tsx`

Open risks / TODO:

- Repo-wide `npm run typecheck` currently fails on pre-existing issues in `src/actions/profile.ts` and `src/lib/profileStorage.ts` unrelated to this refactor.
