# Project Change Entry

- Date/time (UTC): 2026-05-21T15:51:52Z
- Branch: master
- Base commit: f70b81087
  What changed:
- Replaced ad hoc middleware error console strings with a middleware-local structured diagnostic writer.
- Routed the generic `logError` helper through `proofound:client-diagnostic` in browser contexts.
- Added runtime guardrails for middleware and global error-handler diagnostics.

Why:

- Keeps active route/API boundary diagnostics structured while preserving API fail-closed behavior, page fallback behavior, and safe user-message extraction.

How to verify:

- `npm test -- --run tests/lib/runtime-debug-output-guardrails.test.ts src/lib/__tests__/middleware-fail-closed.test.ts src/lib/__tests__/middleware-csrf.test.ts src/lib/__tests__/security-headers.test.ts`
- `npm test -- --run tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- Targeted scan: `rg -n "console\\.(error|warn|log)" src --glob '!src/archive/**' --glob '!src/db/**' --glob '!src/lib/launch/final-launch-validation-runner.ts'`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Shared logger sinks in `src/lib/log.ts` and `src/lib/logger.ts` intentionally still write to `console.*` as structured output sinks.
