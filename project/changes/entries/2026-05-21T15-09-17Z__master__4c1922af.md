# Project Change Entry

- Date/time (UTC): 2026-05-21T15:09:17Z
- Branch: master
- Base commit: 4c1922af
  What changed:
- Replaced raw console diagnostics in client feature-flag/status hooks with local `proofound:client-diagnostic` events.
- Covered Proof Artifact OCR beta status, Start from CV beta status, and assistive AI UI flag load failures.
- Added runtime debug-output guardrails and appended the slice to `.artifacts/mvp-surface-sweep-2026-05-19/SURFACE_SWEEP.md`.

Why:

- Keeps active client capability/status checks fail-closed without leaking raw browser-console errors.
- Preserves existing default-off and fallback behavior while continuing launch-surface diagnostic hygiene.

How to verify:

- `npm test -- --run tests/ui/individual-setup-proof-first.test.tsx tests/ui/profile-context-cv-import.test.tsx tests/ui/portfolio-visibility-card-ai.test.tsx tests/ui/verifications-client.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `rg "console\\.(error|warn|log)" src/hooks/useProofArtifactOcrBetaStatus.ts src/hooks/useStartFromCvBetaStatus.ts src/hooks/useAssistiveAiFlag.ts tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning in this environment, but the suites passed.
- Other active MVP surfaces still have raw client diagnostics and need follow-up slices before the full goal can be completed.
