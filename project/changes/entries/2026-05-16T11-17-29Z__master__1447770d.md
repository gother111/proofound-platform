# Project Change Entry

- Date/time (UTC): 2026-05-16T11:17:29Z
- Branch: master
- Base commit: 1447770d
  What changed:
- Removed the legacy direct Anthropic/Claude execution path from the remaining `src/lib/ai` helpers.
- Kept JD-to-L4 mapping local-only and removed the unused `@anthropic-ai/sdk` dependency.
- Added an AI launch guardrail test that fails if active AI source reintroduces direct legacy Anthropic provider calls.
- Updated the AI verification audit with the JD-to-L4 provider-bypass finding and fix.

Why:

- `/api/expertise/jd-to-l4` is still an active launch API, so raw job-description text must not bypass the current Gemini provider, usage ledger, spend caps, model smoke, and request-safety guardrails.

How to verify:

- `PATH=/opt/homebrew/opt/node@24/bin:$PATH npm run test:launch:ai`

Open risks / TODO:

- Live Gemini provider behavior remains unverified until server-only provider credentials and hard-cap env are available.
