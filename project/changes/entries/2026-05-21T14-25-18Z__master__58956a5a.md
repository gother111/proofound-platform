# Project Change Entry

- Date/time (UTC): 2026-05-21T14:25:18Z
- Branch: master
- Base commit: 58956a5a
  What changed:
- Converted candidate-invite client failure diagnostics from raw console output to shared `proofound:client-diagnostic` events.
- Covered invite state load, claim, and proof submission failure paths.
- Extended runtime debug-output guardrails and appended the sweep artifact entry.

Why:

- The active MVP surface sweep is removing ad hoc client diagnostics from the public candidate-invite workflow while preserving user-facing fallback behavior.

How to verify:

- `npm test -- --run tests/ui/candidate-invite-client.test.tsx tests/lib/runtime-debug-output-guardrails.test.ts`
- `npm test -- --run tests/scripts/launch-gate-config.test.ts`
- `rg "console\\.(error|warn|log)" 'src/app/candidate-invite/[token]/CandidateInviteClient.tsx' tests/lib/runtime-debug-output-guardrails.test.ts -n`
- `npm run typecheck`
- `npm run lint`
- `npm run docs:freshness`
- `git diff --check`

Open risks / TODO:

- Vitest still prints the known sandbox Vite websocket `EPERM` warning while passing.
