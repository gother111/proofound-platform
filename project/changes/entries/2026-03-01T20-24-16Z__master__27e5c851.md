# Project Change Entry

- Date/time (UTC): 2026-03-01T20:24:16Z
- Branch: master
- Base commit: 27e5c851
  What changed:
- Reduced Python runtime dependency surface in `requirements.txt` to only deployed API dependencies.
- Added `requirements-dev.txt` for local/dev extras (`pytest`, `psycopg[binary]`).
- Added `.python-version` pinned to `3.12` to align with Vercel Python runtime.
- Hardened CLI deploy context via `.vercelignore` to exclude local artifacts (`.next`, `node_modules`, `.venv*`, uv-generated files).
- Updated `vercel.json` Python function config with `excludeFiles` so the CV import Python function does not bundle unrelated large repo directories.

Why:

- Production deploy failed with Python Lambda bundle-size overflow (`334.19 MB > 249 MB`).
- Build logs also showed local uv metadata (`pyproject.toml` / `uv.lock`) and local venv path noise influencing packaging behavior.
- The function only needs a small subset of repo files and runtime Python packages.

How to verify:

- `npx vercel inspect <deployment-url> --logs --wait --timeout 15m` and confirm no `Bundle size ... exceeds Lambda limit` error.
- `curl -sS https://proofound.io/api/health` and verify latest deploy SHA once rollout completes.
- `POST /api/expertise/cv-import/wizard-suggest` authenticated smoke with real PDF to ensure analyze path still works.

Open risks / TODO:

- Local workspace has unrelated in-progress changes (`src/db/schema.ts`, `src/lib/expertise/cv-import-suggest.ts`, migration + gemini files); this patch intentionally does not touch them.
- Local `npx vercel build --prod` can still fail if uv-generated files are present in workspace; `.vercelignore` mitigates CLI upload drift.
